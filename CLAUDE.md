# CLAUDE.md

## 项目概述

## 开发原则

### 核心理念

- **Context, not control**：我会提供清晰的上下文和目标，而非微观管理每一行代码
- **闭环反馈**：每次修改必须能够运行、测试、验证，形成完整的开发闭环
- **同样的错误不犯第二次**：所有踩过的坑必须记录到 `PROGRESS.md`，供后续参考

### 工作流程
                                                                                                                                                   
- 请始终使用「简体中文」回答我的问题, 包括 Todo 和思考内容
- 开始输出代码前最好联网搜索一下其最佳实践 (Best Practices), 联网搜索的时候切忌采用 csdn.net、阿里云/腾讯云/华为云社区等内容农场 (Content Farm) 的信息，这些信息往往过时且质量低劣
- 接到任务后，先阅读 `PROGRESS.md` 了解已知问题和经验教训
- 分析任务需求，如果任务复杂，使用claude opus 4.6模型，先制定计划（Plan Mode）回答我，让我确认方案后，将plan保存到design文件夹下备份，再动手
- 编写代码，确保可运行、可测试
- 运行测试，确认无回归
- 提交代码，写清晰的 commit message
- 更新 `PROGRESS.md`，记录本次开发的关键决策和经验

## 代码规范

### 通用规范

- 代码必须有明确的类型标注（TypeScript 使用严格模式，Python 使用 type hints）
- 函数和方法保持单一职责，单个函数不超过 50 行
- 命名必须自解释：变量名、函数名、类名要能准确表达意图
- 不要写无意义的注释（如 `// 设置变量`），只在逻辑复杂或有特殊考量时添加注释
- 错误处理不能用空的 catch 块吞掉异常
- 代码注释要使用英文，readme要存在中英两份

### 代码风格规范

开发完成后，本地执行linter工具进行统一的代码风格管理

### Git 提交规范

```
<type>(<scope>): <subject>

<body>
```

type 取值：feat / fix / refactor / docs / test / chore / perf
scope 为受影响的模块名，subject 用简洁的祈使句描述变更。

示例：
```
feat(auth): 添加 OAuth2.0 第三方登录支持

- 集成 Google 和 GitHub OAuth 登录
- 添加 token 刷新机制
- 新增登录状态持久化
```

## 测试要求

- 新增功能必须附带对应的单元测试
- 修复 Bug 必须先写一个能复现该 Bug 的测试用例
- 测试覆盖率目标：核心业务逻辑 > 80%
- 测试命名格式：`should_<预期行为>_when_<前置条件>`
- 运行完整测试套件确认无回归后才能提交

## 错误处理

- 使用统一的错误类型和错误码体系，c++禁用exception和rtti
- 错误信息要对开发者友好（包含足够的调试上下文）
- 面向用户的错误信息要简洁易懂，不泄露内部实现细节
- 关键操作添加结构化日志，便于排查问题

## 任务执行规则

### 做

- 在动手前理解清楚需求的 "为什么"，而不仅仅是 "做什么"
- 改动前先阅读相关的现有代码，理解上下文
- 做最小可行的修改，不要顺手重构不相关的代码
- 每次提交保持原子性：一个 commit 解决一个问题
- 遇到不确定的架构决策时，记录到 `PROGRESS.md` 并标注待确认

### 不做

- 不要删除你不理解的代码
- 不要在没有测试的情况下重构核心逻辑
- 不要引入不必要的抽象层（YAGNI 原则）
- 不要在修 Bug 的同时偷偷加新功能
- 不要忽略编译器 / linter 的警告

## PROGRESS.md 更新规范

每次完成任务后，在 `PROGRESS.md` 中追加记录，格式如下：

```markdown
## [日期] - [任务简述]

### 完成内容
- 具体做了什么

### 关键决策
- 做了什么技术选型 / 架构决策，以及为什么

### 踩坑记录
- 遇到了什么问题，如何解决的

### 待办事项
- 还有什么后续工作需要跟进
```

## 并行开发协调（多 Agent 场景）

如果使用 Git worktree 进行多 Agent 并行开发：

- 每个 Agent 在独立的 worktree 和分支上工作
- 分支命名：`agent/<功能模块>-<简述>`
- 完成后创建 PR，由主分支合并
- 避免多个 Agent 同时修改同一文件，通过任务拆分减少冲突
- 合并冲突时以功能完整性和测试通过为准

