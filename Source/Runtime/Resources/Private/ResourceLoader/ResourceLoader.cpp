// // Auto generated by formatgen on Sep 21 2019
// #pragma once
// #if !defined(TINYIMAGEFORMAT_APIS_H_) && !defined(TINYIMAGEFORMAT_IMAGEFORMAT_H)
// #define TINYIMAGEFORMAT_APIS_H_ 1

// #include <ThirdParty/tinyimageformat/tinyimageformat_base.h>
// #include <cstdint>

// #ifndef TINYIMAGEFORMAT_VKFORMAT
// #define TINYIMAGEFORMAT_VKFORMAT
// typedef enum TinyImageFormat_VkFormat
// {
//     TIF_VK_FORMAT_UNDEFINED = 0,
//     TIF_VK_FORMAT_R4G4_UNORM_PACK8 = 1,
//     TIF_VK_FORMAT_R4G4B4A4_UNORM_PACK16 = 2,
//     TIF_VK_FORMAT_B4G4R4A4_UNORM_PACK16 = 3,
//     TIF_VK_FORMAT_R5G6B5_UNORM_PACK16 = 4,
//     TIF_VK_FORMAT_B5G6R5_UNORM_PACK16 = 5,
//     TIF_VK_FORMAT_R5G5B5A1_UNORM_PACK16 = 6,
//     TIF_VK_FORMAT_B5G5R5A1_UNORM_PACK16 = 7,
//     TIF_VK_FORMAT_A1R5G5B5_UNORM_PACK16 = 8,
//     TIF_VK_FORMAT_R8_UNORM = 9,
//     TIF_VK_FORMAT_R8_SNORM = 10,
//     TIF_VK_FORMAT_R8_USCALED = 11,
//     TIF_VK_FORMAT_R8_SSCALED = 12,
//     TIF_VK_FORMAT_R8_UINT = 13,
//     TIF_VK_FORMAT_R8_SINT = 14,
//     TIF_VK_FORMAT_R8_SRGB = 15,
//     TIF_VK_FORMAT_R8G8_UNORM = 16,
//     TIF_VK_FORMAT_R8G8_SNORM = 17,
//     TIF_VK_FORMAT_R8G8_USCALED = 18,
//     TIF_VK_FORMAT_R8G8_SSCALED = 19,
//     TIF_VK_FORMAT_R8G8_UINT = 20,
//     TIF_VK_FORMAT_R8G8_SINT = 21,
//     TIF_VK_FORMAT_R8G8_SRGB = 22,
//     TIF_VK_FORMAT_R8G8B8_UNORM = 23,
//     TIF_VK_FORMAT_R8G8B8_SNORM = 24,
//     TIF_VK_FORMAT_R8G8B8_USCALED = 25,
//     TIF_VK_FORMAT_R8G8B8_SSCALED = 26,
//     TIF_VK_FORMAT_R8G8B8_UINT = 27,
//     TIF_VK_FORMAT_R8G8B8_SINT = 28,
//     TIF_VK_FORMAT_R8G8B8_SRGB = 29,
//     TIF_VK_FORMAT_B8G8R8_UNORM = 30,
//     TIF_VK_FORMAT_B8G8R8_SNORM = 31,
//     TIF_VK_FORMAT_B8G8R8_USCALED = 32,
//     TIF_VK_FORMAT_B8G8R8_SSCALED = 33,
//     TIF_VK_FORMAT_B8G8R8_UINT = 34,
//     TIF_VK_FORMAT_B8G8R8_SINT = 35,
//     TIF_VK_FORMAT_B8G8R8_SRGB = 36,
//     TIF_VK_FORMAT_R8G8B8A8_UNORM = 37,
//     TIF_VK_FORMAT_R8G8B8A8_SNORM = 38,
//     TIF_VK_FORMAT_R8G8B8A8_USCALED = 39,
//     TIF_VK_FORMAT_R8G8B8A8_SSCALED = 40,
//     TIF_VK_FORMAT_R8G8B8A8_UINT = 41,
//     TIF_VK_FORMAT_R8G8B8A8_SINT = 42,
//     TIF_VK_FORMAT_R8G8B8A8_SRGB = 43,
//     TIF_VK_FORMAT_B8G8R8A8_UNORM = 44,
//     TIF_VK_FORMAT_B8G8R8A8_SNORM = 45,
//     TIF_VK_FORMAT_B8G8R8A8_USCALED = 46,
//     TIF_VK_FORMAT_B8G8R8A8_SSCALED = 47,
//     TIF_VK_FORMAT_B8G8R8A8_UINT = 48,
//     TIF_VK_FORMAT_B8G8R8A8_SINT = 49,
//     TIF_VK_FORMAT_B8G8R8A8_SRGB = 50,
//     TIF_VK_FORMAT_A8B8G8R8_UNORM_PACK32 = 51,
//     TIF_VK_FORMAT_A8B8G8R8_SNORM_PACK32 = 52,
//     TIF_VK_FORMAT_A8B8G8R8_USCALED_PACK32 = 53,
//     TIF_VK_FORMAT_A8B8G8R8_SSCALED_PACK32 = 54,
//     TIF_VK_FORMAT_A8B8G8R8_UINT_PACK32 = 55,
//     TIF_VK_FORMAT_A8B8G8R8_SINT_PACK32 = 56,
//     TIF_VK_FORMAT_A8B8G8R8_SRGB_PACK32 = 57,
//     TIF_VK_FORMAT_A2R10G10B10_UNORM_PACK32 = 58,
//     TIF_VK_FORMAT_A2R10G10B10_SNORM_PACK32 = 59,
//     TIF_VK_FORMAT_A2R10G10B10_USCALED_PACK32 = 60,
//     TIF_VK_FORMAT_A2R10G10B10_SSCALED_PACK32 = 61,
//     TIF_VK_FORMAT_A2R10G10B10_UINT_PACK32 = 62,
//     TIF_VK_FORMAT_A2R10G10B10_SINT_PACK32 = 63,
//     TIF_VK_FORMAT_A2B10G10R10_UNORM_PACK32 = 64,
//     TIF_VK_FORMAT_A2B10G10R10_SNORM_PACK32 = 65,
//     TIF_VK_FORMAT_A2B10G10R10_USCALED_PACK32 = 66,
//     TIF_VK_FORMAT_A2B10G10R10_SSCALED_PACK32 = 67,
//     TIF_VK_FORMAT_A2B10G10R10_UINT_PACK32 = 68,
//     TIF_VK_FORMAT_A2B10G10R10_SINT_PACK32 = 69,
//     TIF_VK_FORMAT_R16_UNORM = 70,
//     TIF_VK_FORMAT_R16_SNORM = 71,
//     TIF_VK_FORMAT_R16_USCALED = 72,
//     TIF_VK_FORMAT_R16_SSCALED = 73,
//     TIF_VK_FORMAT_R16_UINT = 74,
//     TIF_VK_FORMAT_R16_SINT = 75,
//     TIF_VK_FORMAT_R16_SFLOAT = 76,
//     TIF_VK_FORMAT_R16G16_UNORM = 77,
//     TIF_VK_FORMAT_R16G16_SNORM = 78,
//     TIF_VK_FORMAT_R16G16_USCALED = 79,
//     TIF_VK_FORMAT_R16G16_SSCALED = 80,
//     TIF_VK_FORMAT_R16G16_UINT = 81,
//     TIF_VK_FORMAT_R16G16_SINT = 82,
//     TIF_VK_FORMAT_R16G16_SFLOAT = 83,
//     TIF_VK_FORMAT_R16G16B16_UNORM = 84,
//     TIF_VK_FORMAT_R16G16B16_SNORM = 85,
//     TIF_VK_FORMAT_R16G16B16_USCALED = 86,
//     TIF_VK_FORMAT_R16G16B16_SSCALED = 87,
//     TIF_VK_FORMAT_R16G16B16_UINT = 88,
//     TIF_VK_FORMAT_R16G16B16_SINT = 89,
//     TIF_VK_FORMAT_R16G16B16_SFLOAT = 90,
//     TIF_VK_FORMAT_R16G16B16A16_UNORM = 91,
//     TIF_VK_FORMAT_R16G16B16A16_SNORM = 92,
//     TIF_VK_FORMAT_R16G16B16A16_USCALED = 93,
//     TIF_VK_FORMAT_R16G16B16A16_SSCALED = 94,
//     TIF_VK_FORMAT_R16G16B16A16_UINT = 95,
//     TIF_VK_FORMAT_R16G16B16A16_SINT = 96,
//     TIF_VK_FORMAT_R16G16B16A16_SFLOAT = 97,
//     TIF_VK_FORMAT_R32_UINT = 98,
//     TIF_VK_FORMAT_R32_SINT = 99,
//     TIF_VK_FORMAT_R32_SFLOAT = 100,
//     TIF_VK_FORMAT_R32G32_UINT = 101,
//     TIF_VK_FORMAT_R32G32_SINT = 102,
//     TIF_VK_FORMAT_R32G32_SFLOAT = 103,
//     TIF_VK_FORMAT_R32G32B32_UINT = 104,
//     TIF_VK_FORMAT_R32G32B32_SINT = 105,
//     TIF_VK_FORMAT_R32G32B32_SFLOAT = 106,
//     TIF_VK_FORMAT_R32G32B32A32_UINT = 107,
//     TIF_VK_FORMAT_R32G32B32A32_SINT = 108,
//     TIF_VK_FORMAT_R32G32B32A32_SFLOAT = 109,
//     TIF_VK_FORMAT_R64_UINT = 110,
//     TIF_VK_FORMAT_R64_SINT = 111,
//     TIF_VK_FORMAT_R64_SFLOAT = 112,
//     TIF_VK_FORMAT_R64G64_UINT = 113,
//     TIF_VK_FORMAT_R64G64_SINT = 114,
//     TIF_VK_FORMAT_R64G64_SFLOAT = 115,
//     TIF_VK_FORMAT_R64G64B64_UINT = 116,
//     TIF_VK_FORMAT_R64G64B64_SINT = 117,
//     TIF_VK_FORMAT_R64G64B64_SFLOAT = 118,
//     TIF_VK_FORMAT_R64G64B64A64_UINT = 119,
//     TIF_VK_FORMAT_R64G64B64A64_SINT = 120,
//     TIF_VK_FORMAT_R64G64B64A64_SFLOAT = 121,
//     TIF_VK_FORMAT_B10G11R11_UFLOAT_PACK32 = 122,
//     TIF_VK_FORMAT_E5B9G9R9_UFLOAT_PACK32 = 123,
//     TIF_VK_FORMAT_D16_UNORM = 124,
//     TIF_VK_FORMAT_X8_D24_UNORM_PACK32 = 125,
//     TIF_VK_FORMAT_D32_SFLOAT = 126,
//     TIF_VK_FORMAT_S8_UINT = 127,
//     TIF_VK_FORMAT_D16_UNORM_S8_UINT = 128,
//     TIF_VK_FORMAT_D24_UNORM_S8_UINT = 129,
//     TIF_VK_FORMAT_D32_SFLOAT_S8_UINT = 130,
//     TIF_VK_FORMAT_BC1_RGB_UNORM_BLOCK = 131,
//     TIF_VK_FORMAT_BC1_RGB_SRGB_BLOCK = 132,
//     TIF_VK_FORMAT_BC1_RGBA_UNORM_BLOCK = 133,
//     TIF_VK_FORMAT_BC1_RGBA_SRGB_BLOCK = 134,
//     TIF_VK_FORMAT_BC2_UNORM_BLOCK = 135,
//     TIF_VK_FORMAT_BC2_SRGB_BLOCK = 136,
//     TIF_VK_FORMAT_BC3_UNORM_BLOCK = 137,
//     TIF_VK_FORMAT_BC3_SRGB_BLOCK = 138,
//     TIF_VK_FORMAT_BC4_UNORM_BLOCK = 139,
//     TIF_VK_FORMAT_BC4_SNORM_BLOCK = 140,
//     TIF_VK_FORMAT_BC5_UNORM_BLOCK = 141,
//     TIF_VK_FORMAT_BC5_SNORM_BLOCK = 142,
//     TIF_VK_FORMAT_BC6H_UFLOAT_BLOCK = 143,
//     TIF_VK_FORMAT_BC6H_SFLOAT_BLOCK = 144,
//     TIF_VK_FORMAT_BC7_UNORM_BLOCK = 145,
//     TIF_VK_FORMAT_BC7_SRGB_BLOCK = 146,
//     TIF_VK_FORMAT_ETC2_R8G8B8_UNORM_BLOCK = 147,
//     TIF_VK_FORMAT_ETC2_R8G8B8_SRGB_BLOCK = 148,
//     TIF_VK_FORMAT_ETC2_R8G8B8A1_UNORM_BLOCK = 149,
//     TIF_VK_FORMAT_ETC2_R8G8B8A1_SRGB_BLOCK = 150,
//     TIF_VK_FORMAT_ETC2_R8G8B8A8_UNORM_BLOCK = 151,
//     TIF_VK_FORMAT_ETC2_R8G8B8A8_SRGB_BLOCK = 152,
//     TIF_VK_FORMAT_EAC_R11_UNORM_BLOCK = 153,
//     TIF_VK_FORMAT_EAC_R11_SNORM_BLOCK = 154,
//     TIF_VK_FORMAT_EAC_R11G11_UNORM_BLOCK = 155,
//     TIF_VK_FORMAT_EAC_R11G11_SNORM_BLOCK = 156,
//     TIF_VK_FORMAT_ASTC_4x4_UNORM_BLOCK = 157,
//     TIF_VK_FORMAT_ASTC_4x4_SRGB_BLOCK = 158,
//     TIF_VK_FORMAT_ASTC_5x4_UNORM_BLOCK = 159,
//     TIF_VK_FORMAT_ASTC_5x4_SRGB_BLOCK = 160,
//     TIF_VK_FORMAT_ASTC_5x5_UNORM_BLOCK = 161,
//     TIF_VK_FORMAT_ASTC_5x5_SRGB_BLOCK = 162,
//     TIF_VK_FORMAT_ASTC_6x5_UNORM_BLOCK = 163,
//     TIF_VK_FORMAT_ASTC_6x5_SRGB_BLOCK = 164,
//     TIF_VK_FORMAT_ASTC_6x6_UNORM_BLOCK = 165,
//     TIF_VK_FORMAT_ASTC_6x6_SRGB_BLOCK = 166,
//     TIF_VK_FORMAT_ASTC_8x5_UNORM_BLOCK = 167,
//     TIF_VK_FORMAT_ASTC_8x5_SRGB_BLOCK = 168,
//     TIF_VK_FORMAT_ASTC_8x6_UNORM_BLOCK = 169,
//     TIF_VK_FORMAT_ASTC_8x6_SRGB_BLOCK = 170,
//     TIF_VK_FORMAT_ASTC_8x8_UNORM_BLOCK = 171,
//     TIF_VK_FORMAT_ASTC_8x8_SRGB_BLOCK = 172,
//     TIF_VK_FORMAT_ASTC_10x5_UNORM_BLOCK = 173,
//     TIF_VK_FORMAT_ASTC_10x5_SRGB_BLOCK = 174,
//     TIF_VK_FORMAT_ASTC_10x6_UNORM_BLOCK = 175,
//     TIF_VK_FORMAT_ASTC_10x6_SRGB_BLOCK = 176,
//     TIF_VK_FORMAT_ASTC_10x8_UNORM_BLOCK = 177,
//     TIF_VK_FORMAT_ASTC_10x8_SRGB_BLOCK = 178,
//     TIF_VK_FORMAT_ASTC_10x10_UNORM_BLOCK = 179,
//     TIF_VK_FORMAT_ASTC_10x10_SRGB_BLOCK = 180,
//     TIF_VK_FORMAT_ASTC_12x10_UNORM_BLOCK = 181,
//     TIF_VK_FORMAT_ASTC_12x10_SRGB_BLOCK = 182,
//     TIF_VK_FORMAT_ASTC_12x12_UNORM_BLOCK = 183,
//     TIF_VK_FORMAT_ASTC_12x12_SRGB_BLOCK = 184,

//     TIF_VK_FORMAT_G8B8G8R8_422_UNORM = 1000156000,
//     TIF_VK_FORMAT_B8G8R8G8_422_UNORM = 1000156001,
//     TIF_VK_FORMAT_G8_B8_R8_3PLANE_420_UNORM = 1000156002,
//     TIF_VK_FORMAT_G8_B8R8_2PLANE_420_UNORM = 1000156003,
//     TIF_VK_FORMAT_G8_B8_R8_3PLANE_422_UNORM = 1000156004,
//     TIF_VK_FORMAT_G8_B8R8_2PLANE_422_UNORM = 1000156005,
//     TIF_VK_FORMAT_G8_B8_R8_3PLANE_444_UNORM = 1000156006,
//     TIF_VK_FORMAT_R10X6_UNORM_PACK16 = 1000156007,
//     TIF_VK_FORMAT_R10X6G10X6_UNORM_2PACK16 = 1000156008,
//     TIF_VK_FORMAT_R10X6G10X6B10X6A10X6_UNORM_4PACK16 = 1000156009,
//     TIF_VK_FORMAT_G10X6B10X6G10X6R10X6_422_UNORM_4PACK16 = 1000156010,
//     TIF_VK_FORMAT_B10X6G10X6R10X6G10X6_422_UNORM_4PACK16 = 1000156011,
//     TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_420_UNORM_3PACK16 = 1000156012,
//     TIF_VK_FORMAT_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16 = 1000156013,
//     TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_422_UNORM_3PACK16 = 1000156014,
//     TIF_VK_FORMAT_G10X6_B10X6R10X6_2PLANE_422_UNORM_3PACK16 = 1000156015,
//     TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_444_UNORM_3PACK16 = 1000156016,
//     TIF_VK_FORMAT_R12X4_UNORM_PACK16 = 1000156017,
//     TIF_VK_FORMAT_R12X4G12X4_UNORM_2PACK16 = 1000156018,
//     TIF_VK_FORMAT_R12X4G12X4B12X4A12X4_UNORM_4PACK16 = 1000156019,
//     TIF_VK_FORMAT_G12X4B12X4G12X4R12X4_422_UNORM_4PACK16 = 1000156020,
//     TIF_VK_FORMAT_B12X4G12X4R12X4G12X4_422_UNORM_4PACK16 = 1000156021,
//     TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_420_UNORM_3PACK16 = 1000156022,
//     TIF_VK_FORMAT_G12X4_B12X4R12X4_2PLANE_420_UNORM_3PACK16 = 1000156023,
//     TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_422_UNORM_3PACK16 = 1000156024,
//     TIF_VK_FORMAT_G12X4_B12X4R12X4_2PLANE_422_UNORM_3PACK16 = 1000156025,
//     TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_444_UNORM_3PACK16 = 1000156026,
//     TIF_VK_FORMAT_G16B16G16R16_422_UNORM = 1000156027,
//     TIF_VK_FORMAT_B16G16R16G16_422_UNORM = 1000156028,
//     TIF_VK_FORMAT_G16_B16_R16_3PLANE_420_UNORM = 1000156029,
//     TIF_VK_FORMAT_G16_B16R16_2PLANE_420_UNORM = 1000156030,
//     TIF_VK_FORMAT_G16_B16_R16_3PLANE_422_UNORM = 1000156031,
//     TIF_VK_FORMAT_G16_B16R16_2PLANE_422_UNORM = 1000156032,
//     TIF_VK_FORMAT_G16_B16_R16_3PLANE_444_UNORM = 1000156033,
//     TIF_VK_FORMAT_PVRTC1_2BPP_UNORM_BLOCK_IMG = 1000054000,
//     TIF_VK_FORMAT_PVRTC1_4BPP_UNORM_BLOCK_IMG = 1000054001,
//     TIF_VK_FORMAT_PVRTC2_2BPP_UNORM_BLOCK_IMG = 1000054002,
//     TIF_VK_FORMAT_PVRTC2_4BPP_UNORM_BLOCK_IMG = 1000054003,
//     TIF_VK_FORMAT_PVRTC1_2BPP_SRGB_BLOCK_IMG = 1000054004,
//     TIF_VK_FORMAT_PVRTC1_4BPP_SRGB_BLOCK_IMG = 1000054005,
//     TIF_VK_FORMAT_PVRTC2_2BPP_SRGB_BLOCK_IMG = 1000054006,
//     TIF_VK_FORMAT_PVRTC2_4BPP_SRGB_BLOCK_IMG = 1000054007,
// } TinyImageFormat_VkFormat;
// #endif

// static inline TinyImageFormat TinyImageFormat_FromVkFormat(TinyImageFormat_VkFormat fmt)
// {
//     switch (fmt)
//     {
//     case TIF_VK_FORMAT_UNDEFINED:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_VK_FORMAT_R4G4_UNORM_PACK8:
//         return TinyImageFormat_G4R4_UNORM;
//     case TIF_VK_FORMAT_R4G4B4A4_UNORM_PACK16:
//         return TinyImageFormat_A4B4G4R4_UNORM;
//     case TIF_VK_FORMAT_B4G4R4A4_UNORM_PACK16:
//         return TinyImageFormat_A4R4G4B4_UNORM;
//     case TIF_VK_FORMAT_R5G6B5_UNORM_PACK16:
//         return TinyImageFormat_B5G6R5_UNORM;
//     case TIF_VK_FORMAT_B5G6R5_UNORM_PACK16:
//         return TinyImageFormat_R5G6B5_UNORM;
//     case TIF_VK_FORMAT_R5G5B5A1_UNORM_PACK16:
//         return TinyImageFormat_A1B5G5R5_UNORM;
//     case TIF_VK_FORMAT_B5G5R5A1_UNORM_PACK16:
//         return TinyImageFormat_A1R5G5B5_UNORM;
//     case TIF_VK_FORMAT_A1R5G5B5_UNORM_PACK16:
//         return TinyImageFormat_B5G5R5A1_UNORM;
//     case TIF_VK_FORMAT_R8_UNORM:
//         return TinyImageFormat_R8_UNORM;
//     case TIF_VK_FORMAT_R8_SNORM:
//         return TinyImageFormat_R8_SNORM;
//     case TIF_VK_FORMAT_R8_UINT:
//         return TinyImageFormat_R8_UINT;
//     case TIF_VK_FORMAT_R8_SINT:
//         return TinyImageFormat_R8_SINT;
//     case TIF_VK_FORMAT_R8_SRGB:
//         return TinyImageFormat_R8_SRGB;
//     case TIF_VK_FORMAT_R8G8_UNORM:
//         return TinyImageFormat_R8G8_UNORM;
//     case TIF_VK_FORMAT_R8G8_SNORM:
//         return TinyImageFormat_R8G8_SNORM;
//     case TIF_VK_FORMAT_R8G8_UINT:
//         return TinyImageFormat_R8G8_UINT;
//     case TIF_VK_FORMAT_R8G8_SINT:
//         return TinyImageFormat_R8G8_SINT;
//     case TIF_VK_FORMAT_R8G8_SRGB:
//         return TinyImageFormat_R8G8_SRGB;
//     case TIF_VK_FORMAT_R8G8B8_UNORM:
//         return TinyImageFormat_R8G8B8_UNORM;
//     case TIF_VK_FORMAT_R8G8B8_SNORM:
//         return TinyImageFormat_R8G8B8_SNORM;
//     case TIF_VK_FORMAT_R8G8B8_UINT:
//         return TinyImageFormat_R8G8B8_UINT;
//     case TIF_VK_FORMAT_R8G8B8_SINT:
//         return TinyImageFormat_R8G8B8_SINT;
//     case TIF_VK_FORMAT_R8G8B8_SRGB:
//         return TinyImageFormat_R8G8B8_SRGB;
//     case TIF_VK_FORMAT_B8G8R8_UNORM:
//         return TinyImageFormat_B8G8R8_UNORM;
//     case TIF_VK_FORMAT_B8G8R8_SNORM:
//         return TinyImageFormat_B8G8R8_SNORM;
//     case TIF_VK_FORMAT_B8G8R8_UINT:
//         return TinyImageFormat_B8G8R8_UINT;
//     case TIF_VK_FORMAT_B8G8R8_SINT:
//         return TinyImageFormat_B8G8R8_SINT;
//     case TIF_VK_FORMAT_B8G8R8_SRGB:
//         return TinyImageFormat_B8G8R8_SRGB;
//     case TIF_VK_FORMAT_R8G8B8A8_UNORM:
//         return TinyImageFormat_R8G8B8A8_UNORM;
//     case TIF_VK_FORMAT_R8G8B8A8_SNORM:
//         return TinyImageFormat_R8G8B8A8_SNORM;
//     case TIF_VK_FORMAT_R8G8B8A8_UINT:
//         return TinyImageFormat_R8G8B8A8_UINT;
//     case TIF_VK_FORMAT_R8G8B8A8_SINT:
//         return TinyImageFormat_R8G8B8A8_SINT;
//     case TIF_VK_FORMAT_R8G8B8A8_SRGB:
//         return TinyImageFormat_R8G8B8A8_SRGB;
//     case TIF_VK_FORMAT_B8G8R8A8_UNORM:
//         return TinyImageFormat_B8G8R8A8_UNORM;
//     case TIF_VK_FORMAT_B8G8R8A8_SNORM:
//         return TinyImageFormat_B8G8R8A8_SNORM;
//     case TIF_VK_FORMAT_B8G8R8A8_UINT:
//         return TinyImageFormat_B8G8R8A8_UINT;
//     case TIF_VK_FORMAT_B8G8R8A8_SINT:
//         return TinyImageFormat_B8G8R8A8_SINT;
//     case TIF_VK_FORMAT_B8G8R8A8_SRGB:
//         return TinyImageFormat_B8G8R8A8_SRGB;
//     case TIF_VK_FORMAT_A2R10G10B10_UNORM_PACK32:
//         return TinyImageFormat_B10G10R10A2_UNORM;
//     case TIF_VK_FORMAT_A2R10G10B10_UINT_PACK32:
//         return TinyImageFormat_B10G10R10A2_UINT;
//     case TIF_VK_FORMAT_A2B10G10R10_UNORM_PACK32:
//         return TinyImageFormat_R10G10B10A2_UNORM;
//     case TIF_VK_FORMAT_A2B10G10R10_UINT_PACK32:
//         return TinyImageFormat_R10G10B10A2_UINT;
//     case TIF_VK_FORMAT_R16_UNORM:
//         return TinyImageFormat_R16_UNORM;
//     case TIF_VK_FORMAT_R16_SNORM:
//         return TinyImageFormat_R16_SNORM;
//     case TIF_VK_FORMAT_R16_UINT:
//         return TinyImageFormat_R16_UINT;
//     case TIF_VK_FORMAT_R16_SINT:
//         return TinyImageFormat_R16_SINT;
//     case TIF_VK_FORMAT_R16_SFLOAT:
//         return TinyImageFormat_R16_SFLOAT;
//     case TIF_VK_FORMAT_R16G16_UNORM:
//         return TinyImageFormat_R16G16_UNORM;
//     case TIF_VK_FORMAT_R16G16_SNORM:
//         return TinyImageFormat_R16G16_SNORM;
//     case TIF_VK_FORMAT_R16G16_UINT:
//         return TinyImageFormat_R16G16_UINT;
//     case TIF_VK_FORMAT_R16G16_SINT:
//         return TinyImageFormat_R16G16_SINT;
//     case TIF_VK_FORMAT_R16G16_SFLOAT:
//         return TinyImageFormat_R16G16_SFLOAT;
//     case TIF_VK_FORMAT_R16G16B16_UNORM:
//         return TinyImageFormat_R16G16B16_UNORM;
//     case TIF_VK_FORMAT_R16G16B16_SNORM:
//         return TinyImageFormat_R16G16B16_SNORM;
//     case TIF_VK_FORMAT_R16G16B16_UINT:
//         return TinyImageFormat_R16G16B16_UINT;
//     case TIF_VK_FORMAT_R16G16B16_SINT:
//         return TinyImageFormat_R16G16B16_SINT;
//     case TIF_VK_FORMAT_R16G16B16_SFLOAT:
//         return TinyImageFormat_R16G16B16_SFLOAT;
//     case TIF_VK_FORMAT_R16G16B16A16_UNORM:
//         return TinyImageFormat_R16G16B16A16_UNORM;
//     case TIF_VK_FORMAT_R16G16B16A16_SNORM:
//         return TinyImageFormat_R16G16B16A16_SNORM;
//     case TIF_VK_FORMAT_R16G16B16A16_UINT:
//         return TinyImageFormat_R16G16B16A16_UINT;
//     case TIF_VK_FORMAT_R16G16B16A16_SINT:
//         return TinyImageFormat_R16G16B16A16_SINT;
//     case TIF_VK_FORMAT_R16G16B16A16_SFLOAT:
//         return TinyImageFormat_R16G16B16A16_SFLOAT;
//     case TIF_VK_FORMAT_R32_UINT:
//         return TinyImageFormat_R32_UINT;
//     case TIF_VK_FORMAT_R32_SINT:
//         return TinyImageFormat_R32_SINT;
//     case TIF_VK_FORMAT_R32_SFLOAT:
//         return TinyImageFormat_R32_SFLOAT;
//     case TIF_VK_FORMAT_R32G32_UINT:
//         return TinyImageFormat_R32G32_UINT;
//     case TIF_VK_FORMAT_R32G32_SINT:
//         return TinyImageFormat_R32G32_SINT;
//     case TIF_VK_FORMAT_R32G32_SFLOAT:
//         return TinyImageFormat_R32G32_SFLOAT;
//     case TIF_VK_FORMAT_R32G32B32_UINT:
//         return TinyImageFormat_R32G32B32_UINT;
//     case TIF_VK_FORMAT_R32G32B32_SINT:
//         return TinyImageFormat_R32G32B32_SINT;
//     case TIF_VK_FORMAT_R32G32B32_SFLOAT:
//         return TinyImageFormat_R32G32B32_SFLOAT;
//     case TIF_VK_FORMAT_R32G32B32A32_UINT:
//         return TinyImageFormat_R32G32B32A32_UINT;
//     case TIF_VK_FORMAT_R32G32B32A32_SINT:
//         return TinyImageFormat_R32G32B32A32_SINT;
//     case TIF_VK_FORMAT_R32G32B32A32_SFLOAT:
//         return TinyImageFormat_R32G32B32A32_SFLOAT;
//     case TIF_VK_FORMAT_R64_UINT:
//         return TinyImageFormat_R64_UINT;
//     case TIF_VK_FORMAT_R64_SINT:
//         return TinyImageFormat_R64_SINT;
//     case TIF_VK_FORMAT_R64_SFLOAT:
//         return TinyImageFormat_R64_SFLOAT;
//     case TIF_VK_FORMAT_R64G64_UINT:
//         return TinyImageFormat_R64G64_UINT;
//     case TIF_VK_FORMAT_R64G64_SINT:
//         return TinyImageFormat_R64G64_SINT;
//     case TIF_VK_FORMAT_R64G64_SFLOAT:
//         return TinyImageFormat_R64G64_SFLOAT;
//     case TIF_VK_FORMAT_R64G64B64_UINT:
//         return TinyImageFormat_R64G64B64_UINT;
//     case TIF_VK_FORMAT_R64G64B64_SINT:
//         return TinyImageFormat_R64G64B64_SINT;
//     case TIF_VK_FORMAT_R64G64B64_SFLOAT:
//         return TinyImageFormat_R64G64B64_SFLOAT;
//     case TIF_VK_FORMAT_R64G64B64A64_UINT:
//         return TinyImageFormat_R64G64B64A64_UINT;
//     case TIF_VK_FORMAT_R64G64B64A64_SINT:
//         return TinyImageFormat_R64G64B64A64_SINT;
//     case TIF_VK_FORMAT_R64G64B64A64_SFLOAT:
//         return TinyImageFormat_R64G64B64A64_SFLOAT;
//     case TIF_VK_FORMAT_B10G11R11_UFLOAT_PACK32:
//         return TinyImageFormat_B10G11R11_UFLOAT;
//     case TIF_VK_FORMAT_E5B9G9R9_UFLOAT_PACK32:
//         return TinyImageFormat_E5B9G9R9_UFLOAT;
//     case TIF_VK_FORMAT_D16_UNORM:
//         return TinyImageFormat_D16_UNORM;
//     case TIF_VK_FORMAT_X8_D24_UNORM_PACK32:
//         return TinyImageFormat_X8_D24_UNORM;
//     case TIF_VK_FORMAT_D32_SFLOAT:
//         return TinyImageFormat_D32_SFLOAT;
//     case TIF_VK_FORMAT_S8_UINT:
//         return TinyImageFormat_S8_UINT;
//     case TIF_VK_FORMAT_D16_UNORM_S8_UINT:
//         return TinyImageFormat_D16_UNORM_S8_UINT;
//     case TIF_VK_FORMAT_D24_UNORM_S8_UINT:
//         return TinyImageFormat_D24_UNORM_S8_UINT;
//     case TIF_VK_FORMAT_D32_SFLOAT_S8_UINT:
//         return TinyImageFormat_D32_SFLOAT_S8_UINT;
//     case TIF_VK_FORMAT_BC1_RGB_UNORM_BLOCK:
//         return TinyImageFormat_DXBC1_RGB_UNORM;
//     case TIF_VK_FORMAT_BC1_RGB_SRGB_BLOCK:
//         return TinyImageFormat_DXBC1_RGB_SRGB;
//     case TIF_VK_FORMAT_BC1_RGBA_UNORM_BLOCK:
//         return TinyImageFormat_DXBC1_RGBA_UNORM;
//     case TIF_VK_FORMAT_BC1_RGBA_SRGB_BLOCK:
//         return TinyImageFormat_DXBC1_RGBA_SRGB;
//     case TIF_VK_FORMAT_BC2_UNORM_BLOCK:
//         return TinyImageFormat_DXBC2_UNORM;
//     case TIF_VK_FORMAT_BC2_SRGB_BLOCK:
//         return TinyImageFormat_DXBC2_SRGB;
//     case TIF_VK_FORMAT_BC3_UNORM_BLOCK:
//         return TinyImageFormat_DXBC3_UNORM;
//     case TIF_VK_FORMAT_BC3_SRGB_BLOCK:
//         return TinyImageFormat_DXBC3_SRGB;
//     case TIF_VK_FORMAT_BC4_UNORM_BLOCK:
//         return TinyImageFormat_DXBC4_UNORM;
//     case TIF_VK_FORMAT_BC4_SNORM_BLOCK:
//         return TinyImageFormat_DXBC4_SNORM;
//     case TIF_VK_FORMAT_BC5_UNORM_BLOCK:
//         return TinyImageFormat_DXBC5_UNORM;
//     case TIF_VK_FORMAT_BC5_SNORM_BLOCK:
//         return TinyImageFormat_DXBC5_SNORM;
//     case TIF_VK_FORMAT_BC6H_UFLOAT_BLOCK:
//         return TinyImageFormat_DXBC6H_UFLOAT;
//     case TIF_VK_FORMAT_BC6H_SFLOAT_BLOCK:
//         return TinyImageFormat_DXBC6H_SFLOAT;
//     case TIF_VK_FORMAT_BC7_UNORM_BLOCK:
//         return TinyImageFormat_DXBC7_UNORM;
//     case TIF_VK_FORMAT_BC7_SRGB_BLOCK:
//         return TinyImageFormat_DXBC7_SRGB;
//     case TIF_VK_FORMAT_ETC2_R8G8B8_UNORM_BLOCK:
//         return TinyImageFormat_ETC2_R8G8B8_UNORM;
//     case TIF_VK_FORMAT_ETC2_R8G8B8_SRGB_BLOCK:
//         return TinyImageFormat_ETC2_R8G8B8_SRGB;
//     case TIF_VK_FORMAT_ETC2_R8G8B8A1_UNORM_BLOCK:
//         return TinyImageFormat_ETC2_R8G8B8A1_UNORM;
//     case TIF_VK_FORMAT_ETC2_R8G8B8A1_SRGB_BLOCK:
//         return TinyImageFormat_ETC2_R8G8B8A1_SRGB;
//     case TIF_VK_FORMAT_ETC2_R8G8B8A8_UNORM_BLOCK:
//         return TinyImageFormat_ETC2_R8G8B8A8_UNORM;
//     case TIF_VK_FORMAT_ETC2_R8G8B8A8_SRGB_BLOCK:
//         return TinyImageFormat_ETC2_R8G8B8A8_SRGB;
//     case TIF_VK_FORMAT_EAC_R11_UNORM_BLOCK:
//         return TinyImageFormat_ETC2_EAC_R11_UNORM;
//     case TIF_VK_FORMAT_EAC_R11_SNORM_BLOCK:
//         return TinyImageFormat_ETC2_EAC_R11_SNORM;
//     case TIF_VK_FORMAT_EAC_R11G11_UNORM_BLOCK:
//         return TinyImageFormat_ETC2_EAC_R11G11_UNORM;
//     case TIF_VK_FORMAT_EAC_R11G11_SNORM_BLOCK:
//         return TinyImageFormat_ETC2_EAC_R11G11_SNORM;
//     case TIF_VK_FORMAT_ASTC_4x4_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_4x4_UNORM;
//     case TIF_VK_FORMAT_ASTC_4x4_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_4x4_SRGB;
//     case TIF_VK_FORMAT_ASTC_5x4_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_5x4_UNORM;
//     case TIF_VK_FORMAT_ASTC_5x4_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_5x4_SRGB;
//     case TIF_VK_FORMAT_ASTC_5x5_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_5x5_UNORM;
//     case TIF_VK_FORMAT_ASTC_5x5_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_5x5_SRGB;
//     case TIF_VK_FORMAT_ASTC_6x5_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_6x5_UNORM;
//     case TIF_VK_FORMAT_ASTC_6x5_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_6x5_SRGB;
//     case TIF_VK_FORMAT_ASTC_6x6_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_6x6_UNORM;
//     case TIF_VK_FORMAT_ASTC_6x6_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_6x6_SRGB;
//     case TIF_VK_FORMAT_ASTC_8x5_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_8x5_UNORM;
//     case TIF_VK_FORMAT_ASTC_8x5_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_8x5_SRGB;
//     case TIF_VK_FORMAT_ASTC_8x6_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_8x6_UNORM;
//     case TIF_VK_FORMAT_ASTC_8x6_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_8x6_SRGB;
//     case TIF_VK_FORMAT_ASTC_8x8_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_8x8_UNORM;
//     case TIF_VK_FORMAT_ASTC_8x8_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_8x8_SRGB;
//     case TIF_VK_FORMAT_ASTC_10x5_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_10x5_UNORM;
//     case TIF_VK_FORMAT_ASTC_10x5_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_10x5_SRGB;
//     case TIF_VK_FORMAT_ASTC_10x6_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_10x6_UNORM;
//     case TIF_VK_FORMAT_ASTC_10x6_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_10x6_SRGB;
//     case TIF_VK_FORMAT_ASTC_10x8_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_10x8_UNORM;
//     case TIF_VK_FORMAT_ASTC_10x8_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_10x8_SRGB;
//     case TIF_VK_FORMAT_ASTC_10x10_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_10x10_UNORM;
//     case TIF_VK_FORMAT_ASTC_10x10_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_10x10_SRGB;
//     case TIF_VK_FORMAT_ASTC_12x10_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_12x10_UNORM;
//     case TIF_VK_FORMAT_ASTC_12x10_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_12x10_SRGB;
//     case TIF_VK_FORMAT_ASTC_12x12_UNORM_BLOCK:
//         return TinyImageFormat_ASTC_12x12_UNORM;
//     case TIF_VK_FORMAT_ASTC_12x12_SRGB_BLOCK:
//         return TinyImageFormat_ASTC_12x12_SRGB;

//     case TIF_VK_FORMAT_PVRTC1_2BPP_UNORM_BLOCK_IMG:
//         return TinyImageFormat_PVRTC1_2BPP_UNORM;
//     case TIF_VK_FORMAT_PVRTC1_4BPP_UNORM_BLOCK_IMG:
//         return TinyImageFormat_PVRTC1_4BPP_UNORM;
//     case TIF_VK_FORMAT_PVRTC1_2BPP_SRGB_BLOCK_IMG:
//         return TinyImageFormat_PVRTC1_2BPP_SRGB;
//     case TIF_VK_FORMAT_PVRTC1_4BPP_SRGB_BLOCK_IMG:
//         return TinyImageFormat_PVRTC1_4BPP_SRGB;

//     case TIF_VK_FORMAT_G16B16G16R16_422_UNORM:
//         return TinyImageFormat_G16B16G16R16_422_UNORM;
//     case TIF_VK_FORMAT_B16G16R16G16_422_UNORM:
//         return TinyImageFormat_B16G16R16G16_422_UNORM;
//     case TIF_VK_FORMAT_R12X4G12X4B12X4A12X4_UNORM_4PACK16:
//         return TinyImageFormat_R12X4G12X4B12X4A12X4_UNORM_4PACK16;
//     case TIF_VK_FORMAT_G12X4B12X4G12X4R12X4_422_UNORM_4PACK16:
//         return TinyImageFormat_G12X4B12X4G12X4R12X4_422_UNORM_4PACK16;
//     case TIF_VK_FORMAT_B12X4G12X4R12X4G12X4_422_UNORM_4PACK16:
//         return TinyImageFormat_B12X4G12X4R12X4G12X4_422_UNORM_4PACK16;
//     case TIF_VK_FORMAT_R10X6G10X6B10X6A10X6_UNORM_4PACK16:
//         return TinyImageFormat_R10X6G10X6B10X6A10X6_UNORM_4PACK16;
//     case TIF_VK_FORMAT_G10X6B10X6G10X6R10X6_422_UNORM_4PACK16:
//         return TinyImageFormat_G10X6B10X6G10X6R10X6_422_UNORM_4PACK16;
//     case TIF_VK_FORMAT_B10X6G10X6R10X6G10X6_422_UNORM_4PACK16:
//         return TinyImageFormat_B10X6G10X6R10X6G10X6_422_UNORM_4PACK16;
//     case TIF_VK_FORMAT_G8B8G8R8_422_UNORM:
//         return TinyImageFormat_G8B8G8R8_422_UNORM;
//     case TIF_VK_FORMAT_B8G8R8G8_422_UNORM:
//         return TinyImageFormat_B8G8R8G8_422_UNORM;
//     case TIF_VK_FORMAT_G8_B8_R8_3PLANE_420_UNORM:
//         return TinyImageFormat_G8_B8_R8_3PLANE_420_UNORM;
//     case TIF_VK_FORMAT_G8_B8R8_2PLANE_420_UNORM:
//         return TinyImageFormat_G8_B8R8_2PLANE_420_UNORM;
//     case TIF_VK_FORMAT_G8_B8_R8_3PLANE_422_UNORM:
//         return TinyImageFormat_G8_B8_R8_3PLANE_422_UNORM;
//     case TIF_VK_FORMAT_G8_B8R8_2PLANE_422_UNORM:
//         return TinyImageFormat_G8_B8R8_2PLANE_422_UNORM;
//     case TIF_VK_FORMAT_G8_B8_R8_3PLANE_444_UNORM:
//         return TinyImageFormat_G8_B8_R8_3PLANE_444_UNORM;
//     case TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_420_UNORM_3PACK16:
//         return TinyImageFormat_G10X6_B10X6_R10X6_3PLANE_420_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_422_UNORM_3PACK16:
//         return TinyImageFormat_G10X6_B10X6_R10X6_3PLANE_422_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_444_UNORM_3PACK16:
//         return TinyImageFormat_G10X6_B10X6_R10X6_3PLANE_444_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16:
//         return TinyImageFormat_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G10X6_B10X6R10X6_2PLANE_422_UNORM_3PACK16:
//         return TinyImageFormat_G10X6_B10X6R10X6_2PLANE_422_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_420_UNORM_3PACK16:
//         return TinyImageFormat_G12X4_B12X4_R12X4_3PLANE_420_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_422_UNORM_3PACK16:
//         return TinyImageFormat_G12X4_B12X4_R12X4_3PLANE_422_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_444_UNORM_3PACK16:
//         return TinyImageFormat_G12X4_B12X4_R12X4_3PLANE_444_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G12X4_B12X4R12X4_2PLANE_420_UNORM_3PACK16:
//         return TinyImageFormat_G12X4_B12X4R12X4_2PLANE_420_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G12X4_B12X4R12X4_2PLANE_422_UNORM_3PACK16:
//         return TinyImageFormat_G12X4_B12X4R12X4_2PLANE_422_UNORM_3PACK16;
//     case TIF_VK_FORMAT_G16_B16_R16_3PLANE_420_UNORM:
//         return TinyImageFormat_G16_B16_R16_3PLANE_420_UNORM;
//     case TIF_VK_FORMAT_G16_B16_R16_3PLANE_422_UNORM:
//         return TinyImageFormat_G16_B16_R16_3PLANE_422_UNORM;
//     case TIF_VK_FORMAT_G16_B16_R16_3PLANE_444_UNORM:
//         return TinyImageFormat_G16_B16_R16_3PLANE_444_UNORM;
//     case TIF_VK_FORMAT_G16_B16R16_2PLANE_420_UNORM:
//         return TinyImageFormat_G16_B16R16_2PLANE_420_UNORM;
//     case TIF_VK_FORMAT_G16_B16R16_2PLANE_422_UNORM:
//         return TinyImageFormat_G16_B16R16_2PLANE_422_UNORM;

//     case TIF_VK_FORMAT_A8B8G8R8_UNORM_PACK32:
//     case TIF_VK_FORMAT_R8_USCALED:
//     case TIF_VK_FORMAT_R8_SSCALED:
//     case TIF_VK_FORMAT_R8G8_USCALED:
//     case TIF_VK_FORMAT_R8G8_SSCALED:
//     case TIF_VK_FORMAT_R8G8B8_USCALED:
//     case TIF_VK_FORMAT_R8G8B8_SSCALED:
//     case TIF_VK_FORMAT_B8G8R8_USCALED:
//     case TIF_VK_FORMAT_B8G8R8_SSCALED:
//     case TIF_VK_FORMAT_R8G8B8A8_USCALED:
//     case TIF_VK_FORMAT_R8G8B8A8_SSCALED:
//     case TIF_VK_FORMAT_B8G8R8A8_USCALED:
//     case TIF_VK_FORMAT_B8G8R8A8_SSCALED:
//     case TIF_VK_FORMAT_A8B8G8R8_SNORM_PACK32:
//     case TIF_VK_FORMAT_A8B8G8R8_USCALED_PACK32:
//     case TIF_VK_FORMAT_A8B8G8R8_SSCALED_PACK32:
//     case TIF_VK_FORMAT_A8B8G8R8_UINT_PACK32:
//     case TIF_VK_FORMAT_A8B8G8R8_SINT_PACK32:
//     case TIF_VK_FORMAT_A8B8G8R8_SRGB_PACK32:
//     case TIF_VK_FORMAT_A2R10G10B10_SNORM_PACK32:
//     case TIF_VK_FORMAT_A2R10G10B10_USCALED_PACK32:
//     case TIF_VK_FORMAT_A2R10G10B10_SSCALED_PACK32:
//     case TIF_VK_FORMAT_A2R10G10B10_SINT_PACK32:
//     case TIF_VK_FORMAT_A2B10G10R10_SNORM_PACK32:
//     case TIF_VK_FORMAT_A2B10G10R10_USCALED_PACK32:
//     case TIF_VK_FORMAT_A2B10G10R10_SSCALED_PACK32:
//     case TIF_VK_FORMAT_A2B10G10R10_SINT_PACK32:
//     case TIF_VK_FORMAT_R16_USCALED:
//     case TIF_VK_FORMAT_R16_SSCALED:
//     case TIF_VK_FORMAT_R16G16_USCALED:
//     case TIF_VK_FORMAT_R16G16_SSCALED:
//     case TIF_VK_FORMAT_R16G16B16_USCALED:
//     case TIF_VK_FORMAT_R16G16B16_SSCALED:
//     case TIF_VK_FORMAT_R16G16B16A16_USCALED:
//     case TIF_VK_FORMAT_R16G16B16A16_SSCALED:
//     case TIF_VK_FORMAT_R10X6_UNORM_PACK16:
//     case TIF_VK_FORMAT_R10X6G10X6_UNORM_2PACK16:
//     case TIF_VK_FORMAT_R12X4_UNORM_PACK16:
//     case TIF_VK_FORMAT_R12X4G12X4_UNORM_2PACK16:
//     case TIF_VK_FORMAT_PVRTC2_2BPP_UNORM_BLOCK_IMG:
//     case TIF_VK_FORMAT_PVRTC2_4BPP_UNORM_BLOCK_IMG:
//     case TIF_VK_FORMAT_PVRTC2_2BPP_SRGB_BLOCK_IMG:
//     case TIF_VK_FORMAT_PVRTC2_4BPP_SRGB_BLOCK_IMG:
//         return TinyImageFormat_UNDEFINED;
//     }
//     return TinyImageFormat_UNDEFINED;
// }

// static inline TinyImageFormat_VkFormat TinyImageFormat_ToVkFormat(TinyImageFormat fmt)
// {
//     switch (fmt)
//     {
//     case TinyImageFormat_UNDEFINED:
//         return TIF_VK_FORMAT_UNDEFINED;
//     case TinyImageFormat_G4R4_UNORM:
//         return TIF_VK_FORMAT_R4G4_UNORM_PACK8;
//     case TinyImageFormat_A4B4G4R4_UNORM:
//         return TIF_VK_FORMAT_R4G4B4A4_UNORM_PACK16;
//     case TinyImageFormat_A4R4G4B4_UNORM:
//         return TIF_VK_FORMAT_B4G4R4A4_UNORM_PACK16;
//     case TinyImageFormat_R5G6B5_UNORM:
//         return TIF_VK_FORMAT_B5G6R5_UNORM_PACK16;
//     case TinyImageFormat_B5G6R5_UNORM:
//         return TIF_VK_FORMAT_R5G6B5_UNORM_PACK16;
//     case TinyImageFormat_A1B5G5R5_UNORM:
//         return TIF_VK_FORMAT_R5G5B5A1_UNORM_PACK16;
//     case TinyImageFormat_A1R5G5B5_UNORM:
//         return TIF_VK_FORMAT_B5G5R5A1_UNORM_PACK16;
//     case TinyImageFormat_B5G5R5A1_UNORM:
//         return TIF_VK_FORMAT_A1R5G5B5_UNORM_PACK16;
//     case TinyImageFormat_A2B10G10R10_UNORM:
//         return TIF_VK_FORMAT_A2B10G10R10_UNORM_PACK32;

//     case TinyImageFormat_R8_UNORM:
//         return TIF_VK_FORMAT_R8_UNORM;
//     case TinyImageFormat_R8_SNORM:
//         return TIF_VK_FORMAT_R8_SNORM;
//     case TinyImageFormat_R8_UINT:
//         return TIF_VK_FORMAT_R8_UINT;
//     case TinyImageFormat_R8_SINT:
//         return TIF_VK_FORMAT_R8_SINT;
//     case TinyImageFormat_R8_SRGB:
//         return TIF_VK_FORMAT_R8_SRGB;
//     case TinyImageFormat_R8G8_UNORM:
//         return TIF_VK_FORMAT_R8G8_UNORM;
//     case TinyImageFormat_R8G8_SNORM:
//         return TIF_VK_FORMAT_R8G8_SNORM;
//     case TinyImageFormat_R8G8_UINT:
//         return TIF_VK_FORMAT_R8G8_UINT;
//     case TinyImageFormat_R8G8_SINT:
//         return TIF_VK_FORMAT_R8G8_SINT;
//     case TinyImageFormat_R8G8_SRGB:
//         return TIF_VK_FORMAT_R8G8_SRGB;
//     case TinyImageFormat_R8G8B8_UNORM:
//         return TIF_VK_FORMAT_R8G8B8_UNORM;
//     case TinyImageFormat_R8G8B8_SNORM:
//         return TIF_VK_FORMAT_R8G8B8_SNORM;
//     case TinyImageFormat_R8G8B8_UINT:
//         return TIF_VK_FORMAT_R8G8B8_UINT;
//     case TinyImageFormat_R8G8B8_SINT:
//         return TIF_VK_FORMAT_R8G8B8_SINT;
//     case TinyImageFormat_R8G8B8_SRGB:
//         return TIF_VK_FORMAT_R8G8B8_SRGB;
//     case TinyImageFormat_B8G8R8_UNORM:
//         return TIF_VK_FORMAT_B8G8R8_UNORM;
//     case TinyImageFormat_B8G8R8_SNORM:
//         return TIF_VK_FORMAT_B8G8R8_SNORM;
//     case TinyImageFormat_B8G8R8_UINT:
//         return TIF_VK_FORMAT_B8G8R8_UINT;
//     case TinyImageFormat_B8G8R8_SINT:
//         return TIF_VK_FORMAT_B8G8R8_SINT;
//     case TinyImageFormat_B8G8R8_SRGB:
//         return TIF_VK_FORMAT_B8G8R8_SRGB;
//     case TinyImageFormat_R8G8B8A8_UNORM:
//         return TIF_VK_FORMAT_R8G8B8A8_UNORM;
//     case TinyImageFormat_R8G8B8A8_SNORM:
//         return TIF_VK_FORMAT_R8G8B8A8_SNORM;
//     case TinyImageFormat_R8G8B8A8_UINT:
//         return TIF_VK_FORMAT_R8G8B8A8_UINT;
//     case TinyImageFormat_R8G8B8A8_SINT:
//         return TIF_VK_FORMAT_R8G8B8A8_SINT;
//     case TinyImageFormat_R8G8B8A8_SRGB:
//         return TIF_VK_FORMAT_R8G8B8A8_SRGB;
//     case TinyImageFormat_B8G8R8A8_UNORM:
//         return TIF_VK_FORMAT_B8G8R8A8_UNORM;
//     case TinyImageFormat_B8G8R8A8_SNORM:
//         return TIF_VK_FORMAT_B8G8R8A8_SNORM;
//     case TinyImageFormat_B8G8R8A8_UINT:
//         return TIF_VK_FORMAT_B8G8R8A8_UINT;
//     case TinyImageFormat_B8G8R8A8_SINT:
//         return TIF_VK_FORMAT_B8G8R8A8_SINT;
//     case TinyImageFormat_B8G8R8A8_SRGB:
//         return TIF_VK_FORMAT_B8G8R8A8_SRGB;
//     case TinyImageFormat_R16_UNORM:
//         return TIF_VK_FORMAT_R16_UNORM;
//     case TinyImageFormat_R16_SNORM:
//         return TIF_VK_FORMAT_R16_SNORM;
//     case TinyImageFormat_R16_UINT:
//         return TIF_VK_FORMAT_R16_UINT;
//     case TinyImageFormat_R16_SINT:
//         return TIF_VK_FORMAT_R16_SINT;
//     case TinyImageFormat_R16_SFLOAT:
//         return TIF_VK_FORMAT_R16_SFLOAT;
//     case TinyImageFormat_R16G16_UNORM:
//         return TIF_VK_FORMAT_R16G16_UNORM;
//     case TinyImageFormat_R16G16_SNORM:
//         return TIF_VK_FORMAT_R16G16_SNORM;
//     case TinyImageFormat_R16G16_UINT:
//         return TIF_VK_FORMAT_R16G16_UINT;
//     case TinyImageFormat_R16G16_SINT:
//         return TIF_VK_FORMAT_R16G16_SINT;
//     case TinyImageFormat_R16G16_SFLOAT:
//         return TIF_VK_FORMAT_R16G16_SFLOAT;
//     case TinyImageFormat_R16G16B16_UNORM:
//         return TIF_VK_FORMAT_R16G16B16_UNORM;
//     case TinyImageFormat_R16G16B16_SNORM:
//         return TIF_VK_FORMAT_R16G16B16_SNORM;
//     case TinyImageFormat_R16G16B16_UINT:
//         return TIF_VK_FORMAT_R16G16B16_UINT;
//     case TinyImageFormat_R16G16B16_SINT:
//         return TIF_VK_FORMAT_R16G16B16_SINT;
//     case TinyImageFormat_R16G16B16_SFLOAT:
//         return TIF_VK_FORMAT_R16G16B16_SFLOAT;
//     case TinyImageFormat_R16G16B16A16_UNORM:
//         return TIF_VK_FORMAT_R16G16B16A16_UNORM;
//     case TinyImageFormat_R16G16B16A16_SNORM:
//         return TIF_VK_FORMAT_R16G16B16A16_SNORM;
//     case TinyImageFormat_R16G16B16A16_UINT:
//         return TIF_VK_FORMAT_R16G16B16A16_UINT;
//     case TinyImageFormat_R16G16B16A16_SINT:
//         return TIF_VK_FORMAT_R16G16B16A16_SINT;
//     case TinyImageFormat_R16G16B16A16_SFLOAT:
//         return TIF_VK_FORMAT_R16G16B16A16_SFLOAT;
//     case TinyImageFormat_R32_UINT:
//         return TIF_VK_FORMAT_R32_UINT;
//     case TinyImageFormat_R32_SINT:
//         return TIF_VK_FORMAT_R32_SINT;
//     case TinyImageFormat_R32_SFLOAT:
//         return TIF_VK_FORMAT_R32_SFLOAT;
//     case TinyImageFormat_R32G32_UINT:
//         return TIF_VK_FORMAT_R32G32_UINT;
//     case TinyImageFormat_R32G32_SINT:
//         return TIF_VK_FORMAT_R32G32_SINT;
//     case TinyImageFormat_R32G32_SFLOAT:
//         return TIF_VK_FORMAT_R32G32_SFLOAT;
//     case TinyImageFormat_R32G32B32_UINT:
//         return TIF_VK_FORMAT_R32G32B32_UINT;
//     case TinyImageFormat_R32G32B32_SINT:
//         return TIF_VK_FORMAT_R32G32B32_SINT;
//     case TinyImageFormat_R32G32B32_SFLOAT:
//         return TIF_VK_FORMAT_R32G32B32_SFLOAT;
//     case TinyImageFormat_R32G32B32A32_UINT:
//         return TIF_VK_FORMAT_R32G32B32A32_UINT;
//     case TinyImageFormat_R32G32B32A32_SINT:
//         return TIF_VK_FORMAT_R32G32B32A32_SINT;
//     case TinyImageFormat_R32G32B32A32_SFLOAT:
//         return TIF_VK_FORMAT_R32G32B32A32_SFLOAT;
//     case TinyImageFormat_R64_UINT:
//         return TIF_VK_FORMAT_R64_UINT;
//     case TinyImageFormat_R64_SINT:
//         return TIF_VK_FORMAT_R64_SINT;
//     case TinyImageFormat_R64_SFLOAT:
//         return TIF_VK_FORMAT_R64_SFLOAT;
//     case TinyImageFormat_R64G64_UINT:
//         return TIF_VK_FORMAT_R64G64_UINT;
//     case TinyImageFormat_R64G64_SINT:
//         return TIF_VK_FORMAT_R64G64_SINT;
//     case TinyImageFormat_R64G64_SFLOAT:
//         return TIF_VK_FORMAT_R64G64_SFLOAT;
//     case TinyImageFormat_R64G64B64_UINT:
//         return TIF_VK_FORMAT_R64G64B64_UINT;
//     case TinyImageFormat_R64G64B64_SINT:
//         return TIF_VK_FORMAT_R64G64B64_SINT;
//     case TinyImageFormat_R64G64B64_SFLOAT:
//         return TIF_VK_FORMAT_R64G64B64_SFLOAT;
//     case TinyImageFormat_R64G64B64A64_UINT:
//         return TIF_VK_FORMAT_R64G64B64A64_UINT;
//     case TinyImageFormat_R64G64B64A64_SINT:
//         return TIF_VK_FORMAT_R64G64B64A64_SINT;
//     case TinyImageFormat_R64G64B64A64_SFLOAT:
//         return TIF_VK_FORMAT_R64G64B64A64_SFLOAT;

//     case TinyImageFormat_B10G10R10A2_UNORM:
//         return TIF_VK_FORMAT_A2R10G10B10_UNORM_PACK32;
//     case TinyImageFormat_B10G10R10A2_UINT:
//         return TIF_VK_FORMAT_A2R10G10B10_UINT_PACK32;
//     case TinyImageFormat_R10G10B10A2_UNORM:
//         return TIF_VK_FORMAT_A2B10G10R10_UNORM_PACK32;
//     case TinyImageFormat_R10G10B10A2_UINT:
//         return TIF_VK_FORMAT_A2B10G10R10_UINT_PACK32;

//     case TinyImageFormat_B10G11R11_UFLOAT:
//         return TIF_VK_FORMAT_B10G11R11_UFLOAT_PACK32;
//     case TinyImageFormat_E5B9G9R9_UFLOAT:
//         return TIF_VK_FORMAT_E5B9G9R9_UFLOAT_PACK32;

//     case TinyImageFormat_G16B16G16R16_422_UNORM:
//         return TIF_VK_FORMAT_G16B16G16R16_422_UNORM;
//     case TinyImageFormat_B16G16R16G16_422_UNORM:
//         return TIF_VK_FORMAT_B16G16R16G16_422_UNORM;
//     case TinyImageFormat_R12X4G12X4B12X4A12X4_UNORM_4PACK16:
//         return TIF_VK_FORMAT_R12X4G12X4B12X4A12X4_UNORM_4PACK16;
//     case TinyImageFormat_G12X4B12X4G12X4R12X4_422_UNORM_4PACK16:
//         return TIF_VK_FORMAT_G12X4B12X4G12X4R12X4_422_UNORM_4PACK16;
//     case TinyImageFormat_B12X4G12X4R12X4G12X4_422_UNORM_4PACK16:
//         return TIF_VK_FORMAT_B12X4G12X4R12X4G12X4_422_UNORM_4PACK16;
//     case TinyImageFormat_R10X6G10X6B10X6A10X6_UNORM_4PACK16:
//         return TIF_VK_FORMAT_R10X6G10X6B10X6A10X6_UNORM_4PACK16;
//     case TinyImageFormat_G10X6B10X6G10X6R10X6_422_UNORM_4PACK16:
//         return TIF_VK_FORMAT_G10X6B10X6G10X6R10X6_422_UNORM_4PACK16;
//     case TinyImageFormat_B10X6G10X6R10X6G10X6_422_UNORM_4PACK16:
//         return TIF_VK_FORMAT_B10X6G10X6R10X6G10X6_422_UNORM_4PACK16;
//     case TinyImageFormat_G8B8G8R8_422_UNORM:
//         return TIF_VK_FORMAT_G8B8G8R8_422_UNORM;
//     case TinyImageFormat_B8G8R8G8_422_UNORM:
//         return TIF_VK_FORMAT_B8G8R8G8_422_UNORM;
//     case TinyImageFormat_G8_B8_R8_3PLANE_420_UNORM:
//         return TIF_VK_FORMAT_G8_B8_R8_3PLANE_420_UNORM;
//     case TinyImageFormat_G8_B8R8_2PLANE_420_UNORM:
//         return TIF_VK_FORMAT_G8_B8R8_2PLANE_420_UNORM;
//     case TinyImageFormat_G8_B8_R8_3PLANE_422_UNORM:
//         return TIF_VK_FORMAT_G8_B8_R8_3PLANE_422_UNORM;
//     case TinyImageFormat_G8_B8R8_2PLANE_422_UNORM:
//         return TIF_VK_FORMAT_G8_B8R8_2PLANE_422_UNORM;
//     case TinyImageFormat_G8_B8_R8_3PLANE_444_UNORM:
//         return TIF_VK_FORMAT_G8_B8_R8_3PLANE_444_UNORM;
//     case TinyImageFormat_G10X6_B10X6_R10X6_3PLANE_420_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_420_UNORM_3PACK16;
//     case TinyImageFormat_G10X6_B10X6_R10X6_3PLANE_422_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_422_UNORM_3PACK16;
//     case TinyImageFormat_G10X6_B10X6_R10X6_3PLANE_444_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G10X6_B10X6_R10X6_3PLANE_444_UNORM_3PACK16;
//     case TinyImageFormat_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16;
//     case TinyImageFormat_G10X6_B10X6R10X6_2PLANE_422_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G10X6_B10X6R10X6_2PLANE_422_UNORM_3PACK16;
//     case TinyImageFormat_G12X4_B12X4_R12X4_3PLANE_420_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_420_UNORM_3PACK16;
//     case TinyImageFormat_G12X4_B12X4_R12X4_3PLANE_422_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_422_UNORM_3PACK16;
//     case TinyImageFormat_G12X4_B12X4_R12X4_3PLANE_444_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G12X4_B12X4_R12X4_3PLANE_444_UNORM_3PACK16;
//     case TinyImageFormat_G12X4_B12X4R12X4_2PLANE_420_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G12X4_B12X4R12X4_2PLANE_420_UNORM_3PACK16;
//     case TinyImageFormat_G12X4_B12X4R12X4_2PLANE_422_UNORM_3PACK16:
//         return TIF_VK_FORMAT_G12X4_B12X4R12X4_2PLANE_422_UNORM_3PACK16;
//     case TinyImageFormat_G16_B16_R16_3PLANE_420_UNORM:
//         return TIF_VK_FORMAT_G16_B16_R16_3PLANE_420_UNORM;
//     case TinyImageFormat_G16_B16_R16_3PLANE_422_UNORM:
//         return TIF_VK_FORMAT_G16_B16_R16_3PLANE_422_UNORM;
//     case TinyImageFormat_G16_B16_R16_3PLANE_444_UNORM:
//         return TIF_VK_FORMAT_G16_B16_R16_3PLANE_444_UNORM;
//     case TinyImageFormat_G16_B16R16_2PLANE_420_UNORM:
//         return TIF_VK_FORMAT_G16_B16R16_2PLANE_420_UNORM;
//     case TinyImageFormat_G16_B16R16_2PLANE_422_UNORM:
//         return TIF_VK_FORMAT_G16_B16R16_2PLANE_422_UNORM;

//     case TinyImageFormat_D16_UNORM:
//         return TIF_VK_FORMAT_D16_UNORM;
//     case TinyImageFormat_X8_D24_UNORM:
//         return TIF_VK_FORMAT_X8_D24_UNORM_PACK32;
//     case TinyImageFormat_D32_SFLOAT:
//         return TIF_VK_FORMAT_D32_SFLOAT;
//     case TinyImageFormat_S8_UINT:
//         return TIF_VK_FORMAT_S8_UINT;
//     case TinyImageFormat_D16_UNORM_S8_UINT:
//         return TIF_VK_FORMAT_D16_UNORM_S8_UINT;
//     case TinyImageFormat_D24_UNORM_S8_UINT:
//         return TIF_VK_FORMAT_D24_UNORM_S8_UINT;
//     case TinyImageFormat_D32_SFLOAT_S8_UINT:
//         return TIF_VK_FORMAT_D32_SFLOAT_S8_UINT;
//     case TinyImageFormat_DXBC1_RGB_UNORM:
//         return TIF_VK_FORMAT_BC1_RGB_UNORM_BLOCK;
//     case TinyImageFormat_DXBC1_RGB_SRGB:
//         return TIF_VK_FORMAT_BC1_RGB_SRGB_BLOCK;
//     case TinyImageFormat_DXBC1_RGBA_UNORM:
//         return TIF_VK_FORMAT_BC1_RGBA_UNORM_BLOCK;
//     case TinyImageFormat_DXBC1_RGBA_SRGB:
//         return TIF_VK_FORMAT_BC1_RGBA_SRGB_BLOCK;
//     case TinyImageFormat_DXBC2_UNORM:
//         return TIF_VK_FORMAT_BC2_UNORM_BLOCK;
//     case TinyImageFormat_DXBC2_SRGB:
//         return TIF_VK_FORMAT_BC2_SRGB_BLOCK;
//     case TinyImageFormat_DXBC3_UNORM:
//         return TIF_VK_FORMAT_BC3_UNORM_BLOCK;
//     case TinyImageFormat_DXBC3_SRGB:
//         return TIF_VK_FORMAT_BC3_SRGB_BLOCK;
//     case TinyImageFormat_DXBC4_UNORM:
//         return TIF_VK_FORMAT_BC4_UNORM_BLOCK;
//     case TinyImageFormat_DXBC4_SNORM:
//         return TIF_VK_FORMAT_BC4_SNORM_BLOCK;
//     case TinyImageFormat_DXBC5_UNORM:
//         return TIF_VK_FORMAT_BC5_UNORM_BLOCK;
//     case TinyImageFormat_DXBC5_SNORM:
//         return TIF_VK_FORMAT_BC5_SNORM_BLOCK;
//     case TinyImageFormat_DXBC6H_UFLOAT:
//         return TIF_VK_FORMAT_BC6H_UFLOAT_BLOCK;
//     case TinyImageFormat_DXBC6H_SFLOAT:
//         return TIF_VK_FORMAT_BC6H_SFLOAT_BLOCK;
//     case TinyImageFormat_DXBC7_UNORM:
//         return TIF_VK_FORMAT_BC7_UNORM_BLOCK;
//     case TinyImageFormat_DXBC7_SRGB:
//         return TIF_VK_FORMAT_BC7_SRGB_BLOCK;
//     case TinyImageFormat_PVRTC1_2BPP_UNORM:
//         return TIF_VK_FORMAT_PVRTC1_2BPP_UNORM_BLOCK_IMG;
//     case TinyImageFormat_PVRTC1_4BPP_UNORM:
//         return TIF_VK_FORMAT_PVRTC1_4BPP_UNORM_BLOCK_IMG;
//     case TinyImageFormat_PVRTC1_2BPP_SRGB:
//         return TIF_VK_FORMAT_PVRTC1_2BPP_SRGB_BLOCK_IMG;
//     case TinyImageFormat_PVRTC1_4BPP_SRGB:
//         return TIF_VK_FORMAT_PVRTC1_4BPP_SRGB_BLOCK_IMG;
//     case TinyImageFormat_ETC2_R8G8B8_UNORM:
//         return TIF_VK_FORMAT_ETC2_R8G8B8_UNORM_BLOCK;
//     case TinyImageFormat_ETC2_R8G8B8A1_UNORM:
//         return TIF_VK_FORMAT_ETC2_R8G8B8A1_UNORM_BLOCK;
//     case TinyImageFormat_ETC2_R8G8B8A8_UNORM:
//         return TIF_VK_FORMAT_ETC2_R8G8B8A8_UNORM_BLOCK;
//     case TinyImageFormat_ETC2_R8G8B8_SRGB:
//         return TIF_VK_FORMAT_ETC2_R8G8B8_SRGB_BLOCK;
//     case TinyImageFormat_ETC2_R8G8B8A1_SRGB:
//         return TIF_VK_FORMAT_ETC2_R8G8B8A1_SRGB_BLOCK;
//     case TinyImageFormat_ETC2_R8G8B8A8_SRGB:
//         return TIF_VK_FORMAT_ETC2_R8G8B8A8_SRGB_BLOCK;
//     case TinyImageFormat_ETC2_EAC_R11_UNORM:
//         return TIF_VK_FORMAT_EAC_R11_UNORM_BLOCK;
//     case TinyImageFormat_ETC2_EAC_R11G11_UNORM:
//         return TIF_VK_FORMAT_EAC_R11G11_UNORM_BLOCK;
//     case TinyImageFormat_ETC2_EAC_R11_SNORM:
//         return TIF_VK_FORMAT_EAC_R11_SNORM_BLOCK;
//     case TinyImageFormat_ETC2_EAC_R11G11_SNORM:
//         return TIF_VK_FORMAT_EAC_R11G11_SNORM_BLOCK;
//     case TinyImageFormat_ASTC_4x4_UNORM:
//         return TIF_VK_FORMAT_ASTC_4x4_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_4x4_SRGB:
//         return TIF_VK_FORMAT_ASTC_4x4_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_5x4_UNORM:
//         return TIF_VK_FORMAT_ASTC_5x4_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_5x4_SRGB:
//         return TIF_VK_FORMAT_ASTC_5x4_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_5x5_UNORM:
//         return TIF_VK_FORMAT_ASTC_5x5_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_5x5_SRGB:
//         return TIF_VK_FORMAT_ASTC_5x5_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_6x5_UNORM:
//         return TIF_VK_FORMAT_ASTC_6x5_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_6x5_SRGB:
//         return TIF_VK_FORMAT_ASTC_6x5_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_6x6_UNORM:
//         return TIF_VK_FORMAT_ASTC_6x6_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_6x6_SRGB:
//         return TIF_VK_FORMAT_ASTC_6x6_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_8x5_UNORM:
//         return TIF_VK_FORMAT_ASTC_8x5_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_8x5_SRGB:
//         return TIF_VK_FORMAT_ASTC_8x5_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_8x6_UNORM:
//         return TIF_VK_FORMAT_ASTC_8x6_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_8x6_SRGB:
//         return TIF_VK_FORMAT_ASTC_8x6_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_8x8_UNORM:
//         return TIF_VK_FORMAT_ASTC_8x8_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_8x8_SRGB:
//         return TIF_VK_FORMAT_ASTC_8x8_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_10x5_UNORM:
//         return TIF_VK_FORMAT_ASTC_10x5_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_10x5_SRGB:
//         return TIF_VK_FORMAT_ASTC_10x5_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_10x6_UNORM:
//         return TIF_VK_FORMAT_ASTC_10x6_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_10x6_SRGB:
//         return TIF_VK_FORMAT_ASTC_10x6_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_10x8_UNORM:
//         return TIF_VK_FORMAT_ASTC_10x8_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_10x8_SRGB:
//         return TIF_VK_FORMAT_ASTC_10x8_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_10x10_UNORM:
//         return TIF_VK_FORMAT_ASTC_10x10_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_10x10_SRGB:
//         return TIF_VK_FORMAT_ASTC_10x10_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_12x10_UNORM:
//         return TIF_VK_FORMAT_ASTC_12x10_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_12x10_SRGB:
//         return TIF_VK_FORMAT_ASTC_12x10_SRGB_BLOCK;
//     case TinyImageFormat_ASTC_12x12_UNORM:
//         return TIF_VK_FORMAT_ASTC_12x12_UNORM_BLOCK;
//     case TinyImageFormat_ASTC_12x12_SRGB:
//         return TIF_VK_FORMAT_ASTC_12x12_SRGB_BLOCK;
//     case TinyImageFormat_PVRTC2_2BPP_UNORM:
//         return TIF_VK_FORMAT_PVRTC2_2BPP_UNORM_BLOCK_IMG;
//     case TinyImageFormat_PVRTC2_4BPP_UNORM:
//         return TIF_VK_FORMAT_PVRTC2_4BPP_UNORM_BLOCK_IMG;
//     case TinyImageFormat_PVRTC2_2BPP_SRGB:
//         return TIF_VK_FORMAT_PVRTC2_2BPP_SRGB_BLOCK_IMG;
//     case TinyImageFormat_PVRTC2_4BPP_SRGB:
//         return TIF_VK_FORMAT_PVRTC2_4BPP_SRGB_BLOCK_IMG;

//     default:
//         return TIF_VK_FORMAT_UNDEFINED;
//     }
// }

// #ifndef TINYIMAGEFORMAT_DXGIFORMAT
// #define TINYIMAGEFORMAT_DXGIFORMAT

// typedef enum TinyImageFormat_DXGI_FORMAT
// {

//     TIF_DXGI_FORMAT_UNKNOWN = 0,
//     TIF_DXGI_FORMAT_R32G32B32A32_TYPELESS = 1,
//     TIF_DXGI_FORMAT_R32G32B32A32_FLOAT = 2,
//     TIF_DXGI_FORMAT_R32G32B32A32_UINT = 3,
//     TIF_DXGI_FORMAT_R32G32B32A32_SINT = 4,
//     TIF_DXGI_FORMAT_R32G32B32_TYPELESS = 5,
//     TIF_DXGI_FORMAT_R32G32B32_FLOAT = 6,
//     TIF_DXGI_FORMAT_R32G32B32_UINT = 7,
//     TIF_DXGI_FORMAT_R32G32B32_SINT = 8,
//     TIF_DXGI_FORMAT_R16G16B16A16_TYPELESS = 9,
//     TIF_DXGI_FORMAT_R16G16B16A16_FLOAT = 10,
//     TIF_DXGI_FORMAT_R16G16B16A16_UNORM = 11,
//     TIF_DXGI_FORMAT_R16G16B16A16_UINT = 12,
//     TIF_DXGI_FORMAT_R16G16B16A16_SNORM = 13,
//     TIF_DXGI_FORMAT_R16G16B16A16_SINT = 14,
//     TIF_DXGI_FORMAT_R32G32_TYPELESS = 15,
//     TIF_DXGI_FORMAT_R32G32_FLOAT = 16,
//     TIF_DXGI_FORMAT_R32G32_UINT = 17,
//     TIF_DXGI_FORMAT_R32G32_SINT = 18,
//     TIF_DXGI_FORMAT_R32G8X24_TYPELESS = 19,
//     TIF_DXGI_FORMAT_D32_FLOAT_S8X24_UINT = 20,
//     TIF_DXGI_FORMAT_R32_FLOAT_X8X24_TYPELESS = 21,
//     TIF_DXGI_FORMAT_X32_TYPELESS_G8X24_UINT = 22,
//     TIF_DXGI_FORMAT_R10G10B10A2_TYPELESS = 23,
//     TIF_DXGI_FORMAT_R10G10B10A2_UNORM = 24,
//     TIF_DXGI_FORMAT_R10G10B10A2_UINT = 25,
//     TIF_DXGI_FORMAT_R11G11B10_FLOAT = 26,
//     TIF_DXGI_FORMAT_R8G8B8A8_TYPELESS = 27,
//     TIF_DXGI_FORMAT_R8G8B8A8_UNORM = 28,
//     TIF_DXGI_FORMAT_R8G8B8A8_UNORM_SRGB = 29,
//     TIF_DXGI_FORMAT_R8G8B8A8_UINT = 30,
//     TIF_DXGI_FORMAT_R8G8B8A8_SNORM = 31,
//     TIF_DXGI_FORMAT_R8G8B8A8_SINT = 32,
//     TIF_DXGI_FORMAT_R16G16_TYPELESS = 33,
//     TIF_DXGI_FORMAT_R16G16_FLOAT = 34,
//     TIF_DXGI_FORMAT_R16G16_UNORM = 35,
//     TIF_DXGI_FORMAT_R16G16_UINT = 36,
//     TIF_DXGI_FORMAT_R16G16_SNORM = 37,
//     TIF_DXGI_FORMAT_R16G16_SINT = 38,
//     TIF_DXGI_FORMAT_R32_TYPELESS = 39,
//     TIF_DXGI_FORMAT_D32_FLOAT = 40,
//     TIF_DXGI_FORMAT_R32_FLOAT = 41,
//     TIF_DXGI_FORMAT_R32_UINT = 42,
//     TIF_DXGI_FORMAT_R32_SINT = 43,
//     TIF_DXGI_FORMAT_R24G8_TYPELESS = 44,
//     TIF_DXGI_FORMAT_D24_UNORM_S8_UINT = 45,
//     TIF_DXGI_FORMAT_R24_UNORM_X8_TYPELESS = 46,
//     TIF_DXGI_FORMAT_X24_TYPELESS_G8_UINT = 47,
//     TIF_DXGI_FORMAT_R8G8_TYPELESS = 48,
//     TIF_DXGI_FORMAT_R8G8_UNORM = 49,
//     TIF_DXGI_FORMAT_R8G8_UINT = 50,
//     TIF_DXGI_FORMAT_R8G8_SNORM = 51,
//     TIF_DXGI_FORMAT_R8G8_SINT = 52,
//     TIF_DXGI_FORMAT_R16_TYPELESS = 53,
//     TIF_DXGI_FORMAT_R16_FLOAT = 54,
//     TIF_DXGI_FORMAT_D16_UNORM = 55,
//     TIF_DXGI_FORMAT_R16_UNORM = 56,
//     TIF_DXGI_FORMAT_R16_UINT = 57,
//     TIF_DXGI_FORMAT_R16_SNORM = 58,
//     TIF_DXGI_FORMAT_R16_SINT = 59,
//     TIF_DXGI_FORMAT_R8_TYPELESS = 60,
//     TIF_DXGI_FORMAT_R8_UNORM = 61,
//     TIF_DXGI_FORMAT_R8_UINT = 62,
//     TIF_DXGI_FORMAT_R8_SNORM = 63,
//     TIF_DXGI_FORMAT_R8_SINT = 64,
//     TIF_DXGI_FORMAT_A8_UNORM = 65,
//     TIF_DXGI_FORMAT_R1_UNORM = 66,
//     TIF_DXGI_FORMAT_R9G9B9E5_SHAREDEXP = 67,
//     TIF_DXGI_FORMAT_R8G8_B8G8_UNORM = 68,
//     TIF_DXGI_FORMAT_G8R8_G8B8_UNORM = 69,
//     TIF_DXGI_FORMAT_BC1_TYPELESS = 70,
//     TIF_DXGI_FORMAT_BC1_UNORM = 71,
//     TIF_DXGI_FORMAT_BC1_UNORM_SRGB = 72,
//     TIF_DXGI_FORMAT_BC2_TYPELESS = 73,
//     TIF_DXGI_FORMAT_BC2_UNORM = 74,
//     TIF_DXGI_FORMAT_BC2_UNORM_SRGB = 75,
//     TIF_DXGI_FORMAT_BC3_TYPELESS = 76,
//     TIF_DXGI_FORMAT_BC3_UNORM = 77,
//     TIF_DXGI_FORMAT_BC3_UNORM_SRGB = 78,
//     TIF_DXGI_FORMAT_BC4_TYPELESS = 79,
//     TIF_DXGI_FORMAT_BC4_UNORM = 80,
//     TIF_DXGI_FORMAT_BC4_SNORM = 81,
//     TIF_DXGI_FORMAT_BC5_TYPELESS = 82,
//     TIF_DXGI_FORMAT_BC5_UNORM = 83,
//     TIF_DXGI_FORMAT_BC5_SNORM = 84,
//     TIF_DXGI_FORMAT_B5G6R5_UNORM = 85,
//     TIF_DXGI_FORMAT_B5G5R5A1_UNORM = 86,
//     TIF_DXGI_FORMAT_B8G8R8A8_UNORM = 87,
//     TIF_DXGI_FORMAT_B8G8R8X8_UNORM = 88,
//     TIF_DXGI_FORMAT_R10G10B10_XR_BIAS_A2_UNORM = 89,
//     TIF_DXGI_FORMAT_B8G8R8A8_TYPELESS = 90,
//     TIF_DXGI_FORMAT_B8G8R8A8_UNORM_SRGB = 91,
//     TIF_DXGI_FORMAT_B8G8R8X8_TYPELESS = 92,
//     TIF_DXGI_FORMAT_B8G8R8X8_UNORM_SRGB = 93,
//     TIF_DXGI_FORMAT_BC6H_TYPELESS = 94,
//     TIF_DXGI_FORMAT_BC6H_UF16 = 95,
//     TIF_DXGI_FORMAT_BC6H_SF16 = 96,
//     TIF_DXGI_FORMAT_BC7_TYPELESS = 97,
//     TIF_DXGI_FORMAT_BC7_UNORM = 98,
//     TIF_DXGI_FORMAT_BC7_UNORM_SRGB = 99,
//     TIF_DXGI_FORMAT_AYUV = 100,
//     TIF_DXGI_FORMAT_Y410 = 101,
//     TIF_DXGI_FORMAT_Y416 = 102,
//     TIF_DXGI_FORMAT_NV12 = 103,
//     TIF_DXGI_FORMAT_P010 = 104,
//     TIF_DXGI_FORMAT_P016 = 105,
//     TIF_DXGI_FORMAT_420_OPAQUE = 106,
//     TIF_DXGI_FORMAT_YUY2 = 107,
//     TIF_DXGI_FORMAT_Y210 = 108,
//     TIF_DXGI_FORMAT_Y216 = 109,
//     TIF_DXGI_FORMAT_NV11 = 110,
//     TIF_DXGI_FORMAT_AI44 = 111,
//     TIF_DXGI_FORMAT_IA44 = 112,
//     TIF_DXGI_FORMAT_P8 = 113,
//     TIF_DXGI_FORMAT_A8P8 = 114,
//     TIF_DXGI_FORMAT_B4G4R4A4_UNORM = 115,

//     // ASTC Format
//     TIF_DXGI_FORMAT_ASTC_4X4_TYPELESS = 133,
//     TIF_DXGI_FORMAT_ASTC_4X4_UNORM = 134,
//     TIF_DXGI_FORMAT_ASTC_4X4_UNORM_SRGB = 135,
//     TIF_DXGI_FORMAT_ASTC_5X4_TYPELESS = 137,
//     TIF_DXGI_FORMAT_ASTC_5X4_UNORM = 138,
//     TIF_DXGI_FORMAT_ASTC_5X4_UNORM_SRGB = 139,
//     TIF_DXGI_FORMAT_ASTC_5X5_TYPELESS = 141,
//     TIF_DXGI_FORMAT_ASTC_5X5_UNORM = 142,
//     TIF_DXGI_FORMAT_ASTC_5X5_UNORM_SRGB = 143,
//     TIF_DXGI_FORMAT_ASTC_6X5_TYPELESS = 145,
//     TIF_DXGI_FORMAT_ASTC_6X5_UNORM = 146,
//     TIF_DXGI_FORMAT_ASTC_6X5_UNORM_SRGB = 147,
//     TIF_DXGI_FORMAT_ASTC_6X6_TYPELESS = 149,
//     TIF_DXGI_FORMAT_ASTC_6X6_UNORM = 150,
//     TIF_DXGI_FORMAT_ASTC_6X6_UNORM_SRGB = 151,
//     TIF_DXGI_FORMAT_ASTC_8X5_TYPELESS = 153,
//     TIF_DXGI_FORMAT_ASTC_8X5_UNORM = 154,
//     TIF_DXGI_FORMAT_ASTC_8X5_UNORM_SRGB = 155,
//     TIF_DXGI_FORMAT_ASTC_8X6_TYPELESS = 157,
//     TIF_DXGI_FORMAT_ASTC_8X6_UNORM = 158,
//     TIF_DXGI_FORMAT_ASTC_8X6_UNORM_SRGB = 159,
//     TIF_DXGI_FORMAT_ASTC_8X8_TYPELESS = 161,
//     TIF_DXGI_FORMAT_ASTC_8X8_UNORM = 162,
//     TIF_DXGI_FORMAT_ASTC_8X8_UNORM_SRGB = 163,
//     TIF_DXGI_FORMAT_ASTC_10X5_TYPELESS = 165,
//     TIF_DXGI_FORMAT_ASTC_10X5_UNORM = 166,
//     TIF_DXGI_FORMAT_ASTC_10X5_UNORM_SRGB = 167,
//     TIF_DXGI_FORMAT_ASTC_10X6_TYPELESS = 169,
//     TIF_DXGI_FORMAT_ASTC_10X6_UNORM = 170,
//     TIF_DXGI_FORMAT_ASTC_10X6_UNORM_SRGB = 171,
//     TIF_DXGI_FORMAT_ASTC_10X8_TYPELESS = 173,
//     TIF_DXGI_FORMAT_ASTC_10X8_UNORM = 174,
//     TIF_DXGI_FORMAT_ASTC_10X8_UNORM_SRGB = 175,
//     TIF_DXGI_FORMAT_ASTC_10X10_TYPELESS = 177,
//     TIF_DXGI_FORMAT_ASTC_10X10_UNORM = 178,
//     TIF_DXGI_FORMAT_ASTC_10X10_UNORM_SRGB = 179,
//     TIF_DXGI_FORMAT_ASTC_12X10_TYPELESS = 181,
//     TIF_DXGI_FORMAT_ASTC_12X10_UNORM = 182,
//     TIF_DXGI_FORMAT_ASTC_12X10_UNORM_SRGB = 183,
//     TIF_DXGI_FORMAT_ASTC_12X12_TYPELESS = 185,
//     TIF_DXGI_FORMAT_ASTC_12X12_UNORM = 186,
//     TIF_DXGI_FORMAT_ASTC_12X12_UNORM_SRGB = 187,

//     // xbox 360 formats
//     TIF_DXGI_FORMAT_R10G10B10_7E3_A2_FLOAT = 116,
//     TIF_DXGI_FORMAT_R10G10B10_6E4_A2_FLOAT = 117,
//     TIF_DXGI_FORMAT_D16_UNORM_S8_UINT = 118,
//     TIF_DXGI_FORMAT_R16_UNORM_X8_TYPELESS = 119,
//     TIF_DXGI_FORMAT_X16_TYPELESS_G8_UINT = 120,

//     TIF_DXGI_FORMAT_P208 = 130,
//     TIF_DXGI_FORMAT_V208 = 131,
//     TIF_DXGI_FORMAT_V408 = 132,

//     // XBox One formats
//     TIF_DXGI_FORMAT_R10G10B10_SNORM_A2_UNORM = 189,
//     TIF_DXGI_FORMAT_R4G4_UNORM = 190,

// } TinyImageFormat_DXGI_FORMAT;
// #endif

// static inline TinyImageFormat TinyImageFormat_FromDXGI_FORMAT(TinyImageFormat_DXGI_FORMAT fmt)
// {
//     switch (fmt)
//     {
//     case TIF_DXGI_FORMAT_UNKNOWN:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_DXGI_FORMAT_R32G32B32A32_FLOAT:
//         return TinyImageFormat_R32G32B32A32_SFLOAT;
//     case TIF_DXGI_FORMAT_R32G32B32A32_UINT:
//         return TinyImageFormat_R32G32B32A32_UINT;
//     case TIF_DXGI_FORMAT_R32G32B32A32_SINT:
//         return TinyImageFormat_R32G32B32A32_SINT;
//     case TIF_DXGI_FORMAT_R32G32B32_FLOAT:
//         return TinyImageFormat_R32G32B32_SFLOAT;
//     case TIF_DXGI_FORMAT_R32G32B32_UINT:
//         return TinyImageFormat_R32G32B32_UINT;
//     case TIF_DXGI_FORMAT_R32G32B32_SINT:
//         return TinyImageFormat_R32G32B32_SINT;
//     case TIF_DXGI_FORMAT_R16G16B16A16_FLOAT:
//         return TinyImageFormat_R16G16B16A16_SFLOAT;
//     case TIF_DXGI_FORMAT_R16G16B16A16_UNORM:
//         return TinyImageFormat_R16G16B16A16_UNORM;
//     case TIF_DXGI_FORMAT_R16G16B16A16_UINT:
//         return TinyImageFormat_R16G16B16A16_UINT;
//     case TIF_DXGI_FORMAT_R16G16B16A16_SNORM:
//         return TinyImageFormat_R16G16B16A16_SNORM;
//     case TIF_DXGI_FORMAT_R16G16B16A16_SINT:
//         return TinyImageFormat_R16G16B16A16_SINT;
//     case TIF_DXGI_FORMAT_R32G32_FLOAT:
//         return TinyImageFormat_R32G32_SFLOAT;
//     case TIF_DXGI_FORMAT_R32G32_UINT:
//         return TinyImageFormat_R32G32_UINT;
//     case TIF_DXGI_FORMAT_R32G32_SINT:
//         return TinyImageFormat_R32G32_SINT;
//     case TIF_DXGI_FORMAT_D32_FLOAT_S8X24_UINT:
//         return TinyImageFormat_D32_SFLOAT_S8_UINT;
//     case TIF_DXGI_FORMAT_R10G10B10A2_UNORM:
//         return TinyImageFormat_R10G10B10A2_UNORM;
//     case TIF_DXGI_FORMAT_R10G10B10A2_UINT:
//         return TinyImageFormat_R10G10B10A2_UINT;
//     case TIF_DXGI_FORMAT_R11G11B10_FLOAT:
//         return TinyImageFormat_B10G11R11_UFLOAT; // WRONG!
//     case TIF_DXGI_FORMAT_R8G8B8A8_UNORM:
//         return TinyImageFormat_R8G8B8A8_UNORM;
//     case TIF_DXGI_FORMAT_R16G16_FLOAT:
//         return TinyImageFormat_R16G16_SFLOAT;
//     case TIF_DXGI_FORMAT_R16G16_UNORM:
//         return TinyImageFormat_R16G16_UNORM;
//     case TIF_DXGI_FORMAT_R16G16_UINT:
//         return TinyImageFormat_R16G16_UINT;
//     case TIF_DXGI_FORMAT_R16G16_SNORM:
//         return TinyImageFormat_R16G16_SNORM;
//     case TIF_DXGI_FORMAT_R16G16_SINT:
//         return TinyImageFormat_R16G16_SINT;
//     case TIF_DXGI_FORMAT_D32_FLOAT:
//         return TinyImageFormat_D32_SFLOAT;
//     case TIF_DXGI_FORMAT_R32_FLOAT:
//         return TinyImageFormat_R32_SFLOAT;
//     case TIF_DXGI_FORMAT_R32_UINT:
//         return TinyImageFormat_R32_UINT;
//     case TIF_DXGI_FORMAT_R32_SINT:
//         return TinyImageFormat_R32_SINT;
//     case TIF_DXGI_FORMAT_D24_UNORM_S8_UINT:
//         return TinyImageFormat_D24_UNORM_S8_UINT;
//     case TIF_DXGI_FORMAT_X24_TYPELESS_G8_UINT:
//         return TinyImageFormat_D24_UNORM_S8_UINT;
//     case TIF_DXGI_FORMAT_R8G8_UNORM:
//         return TinyImageFormat_R8G8_UNORM;
//     case TIF_DXGI_FORMAT_R8G8_UINT:
//         return TinyImageFormat_R8G8_UINT;
//     case TIF_DXGI_FORMAT_R8G8_SNORM:
//         return TinyImageFormat_R8G8_SNORM;
//     case TIF_DXGI_FORMAT_R8G8_SINT:
//         return TinyImageFormat_R8G8_SINT;
//     case TIF_DXGI_FORMAT_R16_FLOAT:
//         return TinyImageFormat_R16_SFLOAT;
//     case TIF_DXGI_FORMAT_D16_UNORM:
//         return TinyImageFormat_D16_UNORM;
//     case TIF_DXGI_FORMAT_R16_UNORM:
//         return TinyImageFormat_R16_UNORM;
//     case TIF_DXGI_FORMAT_R16_UINT:
//         return TinyImageFormat_R16_UINT;
//     case TIF_DXGI_FORMAT_R16_SNORM:
//         return TinyImageFormat_R16_SNORM;
//     case TIF_DXGI_FORMAT_R16_SINT:
//         return TinyImageFormat_R16_SINT;
//     case TIF_DXGI_FORMAT_R8_UNORM:
//         return TinyImageFormat_R8_UNORM;
//     case TIF_DXGI_FORMAT_R8_UINT:
//         return TinyImageFormat_R8_UINT;
//     case TIF_DXGI_FORMAT_R8_SNORM:
//         return TinyImageFormat_R8_SNORM;
//     case TIF_DXGI_FORMAT_R8_SINT:
//         return TinyImageFormat_R8_SINT;
//     case TIF_DXGI_FORMAT_A8_UNORM:
//         return TinyImageFormat_A8_UNORM;
//     case TIF_DXGI_FORMAT_R9G9B9E5_SHAREDEXP:
//         return TinyImageFormat_E5B9G9R9_UFLOAT;
//     case TIF_DXGI_FORMAT_BC1_UNORM:
//         return TinyImageFormat_DXBC1_RGBA_UNORM;
//     case TIF_DXGI_FORMAT_BC1_UNORM_SRGB:
//         return TinyImageFormat_DXBC1_RGBA_SRGB;
//     case TIF_DXGI_FORMAT_BC2_UNORM:
//         return TinyImageFormat_DXBC2_UNORM;
//     case TIF_DXGI_FORMAT_BC2_UNORM_SRGB:
//         return TinyImageFormat_DXBC2_SRGB;
//     case TIF_DXGI_FORMAT_BC3_UNORM:
//         return TinyImageFormat_DXBC3_UNORM;
//     case TIF_DXGI_FORMAT_BC3_UNORM_SRGB:
//         return TinyImageFormat_DXBC3_SRGB;
//     case TIF_DXGI_FORMAT_BC4_UNORM:
//         return TinyImageFormat_DXBC4_UNORM;
//     case TIF_DXGI_FORMAT_BC4_SNORM:
//         return TinyImageFormat_DXBC4_SNORM;
//     case TIF_DXGI_FORMAT_BC5_UNORM:
//         return TinyImageFormat_DXBC5_UNORM;
//     case TIF_DXGI_FORMAT_BC5_SNORM:
//         return TinyImageFormat_DXBC5_SNORM;
//     case TIF_DXGI_FORMAT_B5G6R5_UNORM:
//         return TinyImageFormat_R5G6B5_UNORM;
//     case TIF_DXGI_FORMAT_B5G5R5A1_UNORM:
//         return TinyImageFormat_R5G5B5A1_UNORM;
//     case TIF_DXGI_FORMAT_B8G8R8A8_UNORM:
//         return TinyImageFormat_B8G8R8A8_UNORM;
//     case TIF_DXGI_FORMAT_B8G8R8X8_UNORM:
//         return TinyImageFormat_B8G8R8X8_UNORM;
//     case TIF_DXGI_FORMAT_B8G8R8A8_UNORM_SRGB:
//         return TinyImageFormat_B8G8R8A8_SRGB;
//     case TIF_DXGI_FORMAT_B8G8R8X8_UNORM_SRGB:
//         return TinyImageFormat_B8G8R8A8_SRGB; // WRONG should be X8
//     case TIF_DXGI_FORMAT_BC6H_UF16:
//         return TinyImageFormat_DXBC6H_UFLOAT;
//     case TIF_DXGI_FORMAT_BC6H_SF16:
//         return TinyImageFormat_DXBC6H_SFLOAT;
//     case TIF_DXGI_FORMAT_BC7_UNORM:
//         return TinyImageFormat_DXBC7_UNORM;
//     case TIF_DXGI_FORMAT_BC7_UNORM_SRGB:
//         return TinyImageFormat_DXBC7_SRGB;
//     case TIF_DXGI_FORMAT_B4G4R4A4_UNORM:
//         return TinyImageFormat_B4G4R4A4_UNORM;
//     case TIF_DXGI_FORMAT_R8G8B8A8_UNORM_SRGB:
//         return TinyImageFormat_R8G8B8A8_SRGB;
//     case TIF_DXGI_FORMAT_R8G8B8A8_UINT:
//         return TinyImageFormat_R8G8B8A8_UINT;
//     case TIF_DXGI_FORMAT_R8G8B8A8_SNORM:
//         return TinyImageFormat_R8G8B8A8_SNORM;
//     case TIF_DXGI_FORMAT_R8G8B8A8_SINT:
//         return TinyImageFormat_R8G8B8A8_SINT;
//     case TIF_DXGI_FORMAT_R4G4_UNORM:
//         return TinyImageFormat_R4G4_UNORM;
//     case TIF_DXGI_FORMAT_R1_UNORM:
//         return TinyImageFormat_R1_UNORM;

//     case TIF_DXGI_FORMAT_420_OPAQUE:
//         return TinyImageFormat_G8_B8_R8_3PLANE_420_UNORM;
//     case TIF_DXGI_FORMAT_AYUV:
//         return TinyImageFormat_B8G8R8A8_UNORM;
//     case TIF_DXGI_FORMAT_Y410:
//         return TinyImageFormat_A2B10G10R10_UNORM;
//     case TIF_DXGI_FORMAT_Y416:
//         return TinyImageFormat_R16G16B16A16_UNORM;
//     case TIF_DXGI_FORMAT_NV12:
//         return TinyImageFormat_G8_B8R8_2PLANE_420_UNORM;
//     case TIF_DXGI_FORMAT_P010:
//         return TinyImageFormat_G10X6_B10X6R10X6_2PLANE_420_UNORM_3PACK16;
//     case TIF_DXGI_FORMAT_P016:
//         return TinyImageFormat_G16_B16R16_2PLANE_420_UNORM;
//     case TIF_DXGI_FORMAT_YUY2:
//         return TinyImageFormat_G8B8G8R8_422_UNORM;
//     case TIF_DXGI_FORMAT_Y210:
//         return TinyImageFormat_G10X6B10X6G10X6R10X6_422_UNORM_4PACK16;
//     case TIF_DXGI_FORMAT_Y216:
//         return TinyImageFormat_G16B16G16R16_422_UNORM;
//     case TIF_DXGI_FORMAT_P208:
//         return TinyImageFormat_G8_B8R8_2PLANE_422_UNORM;

//     // ASTC
//     case TIF_DXGI_FORMAT_ASTC_4X4_UNORM:
//         return TinyImageFormat_ASTC_4x4_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_4X4_UNORM_SRGB:
//         return TinyImageFormat_ASTC_4x4_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_5X4_UNORM:
//         return TinyImageFormat_ASTC_5x4_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_5X4_UNORM_SRGB:
//         return TinyImageFormat_ASTC_5x4_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_5X5_UNORM:
//         return TinyImageFormat_ASTC_5x5_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_5X5_UNORM_SRGB:
//         return TinyImageFormat_ASTC_5x5_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_6X5_UNORM:
//         return TinyImageFormat_ASTC_6x5_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_6X5_UNORM_SRGB:
//         return TinyImageFormat_ASTC_6x5_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_6X6_UNORM:
//         return TinyImageFormat_ASTC_6x6_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_6X6_UNORM_SRGB:
//         return TinyImageFormat_ASTC_6x6_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_8X5_UNORM:
//         return TinyImageFormat_ASTC_8x5_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_8X5_UNORM_SRGB:
//         return TinyImageFormat_ASTC_8x5_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_8X6_UNORM:
//         return TinyImageFormat_ASTC_8x6_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_8X6_UNORM_SRGB:
//         return TinyImageFormat_ASTC_8x6_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_8X8_UNORM:
//         return TinyImageFormat_ASTC_8x8_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_8X8_UNORM_SRGB:
//         return TinyImageFormat_ASTC_8x8_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_10X5_UNORM:
//         return TinyImageFormat_ASTC_10x5_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_10X5_UNORM_SRGB:
//         return TinyImageFormat_ASTC_10x5_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_10X6_UNORM:
//         return TinyImageFormat_ASTC_10x6_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_10X6_UNORM_SRGB:
//         return TinyImageFormat_ASTC_10x6_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_10X8_UNORM:
//         return TinyImageFormat_ASTC_10x8_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_10X8_UNORM_SRGB:
//         return TinyImageFormat_ASTC_10x8_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_10X10_UNORM:
//         return TinyImageFormat_ASTC_10x10_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_10X10_UNORM_SRGB:
//         return TinyImageFormat_ASTC_10x10_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_12X10_UNORM:
//         return TinyImageFormat_ASTC_12x10_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_12X10_UNORM_SRGB:
//         return TinyImageFormat_ASTC_12x10_SRGB;
//     case TIF_DXGI_FORMAT_ASTC_12X12_UNORM:
//         return TinyImageFormat_ASTC_12x12_UNORM;
//     case TIF_DXGI_FORMAT_ASTC_12X12_UNORM_SRGB:
//         return TinyImageFormat_ASTC_12x12_SRGB;

//     case TIF_DXGI_FORMAT_ASTC_4X4_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_5X4_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_5X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_6X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_6X6_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_8X6_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_8X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_8X8_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X6_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X8_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X10_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_12X10_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_12X12_TYPELESS:

//     case TIF_DXGI_FORMAT_R32G32B32A32_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G32B32_TYPELESS:
//     case TIF_DXGI_FORMAT_R16G16B16A16_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G32_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G8X24_TYPELESS:
//     case TIF_DXGI_FORMAT_R32_FLOAT_X8X24_TYPELESS:
//     case TIF_DXGI_FORMAT_X32_TYPELESS_G8X24_UINT:
//     case TIF_DXGI_FORMAT_R10G10B10A2_TYPELESS:
//     case TIF_DXGI_FORMAT_R8G8B8A8_TYPELESS:
//     case TIF_DXGI_FORMAT_R16G16_TYPELESS:
//     case TIF_DXGI_FORMAT_R32_TYPELESS:
//     case TIF_DXGI_FORMAT_R24G8_TYPELESS:
//     case TIF_DXGI_FORMAT_R24_UNORM_X8_TYPELESS:
//     case TIF_DXGI_FORMAT_R8G8_TYPELESS:
//     case TIF_DXGI_FORMAT_R16_TYPELESS:
//     case TIF_DXGI_FORMAT_R8_TYPELESS:
//     case TIF_DXGI_FORMAT_BC1_TYPELESS:
//     case TIF_DXGI_FORMAT_BC2_TYPELESS:
//     case TIF_DXGI_FORMAT_BC3_TYPELESS:
//     case TIF_DXGI_FORMAT_BC4_TYPELESS:
//     case TIF_DXGI_FORMAT_BC5_TYPELESS:
//     case TIF_DXGI_FORMAT_B8G8R8A8_TYPELESS:
//     case TIF_DXGI_FORMAT_B8G8R8X8_TYPELESS:
//     case TIF_DXGI_FORMAT_BC6H_TYPELESS:
//     case TIF_DXGI_FORMAT_BC7_TYPELESS:
//     case TIF_DXGI_FORMAT_NV11:
//     case TIF_DXGI_FORMAT_AI44:
//     case TIF_DXGI_FORMAT_IA44:
//     case TIF_DXGI_FORMAT_P8:
//     case TIF_DXGI_FORMAT_A8P8:
//     case TIF_DXGI_FORMAT_V208:
//     case TIF_DXGI_FORMAT_V408:
//     case TIF_DXGI_FORMAT_R10G10B10_XR_BIAS_A2_UNORM:
//     case TIF_DXGI_FORMAT_R8G8_B8G8_UNORM:
//     case TIF_DXGI_FORMAT_G8R8_G8B8_UNORM:
//     case TIF_DXGI_FORMAT_R10G10B10_7E3_A2_FLOAT:
//     case TIF_DXGI_FORMAT_R10G10B10_6E4_A2_FLOAT:
//     case TIF_DXGI_FORMAT_D16_UNORM_S8_UINT:
//     case TIF_DXGI_FORMAT_R16_UNORM_X8_TYPELESS:
//     case TIF_DXGI_FORMAT_X16_TYPELESS_G8_UINT:
//     case TIF_DXGI_FORMAT_R10G10B10_SNORM_A2_UNORM:
//         return TinyImageFormat_UNDEFINED;
//     }
//     return TinyImageFormat_UNDEFINED;
// }

// static inline TinyImageFormat_DXGI_FORMAT TinyImageFormat_ToDXGI_FORMAT(TinyImageFormat fmt)
// {
//     switch (fmt)
//     {
//     case TinyImageFormat_R1_UNORM:
//         return TIF_DXGI_FORMAT_R1_UNORM;
//     case TinyImageFormat_R4G4_UNORM:
//         return TIF_DXGI_FORMAT_R4G4_UNORM;
//     case TinyImageFormat_R5G6B5_UNORM:
//         return TIF_DXGI_FORMAT_B5G6R5_UNORM;
//     case TinyImageFormat_B5G6R5_UNORM:
//         return TIF_DXGI_FORMAT_B5G6R5_UNORM;
//     case TinyImageFormat_B5G5R5A1_UNORM:
//         return TIF_DXGI_FORMAT_B5G5R5A1_UNORM;
//     case TinyImageFormat_R8_UNORM:
//         return TIF_DXGI_FORMAT_R8_UNORM;
//     case TinyImageFormat_R8_SNORM:
//         return TIF_DXGI_FORMAT_R8_SNORM;
//     case TinyImageFormat_R8_UINT:
//         return TIF_DXGI_FORMAT_R8_UINT;
//     case TinyImageFormat_R8_SINT:
//         return TIF_DXGI_FORMAT_R8_SINT;
//     case TinyImageFormat_R8G8_UNORM:
//         return TIF_DXGI_FORMAT_R8G8_UNORM;
//     case TinyImageFormat_R8G8_SNORM:
//         return TIF_DXGI_FORMAT_R8G8_SNORM;
//     case TinyImageFormat_R8G8_UINT:
//         return TIF_DXGI_FORMAT_R8G8_UINT;
//     case TinyImageFormat_R8G8_SINT:
//         return TIF_DXGI_FORMAT_R8G8_SINT;
//     case TinyImageFormat_B4G4R4A4_UNORM:
//         return TIF_DXGI_FORMAT_B4G4R4A4_UNORM;

//     case TinyImageFormat_R8G8B8A8_UNORM:
//         return TIF_DXGI_FORMAT_R8G8B8A8_UNORM;
//     case TinyImageFormat_R8G8B8A8_SNORM:
//         return TIF_DXGI_FORMAT_R8G8B8A8_SNORM;
//     case TinyImageFormat_R8G8B8A8_UINT:
//         return TIF_DXGI_FORMAT_R8G8B8A8_UINT;
//     case TinyImageFormat_R8G8B8A8_SINT:
//         return TIF_DXGI_FORMAT_R8G8B8A8_SINT;
//     case TinyImageFormat_R8G8B8A8_SRGB:
//         return TIF_DXGI_FORMAT_R8G8B8A8_UNORM_SRGB;

//     case TinyImageFormat_B8G8R8A8_UNORM:
//         return TIF_DXGI_FORMAT_B8G8R8A8_UNORM;
//     case TinyImageFormat_B8G8R8X8_UNORM:
//         return TIF_DXGI_FORMAT_B8G8R8X8_UNORM;
//     case TinyImageFormat_B8G8R8A8_SRGB:
//         return TIF_DXGI_FORMAT_B8G8R8A8_UNORM_SRGB;

//     case TinyImageFormat_R10G10B10A2_UNORM:
//         return TIF_DXGI_FORMAT_R10G10B10A2_UNORM;
//     case TinyImageFormat_B10G10R10A2_UNORM:
//         return TIF_DXGI_FORMAT_R10G10B10A2_UNORM;
//     case TinyImageFormat_R10G10B10A2_UINT:
//         return TIF_DXGI_FORMAT_R10G10B10A2_UINT;
//     case TinyImageFormat_B10G10R10A2_UINT:
//         return TIF_DXGI_FORMAT_R10G10B10A2_UINT;

//     case TinyImageFormat_R16_UNORM:
//         return TIF_DXGI_FORMAT_R16_UNORM;
//     case TinyImageFormat_R16_SNORM:
//         return TIF_DXGI_FORMAT_R16_SNORM;
//     case TinyImageFormat_R16_UINT:
//         return TIF_DXGI_FORMAT_R16_UINT;
//     case TinyImageFormat_R16_SINT:
//         return TIF_DXGI_FORMAT_R16_SINT;
//     case TinyImageFormat_R16_SFLOAT:
//         return TIF_DXGI_FORMAT_R16_FLOAT;
//     case TinyImageFormat_R16G16_UNORM:
//         return TIF_DXGI_FORMAT_R16G16_UNORM;
//     case TinyImageFormat_R16G16_SNORM:
//         return TIF_DXGI_FORMAT_R16G16_SNORM;
//     case TinyImageFormat_R16G16_UINT:
//         return TIF_DXGI_FORMAT_R16G16_UINT;
//     case TinyImageFormat_R16G16_SINT:
//         return TIF_DXGI_FORMAT_R16G16_SINT;
//     case TinyImageFormat_R16G16_SFLOAT:
//         return TIF_DXGI_FORMAT_R16G16_FLOAT;
//     case TinyImageFormat_R16G16B16A16_UNORM:
//         return TIF_DXGI_FORMAT_R16G16B16A16_UNORM;
//     case TinyImageFormat_R16G16B16A16_SNORM:
//         return TIF_DXGI_FORMAT_R16G16B16A16_SNORM;
//     case TinyImageFormat_R16G16B16A16_UINT:
//         return TIF_DXGI_FORMAT_R16G16B16A16_UINT;
//     case TinyImageFormat_R16G16B16A16_SINT:
//         return TIF_DXGI_FORMAT_R16G16B16A16_SINT;
//     case TinyImageFormat_R16G16B16A16_SFLOAT:
//         return TIF_DXGI_FORMAT_R16G16B16A16_FLOAT;
//     case TinyImageFormat_R32_UINT:
//         return TIF_DXGI_FORMAT_R32_UINT;
//     case TinyImageFormat_R32_SINT:
//         return TIF_DXGI_FORMAT_R32_SINT;
//     case TinyImageFormat_R32_SFLOAT:
//         return TIF_DXGI_FORMAT_R32_FLOAT;
//     case TinyImageFormat_R32G32_UINT:
//         return TIF_DXGI_FORMAT_R32G32_UINT;
//     case TinyImageFormat_R32G32_SINT:
//         return TIF_DXGI_FORMAT_R32G32_SINT;
//     case TinyImageFormat_R32G32_SFLOAT:
//         return TIF_DXGI_FORMAT_R32G32_FLOAT;
//     case TinyImageFormat_R32G32B32_UINT:
//         return TIF_DXGI_FORMAT_R32G32B32_UINT;
//     case TinyImageFormat_R32G32B32_SINT:
//         return TIF_DXGI_FORMAT_R32G32B32_SINT;
//     case TinyImageFormat_R32G32B32_SFLOAT:
//         return TIF_DXGI_FORMAT_R32G32B32_FLOAT;
//     case TinyImageFormat_R32G32B32A32_UINT:
//         return TIF_DXGI_FORMAT_R32G32B32A32_UINT;
//     case TinyImageFormat_R32G32B32A32_SINT:
//         return TIF_DXGI_FORMAT_R32G32B32A32_SINT;
//     case TinyImageFormat_R32G32B32A32_SFLOAT:
//         return TIF_DXGI_FORMAT_R32G32B32A32_FLOAT;
//     case TinyImageFormat_B10G11R11_UFLOAT:
//         return TIF_DXGI_FORMAT_R11G11B10_FLOAT;
//     case TinyImageFormat_E5B9G9R9_UFLOAT:
//         return TIF_DXGI_FORMAT_R9G9B9E5_SHAREDEXP;
//     case TinyImageFormat_D16_UNORM:
//         return TIF_DXGI_FORMAT_D16_UNORM;
//     case TinyImageFormat_X8_D24_UNORM:
//         return TIF_DXGI_FORMAT_D24_UNORM_S8_UINT;
//     case TinyImageFormat_D32_SFLOAT:
//         return TIF_DXGI_FORMAT_D32_FLOAT;
//     case TinyImageFormat_D24_UNORM_S8_UINT:
//         return TIF_DXGI_FORMAT_D24_UNORM_S8_UINT;
//     case TinyImageFormat_D32_SFLOAT_S8_UINT:
//         return TIF_DXGI_FORMAT_D32_FLOAT_S8X24_UINT;
//     case TinyImageFormat_DXBC1_RGB_UNORM:
//         return TIF_DXGI_FORMAT_BC1_UNORM;
//     case TinyImageFormat_DXBC1_RGB_SRGB:
//         return TIF_DXGI_FORMAT_BC1_UNORM_SRGB;
//     case TinyImageFormat_DXBC1_RGBA_UNORM:
//         return TIF_DXGI_FORMAT_BC1_UNORM;
//     case TinyImageFormat_DXBC1_RGBA_SRGB:
//         return TIF_DXGI_FORMAT_BC1_UNORM_SRGB;
//     case TinyImageFormat_DXBC2_UNORM:
//         return TIF_DXGI_FORMAT_BC2_UNORM;
//     case TinyImageFormat_DXBC2_SRGB:
//         return TIF_DXGI_FORMAT_BC2_UNORM_SRGB;
//     case TinyImageFormat_DXBC3_UNORM:
//         return TIF_DXGI_FORMAT_BC3_UNORM;
//     case TinyImageFormat_DXBC3_SRGB:
//         return TIF_DXGI_FORMAT_BC3_UNORM_SRGB;
//     case TinyImageFormat_DXBC4_UNORM:
//         return TIF_DXGI_FORMAT_BC4_UNORM;
//     case TinyImageFormat_DXBC4_SNORM:
//         return TIF_DXGI_FORMAT_BC4_SNORM;
//     case TinyImageFormat_DXBC5_UNORM:
//         return TIF_DXGI_FORMAT_BC5_UNORM;
//     case TinyImageFormat_DXBC5_SNORM:
//         return TIF_DXGI_FORMAT_BC5_SNORM;
//     case TinyImageFormat_DXBC6H_UFLOAT:
//         return TIF_DXGI_FORMAT_BC6H_UF16;
//     case TinyImageFormat_DXBC6H_SFLOAT:
//         return TIF_DXGI_FORMAT_BC6H_SF16;
//     case TinyImageFormat_DXBC7_UNORM:
//         return TIF_DXGI_FORMAT_BC7_UNORM;
//     case TinyImageFormat_DXBC7_SRGB:
//         return TIF_DXGI_FORMAT_BC7_UNORM_SRGB;
//     case TinyImageFormat_D16_UNORM_S8_UINT:
//         return TIF_DXGI_FORMAT_D16_UNORM_S8_UINT;
//     // ASTC
//     case TinyImageFormat_ASTC_4x4_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_4X4_UNORM;
//     case TinyImageFormat_ASTC_4x4_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_4X4_UNORM_SRGB;
//     case TinyImageFormat_ASTC_5x4_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_5X4_UNORM;
//     case TinyImageFormat_ASTC_5x4_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_5X4_UNORM_SRGB;
//     case TinyImageFormat_ASTC_5x5_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_5X5_UNORM;
//     case TinyImageFormat_ASTC_5x5_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_5X5_UNORM_SRGB;
//     case TinyImageFormat_ASTC_6x5_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_6X5_UNORM;
//     case TinyImageFormat_ASTC_6x5_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_6X5_UNORM_SRGB;
//     case TinyImageFormat_ASTC_6x6_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_6X6_UNORM;
//     case TinyImageFormat_ASTC_6x6_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_6X6_UNORM_SRGB;
//     case TinyImageFormat_ASTC_8x5_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_8X5_UNORM;
//     case TinyImageFormat_ASTC_8x5_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_8X5_UNORM_SRGB;
//     case TinyImageFormat_ASTC_8x6_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_8X6_UNORM;
//     case TinyImageFormat_ASTC_8x6_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_8X6_UNORM_SRGB;
//     case TinyImageFormat_ASTC_8x8_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_8X8_UNORM;
//     case TinyImageFormat_ASTC_8x8_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_8X8_UNORM_SRGB;
//     case TinyImageFormat_ASTC_10x5_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_10X5_UNORM;
//     case TinyImageFormat_ASTC_10x5_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X5_UNORM_SRGB;
//     case TinyImageFormat_ASTC_10x6_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_10X6_UNORM;
//     case TinyImageFormat_ASTC_10x6_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X6_UNORM_SRGB;
//     case TinyImageFormat_ASTC_10x8_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_10X8_UNORM;
//     case TinyImageFormat_ASTC_10x8_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X8_UNORM_SRGB;
//     case TinyImageFormat_ASTC_10x10_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_10X10_UNORM;
//     case TinyImageFormat_ASTC_10x10_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X10_UNORM_SRGB;
//     case TinyImageFormat_ASTC_12x10_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_12X10_UNORM;
//     case TinyImageFormat_ASTC_12x10_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_12X10_UNORM_SRGB;
//     case TinyImageFormat_ASTC_12x12_UNORM:
//         return TIF_DXGI_FORMAT_ASTC_12X12_UNORM;
//     case TinyImageFormat_ASTC_12x12_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_12X12_UNORM_SRGB;
//     default:
//         return TIF_DXGI_FORMAT_UNKNOWN;
//     }
// }

// static inline TinyImageFormat_DXGI_FORMAT TinyImageFormat_DXGI_FORMATToTypeless(TinyImageFormat_DXGI_FORMAT fmt)
// {
//     switch (fmt)
//     {
//     case TIF_DXGI_FORMAT_R32G32B32A32_FLOAT:
//     case TIF_DXGI_FORMAT_R32G32B32A32_UINT:
//     case TIF_DXGI_FORMAT_R32G32B32A32_SINT:
//         return TIF_DXGI_FORMAT_R32G32B32A32_TYPELESS;
//     case TIF_DXGI_FORMAT_R32G32B32_FLOAT:
//     case TIF_DXGI_FORMAT_R32G32B32_UINT:
//     case TIF_DXGI_FORMAT_R32G32B32_SINT:
//         return TIF_DXGI_FORMAT_R32G32B32_TYPELESS;

//     case TIF_DXGI_FORMAT_R16G16B16A16_FLOAT:
//     case TIF_DXGI_FORMAT_R16G16B16A16_UNORM:
//     case TIF_DXGI_FORMAT_R16G16B16A16_UINT:
//     case TIF_DXGI_FORMAT_R16G16B16A16_SNORM:
//     case TIF_DXGI_FORMAT_R16G16B16A16_SINT:
//         return TIF_DXGI_FORMAT_R16G16B16A16_TYPELESS;

//     case TIF_DXGI_FORMAT_R32G32_FLOAT:
//     case TIF_DXGI_FORMAT_R32G32_UINT:
//     case TIF_DXGI_FORMAT_R32G32_SINT:
//         return TIF_DXGI_FORMAT_R32G32_TYPELESS;

//     case TIF_DXGI_FORMAT_R10G10B10_XR_BIAS_A2_UNORM:
//     case TIF_DXGI_FORMAT_R10G10B10_SNORM_A2_UNORM:
//     case TIF_DXGI_FORMAT_R10G10B10A2_UNORM:
//     case TIF_DXGI_FORMAT_R10G10B10A2_UINT:
//         return TIF_DXGI_FORMAT_R10G10B10A2_TYPELESS;

//     case TIF_DXGI_FORMAT_R8G8B8A8_UNORM:
//     case TIF_DXGI_FORMAT_R8G8B8A8_UNORM_SRGB:
//     case TIF_DXGI_FORMAT_R8G8B8A8_UINT:
//     case TIF_DXGI_FORMAT_R8G8B8A8_SNORM:
//     case TIF_DXGI_FORMAT_R8G8B8A8_SINT:
//         return TIF_DXGI_FORMAT_R8G8B8A8_TYPELESS;
//     case TIF_DXGI_FORMAT_R16G16_FLOAT:
//     case TIF_DXGI_FORMAT_R16G16_UNORM:
//     case TIF_DXGI_FORMAT_R16G16_UINT:
//     case TIF_DXGI_FORMAT_R16G16_SNORM:
//     case TIF_DXGI_FORMAT_R16G16_SINT:
//         return TIF_DXGI_FORMAT_R16G16_TYPELESS;

//     case TIF_DXGI_FORMAT_R9G9B9E5_SHAREDEXP:
//     case TIF_DXGI_FORMAT_D32_FLOAT:
//     case TIF_DXGI_FORMAT_R32_FLOAT:
//     case TIF_DXGI_FORMAT_R32_UINT:
//     case TIF_DXGI_FORMAT_R32_SINT:
//         return TIF_DXGI_FORMAT_R32_TYPELESS;
//     case TIF_DXGI_FORMAT_R8G8_UNORM:
//     case TIF_DXGI_FORMAT_R8G8_UINT:
//     case TIF_DXGI_FORMAT_R8G8_SNORM:
//     case TIF_DXGI_FORMAT_R8G8_SINT:
//         return TIF_DXGI_FORMAT_R8G8_TYPELESS;
//     case TIF_DXGI_FORMAT_B4G4R4A4_UNORM: // just treats a 16 raw bits
//     case TIF_DXGI_FORMAT_D16_UNORM:
//     case TIF_DXGI_FORMAT_R16_FLOAT:
//     case TIF_DXGI_FORMAT_R16_UNORM:
//     case TIF_DXGI_FORMAT_R16_UINT:
//     case TIF_DXGI_FORMAT_R16_SNORM:
//     case TIF_DXGI_FORMAT_R16_SINT:
//         return TIF_DXGI_FORMAT_R16_TYPELESS;
//     case TIF_DXGI_FORMAT_A8_UNORM:
//     case TIF_DXGI_FORMAT_R8_UNORM:
//     case TIF_DXGI_FORMAT_R8_UINT:
//     case TIF_DXGI_FORMAT_R8_SNORM:
//     case TIF_DXGI_FORMAT_R4G4_UNORM:
//     case TIF_DXGI_FORMAT_R8_SINT:
//         return TIF_DXGI_FORMAT_R8_TYPELESS;
//     case TIF_DXGI_FORMAT_BC1_UNORM:
//     case TIF_DXGI_FORMAT_BC1_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_BC1_TYPELESS;
//     case TIF_DXGI_FORMAT_BC2_UNORM:
//     case TIF_DXGI_FORMAT_BC2_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_BC2_TYPELESS;
//     case TIF_DXGI_FORMAT_BC3_UNORM:
//     case TIF_DXGI_FORMAT_BC3_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_BC3_TYPELESS;
//     case TIF_DXGI_FORMAT_BC4_UNORM:
//     case TIF_DXGI_FORMAT_BC4_SNORM:
//         return TIF_DXGI_FORMAT_BC4_TYPELESS;
//     case TIF_DXGI_FORMAT_BC5_UNORM:
//     case TIF_DXGI_FORMAT_BC5_SNORM:
//         return TIF_DXGI_FORMAT_BC5_TYPELESS;
//     case TIF_DXGI_FORMAT_B5G6R5_UNORM:
//     case TIF_DXGI_FORMAT_B5G5R5A1_UNORM:
//         return TIF_DXGI_FORMAT_R16_TYPELESS;

//     case TIF_DXGI_FORMAT_R11G11B10_FLOAT:
//         return TIF_DXGI_FORMAT_R11G11B10_FLOAT;

//     case TIF_DXGI_FORMAT_B8G8R8X8_UNORM:
//     case TIF_DXGI_FORMAT_B8G8R8X8_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_B8G8R8X8_TYPELESS;

//     case TIF_DXGI_FORMAT_B8G8R8A8_UNORM:
//     case TIF_DXGI_FORMAT_B8G8R8A8_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_B8G8R8A8_TYPELESS;

//     case TIF_DXGI_FORMAT_BC6H_UF16:
//     case TIF_DXGI_FORMAT_BC6H_SF16:
//         return TIF_DXGI_FORMAT_BC6H_TYPELESS;

//     case TIF_DXGI_FORMAT_BC7_UNORM:
//     case TIF_DXGI_FORMAT_BC7_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_BC7_TYPELESS;

//     case TIF_DXGI_FORMAT_X32_TYPELESS_G8X24_UINT:
//     case TIF_DXGI_FORMAT_D32_FLOAT_S8X24_UINT:
//         return TIF_DXGI_FORMAT_R32G8X24_TYPELESS;
//     case TIF_DXGI_FORMAT_X24_TYPELESS_G8_UINT:
//         return TIF_DXGI_FORMAT_R24G8_TYPELESS;
//     case TIF_DXGI_FORMAT_D24_UNORM_S8_UINT:
//         return TIF_DXGI_FORMAT_R24G8_TYPELESS;

//         // ASTC
//     case TIF_DXGI_FORMAT_ASTC_4X4_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_4X4_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_4X4_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_5X4_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_5X4_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_5X4_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_5X5_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_5X5_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_5X5_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_6X5_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_6X5_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_6X5_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_6X6_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_6X6_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_6X6_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_8X5_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_8X5_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_8X5_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_8X6_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_8X6_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_8X6_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_8X8_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_8X8_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_8X8_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_10X5_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_10X5_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X5_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_10X6_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_10X6_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X6_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_10X8_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_10X8_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X8_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_10X10_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_10X10_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_10X10_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_12X10_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_12X10_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_12X10_TYPELESS;

//     case TIF_DXGI_FORMAT_ASTC_12X12_UNORM:
//     case TIF_DXGI_FORMAT_ASTC_12X12_UNORM_SRGB:
//         return TIF_DXGI_FORMAT_ASTC_12X12_TYPELESS;

//         // typeless just return the input format
//     case TIF_DXGI_FORMAT_ASTC_4X4_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_5X4_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_5X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_6X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_6X6_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_8X6_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_8X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_8X8_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X5_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X6_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X8_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_10X10_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_12X10_TYPELESS:
//     case TIF_DXGI_FORMAT_ASTC_12X12_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G32B32A32_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G32B32_TYPELESS:
//     case TIF_DXGI_FORMAT_R16G16B16A16_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G32_TYPELESS:
//     case TIF_DXGI_FORMAT_R32G8X24_TYPELESS:
//     case TIF_DXGI_FORMAT_R32_FLOAT_X8X24_TYPELESS:
//     case TIF_DXGI_FORMAT_R10G10B10A2_TYPELESS:
//     case TIF_DXGI_FORMAT_R8G8B8A8_TYPELESS:
//     case TIF_DXGI_FORMAT_R16G16_TYPELESS:
//     case TIF_DXGI_FORMAT_R32_TYPELESS:
//     case TIF_DXGI_FORMAT_R24G8_TYPELESS:
//     case TIF_DXGI_FORMAT_R24_UNORM_X8_TYPELESS:
//     case TIF_DXGI_FORMAT_R8G8_TYPELESS:
//     case TIF_DXGI_FORMAT_R16_TYPELESS:
//     case TIF_DXGI_FORMAT_R8_TYPELESS:
//     case TIF_DXGI_FORMAT_BC1_TYPELESS:
//     case TIF_DXGI_FORMAT_BC2_TYPELESS:
//     case TIF_DXGI_FORMAT_BC3_TYPELESS:
//     case TIF_DXGI_FORMAT_BC4_TYPELESS:
//     case TIF_DXGI_FORMAT_BC5_TYPELESS:
//     case TIF_DXGI_FORMAT_B8G8R8A8_TYPELESS:
//     case TIF_DXGI_FORMAT_B8G8R8X8_TYPELESS:
//     case TIF_DXGI_FORMAT_BC6H_TYPELESS:
//     case TIF_DXGI_FORMAT_BC7_TYPELESS:
//         return fmt;

//     case TIF_DXGI_FORMAT_R1_UNORM:
//     case TIF_DXGI_FORMAT_R8G8_B8G8_UNORM:
//     case TIF_DXGI_FORMAT_G8R8_G8B8_UNORM:
//     case TIF_DXGI_FORMAT_AYUV:
//     case TIF_DXGI_FORMAT_Y410:
//     case TIF_DXGI_FORMAT_Y416:
//     case TIF_DXGI_FORMAT_NV12:
//     case TIF_DXGI_FORMAT_P010:
//     case TIF_DXGI_FORMAT_P016:
//     case TIF_DXGI_FORMAT_420_OPAQUE:
//     case TIF_DXGI_FORMAT_YUY2:
//     case TIF_DXGI_FORMAT_Y210:
//     case TIF_DXGI_FORMAT_Y216:
//     case TIF_DXGI_FORMAT_NV11:
//     case TIF_DXGI_FORMAT_AI44:
//     case TIF_DXGI_FORMAT_IA44:
//     case TIF_DXGI_FORMAT_P8:
//     case TIF_DXGI_FORMAT_A8P8:
//     case TIF_DXGI_FORMAT_P208:
//     case TIF_DXGI_FORMAT_V208:
//     case TIF_DXGI_FORMAT_V408:
//     case TIF_DXGI_FORMAT_R10G10B10_7E3_A2_FLOAT:
//     case TIF_DXGI_FORMAT_R10G10B10_6E4_A2_FLOAT:
//     case TIF_DXGI_FORMAT_D16_UNORM_S8_UINT:
//     case TIF_DXGI_FORMAT_R16_UNORM_X8_TYPELESS:
//     case TIF_DXGI_FORMAT_X16_TYPELESS_G8_UINT:
//     case TIF_DXGI_FORMAT_UNKNOWN:
//         return TIF_DXGI_FORMAT_UNKNOWN;
//     }
//     return TIF_DXGI_FORMAT_UNKNOWN;
// }
// #ifndef TINYIMAGEFORMAT_MTLPIXEL_FORMAT
// #define TINYIMAGEFORMAT_MTLPIXEL_FORMAT

// typedef enum TinyImageFormat_MTLPixelFormat
// {
//     TIF_MTLPixelFormatInvalid = 0,
//     TIF_MTLPixelFormatA8Unorm = 1,
//     TIF_MTLPixelFormatR8Unorm = 10,
//     TIF_MTLPixelFormatR8Unorm_sRGB = 11,
//     TIF_MTLPixelFormatR8Snorm = 12,
//     TIF_MTLPixelFormatR8Uint = 13,
//     TIF_MTLPixelFormatR8Sint = 14,
//     TIF_MTLPixelFormatR16Unorm = 20,
//     TIF_MTLPixelFormatR16Snorm = 22,
//     TIF_MTLPixelFormatR16Uint = 23,
//     TIF_MTLPixelFormatR16Sint = 24,
//     TIF_MTLPixelFormatR16Float = 25,
//     TIF_MTLPixelFormatRG8Unorm = 30,
//     TIF_MTLPixelFormatRG8Unorm_sRGB = 31,
//     TIF_MTLPixelFormatRG8Snorm = 32,
//     TIF_MTLPixelFormatRG8Uint = 33,
//     TIF_MTLPixelFormatRG8Sint = 34,
//     TIF_MTLPixelFormatB5G6R5Unorm = 40,
//     TIF_MTLPixelFormatA1BGR5Unorm = 41,
//     TIF_MTLPixelFormatABGR4Unorm = 42,
//     TIF_MTLPixelFormatBGR5A1Unorm = 43,
//     TIF_MTLPixelFormatR32Uint = 53,
//     TIF_MTLPixelFormatR32Sint = 54,
//     TIF_MTLPixelFormatR32Float = 55,
//     TIF_MTLPixelFormatRG16Unorm = 60,
//     TIF_MTLPixelFormatRG16Snorm = 62,
//     TIF_MTLPixelFormatRG16Uint = 63,
//     TIF_MTLPixelFormatRG16Sint = 64,
//     TIF_MTLPixelFormatRG16Float = 65,
//     TIF_MTLPixelFormatRGBA8Unorm = 70,
//     TIF_MTLPixelFormatRGBA8Unorm_sRGB = 71,
//     TIF_MTLPixelFormatRGBA8Snorm = 72,
//     TIF_MTLPixelFormatRGBA8Uint = 73,
//     TIF_MTLPixelFormatRGBA8Sint = 74,
//     TIF_MTLPixelFormatBGRA8Unorm = 80,
//     TIF_MTLPixelFormatBGRA8Unorm_sRGB = 81,
//     TIF_MTLPixelFormatRGB10A2Unorm = 90,
//     TIF_MTLPixelFormatRGB10A2Uint = 91,
//     TIF_MTLPixelFormatRG11B10Float = 92,
//     TIF_MTLPixelFormatRGB9E5Float = 93,
//     TIF_MTLPixelFormatBGR10A2Unorm = 94,
//     TIF_MTLPixelFormatBGR10_XR = 554,
//     TIF_MTLPixelFormatBGR10_XR_sRGB = 555,
//     TIF_MTLPixelFormatRG32Uint = 103,
//     TIF_MTLPixelFormatRG32Sint = 104,
//     TIF_MTLPixelFormatRG32Float = 105,
//     TIF_MTLPixelFormatRGBA16Unorm = 110,
//     TIF_MTLPixelFormatRGBA16Snorm = 112,
//     TIF_MTLPixelFormatRGBA16Uint = 113,
//     TIF_MTLPixelFormatRGBA16Sint = 114,
//     TIF_MTLPixelFormatRGBA16Float = 115,
//     TIF_MTLPixelFormatBGRA10_XR = 552,
//     TIF_MTLPixelFormatBGRA10_XR_sRGB = 553,
//     TIF_MTLPixelFormatRGBA32Uint = 123,
//     TIF_MTLPixelFormatRGBA32Sint = 124,
//     TIF_MTLPixelFormatRGBA32Float = 125,
//     TIF_MTLPixelFormatBC1_RGBA = 130,
//     TIF_MTLPixelFormatBC1_RGBA_sRGB = 131,
//     TIF_MTLPixelFormatBC2_RGBA = 132,
//     TIF_MTLPixelFormatBC2_RGBA_sRGB = 133,
//     TIF_MTLPixelFormatBC3_RGBA = 134,
//     TIF_MTLPixelFormatBC3_RGBA_sRGB = 135,
//     TIF_MTLPixelFormatBC4_RUnorm = 140,
//     TIF_MTLPixelFormatBC4_RSnorm = 141,
//     TIF_MTLPixelFormatBC5_RGUnorm = 142,
//     TIF_MTLPixelFormatBC5_RGSnorm = 143,
//     TIF_MTLPixelFormatBC6H_RGBFloat = 150,
//     TIF_MTLPixelFormatBC6H_RGBUfloat = 151,
//     TIF_MTLPixelFormatBC7_RGBAUnorm = 152,
//     TIF_MTLPixelFormatBC7_RGBAUnorm_sRGB = 153,
//     TIF_MTLPixelFormatPVRTC_RGB_2BPP = 160,
//     TIF_MTLPixelFormatPVRTC_RGB_2BPP_sRGB = 161,
//     TIF_MTLPixelFormatPVRTC_RGB_4BPP = 162,
//     TIF_MTLPixelFormatPVRTC_RGB_4BPP_sRGB = 163,
//     TIF_MTLPixelFormatPVRTC_RGBA_2BPP = 164,
//     TIF_MTLPixelFormatPVRTC_RGBA_2BPP_sRGB = 165,
//     TIF_MTLPixelFormatPVRTC_RGBA_4BPP = 166,
//     TIF_MTLPixelFormatPVRTC_RGBA_4BPP_sRGB = 167,
//     TIF_MTLPixelFormatEAC_R11Unorm = 170,
//     TIF_MTLPixelFormatEAC_R11Snorm = 172,
//     TIF_MTLPixelFormatEAC_RG11Unorm = 174,
//     TIF_MTLPixelFormatEAC_RG11Snorm = 176,
//     TIF_MTLPixelFormatEAC_RGBA8 = 178,
//     TIF_MTLPixelFormatEAC_RGBA8_sRGB = 179,
//     TIF_MTLPixelFormatETC2_RGB8 = 180,
//     TIF_MTLPixelFormatETC2_RGB8_sRGB = 181,
//     TIF_MTLPixelFormatETC2_RGB8A1 = 182,
//     TIF_MTLPixelFormatETC2_RGB8A1_sRGB = 183,
//     TIF_MTLPixelFormatASTC_4x4_sRGB = 186,
//     TIF_MTLPixelFormatASTC_5x4_sRGB = 187,
//     TIF_MTLPixelFormatASTC_5x5_sRGB = 188,
//     TIF_MTLPixelFormatASTC_6x5_sRGB = 189,
//     TIF_MTLPixelFormatASTC_6x6_sRGB = 190,
//     TIF_MTLPixelFormatASTC_8x5_sRGB = 192,
//     TIF_MTLPixelFormatASTC_8x6_sRGB = 193,
//     TIF_MTLPixelFormatASTC_8x8_sRGB = 194,
//     TIF_MTLPixelFormatASTC_10x5_sRGB = 195,
//     TIF_MTLPixelFormatASTC_10x6_sRGB = 196,
//     TIF_MTLPixelFormatASTC_10x8_sRGB = 197,
//     TIF_MTLPixelFormatASTC_10x10_sRGB = 198,
//     TIF_MTLPixelFormatASTC_12x10_sRGB = 199,
//     TIF_MTLPixelFormatASTC_12x12_sRGB = 200,
//     TIF_MTLPixelFormatASTC_4x4_LDR = 204,
//     TIF_MTLPixelFormatASTC_5x4_LDR = 205,
//     TIF_MTLPixelFormatASTC_5x5_LDR = 206,
//     TIF_MTLPixelFormatASTC_6x5_LDR = 207,
//     TIF_MTLPixelFormatASTC_6x6_LDR = 208,
//     TIF_MTLPixelFormatASTC_8x5_LDR = 210,
//     TIF_MTLPixelFormatASTC_8x6_LDR = 211,
//     TIF_MTLPixelFormatASTC_8x8_LDR = 212,
//     TIF_MTLPixelFormatASTC_10x5_LDR = 213,
//     TIF_MTLPixelFormatASTC_10x6_LDR = 214,
//     TIF_MTLPixelFormatASTC_10x8_LDR = 215,
//     TIF_MTLPixelFormatASTC_10x10_LDR = 216,
//     TIF_MTLPixelFormatASTC_12x10_LDR = 217,
//     TIF_MTLPixelFormatASTC_12x12_LDR = 218,
//     TIF_MTLPixelFormatGBGR422 = 240,
//     TIF_MTLPixelFormatBGRG422 = 241,
//     TIF_MTLPixelFormatDepth16Unorm = 250,
//     TIF_MTLPixelFormatDepth32Float = 252,
//     TIF_MTLPixelFormatStencil8 = 253,
//     TIF_MTLPixelFormatDepth24Unorm_Stencil8 = 255,
//     TIF_MTLPixelFormatDepth32Float_Stencil8 = 260,
//     TIF_MTLPixelFormatX32_Stencil8 = 261,
//     TIF_MTLPixelFormatX24_Stencil8 = 262,
// } TinyImageFormat_MTLPixelFormat;
// #endif

// static inline TinyImageFormat_MTLPixelFormat TinyImageFormat_ToMTLPixelFormat(TinyImageFormat fmt)
// {
//     switch (fmt)
//     {
//     case TinyImageFormat_A8_UNORM:
//         return TIF_MTLPixelFormatA8Unorm;
//     case TinyImageFormat_R8_UNORM:
//         return TIF_MTLPixelFormatR8Unorm;
//     case TinyImageFormat_R8_SNORM:
//         return TIF_MTLPixelFormatR8Snorm;
//     case TinyImageFormat_R8_UINT:
//         return TIF_MTLPixelFormatR8Uint;
//     case TinyImageFormat_R8_SINT:
//         return TIF_MTLPixelFormatR8Sint;
//     case TinyImageFormat_R8_SRGB:
//         return TIF_MTLPixelFormatR8Unorm_sRGB;
//     case TinyImageFormat_UNDEFINED:
//         return TIF_MTLPixelFormatInvalid;
//     case TinyImageFormat_R4G4B4A4_UNORM:
//         return TIF_MTLPixelFormatABGR4Unorm;
//     case TinyImageFormat_R5G6B5_UNORM:
//         return TIF_MTLPixelFormatB5G6R5Unorm;
//         ;
//     case TinyImageFormat_R5G5B5A1_UNORM:
//         return TIF_MTLPixelFormatA1BGR5Unorm;
//     case TinyImageFormat_R8G8_UNORM:
//         return TIF_MTLPixelFormatRG8Unorm;
//     case TinyImageFormat_R8G8_SNORM:
//         return TIF_MTLPixelFormatRG8Snorm;
//     case TinyImageFormat_R8G8_UINT:
//         return TIF_MTLPixelFormatRG8Uint;
//     case TinyImageFormat_R8G8_SINT:
//         return TIF_MTLPixelFormatRG8Sint;
//     case TinyImageFormat_R8G8_SRGB:
//         return TIF_MTLPixelFormatRG8Unorm_sRGB;
//     case TinyImageFormat_R8G8B8A8_UNORM:
//         return TIF_MTLPixelFormatRGBA8Unorm;
//     case TinyImageFormat_R8G8B8A8_SNORM:
//         return TIF_MTLPixelFormatRGBA8Snorm;
//     case TinyImageFormat_R8G8B8A8_UINT:
//         return TIF_MTLPixelFormatRGBA8Uint;
//     case TinyImageFormat_R8G8B8A8_SINT:
//         return TIF_MTLPixelFormatRGBA8Sint;
//     case TinyImageFormat_R8G8B8A8_SRGB:
//         return TIF_MTLPixelFormatRGBA8Unorm_sRGB;
//     case TinyImageFormat_B8G8R8A8_UNORM:
//         return TIF_MTLPixelFormatBGRA8Unorm;
//     case TinyImageFormat_B8G8R8A8_SRGB:
//         return TIF_MTLPixelFormatBGRA8Unorm_sRGB;
//     case TinyImageFormat_B10G10R10A2_UNORM:
//         return TIF_MTLPixelFormatBGR10A2Unorm;
//     case TinyImageFormat_R10G10B10A2_UNORM:
//         return TIF_MTLPixelFormatRGB10A2Unorm;
//     case TinyImageFormat_R10G10B10A2_UINT:
//         return TIF_MTLPixelFormatRGB10A2Uint;
//     case TinyImageFormat_R16_UNORM:
//         return TIF_MTLPixelFormatR16Unorm;
//     case TinyImageFormat_R16_SNORM:
//         return TIF_MTLPixelFormatR16Snorm;
//     case TinyImageFormat_R16_UINT:
//         return TIF_MTLPixelFormatR16Uint;
//     case TinyImageFormat_R16_SINT:
//         return TIF_MTLPixelFormatR16Sint;
//     case TinyImageFormat_R16_SFLOAT:
//         return TIF_MTLPixelFormatR16Float;
//     case TinyImageFormat_R16G16_UNORM:
//         return TIF_MTLPixelFormatRG16Unorm;
//     case TinyImageFormat_R16G16_SNORM:
//         return TIF_MTLPixelFormatRG16Snorm;
//     case TinyImageFormat_R16G16_UINT:
//         return TIF_MTLPixelFormatRG16Uint;
//     case TinyImageFormat_R16G16_SINT:
//         return TIF_MTLPixelFormatRG16Sint;
//     case TinyImageFormat_R16G16_SFLOAT:
//         return TIF_MTLPixelFormatRG16Float;
//     case TinyImageFormat_R16G16B16A16_UNORM:
//         return TIF_MTLPixelFormatRGBA16Unorm;
//     case TinyImageFormat_R16G16B16A16_SNORM:
//         return TIF_MTLPixelFormatRGBA16Snorm;
//     case TinyImageFormat_R16G16B16A16_UINT:
//         return TIF_MTLPixelFormatRGBA16Uint;
//     case TinyImageFormat_R16G16B16A16_SINT:
//         return TIF_MTLPixelFormatRGBA16Sint;
//     case TinyImageFormat_R16G16B16A16_SFLOAT:
//         return TIF_MTLPixelFormatRGBA16Float;
//     case TinyImageFormat_R32_UINT:
//         return TIF_MTLPixelFormatR32Uint;
//     case TinyImageFormat_R32_SINT:
//         return TIF_MTLPixelFormatR32Sint;
//     case TinyImageFormat_R32_SFLOAT:
//         return TIF_MTLPixelFormatR32Float;
//     case TinyImageFormat_R32G32_UINT:
//         return TIF_MTLPixelFormatRG32Uint;
//     case TinyImageFormat_R32G32_SINT:
//         return TIF_MTLPixelFormatRG32Sint;
//     case TinyImageFormat_R32G32_SFLOAT:
//         return TIF_MTLPixelFormatRG32Float;
//     case TinyImageFormat_R32G32B32A32_UINT:
//         return TIF_MTLPixelFormatRGBA32Uint;
//     case TinyImageFormat_R32G32B32A32_SINT:
//         return TIF_MTLPixelFormatRGBA32Sint;
//     case TinyImageFormat_R32G32B32A32_SFLOAT:
//         return TIF_MTLPixelFormatRGBA32Float;
//     case TinyImageFormat_B10G11R11_UFLOAT:
//         return TIF_MTLPixelFormatRG11B10Float;
//     case TinyImageFormat_E5B9G9R9_UFLOAT:
//         return TIF_MTLPixelFormatRGB9E5Float;
//     case TinyImageFormat_D16_UNORM:
//         return TIF_MTLPixelFormatDepth16Unorm;
//     case TinyImageFormat_X8_D24_UNORM:
//         return TIF_MTLPixelFormatDepth24Unorm_Stencil8;
//     case TinyImageFormat_D32_SFLOAT:
//         return TIF_MTLPixelFormatDepth32Float;
//     case TinyImageFormat_S8_UINT:
//         return TIF_MTLPixelFormatStencil8;
//     case TinyImageFormat_D24_UNORM_S8_UINT:
//         return TIF_MTLPixelFormatDepth24Unorm_Stencil8;
//     case TinyImageFormat_D32_SFLOAT_S8_UINT:
//         return TIF_MTLPixelFormatDepth32Float_Stencil8;
//     case TinyImageFormat_DXBC1_RGB_UNORM:
//         return TIF_MTLPixelFormatBC1_RGBA;
//     case TinyImageFormat_DXBC1_RGB_SRGB:
//         return TIF_MTLPixelFormatBC1_RGBA_sRGB;
//     case TinyImageFormat_DXBC1_RGBA_UNORM:
//         return TIF_MTLPixelFormatBC1_RGBA;
//     case TinyImageFormat_DXBC1_RGBA_SRGB:
//         return TIF_MTLPixelFormatBC1_RGBA_sRGB;
//     case TinyImageFormat_DXBC2_UNORM:
//         return TIF_MTLPixelFormatBC2_RGBA;
//     case TinyImageFormat_DXBC2_SRGB:
//         return TIF_MTLPixelFormatBC2_RGBA_sRGB;
//     case TinyImageFormat_DXBC3_UNORM:
//         return TIF_MTLPixelFormatBC3_RGBA;
//     case TinyImageFormat_DXBC3_SRGB:
//         return TIF_MTLPixelFormatBC3_RGBA_sRGB;
//     case TinyImageFormat_DXBC4_UNORM:
//         return TIF_MTLPixelFormatBC4_RUnorm;
//     case TinyImageFormat_DXBC4_SNORM:
//         return TIF_MTLPixelFormatBC4_RSnorm;
//     case TinyImageFormat_DXBC5_UNORM:
//         return TIF_MTLPixelFormatBC5_RGUnorm;
//     case TinyImageFormat_DXBC5_SNORM:
//         return TIF_MTLPixelFormatBC5_RGSnorm;
//     case TinyImageFormat_DXBC6H_UFLOAT:
//         return TIF_MTLPixelFormatBC6H_RGBUfloat;
//     case TinyImageFormat_DXBC6H_SFLOAT:
//         return TIF_MTLPixelFormatBC6H_RGBFloat;
//     case TinyImageFormat_DXBC7_UNORM:
//         return TIF_MTLPixelFormatBC7_RGBAUnorm;
//     case TinyImageFormat_DXBC7_SRGB:
//         return TIF_MTLPixelFormatBC7_RGBAUnorm_sRGB;
//     case TinyImageFormat_PVRTC1_2BPP_UNORM:
//         return TIF_MTLPixelFormatPVRTC_RGBA_2BPP;
//     case TinyImageFormat_PVRTC1_4BPP_UNORM:
//         return TIF_MTLPixelFormatPVRTC_RGBA_4BPP;
//     case TinyImageFormat_PVRTC1_2BPP_SRGB:
//         return TIF_MTLPixelFormatPVRTC_RGBA_2BPP_sRGB;
//     case TinyImageFormat_PVRTC1_4BPP_SRGB:
//         return TIF_MTLPixelFormatPVRTC_RGBA_4BPP_sRGB;
//     case TinyImageFormat_ETC2_R8G8B8_UNORM:
//         return TIF_MTLPixelFormatETC2_RGB8;
//     case TinyImageFormat_ETC2_R8G8B8A1_UNORM:
//         return TIF_MTLPixelFormatETC2_RGB8A1;
//     case TinyImageFormat_ETC2_R8G8B8A8_UNORM:
//         return TIF_MTLPixelFormatEAC_RGBA8;
//     case TinyImageFormat_ETC2_R8G8B8_SRGB:
//         return TIF_MTLPixelFormatETC2_RGB8_sRGB;
//     case TinyImageFormat_ETC2_R8G8B8A1_SRGB:
//         return TIF_MTLPixelFormatETC2_RGB8A1_sRGB;
//     case TinyImageFormat_ETC2_R8G8B8A8_SRGB:
//         return TIF_MTLPixelFormatEAC_RGBA8_sRGB;
//     case TinyImageFormat_ETC2_EAC_R11_UNORM:
//         return TIF_MTLPixelFormatEAC_R11Unorm;
//     case TinyImageFormat_ETC2_EAC_R11G11_UNORM:
//         return TIF_MTLPixelFormatEAC_RG11Unorm;
//     case TinyImageFormat_ETC2_EAC_R11_SNORM:
//         return TIF_MTLPixelFormatEAC_R11Snorm;
//     case TinyImageFormat_ETC2_EAC_R11G11_SNORM:
//         return TIF_MTLPixelFormatEAC_RG11Snorm;
//     case TinyImageFormat_ASTC_4x4_UNORM:
//         return TIF_MTLPixelFormatASTC_4x4_LDR;
//     case TinyImageFormat_ASTC_4x4_SRGB:
//         return TIF_MTLPixelFormatASTC_4x4_sRGB;
//     case TinyImageFormat_ASTC_5x4_UNORM:
//         return TIF_MTLPixelFormatASTC_5x4_LDR;
//     case TinyImageFormat_ASTC_5x4_SRGB:
//         return TIF_MTLPixelFormatASTC_5x4_sRGB;
//     case TinyImageFormat_ASTC_5x5_UNORM:
//         return TIF_MTLPixelFormatASTC_5x5_LDR;
//     case TinyImageFormat_ASTC_5x5_SRGB:
//         return TIF_MTLPixelFormatASTC_5x5_sRGB;
//     case TinyImageFormat_ASTC_6x5_UNORM:
//         return TIF_MTLPixelFormatASTC_6x5_LDR;
//     case TinyImageFormat_ASTC_6x5_SRGB:
//         return TIF_MTLPixelFormatASTC_6x5_sRGB;
//     case TinyImageFormat_ASTC_6x6_UNORM:
//         return TIF_MTLPixelFormatASTC_6x6_LDR;
//     case TinyImageFormat_ASTC_6x6_SRGB:
//         return TIF_MTLPixelFormatASTC_6x6_sRGB;
//     case TinyImageFormat_ASTC_8x5_UNORM:
//         return TIF_MTLPixelFormatASTC_8x5_LDR;
//     case TinyImageFormat_ASTC_8x5_SRGB:
//         return TIF_MTLPixelFormatASTC_8x5_sRGB;
//     case TinyImageFormat_ASTC_8x6_UNORM:
//         return TIF_MTLPixelFormatASTC_8x6_LDR;
//     case TinyImageFormat_ASTC_8x6_SRGB:
//         return TIF_MTLPixelFormatASTC_8x6_sRGB;
//     case TinyImageFormat_ASTC_8x8_UNORM:
//         return TIF_MTLPixelFormatASTC_8x8_LDR;
//     case TinyImageFormat_ASTC_8x8_SRGB:
//         return TIF_MTLPixelFormatASTC_8x8_sRGB;
//     case TinyImageFormat_ASTC_10x5_UNORM:
//         return TIF_MTLPixelFormatASTC_10x5_LDR;
//     case TinyImageFormat_ASTC_10x5_SRGB:
//         return TIF_MTLPixelFormatASTC_10x5_sRGB;
//     case TinyImageFormat_ASTC_10x6_UNORM:
//         return TIF_MTLPixelFormatASTC_10x6_LDR;
//     case TinyImageFormat_ASTC_10x6_SRGB:
//         return TIF_MTLPixelFormatASTC_10x6_sRGB;
//     case TinyImageFormat_ASTC_10x8_UNORM:
//         return TIF_MTLPixelFormatASTC_10x8_LDR;
//     case TinyImageFormat_ASTC_10x8_SRGB:
//         return TIF_MTLPixelFormatASTC_10x8_sRGB;
//     case TinyImageFormat_ASTC_10x10_UNORM:
//         return TIF_MTLPixelFormatASTC_10x10_LDR;
//     case TinyImageFormat_ASTC_10x10_SRGB:
//         return TIF_MTLPixelFormatASTC_10x10_sRGB;
//     case TinyImageFormat_ASTC_12x10_UNORM:
//         return TIF_MTLPixelFormatASTC_12x10_LDR;
//     case TinyImageFormat_ASTC_12x10_SRGB:
//         return TIF_MTLPixelFormatASTC_12x10_sRGB;
//     case TinyImageFormat_ASTC_12x12_UNORM:
//         return TIF_MTLPixelFormatASTC_12x12_LDR;
//     case TinyImageFormat_ASTC_12x12_SRGB:
//         return TIF_MTLPixelFormatASTC_12x12_sRGB;

//     default:
//         return TIF_MTLPixelFormatInvalid;
//     }

//     return TIF_MTLPixelFormatInvalid;
// }

// static inline TinyImageFormat TinyImageFormat_FromMTLPixelFormat(TinyImageFormat_MTLPixelFormat fmt)
// {
//     switch (fmt)
//     {
//     case TIF_MTLPixelFormatInvalid:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatA8Unorm:
//         return TinyImageFormat_A8_UNORM;
//     case TIF_MTLPixelFormatR8Unorm:
//         return TinyImageFormat_R8_UNORM;
//     case TIF_MTLPixelFormatR8Unorm_sRGB:
//         return TinyImageFormat_R8_SRGB;
//     case TIF_MTLPixelFormatR8Snorm:
//         return TinyImageFormat_R8_SNORM;
//     case TIF_MTLPixelFormatR8Uint:
//         return TinyImageFormat_R8_UINT;
//     case TIF_MTLPixelFormatR8Sint:
//         return TinyImageFormat_R8_SINT;
//     case TIF_MTLPixelFormatR16Unorm:
//         return TinyImageFormat_R16_UNORM;
//     case TIF_MTLPixelFormatR16Snorm:
//         return TinyImageFormat_R16_SNORM;
//     case TIF_MTLPixelFormatR16Uint:
//         return TinyImageFormat_R16_UINT;
//     case TIF_MTLPixelFormatR16Sint:
//         return TinyImageFormat_R16_SINT;
//     case TIF_MTLPixelFormatR16Float:
//         return TinyImageFormat_R16_SFLOAT;
//     case TIF_MTLPixelFormatRG8Unorm:
//         return TinyImageFormat_R8G8B8_UNORM;
//     case TIF_MTLPixelFormatRG8Unorm_sRGB:
//         return TinyImageFormat_R8G8_SRGB;
//     case TIF_MTLPixelFormatRG8Snorm:
//         return TinyImageFormat_R8G8_SNORM;
//     case TIF_MTLPixelFormatRG8Uint:
//         return TinyImageFormat_R8G8_UINT;
//     case TIF_MTLPixelFormatRG8Sint:
//         return TinyImageFormat_R8G8_SINT;
//     case TIF_MTLPixelFormatB5G6R5Unorm:
//         return TinyImageFormat_R5G6B5_UNORM;
//     case TIF_MTLPixelFormatA1BGR5Unorm:
//         return TinyImageFormat_R5G5B5A1_UNORM;
//     case TIF_MTLPixelFormatABGR4Unorm:
//         return TinyImageFormat_R4G4B4A4_UNORM;
//     case TIF_MTLPixelFormatBGR5A1Unorm:
//         return TinyImageFormat_A1R5G5B5_UNORM;
//     case TIF_MTLPixelFormatR32Uint:
//         return TinyImageFormat_R32_UINT;
//     case TIF_MTLPixelFormatR32Sint:
//         return TinyImageFormat_R32_SINT;
//     case TIF_MTLPixelFormatR32Float:
//         return TinyImageFormat_R32_SFLOAT;
//     case TIF_MTLPixelFormatRG16Unorm:
//         return TinyImageFormat_R16G16_UNORM;
//     case TIF_MTLPixelFormatRG16Snorm:
//         return TinyImageFormat_R16G16_SNORM;
//     case TIF_MTLPixelFormatRG16Uint:
//         return TinyImageFormat_R16G16B16_UINT;
//     case TIF_MTLPixelFormatRG16Sint:
//         return TinyImageFormat_R16G16_SINT;
//     case TIF_MTLPixelFormatRG16Float:
//         return TinyImageFormat_R16G16_SFLOAT;
//     case TIF_MTLPixelFormatRGBA8Unorm:
//         return TinyImageFormat_R8G8B8A8_UNORM;
//     case TIF_MTLPixelFormatRGBA8Unorm_sRGB:
//         return TinyImageFormat_R8G8B8A8_SRGB;
//     case TIF_MTLPixelFormatRGBA8Snorm:
//         return TinyImageFormat_R8G8B8A8_SNORM;
//     case TIF_MTLPixelFormatRGBA8Uint:
//         return TinyImageFormat_R8G8B8A8_UINT;
//     case TIF_MTLPixelFormatRGBA8Sint:
//         return TinyImageFormat_R8G8B8A8_SINT;
//     case TIF_MTLPixelFormatBGRA8Unorm:
//         return TinyImageFormat_B8G8R8A8_UNORM;
//     case TIF_MTLPixelFormatRGB10A2Unorm:
//         return TinyImageFormat_R10G10B10A2_UNORM;
//     case TIF_MTLPixelFormatRGB10A2Uint:
//         return TinyImageFormat_R10G10B10A2_UNORM;
//     case TIF_MTLPixelFormatRG11B10Float:
//         return TinyImageFormat_B10G11R11_UFLOAT;
//     case TIF_MTLPixelFormatRGB9E5Float:
//         return TinyImageFormat_E5B9G9R9_UFLOAT;
//     case TIF_MTLPixelFormatBGR10A2Unorm:
//         return TinyImageFormat_B10G10R10A2_UNORM;
//     case TIF_MTLPixelFormatRG32Uint:
//         return TinyImageFormat_R32G32_UINT;
//     case TIF_MTLPixelFormatRG32Sint:
//         return TinyImageFormat_R32G32_SINT;
//     case TIF_MTLPixelFormatRG32Float:
//         return TinyImageFormat_R32G32_SFLOAT;
//     case TIF_MTLPixelFormatRGBA16Unorm:
//         return TinyImageFormat_R16G16B16A16_UNORM;
//     case TIF_MTLPixelFormatRGBA16Snorm:
//         return TinyImageFormat_R16G16B16A16_SNORM;
//     case TIF_MTLPixelFormatRGBA16Uint:
//         return TinyImageFormat_R16G16B16A16_UINT;
//     case TIF_MTLPixelFormatRGBA16Sint:
//         return TinyImageFormat_R16G16B16A16_SINT;
//     case TIF_MTLPixelFormatRGBA16Float:
//         return TinyImageFormat_R16G16B16A16_SFLOAT;
//     case TIF_MTLPixelFormatRGBA32Uint:
//         return TinyImageFormat_R32G32B32A32_UINT;
//     case TIF_MTLPixelFormatRGBA32Sint:
//         return TinyImageFormat_R32G32B32A32_SINT;
//     case TIF_MTLPixelFormatRGBA32Float:
//         return TinyImageFormat_R32G32B32A32_SFLOAT;
//     case TIF_MTLPixelFormatBC1_RGBA:
//         return TinyImageFormat_DXBC1_RGBA_SRGB;
//     case TIF_MTLPixelFormatBC1_RGBA_sRGB:
//         return TinyImageFormat_DXBC1_RGBA_SRGB;
//     case TIF_MTLPixelFormatBC2_RGBA:
//         return TinyImageFormat_DXBC2_UNORM;
//     case TIF_MTLPixelFormatBC2_RGBA_sRGB:
//         return TinyImageFormat_DXBC2_SRGB;
//     case TIF_MTLPixelFormatBC3_RGBA:
//         return TinyImageFormat_DXBC3_UNORM;
//     case TIF_MTLPixelFormatBC3_RGBA_sRGB:
//         return TinyImageFormat_DXBC2_SRGB;
//     case TIF_MTLPixelFormatBC4_RUnorm:
//         return TinyImageFormat_DXBC4_UNORM;
//     case TIF_MTLPixelFormatBC4_RSnorm:
//         return TinyImageFormat_DXBC4_SNORM;
//     case TIF_MTLPixelFormatBC5_RGUnorm:
//         return TinyImageFormat_DXBC5_UNORM;
//     case TIF_MTLPixelFormatBC5_RGSnorm:
//         return TinyImageFormat_DXBC5_SNORM;
//     case TIF_MTLPixelFormatBC6H_RGBFloat:
//         return TinyImageFormat_DXBC6H_SFLOAT;
//     case TIF_MTLPixelFormatBC6H_RGBUfloat:
//         return TinyImageFormat_DXBC6H_UFLOAT;
//     case TIF_MTLPixelFormatBC7_RGBAUnorm:
//         return TinyImageFormat_DXBC7_UNORM;
//     case TIF_MTLPixelFormatBC7_RGBAUnorm_sRGB:
//         return TinyImageFormat_DXBC7_SRGB;
//     case TIF_MTLPixelFormatPVRTC_RGB_2BPP:
//         return TinyImageFormat_PVRTC1_2BPP_UNORM;
//     case TIF_MTLPixelFormatPVRTC_RGB_2BPP_sRGB:
//         return TinyImageFormat_PVRTC1_2BPP_SRGB;
//     case TIF_MTLPixelFormatPVRTC_RGB_4BPP:
//         return TinyImageFormat_PVRTC1_4BPP_UNORM;
//     case TIF_MTLPixelFormatPVRTC_RGB_4BPP_sRGB:
//         return TinyImageFormat_PVRTC1_4BPP_SRGB;
//     case TIF_MTLPixelFormatPVRTC_RGBA_2BPP:
//         return TinyImageFormat_PVRTC1_2BPP_UNORM;
//     case TIF_MTLPixelFormatPVRTC_RGBA_2BPP_sRGB:
//         return TinyImageFormat_PVRTC1_2BPP_SRGB;
//     case TIF_MTLPixelFormatPVRTC_RGBA_4BPP:
//         return TinyImageFormat_PVRTC1_4BPP_UNORM;
//     case TIF_MTLPixelFormatPVRTC_RGBA_4BPP_sRGB:
//         return TinyImageFormat_PVRTC1_4BPP_SRGB;
//     case TIF_MTLPixelFormatEAC_R11Unorm:
//         return TinyImageFormat_ETC2_EAC_R11_UNORM;
//     case TIF_MTLPixelFormatEAC_R11Snorm:
//         return TinyImageFormat_ETC2_EAC_R11_SNORM;
//     case TIF_MTLPixelFormatEAC_RG11Unorm:
//         return TinyImageFormat_ETC2_EAC_R11G11_UNORM;
//     case TIF_MTLPixelFormatEAC_RG11Snorm:
//         return TinyImageFormat_ETC2_EAC_R11G11_SNORM;
//     case TIF_MTLPixelFormatEAC_RGBA8:
//         return TinyImageFormat_ETC2_R8G8B8A8_UNORM;
//     case TIF_MTLPixelFormatEAC_RGBA8_sRGB:
//         return TinyImageFormat_ETC2_R8G8B8A8_SRGB;
//     case TIF_MTLPixelFormatETC2_RGB8:
//         return TinyImageFormat_ETC2_R8G8B8_UNORM;
//     case TIF_MTLPixelFormatETC2_RGB8_sRGB:
//         return TinyImageFormat_ETC2_R8G8B8_SRGB;
//     case TIF_MTLPixelFormatETC2_RGB8A1:
//         return TinyImageFormat_ETC2_R8G8B8A1_UNORM;
//     case TIF_MTLPixelFormatETC2_RGB8A1_sRGB:
//         return TinyImageFormat_ETC2_R8G8B8A1_SRGB;
//     case TIF_MTLPixelFormatASTC_4x4_sRGB:
//         return TinyImageFormat_ASTC_4x4_SRGB;
//     case TIF_MTLPixelFormatASTC_5x4_sRGB:
//         return TinyImageFormat_ASTC_5x4_SRGB;
//     case TIF_MTLPixelFormatASTC_5x5_sRGB:
//         return TinyImageFormat_ASTC_5x4_SRGB;
//     case TIF_MTLPixelFormatASTC_6x5_sRGB:
//         return TinyImageFormat_ASTC_6x5_SRGB;
//     case TIF_MTLPixelFormatASTC_6x6_sRGB:
//         return TinyImageFormat_ASTC_6x6_SRGB;
//     case TIF_MTLPixelFormatASTC_8x5_sRGB:
//         return TinyImageFormat_ASTC_8x5_SRGB;
//     case TIF_MTLPixelFormatASTC_8x6_sRGB:
//         return TinyImageFormat_ASTC_8x6_SRGB;
//     case TIF_MTLPixelFormatASTC_8x8_sRGB:
//         return TinyImageFormat_ASTC_8x8_SRGB;
//     case TIF_MTLPixelFormatASTC_10x5_sRGB:
//         return TinyImageFormat_ASTC_10x5_SRGB;
//     case TIF_MTLPixelFormatASTC_10x6_sRGB:
//         return TinyImageFormat_ASTC_10x6_SRGB;
//     case TIF_MTLPixelFormatASTC_10x8_sRGB:
//         return TinyImageFormat_ASTC_10x8_SRGB;
//     case TIF_MTLPixelFormatASTC_10x10_sRGB:
//         return TinyImageFormat_ASTC_10x10_SRGB;
//     case TIF_MTLPixelFormatASTC_12x10_sRGB:
//         return TinyImageFormat_ASTC_12x10_SRGB;
//     case TIF_MTLPixelFormatASTC_12x12_sRGB:
//         return TinyImageFormat_ASTC_12x12_SRGB;
//     case TIF_MTLPixelFormatASTC_4x4_LDR:
//         return TinyImageFormat_ASTC_4x4_UNORM;
//     case TIF_MTLPixelFormatASTC_5x4_LDR:
//         return TinyImageFormat_ASTC_5x4_UNORM;
//     case TIF_MTLPixelFormatASTC_5x5_LDR:
//         return TinyImageFormat_ASTC_5x4_UNORM;
//     case TIF_MTLPixelFormatASTC_6x5_LDR:
//         return TinyImageFormat_ASTC_6x5_UNORM;
//     case TIF_MTLPixelFormatASTC_6x6_LDR:
//         return TinyImageFormat_ASTC_6x6_UNORM;
//     case TIF_MTLPixelFormatASTC_8x5_LDR:
//         return TinyImageFormat_ASTC_8x5_UNORM;
//     case TIF_MTLPixelFormatASTC_8x6_LDR:
//         return TinyImageFormat_ASTC_8x6_UNORM;
//     case TIF_MTLPixelFormatASTC_8x8_LDR:
//         return TinyImageFormat_ASTC_8x8_UNORM;
//     case TIF_MTLPixelFormatASTC_10x5_LDR:
//         return TinyImageFormat_ASTC_10x5_UNORM;
//     case TIF_MTLPixelFormatASTC_10x6_LDR:
//         return TinyImageFormat_ASTC_10x6_UNORM;
//     case TIF_MTLPixelFormatASTC_10x8_LDR:
//         return TinyImageFormat_ASTC_10x8_UNORM;
//     case TIF_MTLPixelFormatASTC_10x10_LDR:
//         return TinyImageFormat_ASTC_10x10_UNORM;
//     case TIF_MTLPixelFormatASTC_12x10_LDR:
//         return TinyImageFormat_ASTC_12x10_UNORM;
//     case TIF_MTLPixelFormatASTC_12x12_LDR:
//         return TinyImageFormat_ASTC_12x12_UNORM;
//     case TIF_MTLPixelFormatDepth16Unorm:
//         return TinyImageFormat_D16_UNORM;
//     case TIF_MTLPixelFormatDepth32Float:
//         return TinyImageFormat_D32_SFLOAT;
//     case TIF_MTLPixelFormatStencil8:
//         return TinyImageFormat_S8_UINT;
//     case TIF_MTLPixelFormatDepth24Unorm_Stencil8:
//         return TinyImageFormat_D24_UNORM_S8_UINT;
//     case TIF_MTLPixelFormatDepth32Float_Stencil8:
//         return TinyImageFormat_D32_SFLOAT_S8_UINT;
//     case TIF_MTLPixelFormatX32_Stencil8:
//         return TinyImageFormat_D32_SFLOAT_S8_UINT;
//     case TIF_MTLPixelFormatX24_Stencil8:
//         return TinyImageFormat_D24_UNORM_S8_UINT;

//     case TIF_MTLPixelFormatBGRA8Unorm_sRGB:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatBGR10_XR:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatBGR10_XR_sRGB:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatBGRA10_XR:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatBGRA10_XR_sRGB:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatGBGR422:
//         return TinyImageFormat_UNDEFINED;
//     case TIF_MTLPixelFormatBGRG422:
//         return TinyImageFormat_UNDEFINED;
//     }
//     return TinyImageFormat_UNDEFINED;
// }

// static inline bool TinyImageFormat_MTLPixelFormatOnMac(TinyImageFormat_MTLPixelFormat fmt)
// {
//     switch (fmt)
//     {
//     case TIF_MTLPixelFormatA8Unorm:
//     case TIF_MTLPixelFormatR8Unorm:
//     case TIF_MTLPixelFormatR8Snorm:
//     case TIF_MTLPixelFormatR8Uint:
//     case TIF_MTLPixelFormatR8Sint:
//     case TIF_MTLPixelFormatR16Unorm:
//     case TIF_MTLPixelFormatR16Snorm:
//     case TIF_MTLPixelFormatR16Uint:
//     case TIF_MTLPixelFormatR16Sint:
//     case TIF_MTLPixelFormatR16Float:
//     case TIF_MTLPixelFormatRG8Unorm:
//     case TIF_MTLPixelFormatRG8Snorm:
//     case TIF_MTLPixelFormatRG8Uint:
//     case TIF_MTLPixelFormatRG8Sint:
//     case TIF_MTLPixelFormatR32Uint:
//     case TIF_MTLPixelFormatR32Sint:
//     case TIF_MTLPixelFormatR32Float:
//     case TIF_MTLPixelFormatRG16Unorm:
//     case TIF_MTLPixelFormatRG16Snorm:
//     case TIF_MTLPixelFormatRG16Uint:
//     case TIF_MTLPixelFormatRG16Sint:
//     case TIF_MTLPixelFormatRG16Float:
//     case TIF_MTLPixelFormatRGBA8Unorm:
//     case TIF_MTLPixelFormatRGBA8Unorm_sRGB:
//     case TIF_MTLPixelFormatRGBA8Snorm:
//     case TIF_MTLPixelFormatRGBA8Uint:
//     case TIF_MTLPixelFormatRGBA8Sint:
//     case TIF_MTLPixelFormatBGRA8Unorm:
//     case TIF_MTLPixelFormatBGRA8Unorm_sRGB:
//     case TIF_MTLPixelFormatRGB10A2Unorm:
//     case TIF_MTLPixelFormatRGB10A2Uint:
//     case TIF_MTLPixelFormatRG11B10Float:
//     case TIF_MTLPixelFormatRGB9E5Float:
//     case TIF_MTLPixelFormatBGR10A2Unorm:
//     case TIF_MTLPixelFormatRG32Uint:
//     case TIF_MTLPixelFormatRG32Sint:
//     case TIF_MTLPixelFormatRG32Float:
//     case TIF_MTLPixelFormatRGBA16Unorm:
//     case TIF_MTLPixelFormatRGBA16Snorm:
//     case TIF_MTLPixelFormatRGBA16Uint:
//     case TIF_MTLPixelFormatRGBA16Sint:
//     case TIF_MTLPixelFormatRGBA16Float:
//     case TIF_MTLPixelFormatRGBA32Uint:
//     case TIF_MTLPixelFormatRGBA32Sint:
//     case TIF_MTLPixelFormatRGBA32Float:
//     case TIF_MTLPixelFormatBC1_RGBA:
//     case TIF_MTLPixelFormatBC1_RGBA_sRGB:
//     case TIF_MTLPixelFormatBC2_RGBA:
//     case TIF_MTLPixelFormatBC2_RGBA_sRGB:
//     case TIF_MTLPixelFormatBC3_RGBA:
//     case TIF_MTLPixelFormatBC3_RGBA_sRGB:
//     case TIF_MTLPixelFormatBC4_RUnorm:
//     case TIF_MTLPixelFormatBC4_RSnorm:
//     case TIF_MTLPixelFormatBC5_RGUnorm:
//     case TIF_MTLPixelFormatBC5_RGSnorm:
//     case TIF_MTLPixelFormatBC6H_RGBFloat:
//     case TIF_MTLPixelFormatBC6H_RGBUfloat:
//     case TIF_MTLPixelFormatBC7_RGBAUnorm:
//     case TIF_MTLPixelFormatBC7_RGBAUnorm_sRGB:
//     case TIF_MTLPixelFormatGBGR422:
//     case TIF_MTLPixelFormatBGRG422:
//     case TIF_MTLPixelFormatDepth16Unorm:
//     case TIF_MTLPixelFormatDepth32Float:
//     case TIF_MTLPixelFormatStencil8:
//     case TIF_MTLPixelFormatDepth24Unorm_Stencil8:
//     case TIF_MTLPixelFormatDepth32Float_Stencil8:
//     case TIF_MTLPixelFormatX32_Stencil8:
//     case TIF_MTLPixelFormatX24_Stencil8:
//         return true;

//     case TIF_MTLPixelFormatBGRA10_XR:
//     case TIF_MTLPixelFormatBGRA10_XR_sRGB:
//     case TIF_MTLPixelFormatBGR10_XR:
//     case TIF_MTLPixelFormatBGR10_XR_sRGB:
//     case TIF_MTLPixelFormatB5G6R5Unorm:
//     case TIF_MTLPixelFormatA1BGR5Unorm:
//     case TIF_MTLPixelFormatABGR4Unorm:
//     case TIF_MTLPixelFormatBGR5A1Unorm:
//     case TIF_MTLPixelFormatR8Unorm_sRGB:
//     case TIF_MTLPixelFormatRG8Unorm_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGB_2BPP:
//     case TIF_MTLPixelFormatPVRTC_RGB_2BPP_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGB_4BPP:
//     case TIF_MTLPixelFormatPVRTC_RGB_4BPP_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGBA_2BPP:
//     case TIF_MTLPixelFormatPVRTC_RGBA_2BPP_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGBA_4BPP:
//     case TIF_MTLPixelFormatPVRTC_RGBA_4BPP_sRGB:
//     case TIF_MTLPixelFormatEAC_R11Unorm:
//     case TIF_MTLPixelFormatEAC_R11Snorm:
//     case TIF_MTLPixelFormatEAC_RG11Unorm:
//     case TIF_MTLPixelFormatEAC_RG11Snorm:
//     case TIF_MTLPixelFormatEAC_RGBA8:
//     case TIF_MTLPixelFormatEAC_RGBA8_sRGB:
//     case TIF_MTLPixelFormatETC2_RGB8:
//     case TIF_MTLPixelFormatETC2_RGB8_sRGB:
//     case TIF_MTLPixelFormatETC2_RGB8A1:
//     case TIF_MTLPixelFormatETC2_RGB8A1_sRGB:
//     case TIF_MTLPixelFormatASTC_4x4_sRGB:
//     case TIF_MTLPixelFormatASTC_5x4_sRGB:
//     case TIF_MTLPixelFormatASTC_5x5_sRGB:
//     case TIF_MTLPixelFormatASTC_6x5_sRGB:
//     case TIF_MTLPixelFormatASTC_6x6_sRGB:
//     case TIF_MTLPixelFormatASTC_8x5_sRGB:
//     case TIF_MTLPixelFormatASTC_8x6_sRGB:
//     case TIF_MTLPixelFormatASTC_8x8_sRGB:
//     case TIF_MTLPixelFormatASTC_10x5_sRGB:
//     case TIF_MTLPixelFormatASTC_10x6_sRGB:
//     case TIF_MTLPixelFormatASTC_10x8_sRGB:
//     case TIF_MTLPixelFormatASTC_10x10_sRGB:
//     case TIF_MTLPixelFormatASTC_12x10_sRGB:
//     case TIF_MTLPixelFormatASTC_12x12_sRGB:
//     case TIF_MTLPixelFormatASTC_4x4_LDR:
//     case TIF_MTLPixelFormatASTC_5x4_LDR:
//     case TIF_MTLPixelFormatASTC_5x5_LDR:
//     case TIF_MTLPixelFormatASTC_6x5_LDR:
//     case TIF_MTLPixelFormatASTC_6x6_LDR:
//     case TIF_MTLPixelFormatASTC_8x5_LDR:
//     case TIF_MTLPixelFormatASTC_8x6_LDR:
//     case TIF_MTLPixelFormatASTC_8x8_LDR:
//     case TIF_MTLPixelFormatASTC_10x5_LDR:
//     case TIF_MTLPixelFormatASTC_10x6_LDR:
//     case TIF_MTLPixelFormatASTC_10x8_LDR:
//     case TIF_MTLPixelFormatASTC_10x10_LDR:
//     case TIF_MTLPixelFormatASTC_12x10_LDR:
//     case TIF_MTLPixelFormatASTC_12x12_LDR:
//     case TIF_MTLPixelFormatInvalid:
//         return false;
//     }
// }

// static inline bool TinyImageFormat_MTLPixelFormatOnIOS(TinyImageFormat_MTLPixelFormat fmt)
// {
//     switch (fmt)
//     {
//     case TIF_MTLPixelFormatA8Unorm:
//     case TIF_MTLPixelFormatR8Unorm:
//     case TIF_MTLPixelFormatR8Snorm:
//     case TIF_MTLPixelFormatR8Uint:
//     case TIF_MTLPixelFormatR8Sint:
//     case TIF_MTLPixelFormatR16Unorm:
//     case TIF_MTLPixelFormatR16Snorm:
//     case TIF_MTLPixelFormatR16Uint:
//     case TIF_MTLPixelFormatR16Sint:
//     case TIF_MTLPixelFormatR16Float:
//     case TIF_MTLPixelFormatRG8Unorm:
//     case TIF_MTLPixelFormatRG8Snorm:
//     case TIF_MTLPixelFormatRG8Uint:
//     case TIF_MTLPixelFormatRG8Sint:
//     case TIF_MTLPixelFormatR32Uint:
//     case TIF_MTLPixelFormatR32Sint:
//     case TIF_MTLPixelFormatR32Float:
//     case TIF_MTLPixelFormatRG16Unorm:
//     case TIF_MTLPixelFormatRG16Snorm:
//     case TIF_MTLPixelFormatRG16Uint:
//     case TIF_MTLPixelFormatRG16Sint:
//     case TIF_MTLPixelFormatRG16Float:
//     case TIF_MTLPixelFormatRGBA8Unorm:
//     case TIF_MTLPixelFormatRGBA8Unorm_sRGB:
//     case TIF_MTLPixelFormatRGBA8Snorm:
//     case TIF_MTLPixelFormatRGBA8Uint:
//     case TIF_MTLPixelFormatRGBA8Sint:
//     case TIF_MTLPixelFormatBGRA8Unorm:
//     case TIF_MTLPixelFormatBGRA8Unorm_sRGB:
//     case TIF_MTLPixelFormatRGB10A2Unorm:
//     case TIF_MTLPixelFormatRGB10A2Uint:
//     case TIF_MTLPixelFormatRG11B10Float:
//     case TIF_MTLPixelFormatRGB9E5Float:
//     case TIF_MTLPixelFormatBGR10A2Unorm:
//     case TIF_MTLPixelFormatRG32Uint:
//     case TIF_MTLPixelFormatRG32Sint:
//     case TIF_MTLPixelFormatRG32Float:
//     case TIF_MTLPixelFormatRGBA16Unorm:
//     case TIF_MTLPixelFormatRGBA16Snorm:
//     case TIF_MTLPixelFormatRGBA16Uint:
//     case TIF_MTLPixelFormatRGBA16Sint:
//     case TIF_MTLPixelFormatRGBA16Float:
//     case TIF_MTLPixelFormatRGBA32Uint:
//     case TIF_MTLPixelFormatRGBA32Sint:
//     case TIF_MTLPixelFormatRGBA32Float:
//     case TIF_MTLPixelFormatGBGR422:
//     case TIF_MTLPixelFormatBGRG422:
//     case TIF_MTLPixelFormatDepth32Float:
//     case TIF_MTLPixelFormatStencil8:
//     case TIF_MTLPixelFormatDepth32Float_Stencil8:
//     case TIF_MTLPixelFormatX32_Stencil8:
//     case TIF_MTLPixelFormatBGRA10_XR:
//     case TIF_MTLPixelFormatBGRA10_XR_sRGB:
//     case TIF_MTLPixelFormatBGR10_XR:
//     case TIF_MTLPixelFormatBGR10_XR_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGB_2BPP:
//     case TIF_MTLPixelFormatPVRTC_RGB_2BPP_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGB_4BPP:
//     case TIF_MTLPixelFormatPVRTC_RGB_4BPP_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGBA_2BPP:
//     case TIF_MTLPixelFormatPVRTC_RGBA_2BPP_sRGB:
//     case TIF_MTLPixelFormatPVRTC_RGBA_4BPP:
//     case TIF_MTLPixelFormatPVRTC_RGBA_4BPP_sRGB:
//     case TIF_MTLPixelFormatEAC_R11Unorm:
//     case TIF_MTLPixelFormatEAC_R11Snorm:
//     case TIF_MTLPixelFormatEAC_RG11Unorm:
//     case TIF_MTLPixelFormatEAC_RG11Snorm:
//     case TIF_MTLPixelFormatEAC_RGBA8:
//     case TIF_MTLPixelFormatEAC_RGBA8_sRGB:
//     case TIF_MTLPixelFormatETC2_RGB8:
//     case TIF_MTLPixelFormatETC2_RGB8_sRGB:
//     case TIF_MTLPixelFormatETC2_RGB8A1:
//     case TIF_MTLPixelFormatETC2_RGB8A1_sRGB:
//     case TIF_MTLPixelFormatASTC_4x4_sRGB:
//     case TIF_MTLPixelFormatASTC_5x4_sRGB:
//     case TIF_MTLPixelFormatASTC_5x5_sRGB:
//     case TIF_MTLPixelFormatASTC_6x5_sRGB:
//     case TIF_MTLPixelFormatASTC_6x6_sRGB:
//     case TIF_MTLPixelFormatASTC_8x5_sRGB:
//     case TIF_MTLPixelFormatASTC_8x6_sRGB:
//     case TIF_MTLPixelFormatASTC_8x8_sRGB:
//     case TIF_MTLPixelFormatASTC_10x5_sRGB:
//     case TIF_MTLPixelFormatASTC_10x6_sRGB:
//     case TIF_MTLPixelFormatASTC_10x8_sRGB:
//     case TIF_MTLPixelFormatASTC_10x10_sRGB:
//     case TIF_MTLPixelFormatASTC_12x10_sRGB:
//     case TIF_MTLPixelFormatASTC_12x12_sRGB:
//     case TIF_MTLPixelFormatASTC_4x4_LDR:
//     case TIF_MTLPixelFormatASTC_5x4_LDR:
//     case TIF_MTLPixelFormatASTC_5x5_LDR:
//     case TIF_MTLPixelFormatASTC_6x5_LDR:
//     case TIF_MTLPixelFormatASTC_6x6_LDR:
//     case TIF_MTLPixelFormatASTC_8x5_LDR:
//     case TIF_MTLPixelFormatASTC_8x6_LDR:
//     case TIF_MTLPixelFormatASTC_8x8_LDR:
//     case TIF_MTLPixelFormatASTC_10x5_LDR:
//     case TIF_MTLPixelFormatASTC_10x6_LDR:
//     case TIF_MTLPixelFormatASTC_10x8_LDR:
//     case TIF_MTLPixelFormatASTC_10x10_LDR:
//     case TIF_MTLPixelFormatASTC_12x10_LDR:
//     case TIF_MTLPixelFormatASTC_12x12_LDR:
//     case TIF_MTLPixelFormatB5G6R5Unorm:
//     case TIF_MTLPixelFormatA1BGR5Unorm:
//     case TIF_MTLPixelFormatABGR4Unorm:
//     case TIF_MTLPixelFormatBGR5A1Unorm:
//     case TIF_MTLPixelFormatR8Unorm_sRGB:
//     case TIF_MTLPixelFormatRG8Unorm_sRGB:
//         return true;

//     case TIF_MTLPixelFormatDepth16Unorm:
//     case TIF_MTLPixelFormatDepth24Unorm_Stencil8:
//     case TIF_MTLPixelFormatX24_Stencil8:
//     case TIF_MTLPixelFormatBC1_RGBA:
//     case TIF_MTLPixelFormatBC1_RGBA_sRGB:
//     case TIF_MTLPixelFormatBC2_RGBA:
//     case TIF_MTLPixelFormatBC2_RGBA_sRGB:
//     case TIF_MTLPixelFormatBC3_RGBA:
//     case TIF_MTLPixelFormatBC3_RGBA_sRGB:
//     case TIF_MTLPixelFormatBC4_RUnorm:
//     case TIF_MTLPixelFormatBC4_RSnorm:
//     case TIF_MTLPixelFormatBC5_RGUnorm:
//     case TIF_MTLPixelFormatBC5_RGSnorm:
//     case TIF_MTLPixelFormatBC6H_RGBFloat:
//     case TIF_MTLPixelFormatBC6H_RGBUfloat:
//     case TIF_MTLPixelFormatBC7_RGBAUnorm:
//     case TIF_MTLPixelFormatBC7_RGBAUnorm_sRGB:
//     case TIF_MTLPixelFormatInvalid:
//         return false;
//     }
// }
// #undef TIF_CONSTEXPR

// // CONFFX
// #ifndef TINYIMAGEFORMAT_GLFORMAT
// #define TINYIMAGEFORMAT_GLFORMAT

// typedef enum TinyImageFormat_GL_FORMAT
// {
//     TIF_GL_FORMAT_UNKNOWN = 0,
//     // Types
//     TIF_GL_TYPE_COMPRESSED = 0x0,
//     TIF_GL_TYPE_BYTE = 0x1400,
//     TIF_GL_TYPE_UNSIGNED_BYTE = 0x1401,
//     TIF_GL_TYPE_SHORT = 0x1402,
//     TIF_GL_TYPE_UNSIGNED_SHORT = 0x1403,
//     TIF_GL_TYPE_INT = 0x1404,
//     TIF_GL_TYPE_UNSIGNED_INT = 0x1405,
//     TIF_GL_TYPE_FLOAT = 0x1406,
//     TIF_GL_TYPE_DOUBLE = 0x140A,
//     TIF_GL_TYPE_HALF_FLOAT = 0x8D61, // OES half float, was 0x140B,
//     TIF_GL_TYPE_UNSIGNED_BYTE_3_3_2 = 0x8032,
//     TIF_GL_TYPE_UNSIGNED_SHORT_4_4_4_4 = 0x8033,
//     TIF_GL_TYPE_UNSIGNED_SHORT_5_5_5_1 = 0x8034,
//     TIF_GL_TYPE_UNSIGNED_INT_8_8_8_8 = 0x8035,
//     TIF_GL_TYPE_UNSIGNED_INT_10_10_10_2 = 0x8036,
//     TIF_GL_TYPE_UNSIGNED_BYTE_2_3_3_REV = 0x8362,
//     TIF_GL_TYPE_UNSIGNED_SHORT_5_6_5 = 0x8363,
//     TIF_GL_TYPE_UNSIGNED_SHORT_5_6_5_REV = 0x8364,
//     TIF_GL_TYPE_UNSIGNED_SHORT_4_4_4_4_REV = 0x8365,
//     TIF_GL_TYPE_UNSIGNED_SHORT_1_5_5_5_REV = 0x8366,
//     TIF_GL_TYPE_UNSIGNED_INT_8_8_8_8_REV = 0x8367,
//     TIF_GL_TYPE_UNSIGNED_INT_2_10_10_10_REV = 0x8368,
//     TIF_GL_TYPE_UNSIGNED_INT_24_8 = 0x84FA,
//     TIF_GL_TYPE_UNSIGNED_INT_5_9_9_9_REV = 0x8C3E,
//     TIF_GL_TYPE_UNSIGNED_INT_10F_11F_11F_REV = 0x8C3B,
//     TIF_GL_TYPE_FLOAT_32_UNSIGNED_INT_24_8_REV = 0x8DAD,
//     // Formats
//     TIF_GL_FORMAT_RED = 0x1903,
//     TIF_GL_FORMAT_GREEN = 0x1904,
//     TIF_GL_FORMAT_BLUE = 0x1905,
//     TIF_GL_FORMAT_ALPHA = 0x1906,
//     TIF_GL_FORMAT_RGB = 0x1907,
//     TIF_GL_FORMAT_RGBA = 0x1908,
//     TIF_GL_FORMAT_LUMINANCE = 0x1909,
//     TIF_GL_FORMAT_LUMINANCE_ALPHA = 0x190A,
//     TIF_GL_FORMAT_ABGR = 0x8000,
//     TIF_GL_FORMAT_INTENSITY = 0x8049,
//     TIF_GL_FORMAT_BGR = 0x80E0,
//     TIF_GL_FORMAT_BGRA = 0x80E1,
//     TIF_GL_FORMAT_RG = 0x8227,
//     TIF_GL_FORMAT_RG_INTEGER = 0x8228,
//     TIF_GL_FORMAT_SRGB = 0x8C40,
//     TIF_GL_FORMAT_SRGB_ALPHA = 0x8C42,
//     TIF_GL_FORMAT_SLUMINANCE_ALPHA = 0x8C44,
//     TIF_GL_FORMAT_SLUMINANCE = 0x8C46,
//     TIF_GL_FORMAT_RED_INTEGER = 0x8D94,
//     TIF_GL_FORMAT_GREEN_INTEGER = 0x8D95,
//     TIF_GL_FORMAT_BLUE_INTEGER = 0x8D96,
//     TIF_GL_FORMAT_ALPHA_INTEGER = 0x8D97,
//     TIF_GL_FORMAT_RGB_INTEGER = 0x8D98,
//     TIF_GL_FORMAT_RGBA_INTEGER = 0x8D99,
//     TIF_GL_FORMAT_BGR_INTEGER = 0x8D9A,
//     TIF_GL_FORMAT_BGRA_INTEGER = 0x8D9B,
//     TIF_GL_FORMAT_RED_SNORM = 0x8F90,
//     TIF_GL_FORMAT_RG_SNORM = 0x8F91,
//     TIF_GL_FORMAT_RGB_SNORM = 0x8F92,
//     TIF_GL_FORMAT_RGBA_SNORM = 0x8F93,
//     // Internal Formats
//     TIF_GL_FORMAT_ALPHA4 = 0x803B,
//     TIF_GL_FORMAT_ALPHA8 = 0x803C,
//     TIF_GL_FORMAT_ALPHA12 = 0x803D,
//     TIF_GL_FORMAT_ALPHA16 = 0x803E,
//     TIF_GL_FORMAT_LUMINANCE4 = 0x803F,
//     TIF_GL_FORMAT_LUMINANCE8 = 0x8040,
//     TIF_GL_FORMAT_LUMINANCE12 = 0x8041,
//     TIF_GL_FORMAT_LUMINANCE16 = 0x8042,
//     TIF_GL_FORMAT_LUMINANCE4_ALPHA4 = 0x8043,
//     TIF_GL_FORMAT_LUMINANCE6_ALPHA2 = 0x8044,
//     TIF_GL_FORMAT_LUMINANCE8_ALPHA8 = 0x8045,
//     TIF_GL_FORMAT_LUMINANCE12_ALPHA4 = 0x8046,
//     TIF_GL_FORMAT_LUMINANCE12_ALPHA12 = 0x8047,
//     TIF_GL_FORMAT_LUMINANCE16_ALPHA16 = 0x8048,
//     TIF_GL_FORMAT_INTENSITY4 = 0x804A,
//     TIF_GL_FORMAT_INTENSITY8 = 0x804B,
//     TIF_GL_FORMAT_INTENSITY12 = 0x804C,
//     TIF_GL_FORMAT_INTENSITY16 = 0x804D,
//     TIF_GL_FORMAT_RGB2 = 0x804E,
//     TIF_GL_FORMAT_RGB4 = 0x804F,
//     TIF_GL_FORMAT_RGB5 = 0x8050,
//     TIF_GL_FORMAT_RGB8 = 0x8051,
//     TIF_GL_FORMAT_RGB10 = 0x8052,
//     TIF_GL_FORMAT_RGB12 = 0x8053,
//     TIF_GL_FORMAT_RGB16 = 0x8054,
//     TIF_GL_FORMAT_RGBA2 = 0x8055,
//     TIF_GL_FORMAT_RGBA4 = 0x8056,
//     TIF_GL_FORMAT_RGB5_A1 = 0x8057,
//     TIF_GL_FORMAT_RGBA8 = 0x8058,
//     TIF_GL_FORMAT_RGB10_A2 = 0x8059,
//     TIF_GL_FORMAT_RGBA12 = 0x805A,
//     TIF_GL_FORMAT_RGBA16 = 0x805B,
//     TIF_GL_FORMAT_R8 = 0x8229,
//     TIF_GL_FORMAT_R16 = 0x822A,
//     TIF_GL_FORMAT_RG8 = 0x822B,
//     TIF_GL_FORMAT_RG16 = 0x822C,
//     TIF_GL_FORMAT_R16F = 0x822D,
//     TIF_GL_FORMAT_R32F = 0x822E,
//     TIF_GL_FORMAT_RG16F = 0x822F,
//     TIF_GL_FORMAT_RG32F = 0x8230,
//     TIF_GL_FORMAT_R8I = 0x8231,
//     TIF_GL_FORMAT_R8UI = 0x8232,
//     TIF_GL_FORMAT_R16I = 0x8233,
//     TIF_GL_FORMAT_R16UI = 0x8234,
//     TIF_GL_FORMAT_R32I = 0x8235,
//     TIF_GL_FORMAT_R32UI = 0x8236,
//     TIF_GL_FORMAT_RG8I = 0x8237,
//     TIF_GL_FORMAT_RG8UI = 0x8238,
//     TIF_GL_FORMAT_RG16I = 0x8239,
//     TIF_GL_FORMAT_RG16UI = 0x823A,
//     TIF_GL_FORMAT_RG32I = 0x823B,
//     TIF_GL_FORMAT_RG32UI = 0x823C,
//     TIF_GL_FORMAT_RGBA32F = 0x8814,
//     TIF_GL_FORMAT_RGB32F = 0x8815,
//     TIF_GL_FORMAT_RGBA16F = 0x881A,
//     TIF_GL_FORMAT_RGB16F = 0x881B,
//     TIF_GL_FORMAT_R11F_G11F_B10F = 0x8C3A,
//     TIF_GL_FORMAT_UNSIGNED_INT_10F_11F_11F_REV = 0x8C3B,
//     TIF_GL_FORMAT_RGB9_E5 = 0x8C3D,
//     TIF_GL_FORMAT_SRGB8 = 0x8C41,
//     TIF_GL_FORMAT_SRGB8_ALPHA8 = 0x8C43,
//     TIF_GL_FORMAT_SLUMINANCE8_ALPHA8 = 0x8C45,
//     TIF_GL_FORMAT_SLUMINANCE8 = 0x8C47,
//     TIF_GL_FORMAT_RGB565 = 0x8D62,
//     TIF_GL_FORMAT_RGBA32UI = 0x8D70,
//     TIF_GL_FORMAT_RGB32UI = 0x8D71,
//     TIF_GL_FORMAT_RGBA16UI = 0x8D76,
//     TIF_GL_FORMAT_RGB16UI = 0x8D77,
//     TIF_GL_FORMAT_RGBA8UI = 0x8D7C,
//     TIF_GL_FORMAT_RGB8UI = 0x8D7D,
//     TIF_GL_FORMAT_RGBA32I = 0x8D82,
//     TIF_GL_FORMAT_RGB32I = 0x8D83,
//     TIF_GL_FORMAT_RGBA16I = 0x8D88,
//     TIF_GL_FORMAT_RGB16I = 0x8D89,
//     TIF_GL_FORMAT_RGBA8I = 0x8D8E,
//     TIF_GL_FORMAT_RGB8I = 0x8D8F,
//     TIF_GL_FORMAT_FLOAT_32_UNSIGNED_INT_24_8_REV = 0x8DAD,
//     TIF_GL_FORMAT_R8_SNORM = 0x8F94,
//     TIF_GL_FORMAT_RG8_SNORM = 0x8F95,
//     TIF_GL_FORMAT_RGB8_SNORM = 0x8F96,
//     TIF_GL_FORMAT_RGBA8_SNORM = 0x8F97,
//     TIF_GL_FORMAT_R16_SNORM = 0x8F98,
//     TIF_GL_FORMAT_RG16_SNORM = 0x8F99,
//     TIF_GL_FORMAT_RGB16_SNORM = 0x8F9A,
//     TIF_GL_FORMAT_RGBA16_SNORM = 0x8F9B,
//     TIF_GL_FORMAT_ALPHA8_SNORM = 0x9014,
//     TIF_GL_FORMAT_LUMINANCE8_SNORM = 0x9015,
//     TIF_GL_FORMAT_LUMINANCE8_ALPHA8_SNORM = 0x9016,
//     TIF_GL_FORMAT_INTENSITY8_SNORM = 0x9017,
//     TIF_GL_FORMAT_ALPHA16_SNORM = 0x9018,
//     TIF_GL_FORMAT_LUMINANCE16_SNORM = 0x9019,
//     TIF_GL_FORMAT_LUMINANCE16_ALPHA16_SNORM = 0x901A,
//     TIF_GL_FORMAT_INTENSITY16_SNORM = 0x901B,
//     // Compressed Formats
//     TIF_GL_FORMAT_RGBA_ASTC_4x4 = 0x93B0,
//     TIF_GL_FORMAT_RGBA_ASTC_5x4 = 0x93B1,
//     TIF_GL_FORMAT_RGBA_ASTC_5x5 = 0x93B2,
//     TIF_GL_FORMAT_RGBA_ASTC_6x5 = 0x93B3,
//     TIF_GL_FORMAT_RGBA_ASTC_6x6 = 0x93B4,
//     TIF_GL_FORMAT_RGBA_ASTC_8x5 = 0x93B5,
//     TIF_GL_FORMAT_RGBA_ASTC_8x6 = 0x93B6,
//     TIF_GL_FORMAT_RGBA_ASTC_8x8 = 0x93B7,
//     TIF_GL_FORMAT_RGBA_ASTC_10x5 = 0x93B8,
//     TIF_GL_FORMAT_RGBA_ASTC_10x6 = 0x93B9,
//     TIF_GL_FORMAT_RGBA_ASTC_10x8 = 0x93BA,
//     TIF_GL_FORMAT_RGBA_ASTC_10x10 = 0x93BB,
//     TIF_GL_FORMAT_RGBA_ASTC_12x10 = 0x93BC,
//     TIF_GL_FORMAT_RGBA_ASTC_12x12 = 0x93BD,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_4x4 = 0x93D0,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_5x4 = 0x93D1,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_5x5 = 0x93D2,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_6x5 = 0x93D3,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_6x6 = 0x93D4,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_8x5 = 0x93D5,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_8x6 = 0x93D6,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_8x8 = 0x93D7,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_10x5 = 0x93D8,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_10x6 = 0x93D9,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_10x8 = 0x93DA,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_10x10 = 0x93DB,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_12x10 = 0x93DC,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ASTC_12x12 = 0x93DD,
//     TIF_GL_FORMAT_RGB_S3TC_DXT1 = 0x83F0,
//     TIF_GL_FORMAT_RGBA_S3TC_DXT1 = 0x83F1,
//     TIF_GL_FORMAT_RGBA_S3TC_DXT3 = 0x83F2,
//     TIF_GL_FORMAT_RGBA_S3TC_DXT5 = 0x83F3,
//     TIF_GL_FORMAT_3DC_X_AMD = 0x87F9,
//     TIF_GL_FORMAT_3DC_XY_AMD = 0x87FA,
//     TIF_GL_FORMAT_ATC_RGBA_INTERPOLATED_ALPHA = 0x87EE,
//     TIF_GL_FORMAT_SRGB_PVRTC_2BPPV1 = 0x8A54,
//     TIF_GL_FORMAT_SRGB_PVRTC_4BPPV1 = 0x8A55,
//     TIF_GL_FORMAT_SRGB_ALPHA_PVRTC_2BPPV1 = 0x8A56,
//     TIF_GL_FORMAT_SRGB_ALPHA_PVRTC_4BPPV1 = 0x8A57,
//     TIF_GL_FORMAT_RGB_PVRTC_4BPPV1 = 0x8C00,
//     TIF_GL_FORMAT_RGB_PVRTC_2BPPV1 = 0x8C01,
//     TIF_GL_FORMAT_RGBA_PVRTC_4BPPV1 = 0x8C02,
//     TIF_GL_FORMAT_RGBA_PVRTC_2BPPV1 = 0x8C03,
//     TIF_GL_FORMAT_SRGB_S3TC_DXT1 = 0x8C4C,
//     TIF_GL_FORMAT_SRGB_ALPHA_S3TC_DXT1 = 0x8C4D,
//     TIF_GL_FORMAT_SRGB_ALPHA_S3TC_DXT3 = 0x8C4E,
//     TIF_GL_FORMAT_SRGB_ALPHA_S3TC_DXT5 = 0x8C4F,
//     TIF_GL_FORMAT_LUMINANCE_LATC1 = 0x8C70,
//     TIF_GL_FORMAT_SIGNED_LUMINANCE_LATC1 = 0x8C71,
//     TIF_GL_FORMAT_LUMINANCE_ALPHA_LATC2 = 0x8C72,
//     TIF_GL_FORMAT_SIGNED_LUMINANCE_ALPHA_LATC2 = 0x8C73,
//     TIF_GL_FORMAT_ATC_RGB = 0x8C92,
//     TIF_GL_FORMAT_ATC_RGBA_EXPLICIT_ALPHA = 0x8C93,
//     TIF_GL_FORMAT_RED_RGTC1 = 0x8DBB,
//     TIF_GL_FORMAT_SIGNED_RED_RGTC1 = 0x8DBC,
//     TIF_GL_FORMAT_RED_GREEN_RGTC2 = 0x8DBD,
//     TIF_GL_FORMAT_SIGNED_RED_GREEN_RGTC2 = 0x8DBE,
//     TIF_GL_FORMAT_ETC1_RGB8_OES = 0x8D64,
//     TIF_GL_FORMAT_RGBA_BPTC_UNORM = 0x8E8C,
//     TIF_GL_FORMAT_SRGB_ALPHA_BPTC_UNORM = 0x8E8D,
//     TIF_GL_FORMAT_RGB_BPTC_SIGNED_FLOAT = 0x8E8E,
//     TIF_GL_FORMAT_RGB_BPTC_UNSIGNED_FLOAT = 0x8E8F,
//     TIF_GL_FORMAT_R11_EAC = 0x9270,
//     TIF_GL_FORMAT_SIGNED_R11_EAC = 0x9271,
//     TIF_GL_FORMAT_RG11_EAC = 0x9272,
//     TIF_GL_FORMAT_SIGNED_RG11_EAC = 0x9273,
//     TIF_GL_FORMAT_RGB8_ETC2 = 0x9274,
//     TIF_GL_FORMAT_SRGB8_ETC2 = 0x9275,
//     TIF_GL_FORMAT_RGB8_PUNCHTHROUGH_ALPHA1_ETC2 = 0x9276,
//     TIF_GL_FORMAT_SRGB8_PUNCHTHROUGH_ALPHA1_ETC2 = 0x9277,
//     TIF_GL_FORMAT_RGBA8_ETC2_EAC = 0x9278,
//     TIF_GL_FORMAT_SRGB8_ALPHA8_ETC2_EAC = 0x9279,
//     TIF_GL_FORMAT_SRGB_ALPHA_PVRTC_2BPPV2 = 0x93F0,
//     TIF_GL_FORMAT_SRGB_ALPHA_PVRTC_4BPPV2 = 0x93F1,
//     // Depth Formats
//     TIF_GL_FORMAT_DEPTH_COMPONENT = 0x1902,
//     TIF_GL_FORMAT_DEPTH_STENCIL = 0x84F9,
//     // Depth Internal Formats
//     TIF_GL_FORMAT_STENCIL_INDEX8 = 0x8D48,
//     TIF_GL_FORMAT_DEPTH_COMPONENT16 = 0x81A5,
//     TIF_GL_FORMAT_DEPTH24_STENCIL8 = 0x88F0,
//     TIF_GL_FORMAT_DEPTH_COMPONENT32F = 0x8CAC,

// } TinyImageFormat_GL_FORMAT;
// #endif

// #define FT(fmt, type, intfmt, size)             \
//     *glformat = TIF_GL_FORMAT_##fmt;            \
//     *gltype = TIF_GL_TYPE_##type;               \
//     *glinternalformat = TIF_GL_FORMAT_##intfmt; \
//     *typesize = size;                           \
//     return;
// #define FTC(fmt, intfmt)                        \
//     *glformat = TIF_GL_FORMAT_##fmt;            \
//     *gltype = TIF_GL_TYPE_COMPRESSED;           \
//     *glinternalformat = TIF_GL_FORMAT_##intfmt; \
//     *typesize = 1;                              \
//     return;

// static inline void TinyImageFormat_ToGL_FORMAT(TinyImageFormat fmt, uint32_t* glformat, uint32_t* gltype, uint32_t* glinternalformat,
//                                                uint32_t* typesize)
// {
//     switch (fmt)
//     {
//     case TinyImageFormat_UNDEFINED:
//         FT(UNKNOWN, UNSIGNED_SHORT_4_4_4_4, UNKNOWN, 0)

//     case TinyImageFormat_R4G4_UNORM:
//         FT(RG, UNSIGNED_SHORT_4_4_4_4, RGB4, 1)
//     case TinyImageFormat_R4G4B4A4_UNORM:
//         FT(RGBA, UNSIGNED_SHORT_4_4_4_4, RGBA4, 2)
//     case TinyImageFormat_B4G4R4A4_UNORM:
//         FT(BGRA, UNSIGNED_SHORT_4_4_4_4_REV, RGBA4, 2)
//     case TinyImageFormat_R5G6B5_UNORM:
//         FT(RGB, UNSIGNED_SHORT_5_6_5_REV, RGB565, 2)
//     case TinyImageFormat_B5G6R5_UNORM:
//         FT(BGR, UNSIGNED_SHORT_5_6_5, RGB565, 2)
//     case TinyImageFormat_R5G5B5A1_UNORM:
//         FT(RGBA, UNSIGNED_SHORT_5_5_5_1, RGB5_A1, 2)
//     case TinyImageFormat_B5G5R5A1_UNORM:
//         FT(BGRA, UNSIGNED_SHORT_5_5_5_1, RGB5_A1, 2)
//     case TinyImageFormat_A1R5G5B5_UNORM:
//         FT(RGBA, UNSIGNED_SHORT_1_5_5_5_REV, RGB5_A1, 2)
//     case TinyImageFormat_R8_UNORM:
//         FT(LUMINANCE, UNSIGNED_BYTE, R8, 1) // RED -> LUMINANCE For older api's
//     case TinyImageFormat_R8_SNORM:
//         FT(RED, BYTE, R8_SNORM, 1)
//     case TinyImageFormat_R8_UINT:
//         FT(RED_INTEGER, UNSIGNED_BYTE, R8UI, 1)
//     case TinyImageFormat_R8_SINT:
//         FT(RED_INTEGER, BYTE, R8I, 1)
//     case TinyImageFormat_R8_SRGB:
//         FT(SLUMINANCE, UNSIGNED_BYTE, SRGB8, 1)
//     case TinyImageFormat_R8G8_UNORM:
//         FT(RG, UNSIGNED_BYTE, RG8, 1)
//     case TinyImageFormat_R8G8_SNORM:
//         FT(RG, BYTE, RG8_SNORM, 1)
//     case TinyImageFormat_R8G8_UINT:
//         FT(RG_INTEGER, UNSIGNED_BYTE, RG8UI, 1)
//     case TinyImageFormat_R8G8_SINT:
//         FT(RG_INTEGER, BYTE, RG8I, 1)
//     case TinyImageFormat_R8G8_SRGB:
//         FT(SLUMINANCE_ALPHA, UNSIGNED_BYTE, SRGB8, 1)
//     case TinyImageFormat_R8G8B8_UNORM:
//         FT(RGB, UNSIGNED_BYTE, RGB8, 1)
//     case TinyImageFormat_R8G8B8_SNORM:
//         FT(RGB, BYTE, RGB8_SNORM, 1)
//     case TinyImageFormat_R8G8B8_UINT:
//         FT(RGB_INTEGER, UNSIGNED_BYTE, RGB8UI, 1)
//     case TinyImageFormat_R8G8B8_SINT:
//         FT(RGB_INTEGER, BYTE, RGB8I, 1)
//     case TinyImageFormat_R8G8B8_SRGB:
//         FT(SRGB, UNSIGNED_BYTE, SRGB8, 1)
//     case TinyImageFormat_B8G8R8_UNORM:
//         FT(BGR, UNSIGNED_BYTE, RGB8, 1)
//     case TinyImageFormat_B8G8R8_SNORM:
//         FT(BGR, BYTE, RGB8_SNORM, 1)
//     case TinyImageFormat_B8G8R8_UINT:
//         FT(BGR_INTEGER, UNSIGNED_BYTE, RGB8UI, 1)
//     case TinyImageFormat_B8G8R8_SINT:
//         FT(BGR_INTEGER, BYTE, RGB8I, 1)
//     case TinyImageFormat_B8G8R8_SRGB:
//         FT(BGR, UNSIGNED_BYTE, SRGB8, 1)
//     case TinyImageFormat_R8G8B8A8_UNORM:
//         FT(RGBA, UNSIGNED_BYTE, RGBA8, 1)
//     case TinyImageFormat_R8G8B8A8_SNORM:
//         FT(RGBA, BYTE, RGBA8_SNORM, 1)
//     case TinyImageFormat_R8G8B8A8_UINT:
//         FT(RGBA_INTEGER, UNSIGNED_BYTE, RGBA8UI, 1)
//     case TinyImageFormat_R8G8B8A8_SINT:
//         FT(RGBA_INTEGER, BYTE, RGBA8I, 1)
//     case TinyImageFormat_R8G8B8A8_SRGB:
//         FT(SRGB_ALPHA, UNSIGNED_BYTE, SRGB8, 1)
//     case TinyImageFormat_B8G8R8A8_UNORM:
//         FT(BGRA, UNSIGNED_BYTE, RGBA8, 1)
//     case TinyImageFormat_B8G8R8A8_SNORM:
//         FT(BGRA, BYTE, RGBA8_SNORM, 1)
//     case TinyImageFormat_B8G8R8A8_UINT:
//         FT(BGRA_INTEGER, UNSIGNED_BYTE, RGBA8UI, 1)
//     case TinyImageFormat_B8G8R8A8_SINT:
//         FT(BGRA_INTEGER, BYTE, RGBA8I, 1)
//     case TinyImageFormat_B8G8R8A8_SRGB:
//         FT(BGRA, UNSIGNED_BYTE, SRGB8, 1)
//     case TinyImageFormat_R10G10B10A2_UNORM:
//         FT(RGBA, UNSIGNED_INT_2_10_10_10_REV, RGB10_A2, 4)
//     case TinyImageFormat_R10G10B10A2_UINT:
//         FT(RGBA_INTEGER, UNSIGNED_INT_2_10_10_10_REV, RGB10_A2, 4)
//     case TinyImageFormat_B10G10R10A2_UNORM:
//         FT(BGRA, UNSIGNED_INT_2_10_10_10_REV, RGB10_A2, 4)
//     case TinyImageFormat_B10G10R10A2_UINT:
//         FT(BGRA_INTEGER, UNSIGNED_INT_2_10_10_10_REV, RGB10_A2, 4)
//     case TinyImageFormat_R16_UNORM:
//         FT(RED, UNSIGNED_SHORT, R16, 2)
//     case TinyImageFormat_R16_SNORM:
//         FT(RED, SHORT, R16_SNORM, 2)
//     case TinyImageFormat_R16_UINT:
//         FT(RED_INTEGER, UNSIGNED_SHORT, R16UI, 2)
//     case TinyImageFormat_R16_SINT:
//         FT(RED_INTEGER, SHORT, R16I, 2)
//     case TinyImageFormat_R16_SFLOAT:
//         FT(RED, HALF_FLOAT, R16F, 2)
//     case TinyImageFormat_R16G16_UNORM:
//         FT(RG, UNSIGNED_SHORT, RG16, 2)
//     case TinyImageFormat_R16G16_SNORM:
//         FT(RG, SHORT, RG16_SNORM, 2)
//     case TinyImageFormat_R16G16_UINT:
//         FT(RG_INTEGER, UNSIGNED_SHORT, RG16UI, 2)
//     case TinyImageFormat_R16G16_SINT:
//         FT(RG_INTEGER, SHORT, RG16I, 2)
//     case TinyImageFormat_R16G16_SFLOAT:
//         FT(RG, HALF_FLOAT, RG16F, 2)
//     case TinyImageFormat_R16G16B16_UNORM:
//         FT(RGB, UNSIGNED_SHORT, RGB16, 2)
//     case TinyImageFormat_R16G16B16_SNORM:
//         FT(RGB, SHORT, RGB16_SNORM, 2)
//     case TinyImageFormat_R16G16B16_UINT:
//         FT(RGB_INTEGER, UNSIGNED_SHORT, RGB16UI, 2)
//     case TinyImageFormat_R16G16B16_SINT:
//         FT(RGB_INTEGER, SHORT, RGB16I, 2)
//     case TinyImageFormat_R16G16B16_SFLOAT:
//         FT(RGB, HALF_FLOAT, RGB16F, 2)
//     case TinyImageFormat_R16G16B16A16_UNORM:
//         FT(RGBA, UNSIGNED_SHORT, RGBA16, 2)
//     case TinyImageFormat_R16G16B16A16_SNORM:
//         FT(RGBA, SHORT, RGBA16_SNORM, 2)
//     case TinyImageFormat_R16G16B16A16_UINT:
//         FT(RGBA_INTEGER, UNSIGNED_SHORT, RGBA16UI, 2)
//     case TinyImageFormat_R16G16B16A16_SINT:
//         FT(RGBA_INTEGER, SHORT, RGBA16I, 2)
//     case TinyImageFormat_R16G16B16A16_SFLOAT:
//         FT(RGBA, HALF_FLOAT, RGBA16F, 2)
//     case TinyImageFormat_R32_UINT:
//         FT(RED_INTEGER, UNSIGNED_INT, R32UI, 4)
//     case TinyImageFormat_R32_SINT:
//         FT(RED_INTEGER, INT, R32I, 4)
//     case TinyImageFormat_R32_SFLOAT:
//         FT(RED, FLOAT, R32F, 4)
//     case TinyImageFormat_R32G32_UINT:
//         FT(RG_INTEGER, UNSIGNED_INT, RG32UI, 4)
//     case TinyImageFormat_R32G32_SINT:
//         FT(RG_INTEGER, INT, RG32I, 4)
//     case TinyImageFormat_R32G32_SFLOAT:
//         FT(RG, FLOAT, RG32F, 4)
//     case TinyImageFormat_R32G32B32_UINT:
//         FT(RGB_INTEGER, UNSIGNED_INT, RGB32UI, 4)
//     case TinyImageFormat_R32G32B32_SINT:
//         FT(RGB_INTEGER, INT, RGB32I, 4)
//     case TinyImageFormat_R32G32B32_SFLOAT:
//         FT(RGB_INTEGER, FLOAT, RGB32F, 4)
//     case TinyImageFormat_R32G32B32A32_UINT:
//         FT(RGBA_INTEGER, UNSIGNED_INT, RGBA32UI, 4)
//     case TinyImageFormat_R32G32B32A32_SINT:
//         FT(RGBA_INTEGER, INT, RGBA32I, 4)
//     case TinyImageFormat_R32G32B32A32_SFLOAT:
//         FT(RGBA, FLOAT, RGBA32F, 4)
//     case TinyImageFormat_B10G11R11_UFLOAT:
//         FT(BGR, UNSIGNED_INT_10F_11F_11F_REV, R11F_G11F_B10F, 4)
//     case TinyImageFormat_E5B9G9R9_UFLOAT:
//         FT(BGR, UNSIGNED_INT_5_9_9_9_REV, RGB9_E5, 4);

//         // Compressed Formats
//     case TinyImageFormat_DXBC1_RGB_UNORM:
//         FTC(RGB, RGB_S3TC_DXT1)
//     case TinyImageFormat_DXBC1_RGB_SRGB:
//         FTC(RGB, SRGB_S3TC_DXT1)
//     case TinyImageFormat_DXBC1_RGBA_UNORM:
//         FTC(RGBA, RGBA_S3TC_DXT1)
//     case TinyImageFormat_DXBC1_RGBA_SRGB:
//         FTC(RGBA, SRGB_ALPHA_S3TC_DXT1)
//     case TinyImageFormat_DXBC2_UNORM:
//         FTC(RGBA, RGBA_S3TC_DXT3)
//     case TinyImageFormat_DXBC2_SRGB:
//         FTC(RGBA, SRGB_ALPHA_S3TC_DXT3)
//     case TinyImageFormat_DXBC3_UNORM:
//         FTC(RGBA, RGBA_S3TC_DXT5)
//     case TinyImageFormat_DXBC3_SRGB:
//         FTC(RGBA, SRGB_ALPHA_S3TC_DXT5)
//     case TinyImageFormat_DXBC4_UNORM:
//         FTC(RED, RED_RGTC1)
//     case TinyImageFormat_DXBC4_SNORM:
//         FTC(RED, SIGNED_RED_RGTC1)
//     case TinyImageFormat_DXBC5_UNORM:
//         FTC(RG, RED_GREEN_RGTC2)
//     case TinyImageFormat_DXBC5_SNORM:
//         FTC(RG, SIGNED_RED_GREEN_RGTC2)
//     case TinyImageFormat_DXBC6H_UFLOAT:
//         FTC(RGB, RGB_BPTC_UNSIGNED_FLOAT)
//     case TinyImageFormat_DXBC6H_SFLOAT:
//         FTC(RGB, RGB_BPTC_SIGNED_FLOAT)
//     case TinyImageFormat_DXBC7_UNORM:
//         FTC(RGBA, RGBA_BPTC_UNORM)
//     case TinyImageFormat_DXBC7_SRGB:
//         FTC(RGBA, SRGB_ALPHA_BPTC_UNORM)
//     case TinyImageFormat_PVRTC1_2BPP_UNORM:
//         FTC(RGB, RGB_PVRTC_2BPPV1)
//     case TinyImageFormat_PVRTC1_4BPP_UNORM:
//         FTC(RGB, RGB_PVRTC_4BPPV1)
//     case TinyImageFormat_PVRTC1_2BPP_SRGB:
//         FTC(SRGB, SRGB_PVRTC_2BPPV1)
//     case TinyImageFormat_PVRTC1_4BPP_SRGB:
//         FTC(SRGB, SRGB_PVRTC_4BPPV1)

//     case TinyImageFormat_ETC2_R8G8B8_UNORM:
//         FTC(RGB, RGB8_ETC2)
//     case TinyImageFormat_ETC2_R8G8B8A1_UNORM:
//         FTC(RGBA, RGB8_PUNCHTHROUGH_ALPHA1_ETC2)
//     case TinyImageFormat_ETC2_R8G8B8A8_UNORM:
//         FTC(RGBA, RGBA8_ETC2_EAC)
//     case TinyImageFormat_ETC2_R8G8B8_SRGB:
//         FTC(SRGB, SRGB8_ETC2)
//     case TinyImageFormat_ETC2_R8G8B8A1_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_PUNCHTHROUGH_ALPHA1_ETC2)
//     case TinyImageFormat_ETC2_R8G8B8A8_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ETC2_EAC)
//     case TinyImageFormat_ETC2_EAC_R11_UNORM:
//         FTC(RED, R11_EAC)
//     case TinyImageFormat_ETC2_EAC_R11G11_UNORM:
//         FTC(RG, RG11_EAC)
//     case TinyImageFormat_ETC2_EAC_R11_SNORM:
//         FTC(RED, SIGNED_R11_EAC)
//     case TinyImageFormat_ETC2_EAC_R11G11_SNORM:
//         FTC(RG, SIGNED_RG11_EAC)
//     case TinyImageFormat_ASTC_4x4_UNORM:
//         FTC(RGBA, RGBA_ASTC_4x4)
//     case TinyImageFormat_ASTC_4x4_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_4x4)
//     case TinyImageFormat_ASTC_5x4_UNORM:
//         FTC(RGBA, RGBA_ASTC_5x4)
//     case TinyImageFormat_ASTC_5x4_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_5x4)
//     case TinyImageFormat_ASTC_5x5_UNORM:
//         FTC(RGBA, RGBA_ASTC_5x5)
//     case TinyImageFormat_ASTC_5x5_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_5x5)
//     case TinyImageFormat_ASTC_6x5_UNORM:
//         FTC(RGBA, RGBA_ASTC_6x5)
//     case TinyImageFormat_ASTC_6x5_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_6x5)
//     case TinyImageFormat_ASTC_6x6_UNORM:
//         FTC(RGBA, RGBA_ASTC_6x6)
//     case TinyImageFormat_ASTC_6x6_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_6x6)
//     case TinyImageFormat_ASTC_8x5_UNORM:
//         FTC(RGBA, RGBA_ASTC_8x5)
//     case TinyImageFormat_ASTC_8x5_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_8x5)
//     case TinyImageFormat_ASTC_8x6_UNORM:
//         FTC(RGBA, RGBA_ASTC_8x6)
//     case TinyImageFormat_ASTC_8x6_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_8x6)
//     case TinyImageFormat_ASTC_8x8_UNORM:
//         FTC(RGBA, RGBA_ASTC_8x8)
//     case TinyImageFormat_ASTC_8x8_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_8x8)
//     case TinyImageFormat_ASTC_10x5_UNORM:
//         FTC(RGBA, RGBA_ASTC_10x5)
//     case TinyImageFormat_ASTC_10x5_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_10x5)
//     case TinyImageFormat_ASTC_10x6_UNORM:
//         FTC(RGBA, RGBA_ASTC_10x6)
//     case TinyImageFormat_ASTC_10x6_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_10x6)
//     case TinyImageFormat_ASTC_10x8_UNORM:
//         FTC(RGBA, RGBA_ASTC_10x8);
//     case TinyImageFormat_ASTC_10x8_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_10x8)
//     case TinyImageFormat_ASTC_10x10_UNORM:
//         FTC(RGBA, RGBA_ASTC_10x10)
//     case TinyImageFormat_ASTC_10x10_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_10x10)
//     case TinyImageFormat_ASTC_12x10_UNORM:
//         FTC(RGBA, RGBA_ASTC_12x10)
//     case TinyImageFormat_ASTC_12x10_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_12x10)
//     case TinyImageFormat_ASTC_12x12_UNORM:
//         FTC(RGBA, RGBA_ASTC_12x12)
//     case TinyImageFormat_ASTC_12x12_SRGB:
//         FTC(SRGB_ALPHA, SRGB8_ALPHA8_ASTC_12x12)

//         // Depth
//     case TinyImageFormat_D32_SFLOAT:
//         FT(DEPTH_COMPONENT, UNSIGNED_INT, DEPTH_COMPONENT32F, 4)
//     case TinyImageFormat_D16_UNORM:
//         FT(DEPTH_COMPONENT, UNSIGNED_SHORT, DEPTH_COMPONENT16, 2)
//     case TinyImageFormat_S8_UINT:
//         FT(DEPTH_COMPONENT, UNSIGNED_BYTE, STENCIL_INDEX8, 1)
//     case TinyImageFormat_D24_UNORM_S8_UINT:
//         FT(DEPTH_STENCIL, UNSIGNED_INT_24_8, DEPTH24_STENCIL8, 4)
//     default:
//         FT(UNKNOWN, UNSIGNED_SHORT_4_4_4_4, UNKNOWN, 0)
//     }
// }
// #undef FT
// #undef FTC
// // CONFFX
// #endif // TINYIMAGEFORMAT_APIS_H_

/*
 * Copyright (c) 2017-2024 The Forge Interactive Inc.
 *
 * This file is part of The-Forge
 * (see https://github.com/ConfettiFX/The-Forge).
 *
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
#include "Resources/IResourceLoader.h"
#include <ThirdParty/stb/stb_ds.h>
#include <ThirdParty/bstrlib/bstrlib.h>
#include <ThirdParty/tinyimageformat/tinyimageformat_apis.h>
#include <ThirdParty/tinyimageformat/tinyimageformat_base.h>
#include <ThirdParty/tinyimageformat/tinyimageformat_bits.h>
#include <ThirdParty/tinyimageformat/tinyimageformat_query.h>

#include "RHI/IGraphics.h"
#include "../../../RHI/Private/RendererResourceAPI.h"

#define TINYKTX_IMPLEMENTATION
#include <ThirdParty/tinyktx/tinyktx.h>

#define TINYDDS_IMPLEMENTATION
#include <ThirdParty/tinydds/tinydds.h>

#include "Core/IFileSystem.h"
#include "Core/ILog.h"
#include "Core/IThread.h"

//#include "../../Utilities/Math/ShaderUtilities.h" // Packing functions

#include "TextureContainers.h"

#include "Core/IMemory.h"

#ifdef NX64
#include "../../Utilities/ThirdParty/OpenSource/murmurhash3/MurmurHash3_32.h"
#endif

#define CGLTF_IMPLEMENTATION
#include <ThirdParty/cgltf/cgltf.h>

//#include "../../Tools/ReloadServer/ReloadClient.h"

// If facing strange gfx issues, corruption, GPU hangs, enable this for verbose logging of resource loading
#define RESOURCE_LOADER_VERBOSE 0
#if RESOURCE_LOADER_VERBOSE
#define LOADER_LOGF(...) LOGF(eINFO, __VA_ARGS__)
#else
#define LOADER_LOGF(...)
#endif

extern "C"
{
    void fsGetParentPath(const char* path, char* output);
    void fsGetPathExtension(const char* path, char* output);
    bool fsMergeDirAndFileName(const char* dir, const char* path, char separator, size_t dstSize, char* dst);
}

#define MIP_REDUCE(s, mip) (max(1u, (uint32_t)((s) >> (mip))))

#define SAFE_FREE(x)    \
    if ((x) != nullptr) \
    {                   \
        tf_free((x));   \
        (x) = nullptr;  \
    }

#define MAX_FRAMES 3U

struct SubresourceDataDesc
{
    uint64_t srcOffset;
    uint32_t mipLevel;
    uint32_t arrayLayer;
    uint32_t rowPitch;
    uint32_t slicePitch;
};

enum
{
    MAPPED_RANGE_FLAG_UNMAP_BUFFER = (1 << 0),
    MAPPED_RANGE_FLAG_TEMP_BUFFER = (1 << 1),
};

extern PlatformParameters gPlatformParameters;

struct ShaderByteCodeBuffer
{
    // Make sure we don't stack overflow
    static FORGE_CONSTEXPR const uint32_t kStackSize = 128u * TF_KB;

    // Stack memory, no need to deallocate it. Used first, if a shader is too big we allocate heap memory
    void*    pStackMemory;
    uint32_t stackUsed;
};

const char* getShaderPlatformName();

struct FSLDerivative
{
    uint64_t hash, offset, size;
};

struct FSLMetadata
{
    uint32_t useMultiView;
    uint32_t icbCompatible;
    uint32_t numThreadsPerGroup[4];
    uint32_t outputRenderTargetTypesMask;
};

struct FSLHeader
{
    char        magic[4];
    uint32_t    derivativeCount;
    FSLMetadata metadata;
};

/************************************************************************/
// Material Loading
/************************************************************************/

#ifdef ENABLE_FORGE_MATERIALS

#define MATERIAL_MAX_SHADER_STAGES 6

const uint32_t INVALID_MATERIAL_ID = ~0u;

// Order here is the same as the one declared in MaterialDesc::ShaderSet
const char* gMaterialShaderStageExtensions[MATERIAL_MAX_SHADER_STAGES] = { ".vert", ".frag", ".tesc", ".tese", ".geom", ".comp" };

struct MaterialDesc
{
    struct ShaderSet
    {
        uint32_t id; // Identifier that can be used across all ShaderSets in all Materials. Two materials with the same stages will share
                      // the same Id.

        // Indexes into pShaderNames
        // Shader stages are used to create the Pipeline and generate id.
        // Volatile! Don't change variables in this block, we are iterating over them using vertIdx as base address.
        uint32_t vertIdx;
        uint32_t fragIdx;
        uint32_t hullIdx;
        uint32_t domainIdx;
        uint32_t geomIdx;
        uint32_t compIdx;

        // Other data about this ShaderSet, not used to generate id
        uint32_t     textureBindingCount;
        const char** pTextureBindingNames;
    };

    struct TextureSet
    {
        uint32_t  textureCount;
        // Indexes into pTextureIds, pTextureNames
        uint32_t* pTextureIdxs;
    };

    struct MaterialSet
    {
        uint32_t shaderSetIdx;  // Index into pShaderSets
        uint32_t textureSetIdx; // Index into pTextureSets
    };

    enum TextureFlags
    {
        NONE = 0,
        SRGB = 1,
    };

    uint32_t shaderSetCount;  // For pShaderSets, pShaderSetNames
    uint32_t textureSetCount; // For pTextureSets, pTextureSetNames
    uint32_t materialCount;   // For pMaterialSets, pMaterialSetNames

    ShaderSet*   pShaderSets;
    TextureSet*  pTextureSets;
    MaterialSet* pMaterialSets;

    uint32_t  textureCount; // For mTextureIds, pTextureNames
    uint32_t* pTextureIds;   // Global Ids for the textures. Two materials using the same texture will have the same Id here.

    uint32_t shaderCount; // For pShaderNames
    // ShaderIds are stored in ShaderSet::id

    uint32_t maxShaderSetBindings;
    uint32_t maxTextureSetTextures;

    uint8_t* pTextureFlags;

    // These are needed in release because they contain the filenames of the resources
    // Although we could load them at startup and then use the indexes in pTextureIds to access a global array storing these names
    const char** pTextureNames;
    const char** pShaderNames;

    // These could be kept in shipping builds as we could use them to find Materials from gameplay code.
    const char** pMaterialSetNames;

    // Storage for all the strings pointed by this material
    uint32_t stringBufferSize;
    uint32_t stringBufferUsed;
    char*    pStringBuffer;
};

struct Material
{
    // Contains information about the GPU resources used by this material.
    struct LoadedMaterial
    {
        uint32_t shaderIndex; // The index in the global shader cache of the MaterialLibrary

        uint32_t  textureCount;
        uint32_t* pTextureIndexes; // Each ID is the index in the global texture cache of the MaterialLibrary
    };

    // Index in this array is the same as the index in pDesc->pMaterialSets
    LoadedMaterial* pLoaded;

    MaterialDesc* pDesc;
};

// Global storage for resources used by Materials
// This is not thread safe at the moment, addMaterial/removeMaterial need to happen in the same thread (loading of resources is async, like
// when calling addResource with Textures)
struct MaterialLibrary
{
    Renderer* pRenderer;
    uint32_t  loadedMaterialCount;
    uint32_t  loadedShaderCount;  // Number of unique loaded shaders (some shaders might be reused by multiple materials)
    uint32_t  loadedTextureCount; // Number of unique loaded textures (some textures might be reused by multiple materials)

    // Size of ppMaterialShaders and pMaterialShaderRefCount
    uint32_t maxMaterialShaders;

    // Size of ppMaterialTextures and pMaterialTextureRefCount
    uint32_t maxMaterialTextures;

    Shader**  ppMaterialShaders;
    // How many Materials are using this shader currently, when a material is unloaded the refcount is decreased. Shader is only freed when
    // the last material stops using it. Access must be single threaded, no atomic operations are used for increment/decrement.
    uint16_t* pMaterialShaderRefCount;

    Texture** ppMaterialTextures;
    // How many Materials are using this texture currently, when a texture is unloaded the refcount is decreased. Texture is only freed when
    // the last material stops using it. Access must be single threaded, no atomic operations are used for increment/decrement.
    uint16_t* pMaterialTextureRefCount;

    SyncToken syncToken;
};

static MaterialLibrary* pMaterialLibrary = nullptr;

static uint32_t materialNextFileLine(const char* pFile, uint64_t fileSize, uint64_t currOffset, uint64_t* pNextNewline)
{
    uint64_t    newLineOffset = currOffset;
    const char* pStart = pFile + currOffset;

    while (newLineOffset < fileSize && *pStart != '\n')
    {
        if (*pStart == '\0')
            break;

        ++newLineOffset;
        ++pStart;
    }
    *pNextNewline = newLineOffset;

    if (newLineOffset <= fileSize)
    {
        return 1;
    }
    else
    {
        return 0;
    }
}

static bool isBlankToken(char c) { return c == ' ' || c == '\t'; }
static bool isEndOfLine(char c) { return c == '\n' || c == '\0'; }
static bool isBlankOrEOL(char c) { return isBlankToken(c) || isEndOfLine(c); }

static const char* materialStoreString(MaterialDesc* pDesc, const char* pStr, uint32_t length)
{
    ASSERT(length + 1 < pDesc->stringBufferSize - pDesc->stringBufferUsed);

    char* nameBuffer = pDesc->pStringBuffer + pDesc->stringBufferUsed;
    memcpy(nameBuffer, pStr, length);
    nameBuffer[length] = '\0';
    pDesc->stringBufferUsed += length + 1;
    return nameBuffer;
}

static const char* materialParseAndStoreString(MaterialDesc* pDesc, const char* pLineBuffer, uint32_t* pInOutOffset)
{
    ASSERT(pInOutOffset);
    ASSERT(!isBlankOrEOL(pLineBuffer[*pInOutOffset]));

    uint32_t offset = *pInOutOffset;
    uint32_t nameEndOffset = offset;
    while (!isBlankOrEOL(pLineBuffer[++nameEndOffset]))
    {
    }

    *pInOutOffset = nameEndOffset;
    return materialStoreString(pDesc, pLineBuffer + offset, nameEndOffset - offset);
}

static bool load_shader_stage_byte_code(Renderer* pRenderer, const char* name, ShaderStage stage, BinaryShaderStageDesc* pOut,
                                        ShaderByteCodeBuffer* pShaderByteCodeBuffer, FSLMetadata* pOutMetadata);

static void materialLoadBinaryShader(Renderer* pRenderer, const MaterialDesc* pMaterialDesc, const MaterialDesc::ShaderSet* pShaderSet,
                                     BinaryShaderDesc* pBinaryShaderDesc, ShaderByteCodeBuffer* pShaderBytecodeBuffer)
{
    const uint32_t shaderIds[MATERIAL_MAX_SHADER_STAGES] = { pShaderSet->vertIdx,   pShaderSet->fragIdx, pShaderSet->hullIdx,
                                                             pShaderSet->domainIdx, pShaderSet->geomIdx, pShaderSet->compIdx };

    const ShaderStage pStages[MATERIAL_MAX_SHADER_STAGES] = { SHADER_STAGE_VERT, SHADER_STAGE_FRAG, SHADER_STAGE_HULL,
                                                              SHADER_STAGE_DOMN, SHADER_STAGE_GEOM, SHADER_STAGE_COMP };

    BinaryShaderStageDesc* pStageDescriptors[MATERIAL_MAX_SHADER_STAGES] = {
        &pBinaryShaderDesc->vert,   &pBinaryShaderDesc->frag, &pBinaryShaderDesc->hull,
        &pBinaryShaderDesc->domain, &pBinaryShaderDesc->geom, &pBinaryShaderDesc->comp,
    };

    const char* defaultShaderEntryName = "main";

    FSLMetadata metadata = {};

    for (uint32_t i = 0; i < MATERIAL_MAX_SHADER_STAGES; ++i)
    {
        if (shaderIds[i] < MATERIAL_MAX_SHADER_STAGES)
        {
            pBinaryShaderDesc->stages |= pStages[i];

            BinaryShaderStageDesc* pBinaryShaderStage = pStageDescriptors[i];

            // Note: If we want to add support for custom entry points for shaders in materials this is the place to do it.
            //       We would also need to output these entry names during material compilation.
            pBinaryShaderStage->pEntryPoint = defaultShaderEntryName;
            load_shader_stage_byte_code(pRenderer, pMaterialDesc->pShaderNames[shaderIds[i]], pStages[i], pBinaryShaderStage,
                                        pShaderBytecodeBuffer, &metadata);

#if defined(QUEST_VR)
            pBinaryShaderDesc->isMultiviewVR |= metadata.useMultiView;
#endif
        }
    }
}

#endif // ENABLE_FORGE_MATERIALS

/************************************************************************/
/************************************************************************/

#define GFX_DRIVER_MANAGED_VIDEO_MEMORY
static bool gUma = false;

bool isUma() { return gUma; }

#define STRICT_QUEUE_TYPE_BARRIERS

// Can only issue certain resource state barriers on particular queue type
static inline bool StrictQueueTypeBarriers() { return true; }

// Need to issue barriers when doing texture copy operations
static inline bool IssueTextureCopyBarriers() { return true; }

// Need to issue barriers when doing buffer copy operations
static inline bool IssueBufferCopyBarriers() { return true; } //-V524

static inline bool IssueExplicitInitialStateBarrier() { return false; }

ResourceLoaderDesc          gDefaultResourceLoaderDesc = { 8ull * TF_MB, 2, false };
/************************************************************************/
// Surface Utils
/************************************************************************/
static inline ResourceState ResourceStartState(bool uav)
{
    if (uav)
        return RESOURCE_STATE_UNORDERED_ACCESS;
    else
        return RESOURCE_STATE_SHADER_RESOURCE;
}

static inline ResourceState ResourceStartState(const BufferDesc* pDesc)
{
    // Host visible (Upload Heap)
    if (pDesc->memoryUsage == RESOURCE_MEMORY_USAGE_CPU_ONLY || pDesc->memoryUsage == RESOURCE_MEMORY_USAGE_CPU_TO_GPU)
    {
        return RESOURCE_STATE_GENERIC_READ;
    }
    // Device Local (Default Heap)
    else if (pDesc->memoryUsage == RESOURCE_MEMORY_USAGE_GPU_ONLY)
    {
        DescriptorType usage = (DescriptorType)pDesc->descriptors;
        ResourceState  ret = RESOURCE_STATE_UNDEFINED;

        // Try to limit number of states used overall to avoid sync complexities
        if (usage & DESCRIPTOR_TYPE_RW_BUFFER)
        {
            ret = RESOURCE_STATE_UNORDERED_ACCESS;
        }
        else
        {
            if (usage & (DESCRIPTOR_TYPE_VERTEX_BUFFER | DESCRIPTOR_TYPE_UNIFORM_BUFFER))
            {
                ret |= RESOURCE_STATE_VERTEX_AND_CONSTANT_BUFFER;
            }
            if (usage & DESCRIPTOR_TYPE_INDEX_BUFFER)
            {
                ret |= RESOURCE_STATE_INDEX_BUFFER;
            }
            if (usage & DESCRIPTOR_TYPE_BUFFER)
            {
                ret |= RESOURCE_STATE_SHADER_RESOURCE;
            }
        }

        return ret;
    }
    // Host Cached (Readback Heap)
    else
    {
        return RESOURCE_STATE_COPY_DEST;
    }
}

/************************************************************************/
// Internal Structures
/************************************************************************/
typedef void (*PreMipStepFn)(FileStream* pStream, uint32_t mip);

struct BufferLoadDescInternal
{
    Buffer*       pBuffer;
    const void*   pData;
    uint64_t      dataSize;
    Buffer*       pSrcBuffer;
    uint64_t      srcOffset;
    ResourceState startState;
    bool          forceReset;
};

struct TextureLoadDescInternal
{
    Texture** ppTexture;
    union
    {
        struct
        {
            const char*          pFileName;
            Sampler*             pYcbcrSampler;
            TextureCreationFlags flags;
            TextureContainerType container;
        };
        struct
        {
            ResourceState startState;
        };
    };
    bool forceReset;
};

struct TextureUpdateDescInternal
{
    Texture*          pTexture;
    FileStream        stream;
    Cmd*              pCmd;
    MappedMemoryRange range;
    uint32_t          baseMipLevel;
    uint32_t          mipLevels;
    uint32_t          baseArrayLayer;
    uint32_t          layerCount;
    PreMipStepFn      pPreMipFunc;
    ResourceState     currentState;
    bool              mipsAfterSlice;
};

struct CopyResourceSet
{
    Fence*     pFence = nullptr;
    Semaphore* pSemaphore = nullptr;
    Cmd*       pCmd = nullptr;
    CmdPool*   pCmdPool = nullptr;
    Buffer*    buffer = nullptr;
    uint64_t   allocatedSpace = 0;

    /// Buffers created in case we ran out of space in the original staging buffer
    /// Will be cleaned up after the fence for this set is complete
    /// stb_ds array of Buffer*
    Buffer** tempBuffers = nullptr;

#if defined(STRICT_QUEUE_TYPE_BARRIERS)
    Cmd*     pPostCopyBarrierCmd = nullptr;
    CmdPool* pPostCopyBarrierCmdPool = nullptr;
    Fence*   pPostCopyBarrierFence = nullptr;
    bool     postCopyBarrierRecording = false;
#endif
};

// Synchronization?
struct CopyEngineDesc
{
    uint64_t    size;
    const char* pQueueName;
    QueueType   queueType;
    uint32_t    bufferCount;
};

struct CopyEngine
{
    Queue*           pQueue;
    CopyResourceSet* resourceSets;
    uint64_t         bufferSize;
    Semaphore*       pLastSubmittedSemaphore;

    /// For reading back GPU generated textures, we need to ensure writes have completed before performing the copy.
    /// stb_ds array of Semaphore*
    Semaphore** waitSemaphores;

    typedef void (*FlushFunction)(CopyEngine*);
    FlushFunction pFnFlush;

    uint32_t bufferCount;
    uint32_t activeSet;

    bool isRecording;
    bool flushOnOverflow;
};

enum UpdateRequestType : uint32_t
{
    UPDATE_REQUEST_TEXTURE_BARRIER,
    UPDATE_REQUEST_LOAD_BUFFER,
    UPDATE_REQUEST_LOAD_TEXTURE,
    UPDATE_REQUEST_LOAD_GEOMETRY,
    UPDATE_REQUEST_COPY_TEXTURE,
    UPDATE_REQUEST_INVALID,
};

enum UploadFunctionResult : uint32_t
{
    UPLOAD_FUNCTION_RESULT_COMPLETED,
    UPLOAD_FUNCTION_RESULT_STAGING_BUFFER_FULL,
    UPLOAD_FUNCTION_RESULT_INVALID_REQUEST
};

struct UpdateRequest
{
    UpdateRequest(const BufferLoadDescInternal& buffer): type(UPDATE_REQUEST_LOAD_BUFFER), bufLoadDesc(buffer) {}
    UpdateRequest(const TextureLoadDescInternal& texture): type(UPDATE_REQUEST_LOAD_TEXTURE), texLoadDesc(texture) {}
    UpdateRequest(const GeometryLoadDesc& geom): type(UPDATE_REQUEST_LOAD_GEOMETRY), geomLoadDesc(geom) {}
    UpdateRequest(const TextureBarrier& barrier): type(UPDATE_REQUEST_TEXTURE_BARRIER), textureBarrier(barrier) {}
    UpdateRequest(const TextureCopyDesc& texture): type(UPDATE_REQUEST_COPY_TEXTURE), texCopyDesc(texture) {}

    UpdateRequestType type = UPDATE_REQUEST_INVALID;
    uint64_t          waitIndex = 0;
    union
    {
        BufferLoadDescInternal  bufLoadDesc;
        TextureLoadDescInternal texLoadDesc;
        GeometryLoadDesc        geomLoadDesc;
        TextureBarrier          textureBarrier;
        TextureCopyDesc         texCopyDesc;
    };
};

struct ResourceLoader
{
    Renderer* pRenderer;

    ResourceLoaderDesc desc;

    volatile int run;
    ThreadHandle thread;

    Mutex             queueMutex;
    ConditionVariable queueCond;
    Mutex             tokenMutex;
    ConditionVariable tokenCond;
    UpdateRequest*    requestQueue;

    tfrg_atomic64_t tokenCompleted;
    tfrg_atomic64_t tokenSubmitted;
    tfrg_atomic64_t tokenCounter;

    Mutex semaphoreMutex;

    SyncToken currentTokenState[MAX_FRAMES];
    SyncToken maxToken;

    CopyEngine copyEngine;
    CopyEngine uploadEngine;
    Mutex      uploadEngineMutex;
};

static ResourceLoader* pResourceLoader = nullptr;

static uint32_t util_get_texture_row_alignment(Renderer* pRenderer)
{
    return max(1u, pRenderer->pGpu->settings.uploadBufferTextureRowAlignment);
}

static uint32_t util_get_texture_subresource_alignment(Renderer* pRenderer, TinyImageFormat fmt = TinyImageFormat_UNDEFINED)
{
    uint32_t blockSize = max(1u, TinyImageFormat_BitSizeOfBlock(fmt) >> 3);
    uint32_t alignment = round_up(pRenderer->pGpu->settings.uploadBufferTextureAlignment, blockSize);
    return round_up(alignment, util_get_texture_row_alignment(pRenderer));
}

static void* alignMemory(void* ptr, uint64_t alignment)
{
    uintptr_t offset = alignment - ((uintptr_t)ptr % alignment);
    if (offset != 0)
        ptr = (char*)ptr + offset;
    return ptr;
}

static void* allocShaderByteCode(ShaderByteCodeBuffer* pShaderByteCodeBuffer, uint32_t alignment, uint32_t size, const char* filename)
{
    ASSERT(pShaderByteCodeBuffer && pShaderByteCodeBuffer->pStackMemory);
    ASSERT(alignment > 0);

    uint8_t* pBufferStart = (uint8_t*)pShaderByteCodeBuffer->pStackMemory + pShaderByteCodeBuffer->stackUsed;
    uint8_t* pBufferAligned = (uint8_t*)alignMemory(pBufferStart, alignment);

    void* pOutMemory = nullptr;
    if (pBufferAligned + size <= (uint8_t*)pShaderByteCodeBuffer->pStackMemory + pShaderByteCodeBuffer->kStackSize)
    {
        pShaderByteCodeBuffer->stackUsed += (uint32_t)((pBufferAligned + size) - pBufferStart);
        pOutMemory = pBufferAligned;
    }
    else
    {
        LOGF(eINFO, "Loading shader bytecode in heap memory (%s - %u bytes) (Stack total size: %u, Free size: %u)", filename,
             (uint32_t)(size + alignment), pShaderByteCodeBuffer->kStackSize,
             pShaderByteCodeBuffer->kStackSize - pShaderByteCodeBuffer->stackUsed);
        pOutMemory = tf_memalign(alignment, size);
    }

    ASSERT(((uintptr_t)pOutMemory % alignment) == 0);
    return pOutMemory;
}

static void freeShaderByteCode(ShaderByteCodeBuffer* pShaderByteCodeBuffer, BinaryShaderDesc* pBinaryShaderDesc)
{
    ASSERT(pShaderByteCodeBuffer && pBinaryShaderDesc);

    // Free bytecode if it's not allocated on the buffer
#define FREE_BYTECODE_IF_ON_HEAP(stage)                                                                     \
    if (pShaderByteCodeBuffer->pStackMemory > stage.pByteCode ||                                            \
        ((char*)pShaderByteCodeBuffer->pStackMemory + pShaderByteCodeBuffer->kStackSize) < stage.pByteCode) \
    tf_free(stage.pByteCode)

    FREE_BYTECODE_IF_ON_HEAP(pBinaryShaderDesc->vert);
    FREE_BYTECODE_IF_ON_HEAP(pBinaryShaderDesc->frag);
    FREE_BYTECODE_IF_ON_HEAP(pBinaryShaderDesc->geom);
    FREE_BYTECODE_IF_ON_HEAP(pBinaryShaderDesc->hull);
    FREE_BYTECODE_IF_ON_HEAP(pBinaryShaderDesc->domain);
    FREE_BYTECODE_IF_ON_HEAP(pBinaryShaderDesc->comp);

#undef FREE_BYTECODE_IF_ON_HEAP
}

/************************************************************************/
// Internal Functions
/************************************************************************/

/// Return a new staging buffer
static MappedMemoryRange allocateUploadMemory(Renderer* pRenderer, uint64_t memoryRequirement, uint32_t alignment)
{
    Buffer* buffer;
    // LOGF(LogLevel::eINFO, "Allocating temporary staging buffer. Required allocation size of %llu is larger than the staging buffer
    // capacity of %llu", memoryRequirement, size);
    buffer = {};
    BufferDesc bufferDesc = {};
    bufferDesc.size = memoryRequirement;
    bufferDesc.alignment = alignment;
    bufferDesc.memoryUsage = RESOURCE_MEMORY_USAGE_CPU_ONLY;
    bufferDesc.flags = BUFFER_CREATION_FLAG_PERSISTENT_MAP_BIT;
    bufferDesc.pName = "temporary staging buffer";
    addBuffer(pRenderer, &bufferDesc, &buffer);
    return { (uint8_t*)buffer->pCpuMappedAddress, buffer, 0, memoryRequirement, MAPPED_RANGE_FLAG_TEMP_BUFFER };
}

static void setupCopyEngine(Renderer* pRenderer, CopyEngineDesc* pDesc, CopyEngine* pCopyEngine)
{
    QueueDesc desc = { pDesc->queueType, QUEUE_FLAG_NONE, QUEUE_PRIORITY_NORMAL, pDesc->pQueueName };
    addQueue(pRenderer, &desc, &pCopyEngine->pQueue);

    const uint64_t maxBlockSize = 32;
    pDesc->size = max(pDesc->size, maxBlockSize);

    pCopyEngine->resourceSets = (CopyResourceSet*)tf_malloc(sizeof(CopyResourceSet) * pDesc->bufferCount);
    for (uint32_t i = 0; i < pDesc->bufferCount; ++i)
    {
        tf_placement_new<CopyResourceSet>(pCopyEngine->resourceSets + i);

        CopyResourceSet& resourceSet = pCopyEngine->resourceSets[i];
        addFence(pRenderer, &resourceSet.pFence);

        CmdPoolDesc cmdPoolDesc = {};
        cmdPoolDesc.pQueue = pCopyEngine->pQueue;
        addCmdPool(pRenderer, &cmdPoolDesc, &resourceSet.pCmdPool);

        CmdDesc cmdDesc = {};
        cmdDesc.pPool = resourceSet.pCmdPool;
#ifdef ENABLE_GRAPHICS_DEBUG
        static char buffer[MAX_DEBUG_NAME_LENGTH];
        const char* engineName = pDesc->pQueueName ? pDesc->pQueueName : "Unnamed";
        snprintf(buffer, sizeof(buffer), "%s CopyEngine buffer %u Cmd", engineName, i);
        cmdDesc.pName = buffer;
#endif // ENABLE_GRAPHICS_DEBUG
        addCmd(pRenderer, &cmdDesc, &resourceSet.pCmd);

        addSemaphore(pRenderer, &resourceSet.pSemaphore);

        resourceSet.buffer = allocateUploadMemory(pRenderer, pDesc->size, util_get_texture_subresource_alignment(pRenderer)).pBuffer;
    }

    pCopyEngine->bufferSize = pDesc->size;
    pCopyEngine->bufferCount = pDesc->bufferCount;
    pCopyEngine->isRecording = false;
    pCopyEngine->pLastSubmittedSemaphore = NULL;
}

static void cleanupCopyEngine(Renderer* pRenderer, CopyEngine* pCopyEngine)
{
    for (uint32_t i = 0; i < pCopyEngine->bufferCount; ++i)
    {
        CopyResourceSet& resourceSet = pCopyEngine->resourceSets[i];
        removeBuffer(pRenderer, resourceSet.buffer);

        removeSemaphore(pRenderer, resourceSet.pSemaphore);

        removeCmd(pRenderer, resourceSet.pCmd);
        removeCmdPool(pRenderer, resourceSet.pCmdPool);
        removeFence(pRenderer, resourceSet.pFence);

        for (ptrdiff_t j = 0; j < arrlen(resourceSet.tempBuffers); ++j)
        {
            removeBuffer(pRenderer, resourceSet.tempBuffers[j]);
        }
        arrfree(resourceSet.tempBuffers);

#if defined(STRICT_QUEUE_TYPE_BARRIERS)
        if (StrictQueueTypeBarriers() && resourceSet.pPostCopyBarrierFence)
        {
            removeFence(pRenderer, resourceSet.pPostCopyBarrierFence);
            removeCmd(pRenderer, resourceSet.pPostCopyBarrierCmd);
            removeCmdPool(pRenderer, resourceSet.pPostCopyBarrierCmdPool);
        }
#endif
    }

    tf_free(pCopyEngine->resourceSets);
    arrfree(pCopyEngine->waitSemaphores);

    removeQueue(pRenderer, pCopyEngine->pQueue);
}

static void waitCopyEngineSet(Renderer* pRenderer, CopyEngine* pCopyEngine)
{
    ASSERT(!pCopyEngine->isRecording);
    CopyResourceSet& resourceSet = pCopyEngine->resourceSets[pCopyEngine->activeSet];

    FenceStatus status;
    getFenceStatus(pRenderer, resourceSet.pFence, &status);
    if (FENCE_STATUS_INCOMPLETE == status)
    {
        waitForFences(pRenderer, 1, &resourceSet.pFence);
    }

#if defined(STRICT_QUEUE_TYPE_BARRIERS)
    if (StrictQueueTypeBarriers() && resourceSet.pPostCopyBarrierFence)
    {
        getFenceStatus(pRenderer, resourceSet.pPostCopyBarrierFence, &status);
        if (FENCE_STATUS_INCOMPLETE == status)
        {
            waitForFences(pRenderer, 1, &resourceSet.pPostCopyBarrierFence);
        }
    }
#endif
}

static void resetCopyEngineSet(Renderer* pRenderer, CopyEngine* pCopyEngine)
{
    ASSERT(!pCopyEngine->isRecording);
    pCopyEngine->resourceSets[pCopyEngine->activeSet].allocatedSpace = 0;
    pCopyEngine->isRecording = false;

    Buffer**  tempBuffers = pCopyEngine->resourceSets[pCopyEngine->activeSet].tempBuffers;
    ptrdiff_t tempBufferCount = arrlen(tempBuffers);
    for (ptrdiff_t i = 0; i < tempBufferCount; ++i)
    {
        removeBuffer(pRenderer, tempBuffers[i]);
    }
    arrsetlen(tempBuffers, 0);
}

static Cmd* acquireCmd(CopyEngine* pCopyEngine)
{
    CopyResourceSet& resourceSet = pCopyEngine->resourceSets[pCopyEngine->activeSet];
    if (!pCopyEngine->isRecording)
    {
        waitCopyEngineSet(pResourceLoader->pRenderer, pCopyEngine);
        resetCopyEngineSet(pResourceLoader->pRenderer, pCopyEngine);
        resetCmdPool(pResourceLoader->pRenderer, resourceSet.pCmdPool);
        beginCmd(resourceSet.pCmd);
        cmdBeginDebugMarker(resourceSet.pCmd, 1.0f, 0.5f, 0.1f,
                            QUEUE_TYPE_TRANSFER == pCopyEngine->pQueue->type ? "Copy Cmd" : "Upload Cmd");
        pCopyEngine->isRecording = true;
    }
    return resourceSet.pCmd;
}

static Cmd* acquirePostCopyBarrierCmd(CopyEngine* pCopyEngine)
{
    if (!StrictQueueTypeBarriers() || pCopyEngine->pQueue->type != QUEUE_TYPE_TRANSFER)
    {
        return acquireCmd(pCopyEngine);
    }
#if defined(STRICT_QUEUE_TYPE_BARRIERS)
    CopyResourceSet& resourceSet = pCopyEngine->resourceSets[pCopyEngine->activeSet];
    if (!resourceSet.postCopyBarrierRecording)
    {
        resetCmdPool(pResourceLoader->pRenderer, resourceSet.pPostCopyBarrierCmdPool);
        beginCmd(resourceSet.pPostCopyBarrierCmd);
        resourceSet.postCopyBarrierRecording = true;
    }
    return resourceSet.pPostCopyBarrierCmd;
#else
    return acquireCmd(pCopyEngine);
#endif
}

static void streamerFlush(CopyEngine* pCopyEngine)
{
    if (pCopyEngine->isRecording)
    {
        CopyResourceSet& resourceSet = pCopyEngine->resourceSets[pCopyEngine->activeSet];
        cmdEndDebugMarker(resourceSet.pCmd);
        endCmd(resourceSet.pCmd);
        QueueSubmitDesc submitDesc = {};
        submitDesc.cmdCount = 1;
        submitDesc.ppCmds = &resourceSet.pCmd;
        submitDesc.signalSemaphoreCount = 1;
        submitDesc.ppSignalSemaphores = &resourceSet.pSemaphore;
        submitDesc.pSignalFence = resourceSet.pFence;
        if (arrlen(pCopyEngine->waitSemaphores))
        {
            submitDesc.waitSemaphoreCount = (uint32_t)arrlen(pCopyEngine->waitSemaphores);
            submitDesc.ppWaitSemaphores = &pCopyEngine->waitSemaphores[0];
            arrsetlen(pCopyEngine->waitSemaphores, 0);
        }
        queueSubmit(pCopyEngine->pQueue, &submitDesc);

#if defined(STRICT_QUEUE_TYPE_BARRIERS)
        if (StrictQueueTypeBarriers() && resourceSet.postCopyBarrierRecording)
        {
            endCmd(resourceSet.pPostCopyBarrierCmd);
            submitDesc = {};
            submitDesc.cmdCount = 1;
            submitDesc.ppCmds = &resourceSet.pPostCopyBarrierCmd;
            submitDesc.waitSemaphoreCount = 1;
            submitDesc.ppWaitSemaphores = &resourceSet.pSemaphore;
            submitDesc.pSignalFence = resourceSet.pPostCopyBarrierFence;
            queueSubmit(resourceSet.pPostCopyBarrierCmdPool->pQueue, &submitDesc);
            resourceSet.postCopyBarrierRecording = false;
        }
#endif

        pCopyEngine->isRecording = false;
    }
}

/// Return memory from pre-allocated staging buffer or create a temporary buffer if the streamer ran out of memory
static MappedMemoryRange allocateStagingMemory(CopyEngine* pCopyEngine, uint64_t memoryRequirement, uint32_t alignment)
{
    // #NOTE: Call to make sure we dont reset copy engine after staging memory was already allocated
    acquireCmd(pCopyEngine);

    CopyResourceSet* pResourceSet = &pCopyEngine->resourceSets[pCopyEngine->activeSet];
    uint64_t         size = (uint64_t)pResourceSet->buffer->size;
    alignment = max((uint32_t)RESOURCE_BUFFER_ALIGNMENT, alignment);
    memoryRequirement = round_up_64(memoryRequirement, alignment);
    if (memoryRequirement > size)
    {
        MappedMemoryRange range = allocateUploadMemory(pResourceLoader->pRenderer, memoryRequirement, alignment);
        LOADER_LOGF(
            LogLevel::eINFO,
            "Allocating temporary staging buffer. Required allocation size of %llu is larger than the staging buffer capacity of %llu",
            memoryRequirement, size);
        arrpush(pResourceSet->tempBuffers, range.pBuffer);
        return range;
    }

    uint64_t offset = round_up_64(pCopyEngine->resourceSets[pCopyEngine->activeSet].allocatedSpace, alignment);
    bool     memoryAvailable = (offset < size) && (memoryRequirement <= size - offset);
    if (memoryAvailable && pResourceSet->buffer->pCpuMappedAddress)
    {
        Buffer* buffer = pResourceSet->buffer;
        ASSERT(buffer->pCpuMappedAddress);
        uint8_t* pDstData = (uint8_t*)buffer->pCpuMappedAddress + offset;
        pCopyEngine->resourceSets[pCopyEngine->activeSet].allocatedSpace = offset + memoryRequirement;
        return { pDstData, buffer, offset, memoryRequirement };
    }
    else
    {
        if (pCopyEngine->flushOnOverflow)
        {
            ASSERT(pCopyEngine->pFnFlush);
            pCopyEngine->pFnFlush(pCopyEngine);
            return allocateStagingMemory(pCopyEngine, memoryRequirement, alignment);
        }

        return {};
    }
}

static UploadFunctionResult updateBuffer(Renderer* pRenderer, CopyEngine* pCopyEngine, const BufferUpdateDesc& bufUpdateDesc)
{
    UNREF_PARAM(pRenderer);
    Buffer* pBuffer = bufUpdateDesc.pBuffer;
    ASSERT(RESOURCE_MEMORY_USAGE_GPU_ONLY == pBuffer->memoryUsage);

    Cmd* pCmd = acquireCmd(pCopyEngine);

    if (IssueBufferCopyBarriers() && bufUpdateDesc.currentState != RESOURCE_STATE_COPY_DEST)
    {
        BufferBarrier barrier = { bufUpdateDesc.pBuffer, bufUpdateDesc.currentState, RESOURCE_STATE_COPY_DEST };
        cmdResourceBarrier(pCmd, 1, &barrier, 0, NULL, 0, NULL);
    }

    MappedMemoryRange range = bufUpdateDesc.internal.mappedRange;
    cmdUpdateBuffer(pCmd, pBuffer, bufUpdateDesc.dstOffset, range.pBuffer, range.offset,
                    bufUpdateDesc.size ? bufUpdateDesc.size : range.size);

    if (IssueBufferCopyBarriers() && bufUpdateDesc.currentState != RESOURCE_STATE_COPY_DEST)
    {
        BufferBarrier barrier = { bufUpdateDesc.pBuffer, RESOURCE_STATE_COPY_DEST, bufUpdateDesc.currentState };
        cmdResourceBarrier(pCmd, 1, &barrier, 0, NULL, 0, NULL);
    }

    return UPLOAD_FUNCTION_RESULT_COMPLETED;
}

static UploadFunctionResult loadBuffer(Renderer* pRenderer, CopyEngine* pCopyEngine, const UpdateRequest& updateRequest)
{
    const BufferLoadDescInternal& loadDesc = updateRequest.bufLoadDesc;
    BufferUpdateDesc              updateDesc = { loadDesc.pBuffer };
    updateDesc.currentState = RESOURCE_STATE_COPY_DEST;
    MappedMemoryRange range = {};
    bool              mapped = false;
    if (loadDesc.pSrcBuffer)
    {
        range.offset = loadDesc.srcOffset;
        range.size = loadDesc.pBuffer->size;
        range.pBuffer = loadDesc.pSrcBuffer;

        if (!loadDesc.pSrcBuffer->pCpuMappedAddress)
        {
            mapBuffer(pRenderer, loadDesc.pSrcBuffer, NULL);
            mapped = true;
        }

        range.pData = (uint8_t*)loadDesc.pSrcBuffer->pCpuMappedAddress + loadDesc.srcOffset;
    }
    else
    {
        range = allocateStagingMemory(pCopyEngine, loadDesc.pBuffer->size, RESOURCE_BUFFER_ALIGNMENT);
        if (!range.pData)
        {
            return UPLOAD_FUNCTION_RESULT_STAGING_BUFFER_FULL;
        }
    }

    updateDesc.internal.mappedRange = range;
    updateDesc.pMappedData = updateDesc.internal.mappedRange.pData;
    if (loadDesc.forceReset)
    {
        memset(updateDesc.pMappedData, 0, (size_t)loadDesc.pBuffer->size);
    }
    else
    {
        memcpy(updateDesc.pMappedData, loadDesc.pData, (size_t)loadDesc.dataSize);
    }

    if (range.pData == loadDesc.pBuffer->pCpuMappedAddress)
    {
        if (mapped)
        {
            unmapBuffer(pRenderer, loadDesc.pSrcBuffer);
        }

        return UPLOAD_FUNCTION_RESULT_COMPLETED;
    }

    UploadFunctionResult res = updateBuffer(pRenderer, pCopyEngine, updateDesc);
    if (UPLOAD_FUNCTION_RESULT_COMPLETED == res)
    {
        if (IssueBufferCopyBarriers() && loadDesc.startState != RESOURCE_STATE_COPY_DEST)
        {
            BufferBarrier barrier = { loadDesc.pBuffer, RESOURCE_STATE_COPY_DEST, loadDesc.startState };
            Cmd*          cmd = acquirePostCopyBarrierCmd(pCopyEngine);
            cmdResourceBarrier(cmd, 1, &barrier, 0, NULL, 0, NULL);
        }
    }

    return res;
}

static UploadFunctionResult updateTexture(Renderer* pRenderer, CopyEngine* pCopyEngine, const TextureUpdateDescInternal& texUpdateDesc)
{
    // When this call comes from updateResource, staging buffer data is already filled
    // All that is left to do is record and execute the Copy commands
    bool                  dataAlreadyFilled = texUpdateDesc.range.pBuffer ? true : false;
    Texture*              texture = texUpdateDesc.pTexture;
    const TinyImageFormat fmt = (TinyImageFormat)texture->format;
    FileStream            stream = texUpdateDesc.stream;

    const uint32_t sliceAlignment = util_get_texture_subresource_alignment(pRenderer, fmt);
    const uint32_t rowAlignment = util_get_texture_row_alignment(pRenderer);
    const uint64_t requiredSize = util_get_surface_size(fmt, texture->width, texture->height, texture->depth, rowAlignment,
                                                        sliceAlignment, texUpdateDesc.baseMipLevel, texUpdateDesc.mipLevels,
                                                        texUpdateDesc.baseArrayLayer, texUpdateDesc.layerCount);

    MappedMemoryRange upload = dataAlreadyFilled ? texUpdateDesc.range : allocateStagingMemory(pCopyEngine, requiredSize, sliceAlignment);
    uint64_t          offset = 0;

    Cmd* cmd = texUpdateDesc.pCmd ? texUpdateDesc.pCmd : acquireCmd(pCopyEngine);
    if (IssueTextureCopyBarriers() && texUpdateDesc.currentState != RESOURCE_STATE_COPY_DEST)
    {
        TextureBarrier barrier = { texture, texUpdateDesc.currentState, RESOURCE_STATE_COPY_DEST };
        cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);
    }

    // #TODO: Investigate - fsRead crashes if we pass the upload buffer mapped address. Allocating temporary buffer as a workaround. Does NX
    // support loading from disk to GPU shared memory?
#ifdef NX64
    void* nxTempBuffer = NULL;
    if (!dataAlreadyFilled)
    {
        size_t remainingBytes = fsGetStreamFileSize(&stream) - fsGetStreamSeekPosition(&stream);
        nxTempBuffer = tf_malloc(remainingBytes);
        ssize_t bytesRead = fsReadFromStream(&stream, nxTempBuffer, remainingBytes);
        if (bytesRead != remainingBytes)
        {
            fsCloseStream(&stream);
            tf_free(nxTempBuffer);
            return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
        }

        fsCloseStream(&stream);
        fsOpenStreamFromMemory(nxTempBuffer, remainingBytes, FM_READ, true, &stream);
    }
#endif

    if (!upload.pData)
    {
        return UPLOAD_FUNCTION_RESULT_STAGING_BUFFER_FULL;
    }

    uint32_t firstStart = texUpdateDesc.mipsAfterSlice ? texUpdateDesc.baseMipLevel : texUpdateDesc.baseArrayLayer;
    uint32_t firstEnd = texUpdateDesc.mipsAfterSlice ? (texUpdateDesc.baseMipLevel + texUpdateDesc.mipLevels)
                                                      : (texUpdateDesc.baseArrayLayer + texUpdateDesc.layerCount);
    uint32_t secondStart = texUpdateDesc.mipsAfterSlice ? texUpdateDesc.baseArrayLayer : texUpdateDesc.baseMipLevel;
    uint32_t secondEnd = texUpdateDesc.mipsAfterSlice ? (texUpdateDesc.baseArrayLayer + texUpdateDesc.layerCount)
                                                       : (texUpdateDesc.baseMipLevel + texUpdateDesc.mipLevels);

    for (uint32_t p = 0; p < 1; ++p)
    {
        for (uint32_t j = firstStart; j < firstEnd; ++j)
        {
            if (texUpdateDesc.mipsAfterSlice && texUpdateDesc.pPreMipFunc)
            {
                texUpdateDesc.pPreMipFunc(&stream, j);
            }

            for (uint32_t i = secondStart; i < secondEnd; ++i)
            {
                if (!texUpdateDesc.mipsAfterSlice && texUpdateDesc.pPreMipFunc)
                {
                    texUpdateDesc.pPreMipFunc(&stream, i);
                }

                uint32_t mip = texUpdateDesc.mipsAfterSlice ? j : i;
                uint32_t layer = texUpdateDesc.mipsAfterSlice ? i : j;

                uint32_t w = MIP_REDUCE(texture->width, mip);
                uint32_t h = MIP_REDUCE(texture->height, mip);
                uint32_t d = MIP_REDUCE(texture->depth, mip);

                uint32_t numBytes = 0;
                uint32_t rowBytes = 0;
                uint32_t numRows = 0;

                bool ret = util_get_surface_info(w, h, fmt, &numBytes, &rowBytes, &numRows);
                if (!ret)
                {
                    return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
                }

                uint32_t subRowPitch = round_up(rowBytes, rowAlignment);
                uint32_t subSlicePitch = round_up(subRowPitch * numRows, sliceAlignment);
                uint32_t subNumRows = numRows;
                uint32_t subDepth = d;
                uint8_t* data = upload.pData + offset;

                if (!dataAlreadyFilled)
                {
                    for (uint32_t z = 0; z < subDepth; ++z)
                    {
                        uint8_t* dstData = data + subSlicePitch * z;
                        for (uint32_t r = 0; r < subNumRows; ++r)
                        {
                            ssize_t bytesRead = fsReadFromStream(&stream, dstData + r * subRowPitch, rowBytes);
                            if (bytesRead != rowBytes)
                            {
                                return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
                            }
                        }
                    }
                }
                SubresourceDataDesc subresourceDesc = {};
                subresourceDesc.arrayLayer = layer;
                subresourceDesc.mipLevel = mip;
                subresourceDesc.srcOffset = upload.offset + offset;
                subresourceDesc.rowPitch = subRowPitch;
                subresourceDesc.slicePitch = subSlicePitch;
                cmdUpdateSubresource(cmd, texture, upload.pBuffer, &subresourceDesc);
                offset += subDepth * subSlicePitch;
            }
        }
    }

    if (IssueTextureCopyBarriers() && texUpdateDesc.currentState != RESOURCE_STATE_COPY_DEST)
    {
        TextureBarrier barrier = { texture, RESOURCE_STATE_COPY_DEST, texUpdateDesc.currentState };
        cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);
    }

    if (stream.pIO)
    {
        fsCloseStream(&stream);
    }

    return UPLOAD_FUNCTION_RESULT_COMPLETED;
}

static UploadFunctionResult loadTexture(Renderer* pRenderer, CopyEngine* pCopyEngine, const UpdateRequest& pTextureUpdate)
{
    const TextureLoadDescInternal* pTextureDesc = &pTextureUpdate.texLoadDesc;
    uint8_t                        singleTexelNormal[2] = {};

    if (pTextureDesc->forceReset)
    {
        Texture* texture = *pTextureDesc->ppTexture;

        if (IssueExplicitInitialStateBarrier())
        {
            Cmd*           cmd = acquireCmd(pCopyEngine);
            TextureBarrier barrier = { texture, RESOURCE_STATE_UNDEFINED, RESOURCE_STATE_COPY_DEST };
            cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);
        }

        TinyImageFormat   fmt = (TinyImageFormat)texture->format;
        const uint32_t    sliceAlignment = util_get_texture_subresource_alignment(pRenderer, fmt);
        const uint32_t    rowAlignment = util_get_texture_row_alignment(pRenderer);
        const uint64_t    requiredSize = util_get_surface_size(fmt, texture->width, texture->height, texture->depth, rowAlignment,
                                                            sliceAlignment, 0, texture->mipLevels, 0, texture->arraySizeMinusOne + 1u);
        MappedMemoryRange range = allocateStagingMemory(pCopyEngine, requiredSize, sliceAlignment);
        memset(range.pData, 0, range.size);

        // Zero out all subresources
        TextureUpdateDescInternal updateDesc = {};
        updateDesc.layerCount = texture->arraySizeMinusOne + 1u;
        updateDesc.mipLevels = texture->mipLevels;
        updateDesc.pTexture = texture;
        updateDesc.range = range;
        updateDesc.currentState = RESOURCE_STATE_COPY_DEST;
        updateTexture(pRenderer, pCopyEngine, updateDesc);

        if (IssueTextureCopyBarriers() && pTextureDesc->startState != RESOURCE_STATE_COPY_DEST)
        {
            TextureBarrier barrier = { texture, RESOURCE_STATE_COPY_DEST, pTextureDesc->startState };
            Cmd*           cmd = acquirePostCopyBarrierCmd(pCopyEngine);
            cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);
        }

        return UPLOAD_FUNCTION_RESULT_COMPLETED;
    }

    ASSERT((((pTextureDesc->flags & TEXTURE_CREATION_FLAG_SRGB) == 0) || (pTextureDesc->pFileName != NULL)) &&
           "Only textures loaded from file can have TEXTURE_CREATION_FLAG_SRGB. "
           "Please change format of the provided texture if you need srgb format.");

    if (pTextureDesc->pFileName)
    {
        FileStream stream = {};
        bool       success = false;

        TextureUpdateDescInternal updateDesc = {};
        TextureContainerType      container = pTextureDesc->container;

        if (TEXTURE_CONTAINER_DEFAULT == container)
        {
            container = TEXTURE_CONTAINER_DDS;
        }

        TextureDesc textureDesc = {};
        textureDesc.pName = pTextureDesc->pFileName;
        textureDesc.flags |= pTextureDesc->flags;

        // Validate that we have found the file format now
        ASSERT(container != TEXTURE_CONTAINER_DEFAULT); //-V547
        if (TEXTURE_CONTAINER_DEFAULT == container)     //-V547
        {
            return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
        }

        switch (container)
        {
        case TEXTURE_CONTAINER_DDS:
        {
            success = fsOpenStreamFromPath(RD_TEXTURES, pTextureDesc->pFileName, FM_READ, &stream);
            if (success)
            {
                success = loadDDSTextureDesc(&stream, &textureDesc);
            }
            break;
        }
        case TEXTURE_CONTAINER_KTX:
        {
            success = fsOpenStreamFromPath(RD_TEXTURES, pTextureDesc->pFileName, FM_READ, &stream);
            if (success)
            {
                success = loadKTXTextureDesc(&stream, &textureDesc);
                updateDesc.mipsAfterSlice = true;
                // KTX stores mip size before the mip data
                // This function gets called to skip the mip size so we read the mip data
                updateDesc.pPreMipFunc = [](FileStream* pStream, uint32_t)
                {
                    uint32_t mipSize = 0;
                    fsReadFromStream(pStream, &mipSize, sizeof(mipSize));
                };
            }
            break;
        }
        case TEXTURE_CONTAINER_GNF:
            return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
        default:
            break;
        }

        if (success)
        {
            // D3D12 requires block-aligned base dimensions for BC textures. Preserve the exact
            // texel of the 1x1 BC5 normal maps emitted by some glTF exporters as an RG8 texture.
            if (textureDesc.width == 1 && textureDesc.height == 1 && textureDesc.depth == 1 && textureDesc.arraySize == 1 &&
                textureDesc.mipLevels == 1 && textureDesc.format == TinyImageFormat_DXBC5_UNORM)
            {
                uint8_t    block[16] = {};
                const bool read = fsReadFromStream(&stream, block, sizeof(block)) == sizeof(block);
                fsCloseStream(&stream);
                if (!read)
                    return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
                singleTexelNormal[0] = decodeBC5SingleTexelChannel(block);
                singleTexelNormal[1] = decodeBC5SingleTexelChannel(block + 8);
                if (!fsOpenStreamFromMemory(singleTexelNormal, sizeof(singleTexelNormal), FM_READ, false, &stream))
                    return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
                textureDesc.format = TinyImageFormat_R8G8_UNORM;
            }
            textureDesc.startState = RESOURCE_STATE_COPY_DEST;

            if (pTextureDesc->flags & TEXTURE_CREATION_FLAG_SRGB)
            {
                TinyImageFormat srgbFormat = TinyImageFormat_ToSRGB(textureDesc.format);
                if (srgbFormat != TinyImageFormat_UNDEFINED)
                    textureDesc.format = srgbFormat;
                else
                {
                    LOGF(eWARNING,
                         "Trying to load '%s' image using SRGB profile. "
                         "But image has '%s' format, which doesn't have SRGB counterpart.",
                         pTextureDesc->pFileName, TinyImageFormat_Name(textureDesc.format));
                }
            }

            addTexture(pRenderer, &textureDesc, pTextureDesc->ppTexture);

            updateDesc.stream = stream;
            updateDesc.pTexture = *pTextureDesc->ppTexture;
            updateDesc.baseMipLevel = 0;
            updateDesc.mipLevels = textureDesc.mipLevels;
            updateDesc.baseArrayLayer = 0;
            updateDesc.layerCount = textureDesc.arraySize;
            updateDesc.currentState = RESOURCE_STATE_COPY_DEST;

            if (IssueExplicitInitialStateBarrier())
            {
                TextureBarrier barrier = { updateDesc.pTexture, RESOURCE_STATE_UNDEFINED, RESOURCE_STATE_COPY_DEST };
                Cmd*           cmd = acquireCmd(pCopyEngine);
                cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);
            }

            UploadFunctionResult res = updateTexture(pRenderer, pCopyEngine, updateDesc);

            if (IssueTextureCopyBarriers() && UPLOAD_FUNCTION_RESULT_COMPLETED == res)
            {
                TextureBarrier barrier = { *pTextureDesc->ppTexture, RESOURCE_STATE_COPY_DEST, RESOURCE_STATE_SHADER_RESOURCE };
                Cmd*           cmd = acquirePostCopyBarrierCmd(pCopyEngine);
                cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);
            }

            return res;
        }
    }

    LOGF(eERROR, "Failed to open texture file %s", pTextureDesc->pFileName ? pTextureDesc->pFileName : "<NULL>");
    ASSERT(false);
    return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
}

static void fillGeometryUpdateDesc(Renderer* pRenderer, CopyEngine* pCopyEngine, GeometryLoadDesc* pDesc, Geometry* geom,
                                   uint32_t* indexStride, BufferUpdateDesc vertexUpdateDesc[MAX_VERTEX_BINDINGS],
                                   BufferUpdateDesc indexUpdateDesc[1])
{
    UNREF_PARAM(pCopyEngine);
    bool     structuredBuffers = (pDesc->flags & GEOMETRY_LOAD_FLAG_STRUCTURED_BUFFERS) > 0;
    uint32_t indexBufferSize = *indexStride * geom->indexCount;

    if (pDesc->pGeometryBuffer)
    {
        if (pDesc->pGeometryBufferLayoutDesc)
        {
            *indexStride = pDesc->pGeometryBufferLayoutDesc->indexType == INDEX_TYPE_UINT16 ? sizeof(uint16_t) : sizeof(uint32_t);
            indexBufferSize = *indexStride * geom->indexCount;
        }

        addGeometryBufferPart(&pDesc->pGeometryBuffer->index, indexBufferSize, *indexStride, &geom->indexBufferChunk);

        indexUpdateDesc->pBuffer = pDesc->pGeometryBuffer->index.pBuffer;
        indexUpdateDesc->dstOffset = geom->indexBufferChunk.offset;
    }
    else
    {
        BufferDesc loadDesc = {};
        loadDesc.descriptors =
            DESCRIPTOR_TYPE_INDEX_BUFFER | (structuredBuffers ? (DESCRIPTOR_TYPE_BUFFER | DESCRIPTOR_TYPE_RW_BUFFER)
                                                              : (DESCRIPTOR_TYPE_BUFFER_RAW | DESCRIPTOR_TYPE_RW_BUFFER_RAW));
        loadDesc.flags |= (pDesc->flags & GEOMETRY_LOAD_FLAG_RAYTRACING_INPUT)
                               ? (BUFFER_CREATION_FLAG_SHADER_DEVICE_ADDRESS | BUFFER_CREATION_FLAG_ACCELERATION_STRUCTURE_BUILD_INPUT)
                               : BUFFER_CREATION_FLAG_NONE;
        loadDesc.size = indexBufferSize;
        loadDesc.elementCount = (uint32_t)(loadDesc.size / (structuredBuffers ? *indexStride : sizeof(uint32_t)));
        loadDesc.structStride = *indexStride;
        loadDesc.memoryUsage = RESOURCE_MEMORY_USAGE_GPU_ONLY;
        loadDesc.startState = gUma ? gIndexBufferState : RESOURCE_STATE_COPY_DEST;
        addBuffer(pRenderer, &loadDesc, &geom->pIndexBuffer);
        indexUpdateDesc->pBuffer = geom->pIndexBuffer;
        indexUpdateDesc->dstOffset = 0;
    }

    indexUpdateDesc->size = geom->indexCount * *indexStride;

    // We need to check for pCpuMappedAddress because when we allocate a custom ResourceHeap with GPU_ONLY memory we don't get any CPU
    // mapped address and we need staging memory
    if (gUma && indexUpdateDesc->pBuffer->pCpuMappedAddress)
    {
        indexUpdateDesc->internal.mappedRange = { (uint8_t*)indexUpdateDesc->pBuffer->pCpuMappedAddress + indexUpdateDesc->dstOffset };
    }
    else
    {
        indexUpdateDesc->internal.mappedRange.pData = (uint8_t*)tf_calloc_memalign(1, RESOURCE_BUFFER_ALIGNMENT, indexUpdateDesc->size);
    }
    indexUpdateDesc->pMappedData = indexUpdateDesc->internal.mappedRange.pData;

    // Vertex buffers
    uint32_t bufferCounter = 0;
    for (uint32_t i = 0; i < MAX_VERTEX_BINDINGS; ++i)
    {
        if (!geom->vertexStrides[i])
            continue;

        uint32_t size = geom->vertexStrides[i] * geom->vertexCount;

        if (pDesc->pGeometryBuffer)
        {
            addGeometryBufferPart(&pDesc->pGeometryBuffer->vertex[i], size, geom->vertexStrides[i], &geom->vertexBufferChunks[i]);
            vertexUpdateDesc[i].pBuffer = pDesc->pGeometryBuffer->vertex[i].pBuffer;
            vertexUpdateDesc[i].dstOffset = geom->vertexBufferChunks[i].offset;
        }
        else
        {
            BufferDesc vertexBufferDesc = {};
            vertexBufferDesc.descriptors =
                DESCRIPTOR_TYPE_VERTEX_BUFFER | (structuredBuffers ? (DESCRIPTOR_TYPE_BUFFER | DESCRIPTOR_TYPE_RW_BUFFER)
                                                                   : (DESCRIPTOR_TYPE_BUFFER_RAW | DESCRIPTOR_TYPE_RW_BUFFER_RAW));
            vertexBufferDesc.flags |=
                (pDesc->flags & GEOMETRY_LOAD_FLAG_RAYTRACING_INPUT)
                    ? (BUFFER_CREATION_FLAG_SHADER_DEVICE_ADDRESS | BUFFER_CREATION_FLAG_ACCELERATION_STRUCTURE_BUILD_INPUT)
                    : BUFFER_CREATION_FLAG_NONE;
            vertexBufferDesc.size = size;
            vertexBufferDesc.elementCount =
                (uint32_t)(vertexBufferDesc.size / (structuredBuffers ? geom->vertexStrides[i] : sizeof(uint32_t)));
            vertexBufferDesc.structStride = geom->vertexStrides[i];
            vertexBufferDesc.memoryUsage = RESOURCE_MEMORY_USAGE_GPU_ONLY;
            vertexBufferDesc.startState = gUma ? gVertexBufferState : RESOURCE_STATE_COPY_DEST;
            vertexBufferDesc.pName = "VertexBuffer";
            addBuffer(pRenderer, &vertexBufferDesc, &geom->pVertexBuffers[bufferCounter]);

            vertexUpdateDesc[i].pBuffer = geom->pVertexBuffers[bufferCounter];
            vertexUpdateDesc[i].dstOffset = 0;
        }

        vertexUpdateDesc[i].size = size;

        // We need to check for pCpuMappedAddress because when we allocate a custom ResourceHeap with GPU_ONLY memory we don't get any CPU
        // mapped address and we need staging memory
        if (gUma && vertexUpdateDesc[i].pBuffer->pCpuMappedAddress)
        {
            vertexUpdateDesc[i].internal.mappedRange = { (uint8_t*)vertexUpdateDesc[i].pBuffer->pCpuMappedAddress +
                                                           vertexUpdateDesc[i].dstOffset };
        }
        else
        {
            vertexUpdateDesc[i].internal.mappedRange.pData =
                (uint8_t*)tf_calloc_memalign(1, RESOURCE_BUFFER_ALIGNMENT, vertexUpdateDesc[i].size);
        }
        vertexUpdateDesc[i].pMappedData = vertexUpdateDesc[i].internal.mappedRange.pData;
        ++bufferCounter;
    }

    geom->vertexBufferCount = bufferCounter;
}

static UploadFunctionResult loadGeometryCustomMeshFormat(Renderer* pRenderer, CopyEngine* pCopyEngine, GeometryLoadDesc* pDesc,
                                                         BufferUpdateDesc vertexUpdateDesc[MAX_VERTEX_BINDINGS],
                                                         BufferUpdateDesc indexUpdateDesc[1])
{
    FileStream file = {};
    if (!fsOpenStreamFromPath(RD_MESHES, pDesc->pFileName, FM_READ, &file))
    {
        LOGF(eERROR, "Failed to open bin file %s", pDesc->pFileName);
        ASSERT(false);
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    char magic[TF_ARRAY_COUNT(GEOMETRY_FILE_MAGIC_STR)] = { 0 };
    static_assert(sizeof(magic) == sizeof(GEOMETRY_FILE_MAGIC_STR));
    fsReadFromStream(&file, magic, sizeof(magic));

    if (strncmp(magic, GEOMETRY_FILE_MAGIC_STR, TF_ARRAY_COUNT(magic)) != 0)
    {
        LOGF(eERROR, "File '%s' is not a Geometry file.", pDesc->pFileName);
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    uint32_t geomSize = 0;
    fsReadFromStream(&file, &geomSize, sizeof(uint32_t));
    if (!VERIFYMSG(geomSize >= 352, "File '%s': Geometry object must have a size >= 352.", pDesc->pFileName))
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    Geometry* geom = (Geometry*)tf_calloc(1, geomSize);

    if (!VERIFYMSG(geom, "File '%s': Geometry object is a nullptr.", pDesc->pFileName))
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    fsReadFromStream(&file, geom, geomSize);

    uint32_t geomDataSize = 0;
    fsReadFromStream(&file, &geomDataSize, sizeof(uint32_t));
    if (!VERIFYMSG(geomDataSize > 0, "File '%s': Geometry object must have a size greater than 0.", pDesc->pFileName))
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    GeometryData* geomData = (GeometryData*)tf_calloc(1, geomDataSize);
    if (!VERIFYMSG(geomData, "File '%s': Geometry object is a nullptr.", pDesc->pFileName))
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    fsReadFromStream(&file, geomData, geomDataSize);

    uint32_t shadowSize = 0;
    fsReadFromStream(&file, &shadowSize, sizeof(uint32_t));
    ASSERT(shadowSize > 0);
    if (shadowSize < sizeof(*geomData->pShadow))
    {
        LOGF(eERROR, "File '%s': Geometry object has shadow with size less than %x, got %x", pDesc->pFileName,
             (int)sizeof(*geomData->pShadow), (int)shadowSize);
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    geomData->pShadow = (GeometryData::ShadowData*)tf_malloc(shadowSize);
    if (!geomData->pShadow)
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    if (!VERIFYMSG(fsReadFromStream(&file, geomData->pShadow, shadowSize) == shadowSize,
                   "File '%s': Failed to read Geometry object's shadow.", pDesc->pFileName))
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    if (geom->meshlets.meshletCount)
    {
        uint64_t meshlets_size = geom->meshlets.meshletCount * sizeof *geom->meshlets.meshlets;
        uint64_t meshlets_data_size = geom->meshlets.meshletCount * sizeof *geom->meshlets.meshletsData;
        uint64_t vertices_size = geom->meshlets.vertexCount * sizeof *geom->meshlets.vertices;
        uint64_t triangles_size = geom->meshlets.triangleCount * sizeof *geom->meshlets.triangles;

        uint64_t alloc_size = meshlets_size + meshlets_data_size + vertices_size + triangles_size;

        void* mem = tf_malloc(alloc_size);

        geom->meshlets.meshlets = (Meshlet*)mem;
        geom->meshlets.meshletsData = (MeshletData*)(geom->meshlets.meshlets + geom->meshlets.meshletCount);
        geom->meshlets.vertices = (uint32_t*)(geom->meshlets.meshletsData + geom->meshlets.meshletCount);
        geom->meshlets.triangles = (uint8_t*)(geom->meshlets.vertices + geom->meshlets.vertexCount);

        size_t read = fsReadFromStream(&file, mem, alloc_size);
        if (alloc_size != read)
        {
            return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
        }
    }

    fsCloseStream(&file);

    geom->pDrawArgs = (IndirectDrawIndexArguments*)(geom + 1); //-V1027

    if (geomData->jointCount > 0)
    {
        geomData->pInverseBindPoses = (mat4*)(geomData + 1); //-V1027
        geomData->pJointRemaps =
            (uint32_t*)((uint8_t*)geomData->pInverseBindPoses + round_up(geomData->jointCount * sizeof(*geomData->pInverseBindPoses), 16));
    }

    if (geomData->userDataSize > 0)
    {
        geomData->pUserData = geomData->jointCount > 0
                                  ? ((uint8_t*)geomData->pJointRemaps + round_up(geomData->jointCount * sizeof(uint32_t), 16))
                                  : (uint8_t*)(geomData + 1);
    }

    // Determine index stride
    const uint32_t indexStride = geom->vertexCount > UINT16_MAX ? sizeof(uint32_t) : sizeof(uint16_t);

    geomData->pShadow->pIndices = geomData->pShadow + 1;

    geomData->pShadow->pAttributes[SEMANTIC_POSITION] = (uint8_t*)geomData->pShadow->pIndices + (geom->indexCount * indexStride);

    for (uint32_t s = SEMANTIC_POSITION + 1; s < MAX_SEMANTICS; ++s)
        geomData->pShadow->pAttributes[s] = (uint8_t*)geomData->pShadow->pAttributes[s - 1] +
                                            geomData->pShadow->vertexStrides[s - 1] * geomData->pShadow->attributeCount[s - 1];

    for (uint32_t i = 0; i < TF_ARRAY_COUNT(geomData->pShadow->vertexStrides); ++i)
    {
        if (geomData->pShadow->vertexStrides[i] == 0)
            geomData->pShadow->pAttributes[i] = nullptr;
    }

    uint32_t vertexAttribCount[MAX_SEMANTICS] = {};
    uint32_t vertexOffsets[MAX_SEMANTICS] = {}; // offset in the GPU layout
    uint32_t vertexBindings[MAX_SEMANTICS] = {};
    for (uint32_t i = 0; i < TF_ARRAY_COUNT(vertexOffsets); ++i)
        vertexOffsets[i] = UINT_MAX;

    uint32_t defaultTexcoordSemantic = SEMANTIC_UNDEFINED;

    // Determine vertex stride for each binding
    for (uint32_t i = 0; i < pDesc->pVertexLayout->attribCount; ++i)
    {
        const VertexAttrib* attr = &pDesc->pVertexLayout->attribs[i];

        const uint32_t dstFormatSize = TinyImageFormat_BitSizeOfBlock(attr->format) / 8;

        if (defaultTexcoordSemantic == SEMANTIC_UNDEFINED) // #nocheckin Revisit this if statement
        {
            if (attr->semantic >= SEMANTIC_TEXCOORD0 && attr->semantic <= SEMANTIC_TEXCOORD9)
            {
                // Make sure there are only 1 set of default texcoords
                ASSERT(defaultTexcoordSemantic == SEMANTIC_UNDEFINED);
                defaultTexcoordSemantic = attr->semantic;
            }
        }

        const uint32_t srcFormatSize = (uint32_t)geomData->pShadow->vertexStrides[attr->semantic]; //-V522

        uint32_t binding =
            pDesc->pGeometryBufferLayoutDesc ? pDesc->pGeometryBufferLayoutDesc->semanticBindings[attr->semantic] : attr->binding;

        geom->vertexStrides[binding] += dstFormatSize ? dstFormatSize : srcFormatSize;
        vertexOffsets[attr->semantic] = attr->offset;
        vertexBindings[attr->semantic] = binding;
        ++vertexAttribCount[binding];

        // src and dst formats must match because the AssetPipeline converts to the destination formats already
        ASSERT(srcFormatSize == 0 || dstFormatSize == 0 || dstFormatSize == srcFormatSize);
    }

    uint32_t dstIndexStride = indexStride;

    fillGeometryUpdateDesc(pRenderer, pCopyEngine, pDesc, geom, &dstIndexStride, vertexUpdateDesc, indexUpdateDesc);

    if (indexStride == dstIndexStride)
        memcpy(indexUpdateDesc->pMappedData, geomData->pShadow->pIndices, indexStride * geom->indexCount);
    else
    {
        if (sizeof(uint16_t) == indexStride)
        {
            uint32_t*       dst = (uint32_t*)indexUpdateDesc->pMappedData;
            const uint16_t* src = (uint16_t*)geomData->pShadow->pIndices;
            for (uint32_t idx = 0; idx < geom->indexCount; ++idx)
                dst[idx] = src[idx];
        }
        else
        {
            LOGF(eERROR, "Trying to copy uint32 indexes into uint16 buffers, data will be lost: '%s'", pDesc->pFileName);
            ASSERT(false);
        }
    }

    for (uint32_t i = 0; i < MAX_SEMANTICS; ++i)
    {
        if (!geomData->pShadow->pAttributes[i])
            continue;
        // Invalid vertexOffset means pVertexLayout doesn't use this attribute, no need to copy it
        if (vertexOffsets[i] == UINT_MAX)
            continue;

        const uint32_t binding = vertexBindings[i];
        const uint32_t offset = vertexOffsets[i];
        const uint32_t stride = geom->vertexStrides[binding];

        const uint8_t* src = (uint8_t*)geomData->pShadow->pAttributes[i];
        uint8_t*       dst = (uint8_t*)vertexUpdateDesc[binding].pMappedData;
        ASSERT(src && dst);

        // If this vertex attribute is not interleaved with any other attribute use fast path instead of copying one by one
        // In this case a simple memcpy will be enough to transfer the data to the buffer
        if (1 == vertexAttribCount[binding])
        {
            memcpy(dst, src, geomData->pShadow->vertexStrides[i] * geomData->pShadow->attributeCount[i]);
        }
        else
        {
            // Loop through all vertices copying into the correct place in the vertex buffer
            // Example:
            // [ POSITION | NORMAL | TEXCOORD ] => [ 0 | 12 | 24 ], [ 32 | 44 | 52 ], ... (vertex stride of 32 => 12 + 12 + 8)
            for (uint32_t e = 0; e < geomData->pShadow->attributeCount[i]; ++e)
                memcpy(dst + e * stride + offset, src + e * geomData->pShadow->vertexStrides[i], geomData->pShadow->vertexStrides[i]);
        }
    }

    // If the user doesn't want the shadowed data we don't need it any more
    if ((pDesc->flags & GEOMETRY_LOAD_FLAG_SHADOWED) != GEOMETRY_LOAD_FLAG_SHADOWED)
    {
        tf_free(geomData->pShadow);
        geomData->pShadow = nullptr;
    }

    geom->pGeometryBuffer = pDesc->pGeometryBuffer;
    if (pDesc->pGeometryBufferLayoutDesc)
    {
        geom->indexType = pDesc->pGeometryBufferLayoutDesc->indexType;
    }

    *pDesc->ppGeometry = geom;

    if (pDesc->ppGeometryData)
        *pDesc->ppGeometryData = geomData;
    else
    {
        tf_free(geomData->pShadow);
        tf_free(geomData);
    }

    tf_free((void*)pDesc->pVertexLayout);

    return UPLOAD_FUNCTION_RESULT_COMPLETED;
}

static UploadFunctionResult loadGeometry(Renderer* pRenderer, CopyEngine* pCopyEngine, UpdateRequest& pGeometryLoad)
{
    GeometryLoadDesc* pDesc = &pGeometryLoad.geomLoadDesc;

    BufferUpdateDesc indexUpdateDesc = {};
    BufferUpdateDesc vertexUpdateDesc[MAX_VERTEX_BINDINGS] = {};

    UploadFunctionResult res = loadGeometryCustomMeshFormat(pRenderer, pCopyEngine, pDesc, vertexUpdateDesc, &indexUpdateDesc);
    if (res != UPLOAD_FUNCTION_RESULT_COMPLETED)
        return res;

    // Upload mesh
    UploadFunctionResult uploadResult = UPLOAD_FUNCTION_RESULT_COMPLETED;
    BufferBarrier        barriers[MAX_VERTEX_BINDINGS + 1] = {};
    uint32_t             barrierCount = 0;

    if (!gUma || (indexUpdateDesc.pMappedData && !indexUpdateDesc.pBuffer->pCpuMappedAddress))
    {
        indexUpdateDesc.currentState = gUma ? indexUpdateDesc.currentState : RESOURCE_STATE_COPY_DEST;
        indexUpdateDesc.internal.mappedRange = allocateStagingMemory(pCopyEngine, indexUpdateDesc.size, RESOURCE_BUFFER_ALIGNMENT);
        ASSERT(indexUpdateDesc.pMappedData);
        memcpy(indexUpdateDesc.internal.mappedRange.pData, indexUpdateDesc.pMappedData, indexUpdateDesc.size);
        tf_free(indexUpdateDesc.pMappedData);
        if (indexUpdateDesc.internal.mappedRange.flags & MAPPED_RANGE_FLAG_TEMP_BUFFER)
        {
            setBufferName(pRenderer, indexUpdateDesc.internal.mappedRange.pBuffer, pDesc->pFileName);
        }
        indexUpdateDesc.pMappedData = indexUpdateDesc.internal.mappedRange.pData;
        uploadResult = updateBuffer(pRenderer, pCopyEngine, indexUpdateDesc);
    }

    barriers[barrierCount++] = { indexUpdateDesc.pBuffer, RESOURCE_STATE_COPY_DEST, gIndexBufferState };

    for (uint32_t i = 0; i < MAX_VERTEX_BINDINGS; ++i)
    {
        if (vertexUpdateDesc[i].pBuffer)
        {
            if (!gUma || (vertexUpdateDesc[i].pMappedData && !vertexUpdateDesc[i].pBuffer->pCpuMappedAddress))
            {
                vertexUpdateDesc[i].currentState = gUma ? vertexUpdateDesc[i].currentState : RESOURCE_STATE_COPY_DEST;
                vertexUpdateDesc[i].internal.mappedRange =
                    allocateStagingMemory(pCopyEngine, vertexUpdateDesc[i].size, RESOURCE_BUFFER_ALIGNMENT);
                ASSERT(vertexUpdateDesc[i].pMappedData);
                memcpy(vertexUpdateDesc[i].internal.mappedRange.pData, vertexUpdateDesc[i].pMappedData, vertexUpdateDesc[i].size);
                tf_free(vertexUpdateDesc[i].pMappedData);
                if (vertexUpdateDesc[i].internal.mappedRange.flags & MAPPED_RANGE_FLAG_TEMP_BUFFER)
                {
                    setBufferName(pRenderer, vertexUpdateDesc[i].internal.mappedRange.pBuffer, pDesc->pFileName);
                }
                vertexUpdateDesc[i].pMappedData = vertexUpdateDesc[i].internal.mappedRange.pData;
                uploadResult = updateBuffer(pRenderer, pCopyEngine, vertexUpdateDesc[i]);
            }
            barriers[barrierCount++] = { vertexUpdateDesc[i].pBuffer, RESOURCE_STATE_COPY_DEST, gVertexBufferState };
        }
    }

    if (!gUma && IssueBufferCopyBarriers())
    {
        Cmd* cmd = acquirePostCopyBarrierCmd(pCopyEngine);
        cmdResourceBarrier(cmd, barrierCount, barriers, 0, NULL, 0, NULL);
    }

    return uploadResult;
}

static UploadFunctionResult copyTexture(Renderer* pRenderer, CopyEngine* pCopyEngine, TextureCopyDesc& pTextureCopy)
{
    UNREF_PARAM(pRenderer);
    Texture*              texture = pTextureCopy.pTexture;
    const TinyImageFormat fmt = (TinyImageFormat)texture->format;

    Cmd* cmd = acquireCmd(pCopyEngine);

    if (pTextureCopy.pWaitSemaphore)
        arrpush(pCopyEngine->waitSemaphores, pTextureCopy.pWaitSemaphore);

    TextureBarrier barrier = { texture, pTextureCopy.textureState, RESOURCE_STATE_COPY_SOURCE };
    barrier.acquire = 1;
    barrier.queueType = pTextureCopy.queueType;
    cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);

    uint32_t numBytes = 0;
    uint32_t rowBytes = 0;
    uint32_t numRows = 0;

    bool ret = util_get_surface_info(texture->width, texture->height, fmt, &numBytes, &rowBytes, &numRows);
    if (!ret)
    {
        return UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;
    }

    SubresourceDataDesc subresourceDesc = {};
    subresourceDesc.arrayLayer = pTextureCopy.textureArrayLayer;
    subresourceDesc.mipLevel = pTextureCopy.textureMipLevel;
    subresourceDesc.srcOffset = pTextureCopy.bufferOffset;
    const uint32_t sliceAlignment = util_get_texture_subresource_alignment(pRenderer, fmt);
    const uint32_t rowAlignment = util_get_texture_row_alignment(pRenderer);
    uint32_t       subRowPitch = round_up(rowBytes, rowAlignment);
    uint32_t       subSlicePitch = round_up(subRowPitch * numRows, sliceAlignment);
    subresourceDesc.rowPitch = subRowPitch;
    subresourceDesc.slicePitch = subSlicePitch;
    cmdCopySubresource(cmd, pTextureCopy.pBuffer, pTextureCopy.pTexture, &subresourceDesc);

    barrier = { texture, RESOURCE_STATE_COPY_SOURCE, pTextureCopy.textureState };
    barrier.release = 1;
    barrier.queueType = pTextureCopy.queueType;
    cmdResourceBarrier(cmd, 0, NULL, 1, &barrier, 0, NULL);

    return UPLOAD_FUNCTION_RESULT_COMPLETED;
}
/************************************************************************/
// Internal Resource Loader Implementation
/************************************************************************/
static bool areTasksAvailable(ResourceLoader* pLoader) { return arrlen(pLoader->requestQueue) > 0; }

static void streamerThreadFunc(void* pThreadData)
{
    ResourceLoader* pLoader = (ResourceLoader*)pThreadData;
    ASSERT(pLoader);

    while (pLoader->run)
    {
        acquireMutex(&pLoader->queueMutex);

        // Check for pending tokens
        // Safe to use tokenCounter as we are inside critical section
        bool allTokensSignaled = (pLoader->tokenCompleted == tfrg_atomic64_load_relaxed(&pLoader->tokenCounter));

        while (!areTasksAvailable(pLoader) && allTokensSignaled && pLoader->run)
        {
            // No waiting if not running dedicated resource loader thread.
            if (pLoader->desc.singleThreaded)
            {
                releaseMutex(&pLoader->queueMutex);
                return;
            }
            // Sleep until someone adds an update request to the queue
            waitConditionVariable(&pLoader->queueCond, &pLoader->queueMutex, TIMEOUT_INFINITE);
        }

        releaseMutex(&pLoader->queueMutex);

        CopyEngine* copyEngine = &pLoader->copyEngine;
        waitCopyEngineSet(pLoader->pRenderer, copyEngine);
        resetCopyEngineSet(pLoader->pRenderer, copyEngine);
        copyEngine->activeSet = (copyEngine->activeSet + 1) % pLoader->desc.bufferCount;

        // Signal pending tokens from previous frames
        acquireMutex(&pLoader->tokenMutex);
        tfrg_atomic64_store_release(&pLoader->tokenCompleted, pLoader->currentTokenState[pLoader->copyEngine.activeSet]);
        releaseMutex(&pLoader->tokenMutex);
        wakeAllConditionVariable(&pLoader->tokenCond);

        {
            acquireMutex(&pLoader->queueMutex);

            UpdateRequest** pRequestQueue = &pLoader->requestQueue;
            CopyEngine*     pCopyEngine = &pLoader->copyEngine;

            if (!arrlen(*pRequestQueue))
            {
                releaseMutex(&pLoader->queueMutex);
                if (pResourceLoader->desc.singleThreaded)
                {
                    return;
                }
                continue;
            }

            UpdateRequest* activeQueue = *pRequestQueue;
            *pRequestQueue = NULL;
            releaseMutex(&pLoader->queueMutex);

            Renderer* pRenderer = pLoader->pRenderer;
            SyncToken maxNodeToken = {};

            ASSERT(arrlen(activeQueue));

            for (ptrdiff_t j = 0; j < arrlen(activeQueue); ++j)
            {
                UpdateRequest updateState = activeQueue[j];
                // #NOTE: acquireCmd also resets copy engine on first use
                Cmd*          cmd = acquireCmd(pCopyEngine);

                UploadFunctionResult result = UPLOAD_FUNCTION_RESULT_COMPLETED;
                switch (updateState.type)
                {
                case UPDATE_REQUEST_TEXTURE_BARRIER:
                    cmdResourceBarrier(cmd, 0, NULL, 1, &updateState.textureBarrier, 0, NULL);
                    result = UPLOAD_FUNCTION_RESULT_COMPLETED;
                    break;
                case UPDATE_REQUEST_LOAD_BUFFER:
                    result = loadBuffer(pRenderer, pCopyEngine, updateState);
                    break;
                case UPDATE_REQUEST_LOAD_TEXTURE:
                    result = loadTexture(pRenderer, pCopyEngine, updateState);
                    break;
                case UPDATE_REQUEST_LOAD_GEOMETRY:
                    result = loadGeometry(pRenderer, pCopyEngine, updateState);
                    break;
                case UPDATE_REQUEST_COPY_TEXTURE:
                    result = copyTexture(pRenderer, pCopyEngine, updateState.texCopyDesc);
                    break;
                case UPDATE_REQUEST_INVALID:
                    break;
                }

                bool completed = result == UPLOAD_FUNCTION_RESULT_COMPLETED || result == UPLOAD_FUNCTION_RESULT_INVALID_REQUEST;

                if (updateState.waitIndex && completed)
                {
                    ASSERT(maxNodeToken < updateState.waitIndex);
                    maxNodeToken = updateState.waitIndex;
                }

                ASSERT(result != UPLOAD_FUNCTION_RESULT_STAGING_BUFFER_FULL);
            }

            arrfree(activeQueue);
            pLoader->maxToken = max(pLoader->maxToken, maxNodeToken);

            streamerFlush(pCopyEngine);
            acquireMutex(&pLoader->semaphoreMutex);
            pCopyEngine->pLastSubmittedSemaphore = pCopyEngine->resourceSets[pCopyEngine->activeSet].pSemaphore;
            releaseMutex(&pLoader->semaphoreMutex);
        }

        SyncToken nextToken = max(pLoader->maxToken, getLastTokenCompleted());
        pLoader->currentTokenState[pLoader->copyEngine.activeSet] = nextToken;

        // Signal submitted tokens
        acquireMutex(&pLoader->tokenMutex);
        tfrg_atomic64_store_release(&pLoader->tokenSubmitted, pLoader->currentTokenState[pLoader->copyEngine.activeSet]);
        releaseMutex(&pLoader->tokenMutex);
        wakeAllConditionVariable(&pLoader->tokenCond);

        if (pResourceLoader->desc.singleThreaded)
        {
            return;
        }
    }

    streamerFlush(&pLoader->copyEngine);
    waitQueueIdle(pLoader->copyEngine.pQueue);
    cleanupCopyEngine(pLoader->pRenderer, &pLoader->copyEngine);
}

static void CopyEngineFlush(CopyEngine* pCopyEngine)
{
    streamerFlush(pCopyEngine);
    acquireMutex(&pResourceLoader->semaphoreMutex);
    pCopyEngine->pLastSubmittedSemaphore = pCopyEngine->resourceSets[pCopyEngine->activeSet].pSemaphore;
    releaseMutex(&pResourceLoader->semaphoreMutex);

    SyncToken nextToken = max(pResourceLoader->maxToken, getLastTokenCompleted());
    pResourceLoader->currentTokenState[pResourceLoader->copyEngine.activeSet] = nextToken;

    // Signal submitted tokens
    acquireMutex(&pResourceLoader->tokenMutex);
    tfrg_atomic64_store_release(&pResourceLoader->tokenSubmitted,
                                pResourceLoader->currentTokenState[pResourceLoader->copyEngine.activeSet]);
    releaseMutex(&pResourceLoader->tokenMutex);
    wakeAllConditionVariable(&pResourceLoader->tokenCond);

    pCopyEngine->activeSet = (pCopyEngine->activeSet + 1) % pResourceLoader->desc.bufferCount;
    acquireCmd(pCopyEngine);
}

static void initResourceLoader(Renderer** ppRenderers, uint32_t rendererCount, ResourceLoaderDesc* pDesc, ResourceLoader** ppLoader)
{
    ASSERT(rendererCount == 1);

    if (!pDesc)
        pDesc = &gDefaultResourceLoaderDesc;

    ResourceLoader* pLoader = tf_new(ResourceLoader);

    pLoader->pRenderer = ppRenderers[0];

    pLoader->run = true; //-V601
    pLoader->desc = *pDesc;

    initMutex(&pLoader->queueMutex);
    initMutex(&pLoader->tokenMutex);
    initConditionVariable(&pLoader->queueCond);
    initConditionVariable(&pLoader->tokenCond);
    initMutex(&pLoader->semaphoreMutex);
    initMutex(&pLoader->uploadEngineMutex);

    pLoader->tokenCounter = 0;
    pLoader->tokenCompleted = 0;
    pLoader->tokenSubmitted = 0;

    CopyEngineDesc desc = {};
    desc.bufferCount = pLoader->desc.bufferCount;
    desc.queueType = QUEUE_TYPE_GRAPHICS;
    desc.size = pLoader->desc.bufferSize;
    desc.pQueueName = "UPLOAD";
    setupCopyEngine(pLoader->pRenderer, &desc, &pLoader->uploadEngine);

    desc = {};
    desc.bufferCount = pLoader->desc.bufferCount;
    desc.queueType = QUEUE_TYPE_TRANSFER;
    desc.size = pLoader->desc.bufferSize;
    desc.pQueueName = "COPY";
    setupCopyEngine(pLoader->pRenderer, &desc, &pLoader->copyEngine);

    CopyEngine* copyEngine = &pLoader->copyEngine;
    copyEngine->flushOnOverflow = true;
    copyEngine->pFnFlush = CopyEngineFlush;

#if defined(STRICT_QUEUE_TYPE_BARRIERS)
    if (StrictQueueTypeBarriers())
    {
        for (uint32_t b = 0; b < pDesc->bufferCount; ++b)
        {
            CopyResourceSet& resourceSet = pLoader->copyEngine.resourceSets[b];
            CmdPoolDesc      poolDesc = {};
            poolDesc.pQueue = pLoader->uploadEngine.pQueue;
            addCmdPool(pLoader->pRenderer, &poolDesc, &resourceSet.pPostCopyBarrierCmdPool);
            CmdDesc cmdDesc = {};
            cmdDesc.pPool = resourceSet.pPostCopyBarrierCmdPool;
#ifdef ENABLE_GRAPHICS_DEBUG
            static char buffer[MAX_DEBUG_NAME_LENGTH];
            snprintf(buffer, sizeof(buffer), "Strict Queue buffer %u Cmd", b);
            cmdDesc.pName = buffer;
#endif // ENABLE_GRAPHICS_DEBUG
            addCmd(pLoader->pRenderer, &cmdDesc, &resourceSet.pPostCopyBarrierCmd);
            addFence(pLoader->pRenderer, &resourceSet.pPostCopyBarrierFence);
        }
    }
#endif

    ThreadDesc threadDesc = {};
    threadDesc.pFunc = streamerThreadFunc;
    threadDesc.pData = pLoader;
    strncpy(threadDesc.threadName, "ResourceLoaderTask", sizeof(threadDesc.threadName));

#if defined(NX64)
    threadDesc.setAffinityMask = true;
    threadDesc.affinityMask[0] = 1;
#endif

    // Create dedicated resource loader thread.
    if (!pLoader->desc.singleThreaded)
    {
        initThread(&threadDesc, &pLoader->thread);
    }

    *ppLoader = pLoader;
}

static void exitResourceLoader(ResourceLoader* pLoader)
{
    pLoader->run = false; //-V601

    if (pLoader->desc.singleThreaded)
    {
        streamerThreadFunc(pLoader);
    }
    else
    {
        wakeOneConditionVariable(&pLoader->queueCond);
        joinThread(pLoader->thread);
    }

    waitQueueIdle(pLoader->uploadEngine.pQueue);
    cleanupCopyEngine(pLoader->pRenderer, &pLoader->uploadEngine);

    destroyConditionVariable(&pLoader->queueCond);
    destroyConditionVariable(&pLoader->tokenCond);
    destroyMutex(&pLoader->queueMutex);
    destroyMutex(&pLoader->tokenMutex);
    destroyMutex(&pLoader->semaphoreMutex);
    destroyMutex(&pLoader->uploadEngineMutex);

    tf_delete(pLoader);
}

static void queueBufferLoad(ResourceLoader* pLoader, BufferLoadDescInternal* pBufferLoad, SyncToken* token)
{
    acquireMutex(&pLoader->queueMutex);

    SyncToken t = tfrg_atomic64_add_relaxed(&pLoader->tokenCounter, 1) + 1;

    arrpush(pLoader->requestQueue, UpdateRequest(*pBufferLoad));
    UpdateRequest* pLastRequest = arrback(pLoader->requestQueue);
    if (pLastRequest)
        pLastRequest->waitIndex = t;

    releaseMutex(&pLoader->queueMutex);
    wakeOneConditionVariable(&pLoader->queueCond);
    if (token)
        *token = max(t, *token);

    if (pResourceLoader->desc.singleThreaded)
    {
        streamerThreadFunc(pResourceLoader);
    }
}

static void queueTextureLoad(ResourceLoader* pLoader, TextureLoadDescInternal* pTextureLoad, SyncToken* token)
{
    acquireMutex(&pLoader->queueMutex);

    SyncToken t = tfrg_atomic64_add_relaxed(&pLoader->tokenCounter, 1) + 1;

    arrpush(pLoader->requestQueue, UpdateRequest(*pTextureLoad));
    UpdateRequest* pLastRequest = arrback(pLoader->requestQueue);
    if (pLastRequest)
        pLastRequest->waitIndex = t;

    releaseMutex(&pLoader->queueMutex);
    wakeOneConditionVariable(&pLoader->queueCond);
    if (token)
        *token = max(t, *token);

    if (pResourceLoader->desc.singleThreaded)
    {
        streamerThreadFunc(pResourceLoader);
    }
}

static void queueGeometryLoad(ResourceLoader* pLoader, GeometryLoadDesc* pGeometryLoad, SyncToken* token)
{
    acquireMutex(&pLoader->queueMutex);

    SyncToken t = tfrg_atomic64_add_relaxed(&pLoader->tokenCounter, 1) + 1;

    arrpush(pLoader->requestQueue, UpdateRequest(*pGeometryLoad));
    UpdateRequest* pLastRequest = arrback(pLoader->requestQueue);
    if (pLastRequest)
        pLastRequest->waitIndex = t;

    releaseMutex(&pLoader->queueMutex);
    wakeOneConditionVariable(&pLoader->queueCond);
    if (token)
        *token = max(t, *token);

    if (pResourceLoader->desc.singleThreaded)
    {
        streamerThreadFunc(pResourceLoader);
    }
}

static void queueTextureBarrier(ResourceLoader* pLoader, Texture* pTexture, ResourceState state, SyncToken* token)
{
    acquireMutex(&pLoader->queueMutex);

    SyncToken t = tfrg_atomic64_add_relaxed(&pLoader->tokenCounter, 1) + 1;

    arrpush(pLoader->requestQueue, UpdateRequest(TextureBarrier{ pTexture, RESOURCE_STATE_UNDEFINED, state }));
    UpdateRequest* pLastRequest = arrback(pLoader->requestQueue);
    if (pLastRequest)
        pLastRequest->waitIndex = t;

    releaseMutex(&pLoader->queueMutex);
    wakeOneConditionVariable(&pLoader->queueCond);
    if (token)
        *token = max(t, *token);

    if (pResourceLoader->desc.singleThreaded)
    {
        streamerThreadFunc(pResourceLoader);
    }
}

static void queueTextureCopy(ResourceLoader* pLoader, TextureCopyDesc* pTextureCopy, SyncToken* token)
{
    acquireMutex(&pLoader->queueMutex);

    SyncToken t = tfrg_atomic64_add_relaxed(&pLoader->tokenCounter, 1) + 1;

    arrpush(pLoader->requestQueue, UpdateRequest(*pTextureCopy));
    UpdateRequest* pLastRequest = arrback(pLoader->requestQueue);
    if (pLastRequest)
        pLastRequest->waitIndex = t;

    releaseMutex(&pLoader->queueMutex);
    wakeOneConditionVariable(&pLoader->queueCond);
    if (token)
        *token = max(t, *token);

    if (pResourceLoader->desc.singleThreaded)
    {
        streamerThreadFunc(pResourceLoader);
    }
}

static void waitForToken(ResourceLoader* pLoader, const SyncToken* token)
{
    if (pLoader->desc.singleThreaded)
    {
        return;
    }
    acquireMutex(&pLoader->tokenMutex);
    while (!isTokenCompleted(token))
    {
        waitConditionVariable(&pLoader->tokenCond, &pLoader->tokenMutex, TIMEOUT_INFINITE);
    }
    releaseMutex(&pLoader->tokenMutex);
}

static void waitForTokenSubmitted(ResourceLoader* pLoader, const SyncToken* token)
{
    if (pLoader->desc.singleThreaded)
    {
        return;
    }
    acquireMutex(&pLoader->tokenMutex);
    while (!isTokenSubmitted(token))
    {
        waitConditionVariable(&pLoader->tokenCond, &pLoader->tokenMutex, TIMEOUT_INFINITE);
    }
    releaseMutex(&pLoader->tokenMutex);
}

/************************************************************************/
// Resource Loader Interface Implementation
/************************************************************************/
void initResourceLoaderInterface(Renderer* pRenderer, ResourceLoaderDesc* pDesc)
{
    initResourceLoader(&pRenderer, 1, pDesc, &pResourceLoader);

#ifdef ENABLE_FORGE_MATERIALS
    if (pDesc && pDesc->useMaterials)
    {
        uint32_t maxShaders = 0;
        uint32_t maxTextures = 0;

        {
            // During material compilation we generate an ini file with data to initialize the material library
            FileStream stream = {};
            if (fsOpenStreamFromPath(RD_COMPILED_MATERIALS, "material_lib.ini", FM_READ, &stream))
            {
                char buffer[256] = {};
                ASSERT((uint64_t)fsGetStreamFileSize(&stream) <= sizeof(buffer));
                fsReadFromStream(&stream, buffer, sizeof(buffer));
                sscanf(buffer, "%u %u", &maxShaders, &maxTextures);

                fsCloseStream(&stream);
            }
            else
            {
                ASSERT(false);
                return;
            }
        }

        uint32_t totalSize = sizeof(MaterialLibrary);
        totalSize += (sizeof(Shader**) + sizeof(uint16_t)) * maxShaders + alignof(Shader**); // Shaders
        totalSize += (sizeof(Texture**) + sizeof(uint16_t)) * maxTextures;                   // Textures
        totalSize += sizeof(uint16_t) * maxShaders;                                          // pMaterialShaderRefCount
        totalSize += sizeof(uint16_t) * maxTextures;                                         // pMaterialTextureRefCount

        pMaterialLibrary = (MaterialLibrary*)tf_calloc(1, totalSize);
        MaterialLibrary* pLib = pMaterialLibrary;
        pLib->pRenderer = pRenderer;
        pLib->ppMaterialShaders = (Shader**)alignMemory(pLib + 1, alignof(Shader**));
        pLib->ppMaterialTextures = (Texture**)(pLib->ppMaterialShaders + maxShaders); //-V1027 // Memory already aligned to pointer
        pLib->pMaterialShaderRefCount = (uint16_t*)(pLib->ppMaterialTextures + maxTextures);
        pLib->pMaterialTextureRefCount = (uint16_t*)(pLib->pMaterialShaderRefCount + maxShaders);
        pLib->maxMaterialShaders = maxShaders;
        pLib->maxMaterialTextures = maxTextures;
        ASSERT((char*)(pLib->pMaterialTextureRefCount + maxTextures) <= ((char*)pLib) + totalSize);
    }
#endif

#if defined(ENABLE_FORGE_RELOAD_SHADER)
    if (!platformInitReloadClient())
    {
        LOGF(eERROR, "Failed to initialize ReloadServer");
#ifdef AUTOMATED_TESTING
        ASSERT(false);
#endif
    }
#else
    LOGF(eWARNING, "ReloadServer is DISABLED. Shaders will not be updated dynamically on the device "
                   "when they are recompiled while App is running. If this behaviour is desired then you can ignore this warning. "
                   "If you want to re-enable ReloadServer you need to #define ENABLE_FORGE_RELOAD_SHADER in Application/Config.h"
                   "This message is here to remind everyone to re-enable ReloadServer if it has been disabled for whatever reason.");
#endif
}

void exitResourceLoaderInterface(Renderer* pRenderer)
{
    UNREF_PARAM(pRenderer);
#ifdef ENABLE_FORGE_MATERIALS
    if (pMaterialLibrary)
    {
        MaterialLibrary* pLib = pMaterialLibrary;
        ASSERT(pLib->loadedMaterialCount == 0);

#if defined(FORGE_DEBUG)
        for (uint32_t i = 0; i < pLib->maxMaterialShaders; ++i)
        {
            ASSERT(pLib->ppMaterialShaders[i] == NULL);
            ASSERT(pLib->pMaterialShaderRefCount[i] == 0);
        }
        for (uint32_t i = 0; i < pLib->maxMaterialTextures; ++i)
        {
            ASSERT(pLib->ppMaterialTextures[i] == NULL);
            ASSERT(pLib->pMaterialTextureRefCount[i] == 0);
        }
#endif
        tf_free(pMaterialLibrary);
        pMaterialLibrary = NULL;
    }
#endif

    exitResourceLoader(pResourceLoader);

#if defined(ENABLE_FORGE_RELOAD_SHADER)
    platformExitReloadClient();
#endif
}

void initResourceLoaderInterface(Renderer** ppRenderers, uint32_t rendererCount, ResourceLoaderDesc* pDesc)
{
    initResourceLoader(ppRenderers, rendererCount, pDesc, &pResourceLoader);
}

void exitResourceLoaderInterface(Renderer** pRenderers, uint32_t rendererCount)
{
    UNREF_PARAM(pRenderers);
    UNREF_PARAM(rendererCount);
    exitResourceLoader(pResourceLoader);
}

#ifdef ENABLE_FORGE_MATERIALS
static void parseMaterial(const char* pFileBuffer, uint64_t fileSize, Material** pOut)
{
    ASSERT(pOut);

    uint64_t offset = 0;
    uint64_t nextLineOffset = 0;

    const bool bCompiled = strncmp(pFileBuffer, ":FMC", 4) == 0;
    if (!bCompiled)
    {
        ASSERT(false && "This file doesn't contain a compiled material");
        return;
    }

    offset = 4; // Skip ":FMC"

    // Values from material compilation
    uint32_t precomputedValues[7] = {};

    for (uint32_t i = 0; i < TF_ARRAY_COUNT(precomputedValues); ++i)
    {
        while (pFileBuffer[offset] == ' ')
        {
            offset++;
        }

        precomputedValues[i] = atoi(pFileBuffer + offset);

        while (pFileBuffer[offset] != '\n' && pFileBuffer[offset] != ' ')
        {
            offset++;
        }
    }
    while (pFileBuffer[offset++] != '\n')
    {
    }

    const uint32_t numShaderSets = precomputedValues[0];
    const uint32_t numTextureSets = precomputedValues[1];
    const uint32_t numMaterialSets = precomputedValues[2];
    const uint32_t numShaders = precomputedValues[3];
    const uint32_t numTextures = precomputedValues[4];
    const uint32_t maxShaderSetBindings = precomputedValues[5];
    const uint32_t maxTextureSetTextures = precomputedValues[6];

    uint64_t extraSize = 0;
    extraSize += sizeof(Material::LoadedMaterial) * numMaterialSets + alignof(Material::LoadedMaterial); // Material::pLoaded
    extraSize += sizeof(uint32_t) * numMaterialSets * maxTextureSetTextures; // Material::LoadedMaterial::pTextureIndexes;
    extraSize += sizeof(MaterialDesc::ShaderSet) * numShaderSets + alignof(MaterialDesc::ShaderSet); // MaterialDesc::pShaderSets
    extraSize +=
        sizeof(const char*) * numShaderSets * maxShaderSetBindings + alignof(void*); // MaterialDesc::ShaderSet::pTextureBindingNames
    extraSize += sizeof(MaterialDesc::TextureSet) * numTextureSets + alignof(MaterialDesc::TextureSet); // MaterialDesc::pTextureSets
    extraSize += sizeof(uint32_t) * numTextureSets * maxTextureSetTextures; // MaterialDesc::TextureSet::pTextureIdxs
    extraSize += sizeof(MaterialDesc::MaterialSet) * numMaterialSets + alignof(MaterialDesc::MaterialSet); // MaterialDesc::pMaterialSets
    extraSize += sizeof(uint32_t) * numTextures;                                                           // MaterialDesc::pTextureIds
    extraSize += sizeof(*MaterialDesc::pTextureFlags) * numTextures;                                       // MaterialDesc::pTextureFlags;
    extraSize += alignof(void*);                       // Alignment for the pointers below
    extraSize += sizeof(const char*) * numTextureSets; // MaterialDesc::pMaterialSetNames
    extraSize += sizeof(const char*) * numTextures;    // MaterialDesc::pTextureNames
    extraSize += sizeof(const char*) * numShaders;     // MaterialDesc::pShaderNames

    // Note: We use file size for the string buffer size as it won't be bigger than that.
    //       We could also compute a tighter buffer size during material compilation.
    extraSize += fileSize;

    const uint64_t totalSize = sizeof(Material) + sizeof(MaterialDesc) + alignof(MaterialDesc) + extraSize;
    Material*      pMaterial = (Material*)tf_calloc(1, totalSize);
    pMaterial->pLoaded = (Material::LoadedMaterial*)alignMemory(pMaterial + 1, alignof(Material::LoadedMaterial));
    for (uint32_t i = 0; i < numMaterialSets; ++i)
    {
        pMaterial->pLoaded[i].pTextureIndexes = (uint32_t*)(pMaterial->pLoaded + numMaterialSets) + i * maxTextureSetTextures;
    }
    MaterialDesc* pMaterialDesc =
        (MaterialDesc*)alignMemory(pMaterial->pLoaded[numMaterialSets - 1].pTextureIndexes + maxTextureSetTextures, alignof(MaterialDesc));
    pMaterial->pDesc = pMaterialDesc;
    pMaterialDesc->pShaderSets = (MaterialDesc::ShaderSet*)alignMemory(pMaterialDesc + 1, alignof(MaterialDesc::ShaderSet));
    for (uint32_t i = 0; i < numShaderSets; ++i)
    {
        pMaterialDesc->pShaderSets[i].pTextureBindingNames =
            (const char**)alignMemory((pMaterialDesc->pShaderSets + numShaderSets) + i * maxShaderSetBindings, alignof(void*));
    }
    pMaterialDesc->pTextureSets = (MaterialDesc::TextureSet*)alignMemory(
        pMaterialDesc->pShaderSets[numShaderSets - 1].pTextureBindingNames + maxShaderSetBindings, alignof(MaterialDesc::TextureSet));
    for (uint32_t i = 0; i < numTextureSets; ++i)
    {
        pMaterialDesc->pTextureSets[i].pTextureIdxs = (uint32_t*)(pMaterialDesc->pTextureSets + numTextureSets) + i * maxTextureSetTextures;
    }
    pMaterialDesc->pMaterialSets = (MaterialDesc::MaterialSet*)alignMemory(
        pMaterialDesc->pTextureSets[numTextureSets - 1].pTextureIdxs + maxTextureSetTextures, alignof(MaterialDesc::MaterialSet));
    pMaterialDesc->pTextureIds = (uint32_t*)(pMaterialDesc->pMaterialSets + numMaterialSets);
    pMaterialDesc->pTextureFlags = (uint8_t*)(pMaterialDesc->pTextureIds + numTextures);
    pMaterialDesc->pMaterialSetNames = (const char**)alignMemory(pMaterialDesc->pTextureFlags + numTextures, alignof(void*));
    pMaterialDesc->pTextureNames = (const char**)pMaterialDesc->pMaterialSetNames + numMaterialSets;
    pMaterialDesc->pShaderNames = (const char**)(pMaterialDesc->pTextureNames + numTextures);
    pMaterialDesc->pStringBuffer = (char*)(pMaterialDesc->pShaderNames + numShaders);
    pMaterialDesc->stringBufferSize = (uint32_t)fileSize;
    ASSERT(pMaterialDesc->pStringBuffer + fileSize <= ((const char*)pMaterial) + totalSize);

    pMaterialDesc->maxShaderSetBindings = maxShaderSetBindings;
    pMaterialDesc->maxTextureSetTextures = maxTextureSetTextures;

    // Parse material file
    while (materialNextFileLine(pFileBuffer, fileSize, offset, &nextLineOffset))
    {
        MaterialDesc::TextureSet*  pTextureSet = NULL;
        MaterialDesc::ShaderSet*   pShaderSet = NULL;
        MaterialDesc::MaterialSet* pMaterialSet = NULL;

        const char* pSetLine = pFileBuffer + offset;
        switch (pSetLine[0])
        {
        case 'S':
        {
            pShaderSet = pMaterialDesc->pShaderSets + pMaterialDesc->shaderSetCount;
            pShaderSet->vertIdx = INVALID_MATERIAL_ID;
            pShaderSet->fragIdx = INVALID_MATERIAL_ID;
            pShaderSet->hullIdx = INVALID_MATERIAL_ID;
            pShaderSet->domainIdx = INVALID_MATERIAL_ID;
            pShaderSet->geomIdx = INVALID_MATERIAL_ID;
            pShaderSet->compIdx = INVALID_MATERIAL_ID;

            ASSERT(pSetLine[0] == 'S' && pSetLine[1] == ' ' && pSetLine[2] != ' ');
            pShaderSet->id = atoi(pSetLine + 2);

            ASSERT(pMaterialDesc->shaderSetCount < numShaderSets);
            pMaterialDesc->shaderSetCount++;
            break;
        }
        case 'T':
        {
            pTextureSet = pMaterialDesc->pTextureSets + pMaterialDesc->textureSetCount;
            pTextureSet->pTextureIdxs[0] = INVALID_MATERIAL_ID;

            ASSERT(pMaterialDesc->textureSetCount < numTextureSets);
            pMaterialDesc->textureSetCount++;
            break;
        }
        case 'M':
        {
            pMaterialSet = pMaterialDesc->pMaterialSets + pMaterialDesc->materialCount;
            pMaterialSet->shaderSetIdx = INVALID_MATERIAL_ID;
            pMaterialSet->textureSetIdx = INVALID_MATERIAL_ID;

            uint32_t offset32 = 0;
            pMaterialDesc->pMaterialSetNames[pMaterialDesc->materialCount] =
                materialParseAndStoreString(pMaterialDesc, pSetLine + 2, &offset32);

            ASSERT(pMaterialDesc->materialCount < numMaterialSets);
            pMaterialDesc->materialCount++;
            break;
        }
        case '#': // Comment
        default:
            break;
        }

        if (pTextureSet || pShaderSet || pMaterialSet)
        {
            offset = nextLineOffset + 1;

            while (materialNextFileLine(pFileBuffer, fileSize, offset, &nextLineOffset))
            {
                const char* pLine = pFileBuffer + offset;
                char        prefix = pLine[0];

                if (prefix >= 'A' && prefix <= 'Z')
                    break;

                switch (prefix)
                {
                case 's':
                {
                    if (pShaderSet) // Shader that belongs to a ShaderSet
                    {
                        ASSERT(pMaterialDesc->shaderCount < numShaders);

                        const uint32_t shaderIdx = pMaterialDesc->shaderCount++;

                        // Parse shader stage
                        const char stage = pLine[2]; // stage comes after "s "
                        switch (stage)
                        {
                        case 'v':
                            pShaderSet->vertIdx = shaderIdx;
                            break;
                        case 'f':
                            pShaderSet->fragIdx = shaderIdx;
                            break;
                        case 'h':
                            pShaderSet->hullIdx = shaderIdx;
                            break;
                        case 'd':
                            pShaderSet->domainIdx = shaderIdx;
                            break;
                        case 'g':
                            pShaderSet->geomIdx = shaderIdx;
                            break;
                        case 'c':
                            pShaderSet->compIdx = shaderIdx;
                            break;
                        default:
                            ASSERT(false);
                            break;
                        }

                        uint32_t shaderNameOffset = 4; // Name comes after "s X " where X is the stage
                        pMaterialDesc->pShaderNames[shaderIdx] = materialParseAndStoreString(pMaterialDesc, pLine, &shaderNameOffset);
                    }
                    else if (pMaterialSet) // ShaderSet reference from a MaterialSet
                    {
                        ASSERT(pLine[0] == 's' && pLine[1] == ' ' && pLine[2] != ' ');
                        pMaterialSet->shaderSetIdx = atoi(pLine + 2); // Skip "s "
                    }
                    else
                        ASSERT(false);

                    break;
                }
                case 'b':
                {
                    ASSERT(pShaderSet);

                    // We store all bindings in the same line
                    uint32_t parseBindingsOffset = 1;
                    while (pLine[parseBindingsOffset] == ' ')
                    {
                        parseBindingsOffset++;

                        ASSERT(pShaderSet->textureBindingCount < pMaterialDesc->maxShaderSetBindings);
                        pShaderSet->pTextureBindingNames[pShaderSet->textureBindingCount++] =
                            materialParseAndStoreString(pMaterialDesc, pLine, &parseBindingsOffset); //-V522
                    }
                    break;
                }
                case 't':
                {
                    if (pTextureSet) // Parsing a texture that belongs to a TextureSet
                    {
                        ASSERT(pMaterialDesc->textureCount < numTextures);

                        // Copy the texture name to our buffer in pMaterial
                        const uint32_t textureIdxInMaterial = pMaterialDesc->textureCount++;

                        uint32_t textureLineOffset = 2; // Skip "t "
                        pMaterialDesc->pTextureNames[textureIdxInMaterial] =
                            materialParseAndStoreString(pMaterialDesc, pLine, &textureLineOffset);

                        // Move to flags
                        textureLineOffset += 1; // Skip space
                        const int32_t flags = atoi(pLine + textureLineOffset);
                        ASSERT(flags < 256);
                        pMaterialDesc->pTextureFlags[textureIdxInMaterial] = (uint8_t)flags;

                        // Move to global ID
                        while (pLine[textureLineOffset++] != ' ')
                        {
                        }
                        pMaterialDesc->pTextureIds[textureIdxInMaterial] = atoi(pLine + textureLineOffset);

                        ASSERT(pTextureSet->textureCount < pMaterialDesc->maxTextureSetTextures);
                        const uint32_t textureIdxInSet = pTextureSet->textureCount++;
                        pTextureSet->pTextureIdxs[textureIdxInSet] = textureIdxInMaterial;

                        // materialParseLineTexture(pMaterialDesc, pTextureSet, pLine);
                    }
                    else if (pMaterialSet) // TextureSet reference from a MaterialSet
                    {
                        ASSERT(pLine[0] == 't' && pLine[1] == ' ' && pLine[2] != ' ');
                        pMaterialSet->textureSetIdx = atoi(pLine + 2); // Skip "t "
                    }
                    else
                        ASSERT(false);

                    break;
                }
                case '#':
                    break; // Comment
                default:
                    break;
                }

                offset = nextLineOffset + 1;
            }
        }
        else
            offset = nextLineOffset + 1;
    }

    ASSERT(pMaterialDesc->shaderCount == numShaders);
    ASSERT(pMaterialDesc->shaderSetCount == numShaderSets);
    ASSERT(pMaterialDesc->textureSetCount == numTextureSets);
    ASSERT(pMaterialDesc->stringBufferUsed < pMaterialDesc->stringBufferSize);

    *pOut = pMaterial;
}

uint32_t addMaterial(const char* pMaterialFileName, Material** pOutMaterial, SyncToken* pSyncToken)
{
    MaterialLibrary* pLib = pMaterialLibrary;
    ASSERT(pLib);

    char materialFileStackBuffer[ShaderByteCodeBuffer::kStackSize];

    char*      materialFileBuffer = NULL;
    uint64_t   fileSize = 0;
    FileStream stream = {};

    if (fsOpenStreamFromPath(RD_COMPILED_MATERIALS, pMaterialFileName, FM_READ, &stream))
    {
        fileSize = fsGetStreamFileSize(&stream);

        if (fileSize < sizeof(materialFileStackBuffer))
            materialFileBuffer = materialFileStackBuffer;
        else
            materialFileBuffer = (char*)tf_malloc(fileSize + 1);

        const uint64_t readSize = fsReadFromStream(&stream, materialFileBuffer, fileSize);
        ASSERT(readSize <= fileSize);
        materialFileBuffer[readSize] = '\0';

        fsCloseStream(&stream);
    }
    else
    {
        return REGISTER_MATERIAL_BADFILE;
    }

    Material* pMaterial = nullptr;
    parseMaterial(materialFileBuffer, fileSize, &pMaterial);

    if (materialFileBuffer != materialFileStackBuffer)
        tf_free(materialFileBuffer);

    materialFileBuffer = nullptr;

    ASSERT(pMaterial && pMaterial->pDesc);
    MaterialDesc* pMaterialDesc = pMaterial->pDesc;

    // Load all resources
    // (this step could be delayed in time, by extending the API we could allow the user to only load specific MaterialSets)

    ShaderByteCodeBuffer shaderByteCodeBuffer = {};
    char                 bytecodeStack[ShaderByteCodeBuffer::kStackSize] = {};
    shaderByteCodeBuffer.pStackMemory = bytecodeStack;

    SyncToken token = {};

    for (uint32_t i = 0; i < pMaterialDesc->materialCount; ++i)
    {
        Material::LoadedMaterial*        pLoadedMaterial = pMaterial->pLoaded + i;
        const MaterialDesc::MaterialSet* pMaterialSet = pMaterialDesc->pMaterialSets + i;

        // Load Shader

        ASSERT(pMaterialSet->shaderSetIdx < pMaterialDesc->shaderSetCount);
        const MaterialDesc::ShaderSet* pShaderSet = pMaterialDesc->pShaderSets + pMaterialSet->shaderSetIdx;

        // If the shader is not loaded we need to load it
        const uint32_t shaderIndex = pShaderSet->id;
        ASSERT(shaderIndex < pLib->maxMaterialShaders);

        // Check refcount because ppMaterialShaders[shaderIndex] might still be null in case the loading is still queued in another thread
        if (pLib->pMaterialShaderRefCount[shaderIndex] == 0)
        {
            ASSERT(pLib->loadedShaderCount <= pLib->maxMaterialShaders && "Seems like we loaded some shader multiple times");
            pLib->loadedShaderCount++;

            Shader** ppOutShader = &pLib->ppMaterialShaders[shaderIndex];

            BinaryShaderDesc shaderDesc = {};
            materialLoadBinaryShader(pLib->pRenderer, pMaterialDesc, pShaderSet, &shaderDesc, &shaderByteCodeBuffer);
            addShaderBinary(pLib->pRenderer, &shaderDesc, ppOutShader);
            freeShaderByteCode(&shaderByteCodeBuffer, &shaderDesc);
        }

        pLib->pMaterialShaderRefCount[shaderIndex]++;
        pLoadedMaterial->shaderIndex = shaderIndex;

        // Load Textutes

        ASSERT(pMaterialSet->textureSetIdx < pMaterialDesc->textureSetCount);
        const MaterialDesc::TextureSet* pTextureSet = pMaterialDesc->pTextureSets + pMaterialSet->textureSetIdx;

        const uint32_t textureCount = pTextureSet->textureCount;
        for (uint32_t j = 0; j < textureCount; ++j)
        {
            // Try to get the texture from the global cache
            const uint32_t textureIndex = pMaterialDesc->pTextureIds[pTextureSet->pTextureIdxs[j]];
            ASSERT(textureIndex < pLib->maxMaterialTextures);

            // Check refcount because ppMaterialTextures[textureIndex] might still be null in case the loading is still queued in another
            // thread
            if (pLib->pMaterialTextureRefCount[textureIndex] == 0)
            {
                ASSERT(pLib->loadedTextureCount < pLib->maxMaterialTextures && "Seems like we loaded some texture multiple times");
                pLib->loadedTextureCount++;

                // Texture is not in the cache yet, load it into the cache
                TextureLoadDesc desc = {};
                desc.pFileName = pMaterialDesc->pTextureNames[pTextureSet->pTextureIdxs[j]];
                desc.ppTexture = &pLib->ppMaterialTextures[textureIndex];
                if (pMaterialDesc->pTextureFlags[j] & MaterialDesc::TextureFlags::SRGB)
                    desc.creationFlag |= TEXTURE_CREATION_FLAG_SRGB;
                addResource(&desc, &token);
#ifdef TARGET_IOS
                // Wait here to prevent overloading the staging buffer on iOS.
                waitForToken(&token);
#endif
            }

            pLib->pMaterialTextureRefCount[textureIndex]++;
            pLoadedMaterial->pTextureIndexes[j] = textureIndex;
        }

        pLoadedMaterial->textureCount = textureCount;
    }

    ++pLib->loadedMaterialCount;

    if (pSyncToken)
        *pSyncToken = max(*pSyncToken, token);

    pLib->syncToken = max(pLib->syncToken, token);

    *pOutMaterial = pMaterial;
    return REGISTER_MATERIAL_SUCCESS;
}

void removeMaterial(Material* pMaterial)
{
    MaterialLibrary* pLib = pMaterialLibrary;
    ASSERT(pLib);
    ASSERT(pMaterial && pMaterial->pDesc);

    // We wait for all material related requests before we unload this material.
    // This is needed in case pMaterial resources are still queued to upload to the GPU.
    // A better approach might be to store one sync token per request (but that might me a lot of SyncTokens), to be decided in the future
    waitForToken(&pLib->syncToken);

    for (uint32_t i = 0; i < pMaterial->pDesc->materialCount; ++i)
    {
        Material::LoadedMaterial* pLoadedMaterial = pMaterial->pLoaded + i;
        const uint32_t            shaderIndex = pLoadedMaterial->shaderIndex;
        pLoadedMaterial->shaderIndex = INVALID_MATERIAL_ID;

        // Right now we are loading all material resources when we add them.
        ASSERT(shaderIndex < pLib->maxMaterialShaders);
        ASSERT(pLib->ppMaterialShaders[shaderIndex] != NULL);
        ASSERT(pLib->pMaterialShaderRefCount[shaderIndex] > 0);

        if (--pLib->pMaterialShaderRefCount[shaderIndex] == 0)
        {
            // Make sure pointer is valid, if removeMaterial is called before the ResourceLoader thread can process requests from
            // addMaterial this pointer will still be nullptr
            ASSERT(pLib->ppMaterialShaders[shaderIndex] != nullptr);

            // Last Material using this shader
            removeShader(pLib->pRenderer, pLib->ppMaterialShaders[shaderIndex]);
            pLib->ppMaterialShaders[shaderIndex] = NULL;
            ASSERT(pLib->loadedShaderCount > 0);
            pLib->loadedShaderCount--;
        }

        for (uint32_t j = 0; j < pLoadedMaterial->textureCount; ++j)
        {
            const uint32_t textureIndex = pLoadedMaterial->pTextureIndexes[j];
            pLoadedMaterial->pTextureIndexes[j] = INVALID_MATERIAL_ID;
            ASSERT(textureIndex < pLib->maxMaterialTextures);
            ASSERT(pLib->ppMaterialTextures[textureIndex] != NULL);
            ASSERT(pLib->pMaterialTextureRefCount[textureIndex] > 0);

            if (--pLib->pMaterialTextureRefCount[textureIndex] == 0)
            {
                // Make sure pointer is valid, if removeMaterial is called before the ResourceLoader thread can process requests from
                // addMaterial this pointer will still be nullptr
                ASSERT(pLib->ppMaterialTextures[textureIndex] != nullptr);

                // Last material using this texture
                removeResource(pLib->ppMaterialTextures[textureIndex]);
                pLib->ppMaterialTextures[textureIndex] = NULL;

                ASSERT(pLib->loadedTextureCount > 0);
                pLib->loadedTextureCount--;
            }
        }

        pLoadedMaterial->textureCount = 0;
    }

    --pLib->loadedMaterialCount;
    tf_free(pMaterial);
}

uint32_t getMaterialSetIndex(Material* pMaterial, const char* name)
{
    ASSERT(pMaterial && pMaterial->pDesc);
    ASSERT(pMaterial && name && name[0] != '\0');

    for (uint32_t i = 0; i < pMaterial->pDesc->materialCount; ++i)
    {
        if (strcmp(name, pMaterial->pDesc->pMaterialSetNames[i]) == 0)
            return i;
    }

    return INVALID_MATERIAL_ID;
}

void getMaterialShader(Material* pMaterial, uint32_t materialSetIndex, Shader** ppOutShader)
{
    MaterialLibrary* pLib = pMaterialLibrary;
    ASSERT(pLib);
    ASSERT(pMaterial && pMaterial->pDesc);
    ASSERT(materialSetIndex < pMaterial->pDesc->materialCount);

    (*ppOutShader) = pLib->ppMaterialShaders[pMaterial->pLoaded[materialSetIndex].shaderIndex];
}

void getMaterialTextures(Material* pMaterial, uint32_t materialSetIndex, const char** ppOutTextureBindingNames, Texture** ppOutTextures,
                         uint32_t outTexturesSize)
{
    MaterialLibrary* pLib = pMaterialLibrary;
    ASSERT(pLib);
    ASSERT(pMaterial && pMaterial->pDesc);
    ASSERT(materialSetIndex < pMaterial->pDesc->materialCount);

    const Material::LoadedMaterial* pLoadedMaterial = pMaterial->pLoaded + materialSetIndex;
    ASSERT(outTexturesSize ==
           pLoadedMaterial
               ->textureCount); // Probably should be >= but at this point we want to make sure the user knows how many textures is getting

    const MaterialDesc::MaterialSet* pMaterialSet = pMaterial->pDesc->pMaterialSets + materialSetIndex;
    const MaterialDesc::ShaderSet*   pShaderSet = pMaterial->pDesc->pShaderSets + pMaterialSet->shaderSetIdx;
    ASSERT(pShaderSet->textureBindingCount == pLoadedMaterial->textureCount);

    for (uint32_t i = 0; i < pLoadedMaterial->textureCount; ++i)
    {
        ppOutTextureBindingNames[i] = pShaderSet->pTextureBindingNames[i];

        ASSERT(pLoadedMaterial->pTextureIndexes[i] < pLib->maxMaterialTextures);
        ppOutTextures[i] = pLib->ppMaterialTextures[pLoadedMaterial->pTextureIndexes[i]];
    }
}
#endif // ENABLE_FORGE_MATERIALS

void getResourceSizeAlign(const BufferLoadDesc* pDesc, ResourceSizeAlign* pOut)
{
    BufferDesc desc = pDesc->desc;
    getBufferSizeAlign(pResourceLoader->pRenderer, &desc, pOut);
}

void getResourceSizeAlign(const TextureLoadDesc* pDesc, ResourceSizeAlign* pOut)
{
    TextureDesc desc = *pDesc->pDesc;
    getTextureSizeAlign(pResourceLoader->pRenderer, &desc, pOut);
}

void addResource(BufferLoadDesc* pBufferDesc, SyncToken* token)
{
    if (token)
    {
        *token = max<uint64_t>(0, *token);
    }

    if (pBufferDesc->pData && !token)
    {
        LOADER_LOGF(eWARNING,
                    "addResource : BufferLoadDesc(%s)::pData is non NULL but token is NULL. It is undefined behavior if pData is freed "
                    "before the buffer load has completed on the ResourceLoader thread. Use waitForAllResourceLoads before freeing pData "
                    "when explicit token was not passed",
                    pBufferDesc->desc.pName ? pBufferDesc->desc.pName : "Unnamed");
    }

    ResourceState startState = pBufferDesc->desc.startState;
    if (RESOURCE_MEMORY_USAGE_GPU_ONLY == pBufferDesc->desc.memoryUsage && !pBufferDesc->desc.startState)
    {
        startState = ResourceStartState(&pBufferDesc->desc);
        LOADER_LOGF(eWARNING, "Buffer start state not provided. Determined the start state as (%u) based on the provided BufferDesc",
                    (uint32_t)pBufferDesc->desc.startState);
    }

    Renderer*  pRenderer = pResourceLoader->pRenderer;
    const bool update = pBufferDesc->pData || pBufferDesc->forceReset;
    const bool gpuUpdate = pBufferDesc->desc.memoryUsage == RESOURCE_MEMORY_USAGE_GPU_ONLY && update && !gUma;

    if (gpuUpdate)
    {
        pBufferDesc->desc.startState = RESOURCE_STATE_COPY_DEST;
    }
    else
    {
        pBufferDesc->desc.startState = startState;
    }

    const uint64_t bufferSize = pBufferDesc->desc.size;
    addBuffer(pRenderer, &pBufferDesc->desc, pBufferDesc->ppBuffer);

    if (update)
    {
        BufferLoadDescInternal loadDesc = {};
        loadDesc.forceReset = pBufferDesc->forceReset;
        loadDesc.startState = startState;
        loadDesc.pBuffer = *pBufferDesc->ppBuffer;
        loadDesc.pData = pBufferDesc->pData;
        loadDesc.dataSize = bufferSize;
        if (gpuUpdate && RESOURCE_MEMORY_USAGE_GPU_ONLY == loadDesc.pBuffer->memoryUsage)
        {
            loadDesc.pSrcBuffer = pBufferDesc->pSrcBuffer;
            loadDesc.srcOffset = pBufferDesc->srcOffset;
        }
        else
        {
            ASSERT(!pBufferDesc->pSrcBuffer);
            loadDesc.pSrcBuffer = loadDesc.pBuffer;
            loadDesc.srcOffset = 0;
        }
        queueBufferLoad(pResourceLoader, &loadDesc, token);
    }
}

void addResource(TextureLoadDesc* pTextureDesc, SyncToken* token)
{
    ASSERT(pTextureDesc->ppTexture);

    if (token)
    {
        *token = max<uint64_t>(0, *token);
    }

    if (!pTextureDesc->pFileName && pTextureDesc->pDesc)
    {
        ASSERT(pTextureDesc->pDesc->startState);

        TextureDesc textureDesc = *pTextureDesc->pDesc;
#if defined(GFX_DRIVER_MANAGED_VIDEO_MEMORY)
        if (pTextureDesc->forceReset)
        {
            // If we are going to mem zero using staging buffer set start state to copy dest to avoid one barrier in the beginning
            textureDesc.startState = RESOURCE_STATE_COPY_DEST;
        }
#endif
        // If texture is supposed to be filled later (UAV / Update later / ...) proceed with the startState provided by the user in the
        // texture description
        addTexture(pResourceLoader->pRenderer, &textureDesc, pTextureDesc->ppTexture);

        if (pTextureDesc->forceReset)
        {
            TextureLoadDescInternal loadDesc = {};
            loadDesc.ppTexture = pTextureDesc->ppTexture;
            loadDesc.forceReset = true;
            loadDesc.startState = pTextureDesc->pDesc->startState;
            queueTextureLoad(pResourceLoader, &loadDesc, token);
            return;
        }

        if (IssueExplicitInitialStateBarrier())
        {
            ResourceState startState = pTextureDesc->pDesc->startState;
            // Check whether this is required (user specified a state other than undefined / common)
            if (startState == RESOURCE_STATE_UNDEFINED || startState == RESOURCE_STATE_COMMON) //-V560
            {
                startState = ResourceStartState(pTextureDesc->pDesc->descriptors & DESCRIPTOR_TYPE_RW_TEXTURE);
            }
            queueTextureBarrier(pResourceLoader, *pTextureDesc->ppTexture, startState, token);
        }
    }
    else
    {
        TextureLoadDescInternal loadDesc = {};
        loadDesc.ppTexture = pTextureDesc->ppTexture;
        loadDesc.container = pTextureDesc->container;
        loadDesc.flags = pTextureDesc->creationFlag;
        loadDesc.pFileName = pTextureDesc->pFileName;
        loadDesc.pYcbcrSampler = pTextureDesc->pYcbcrSampler;
        queueTextureLoad(pResourceLoader, &loadDesc, token);
    }
}

void addResource(GeometryLoadDesc* pDesc, SyncToken* token)
{
    ASSERT(pDesc->pVertexLayout);
    ASSERT(pDesc->ppGeometry);

    GeometryLoadDesc updateDesc = *pDesc;
    updateDesc.pFileName = pDesc->pFileName;

    uint32_t extraSize = sizeof(VertexLayout);

    VertexLayout* pCopyVertexLayout = (VertexLayout*)tf_malloc(extraSize);
    memcpy(pCopyVertexLayout, pDesc->pVertexLayout, sizeof(VertexLayout));
    updateDesc.pVertexLayout = pCopyVertexLayout;

    queueGeometryLoad(pResourceLoader, &updateDesc, token);
}

void removeResource(Buffer* pBuffer) { removeBuffer(pResourceLoader->pRenderer, pBuffer); }

void removeResource(Texture* pTexture) { removeTexture(pResourceLoader->pRenderer, pTexture); }

void removeResource(Geometry* pGeom)
{
    if (!pGeom)
        return;

    if (pGeom->pGeometryBuffer)
    {
        removeGeometryBufferPart(&pGeom->pGeometryBuffer->index, &pGeom->indexBufferChunk);

        for (uint32_t i = 0; i < pGeom->vertexBufferCount; ++i)
        {
            removeGeometryBufferPart(&pGeom->pGeometryBuffer->vertex[i], &pGeom->vertexBufferChunks[i]);
        }
    }
    else
    {
        // Buffers may have been transferred to another owner after upload.
        if (pGeom->pIndexBuffer)
            removeResource(pGeom->pIndexBuffer);

        for (uint32_t i = 0; i < pGeom->vertexBufferCount; ++i)
        {
            if (pGeom->pVertexBuffers[i])
                removeResource(pGeom->pVertexBuffers[i]);
        }
    }

    tf_free(pGeom->meshlets.meshlets);
    tf_free(pGeom);
}

void removeResource(GeometryData* pGeom)
{
    removeGeometryShadowData(pGeom);
    tf_free(pGeom);
}

void removeGeometryShadowData(GeometryData* pGeom)
{
    if (pGeom->pShadow)
    {
        tf_free(pGeom->pShadow);
        pGeom->pShadow = nullptr;
    }
}

// Interface to add/remove BufferChunkAllocators is currently private but we could expose it in the IResourceLoader interface if needed
struct BufferChunkAllocatorDesc
{
    Buffer* pBuffer;
};

static void addBufferChunkAllocator(BufferChunkAllocatorDesc* pDesc, BufferChunkAllocator* pOut)
{
    ASSERT(pDesc);
    ASSERT(pOut);

    pOut->pBuffer = pDesc->pBuffer;
    pOut->size = (uint32_t)pDesc->pBuffer->size;

    BufferChunk firstUnusedChunk{ 0, (uint32_t)pDesc->pBuffer->size };
    arrpush(pOut->unusedChunks, firstUnusedChunk);
}

static void removeBufferChunkAllocator(BufferChunkAllocator* pBuffer)
{
    ASSERT(pBuffer);
    ASSERT(pBuffer->usedChunkCount == 0 && "Expecting all parts to be released at this point");

    if (pBuffer->pBuffer)
    {
        ASSERT(arrlen(pBuffer->unusedChunks) == 1 && "Expecting just one chunk since the buffer is completely empty");

        // We are checking that the unnused chunk offset is 0 because we currently assume that a BufferChunkAllocator covers the entire
        // buffer, but we could change this to allow to have several BufferChunkAllocators over the same buffer, each working on a fixed
        // memory range of the buffer.
        //
        // For example: In buffer below we could do the following splits
        // Buffer: [------------------------------------------------------]
        // Splits: [------------------A-------------------|--------B------]
        //
        // One BufferChunkAllocator would cover memory range A and would be use by the App to store big chunks of data while other
        // BufferChunkAllocator would cover memory range B and be used to fit smaller ammounts of data.
        //
        // Note: If we want this behavior we would need extend BufferChunkAllocatorDesc to provide offset and size that the
        // BufferChunkAllocator would cover,
        //       if size is 0 we would use the size of the buffer.
        //       We would also need to consider if we want to expose the add/removeBufferChunkAllocator interface to the user and let him
        //       allocate the BufferChunkAllocator or we want to include this splitting logic in addGeometryBuffer.
        ASSERT(pBuffer->unusedChunks && (pBuffer->unusedChunks[0].offset == 0) && (pBuffer->unusedChunks[0].size == pBuffer->size) &&
               "Expecting just one chunk since the buffer is completely empty");

        arrfree(pBuffer->unusedChunks);
    }
}

void addGeometryBuffer(GeometryBufferLoadDesc* pDesc)
{
    DescriptorType flags = DESCRIPTOR_TYPE_BUFFER_RAW | DESCRIPTOR_TYPE_RW_BUFFER_RAW;

    GeometryBuffer* pBuffer = (GeometryBuffer*)tf_calloc(1, sizeof *pBuffer);

    if (!VERIFYMSG(pBuffer, "Couldn't allocate GeometryBuffer"))
    {
        return;
    }

    *pDesc->pOutGeometryBuffer = pBuffer;

    BufferLoadDesc loadDesc = {};

    Buffer* pIndexBuffer = NULL;
    Buffer* pVertexBuffer = NULL;

    loadDesc.desc.memoryUsage = RESOURCE_MEMORY_USAGE_GPU_ONLY;
    loadDesc.desc.size = pDesc->indicesSize;
    loadDesc.ppBuffer = &pIndexBuffer;
    loadDesc.desc.descriptors = DESCRIPTOR_TYPE_INDEX_BUFFER | flags;
    loadDesc.desc.structStride = sizeof(uint32_t);
    loadDesc.desc.elementCount = (uint32_t)(loadDesc.desc.size / loadDesc.desc.structStride);
    loadDesc.desc.startState = gUma ? gIndexBufferState : pDesc->startState;
    loadDesc.desc.pName = pDesc->pNameIndexBuffer ? pDesc->pNameIndexBuffer : "GeometryBuffer Indices (unnamed)";
    loadDesc.desc.pPlacement = pDesc->pIndicesPlacement;
    addResource(&loadDesc, nullptr);

    BufferChunkAllocatorDesc allocDesc = { pIndexBuffer };
    addBufferChunkAllocator(&allocDesc, &pBuffer->index);

    for (size_t i = 0; i < TF_ARRAY_COUNT(pDesc->verticesSizes); ++i)
    {
        if (!pDesc->verticesSizes[i])
            continue;

        loadDesc.desc.size = pDesc->verticesSizes[i];
        loadDesc.ppBuffer = &pVertexBuffer;
        loadDesc.desc.descriptors = DESCRIPTOR_TYPE_VERTEX_BUFFER | flags;
        loadDesc.desc.structStride = sizeof(uint32_t);
        loadDesc.desc.elementCount = (uint32_t)(loadDesc.desc.size / loadDesc.desc.structStride);
        loadDesc.desc.startState = gUma ? gVertexBufferState : pDesc->startState;
        loadDesc.desc.pName = pDesc->pNamesVertexBuffers[i] ? pDesc->pNamesVertexBuffers[i] : "GeometryBuffer Vertices (unnamed)";
        loadDesc.desc.pPlacement = pDesc->pVerticesPlacements[i];
        addResource(&loadDesc, nullptr);

        allocDesc = { pVertexBuffer };
        addBufferChunkAllocator(&allocDesc, &pBuffer->vertex[i]);
    }
}

void removeGeometryBuffer(GeometryBuffer* pGeomBuffer)
{
    if (!pGeomBuffer)
        return;

    removeBufferChunkAllocator(&pGeomBuffer->index);
    if (pGeomBuffer->index.pBuffer)
        removeResource(pGeomBuffer->index.pBuffer);

    for (size_t i = 0; i < TF_ARRAY_COUNT(pGeomBuffer->vertex); ++i)
    {
        removeBufferChunkAllocator(&pGeomBuffer->vertex[i]);
        if (pGeomBuffer->vertex[i].pBuffer)
            removeResource(pGeomBuffer->vertex[i].pBuffer);
    }

    tf_free(pGeomBuffer);
}

void addGeometryBufferPart(BufferChunkAllocator* pBuffer, uint32_t size, uint32_t alignment, BufferChunk* pOut,
                           BufferChunk* pRequestedChunk)
{
    if (size == 0)
        return;
    if (size > pBuffer->size)
    {
        *pOut = {};
        ASSERT(false);
        return;
    }

    if (pRequestedChunk)
    {
        ASSERT(pRequestedChunk->offset + pRequestedChunk->size <= pBuffer->size);

        // Try to allocate the requested slot
        for (uint32_t i = 0; i < arrlenu(pBuffer->unusedChunks); ++i)
        {
            BufferChunk* chunk = &pBuffer->unusedChunks[i];

            const uint32_t chunkEnd = chunk->offset + chunk->size;
            const uint32_t requestedEnd = pRequestedChunk->offset + pRequestedChunk->size;
            if (chunk->offset <= pRequestedChunk->offset && chunkEnd >= requestedEnd)
            {
                ++pBuffer->usedChunkCount;

                *pOut = *pRequestedChunk;

                if (chunk->offset == pRequestedChunk->offset && chunkEnd == requestedEnd)
                {
                    // Exact chunk
                    arrdel(pBuffer->unusedChunks, i);
                    return;
                }

                // There's unnused memory before the requested chunk
                if (chunk->offset < pRequestedChunk->offset)
                {
                    BufferChunk prevChunk = { chunk->offset, pRequestedChunk->offset - chunk->offset };
                    arrins(pBuffer->unusedChunks, i, prevChunk);
                    i++;
                    chunk = &pBuffer->unusedChunks[i];
                }

                if (chunkEnd == requestedEnd)
                {
                    // We consummed the full chunk
                    arrdel(pBuffer->unusedChunks, i);
                }
                else
                {
                    // There's unnused memory after the requested chunk
                    chunk->size = chunkEnd - requestedEnd;
                    chunk->offset = pRequestedChunk->offset + pRequestedChunk->size;
                    ASSERT(chunk->size > 0);
                }

                return;
            }
        }

        ASSERT(false && "Failed to allocate the requested chunk");
        return;
    }

    // TODO binary search. (unused chunk array is sorted already)
    for (uint32_t i = 0; i < arrlenu(pBuffer->unusedChunks); ++i)
    {
        BufferChunk* chunk = &pBuffer->unusedChunks[i];

        if (chunk->size < size)
            continue;

        if (alignment > 0)
        {
            uint32_t padding = chunk->offset % alignment;
            if (padding > 0)
                padding = alignment - padding;

            if (chunk->size - padding < size)
                continue;

            if (padding > 0)
            {
                BufferChunk paddingChunk = {
                    chunk->offset,
                    padding,
                };

                arrins(pBuffer->unusedChunks, i, paddingChunk);
                i++;

                chunk = &pBuffer->unusedChunks[i];
                chunk->offset += padding;
                chunk->size -= padding;
            }
        }

        pOut->offset = chunk->offset;
        pOut->size = size;

        chunk->offset += size;
        chunk->size -= size;

        if (chunk->size == 0)
        {
            arrdel(pBuffer->unusedChunks, i);
        }

        ++pBuffer->usedChunkCount;
        return;
    }

    *pOut = {};
    ASSERT(false);
}

void removeGeometryBufferPart(BufferChunkAllocator* pBuffer, BufferChunk* pChunk)
{
    ASSERT(pChunk->size ? pBuffer != NULL : true);
    if (!pBuffer || pChunk->size == 0)
        return;

    ASSERT(pBuffer->usedChunkCount);

    --pBuffer->usedChunkCount;

    const uint64_t partEnd = pChunk->offset + pChunk->size;

    // TODO binary search. (unused chunk array is sorted already)
    uint64_t i = 0;
    for (; i < arrlenu(pBuffer->unusedChunks); ++i)
    {
        BufferChunk* chunk = &pBuffer->unusedChunks[i];
        if (partEnd < chunk->offset)
            break;

        auto chunkEnd = chunk->offset + chunk->size;
        if (chunkEnd == pChunk->offset) // if pChunk goes after chunk, merge both
        {
            chunk->size += pChunk->size;
            if (i + 1 < arrlenu(pBuffer->unusedChunks)) // If there's another empty chunk after pChunk, merge that one too
            {
                if (partEnd == pBuffer->unusedChunks[i + 1].offset)
                {
                    chunk->size += pBuffer->unusedChunks[i + 1].size;
                    arrdel(pBuffer->unusedChunks, i + 1);
                }
            }
            return;
        }

        if (partEnd == chunk->offset) // If pChunk goes after chunk, merge both
        {
            chunk->offset = pChunk->offset;
            chunk->size += pChunk->size;
            return;
        }
    }

    arrins(pBuffer->unusedChunks, i, *pChunk);
}

void beginUpdateResource(BufferUpdateDesc* pBufferUpdate)
{
    Buffer*   pBuffer = pBufferUpdate->pBuffer;
    Renderer* pRenderer = pResourceLoader->pRenderer;
    ASSERT(pBuffer);

    uint64_t size = pBufferUpdate->size > 0 ? pBufferUpdate->size : (pBufferUpdate->pBuffer->size - pBufferUpdate->dstOffset);
    ASSERT(pBufferUpdate->dstOffset + size <= pBuffer->size);

    ResourceMemoryUsage memoryUsage = (ResourceMemoryUsage)pBufferUpdate->pBuffer->memoryUsage;
    if (gUma || memoryUsage != RESOURCE_MEMORY_USAGE_GPU_ONLY)
    {
        ASSERTMSG(!pBufferUpdate->pSrcBuffer, "No point in staging buffer when we are directly writing into dst buffer. "
                                              "If this is not a GPU_ONLY buffer you can use use isUma() to handle this case, no need to "
                                              "create this staging buffer on the App side.");
        bool map = !pBuffer->pCpuMappedAddress;
        if (map)
        {
            mapBuffer(pRenderer, pBuffer, NULL);
        }

        pBufferUpdate->internal.mappedRange = { (uint8_t*)pBuffer->pCpuMappedAddress + pBufferUpdate->dstOffset, pBuffer };
        pBufferUpdate->pMappedData = pBufferUpdate->internal.mappedRange.pData;
        pBufferUpdate->internal.mappedRange.flags = map ? MAPPED_RANGE_FLAG_UNMAP_BUFFER : 0;
    }
    else
    {
        // Staging buffer provided by user
        if (pBufferUpdate->pSrcBuffer)
        {
            pBufferUpdate->internal.mappedRange.pBuffer = pBufferUpdate->pSrcBuffer;
            pBufferUpdate->internal.mappedRange.offset = pBufferUpdate->srcOffset;
            pBufferUpdate->internal.mappedRange.size = size;
            pBufferUpdate->internal.mappedRange.pData =
                (uint8_t*)pBufferUpdate->pSrcBuffer->pCpuMappedAddress + pBufferUpdate->srcOffset;
            pBufferUpdate->pMappedData = pBufferUpdate->internal.mappedRange.pData;
            return;
        }

        MutexLock         lock(pResourceLoader->uploadEngineMutex);
        CopyEngine*       pCopyEngine = &pResourceLoader->uploadEngine;
        MappedMemoryRange range = allocateStagingMemory(pCopyEngine, size, RESOURCE_BUFFER_ALIGNMENT);
        if (!range.pData)
        {
            range = allocateUploadMemory(pRenderer, size, RESOURCE_BUFFER_ALIGNMENT);
            arrpush(pCopyEngine->resourceSets[pCopyEngine->activeSet].tempBuffers, range.pBuffer);
        }

        pBufferUpdate->pMappedData = range.pData;
        pBufferUpdate->internal.mappedRange = range;
    }
}

void endUpdateResource(BufferUpdateDesc* pBufferUpdate)
{
    if (pBufferUpdate->internal.mappedRange.flags & MAPPED_RANGE_FLAG_UNMAP_BUFFER)
    {
        unmapBuffer(pResourceLoader->pRenderer, pBufferUpdate->pBuffer);
    }

    ResourceMemoryUsage memoryUsage = (ResourceMemoryUsage)pBufferUpdate->pBuffer->memoryUsage;
    if (!gUma && memoryUsage == RESOURCE_MEMORY_USAGE_GPU_ONLY)
    {
        MutexLock   lock(pResourceLoader->uploadEngineMutex);
        CopyEngine* pCopyEngine = &pResourceLoader->uploadEngine;
        updateBuffer(pResourceLoader->pRenderer, pCopyEngine, *pBufferUpdate);
    }

    // Restore the state to before the beginUpdateResource call.
    pBufferUpdate->pMappedData = NULL;
    pBufferUpdate->internal = {};
}

TextureSubresourceUpdate TextureUpdateDesc::getSubresourceUpdateDesc(uint32_t mip, uint32_t layer)
{
    TextureSubresourceUpdate ret = {};
    Texture*                 texture = pTexture;
    const TinyImageFormat    fmt = (TinyImageFormat)texture->format;
    Renderer*                pRenderer = pResourceLoader->pRenderer;
    const uint32_t           sliceAlignment = util_get_texture_subresource_alignment(pRenderer, fmt);

    bool success = util_get_surface_info(MIP_REDUCE(texture->width, mip), MIP_REDUCE(texture->height, mip), fmt, &ret.srcSliceStride,
                                         &ret.srcRowStride, &ret.rowCount);
    ASSERT(success);
    UNREF_PARAM(success);

    ret.dstRowStride = round_up(ret.srcRowStride, util_get_texture_row_alignment(pRenderer));
    ret.dstSliceStride = round_up(ret.dstRowStride * ret.rowCount, sliceAlignment);
    ret.pMappedData = internal.mappedRange.pData + (internal.dstSliceStride * (layer - baseArrayLayer));
    // Calculate the offset for the mip in this array layer
    for (uint32_t i = baseMipLevel; i < mip; ++i)
    {
        uint32_t srcSliceStride = 0;
        uint32_t srcRowStride = 0;
        uint32_t rowCount = 0;
        success = util_get_surface_info(MIP_REDUCE(texture->width, i), MIP_REDUCE(texture->height, i), fmt, &srcSliceStride,
                                        &srcRowStride, &rowCount);
        ASSERT(success);
        uint32_t d = MIP_REDUCE(texture->depth, i);

        uint32_t dstRowStride = round_up(srcRowStride, util_get_texture_row_alignment(pRenderer));
        uint32_t dstSliceStride = round_up(dstRowStride * rowCount, sliceAlignment);
        ret.pMappedData += (dstSliceStride * d);
    }

    return ret;
}

void beginUpdateResource(TextureUpdateDesc* pTextureUpdate)
{
    const Texture*        texture = pTextureUpdate->pTexture;
    const TinyImageFormat fmt = (TinyImageFormat)texture->format;
    Renderer*             pRenderer = pResourceLoader->pRenderer;
    const uint32_t        sliceAlignment = util_get_texture_subresource_alignment(pRenderer, fmt);
    pTextureUpdate->mipLevels = max(1u, pTextureUpdate->mipLevels);
    pTextureUpdate->layerCount = max(1u, pTextureUpdate->layerCount);

    const uint32_t rowAlignment = util_get_texture_row_alignment(pRenderer);
    const uint64_t requiredSize = util_get_surface_size(fmt, texture->width, texture->height, texture->depth, rowAlignment,
                                                        sliceAlignment, pTextureUpdate->baseMipLevel, pTextureUpdate->mipLevels,
                                                        pTextureUpdate->baseArrayLayer, pTextureUpdate->layerCount);

    // We need to use a staging buffer.
    MutexLock         lock(pResourceLoader->uploadEngineMutex);
    CopyEngine*       pCopyEngine = &pResourceLoader->uploadEngine;
    MappedMemoryRange range = allocateStagingMemory(pCopyEngine, requiredSize, sliceAlignment);
    if (!range.pData)
    {
        range = allocateUploadMemory(pRenderer, requiredSize, sliceAlignment);
        arrpush(pCopyEngine->resourceSets[pCopyEngine->activeSet].tempBuffers, range.pBuffer);
    }

    pTextureUpdate->internal = {};
    pTextureUpdate->internal.mappedRange = range;

    // Pre-calculate stride for the mip chain. Will be used in getSubresourceUpdateDesc
    for (uint32_t mip = pTextureUpdate->baseMipLevel; mip < pTextureUpdate->mipLevels; ++mip)
    {
        uint32_t srcSliceStride = 0;
        uint32_t srcRowStride = 0;
        uint32_t rowCount = 0;
        bool     success = util_get_surface_info(MIP_REDUCE(texture->width, mip), MIP_REDUCE(texture->height, mip), fmt, &srcSliceStride,
                                             &srcRowStride, &rowCount);
        ASSERT(success);
        uint32_t d = MIP_REDUCE(texture->depth, mip);

        uint32_t dstRowStride = round_up(srcRowStride, util_get_texture_row_alignment(pRenderer));
        uint32_t dstSliceStride = round_up(dstRowStride * rowCount, sliceAlignment);
        pTextureUpdate->internal.dstSliceStride += (dstSliceStride * d);
    }
}

void endUpdateResource(TextureUpdateDesc* pTextureUpdate)
{
    TextureUpdateDescInternal desc = {};
    desc.pTexture = pTextureUpdate->pTexture;
    desc.range = pTextureUpdate->internal.mappedRange;
    desc.pCmd = pTextureUpdate->pCmd;
    desc.baseMipLevel = pTextureUpdate->baseMipLevel;
    desc.mipLevels = pTextureUpdate->mipLevels;
    desc.baseArrayLayer = pTextureUpdate->baseArrayLayer;
    desc.layerCount = pTextureUpdate->layerCount;
    desc.currentState = pTextureUpdate->currentState;
    MutexLock   lock(pResourceLoader->uploadEngineMutex);
    CopyEngine* pCopyEngine = &pResourceLoader->uploadEngine;
    updateTexture(pResourceLoader->pRenderer, pCopyEngine, desc);

    // Restore the state to before the beginUpdateResource call.
    pTextureUpdate->internal = {};
}

void copyResource(TextureCopyDesc* pTextureDesc, SyncToken* token) { queueTextureCopy(pResourceLoader, pTextureDesc, token); }

void flushResourceUpdates(FlushResourceUpdateDesc* pDesc)
{
    MutexLock lock(pResourceLoader->uploadEngineMutex);

    static FlushResourceUpdateDesc dummyDesc = {};
    FlushResourceUpdateDesc&       desc = pDesc ? *pDesc : dummyDesc;
    CopyEngine*                    pCopyEngine = &pResourceLoader->uploadEngine;
    const uint32_t                 activeSet = pCopyEngine->activeSet;

    desc.pOutFence = pCopyEngine->resourceSets[activeSet].pFence;
    desc.pOutSubmittedSemaphore = pCopyEngine->resourceSets[activeSet].pSemaphore;

    if (!pCopyEngine->isRecording)
    {
        return;
    }
    for (uint32_t i = 0; i < desc.waitSemaphoreCount; ++i)
    {
        arrpush(pCopyEngine->waitSemaphores, desc.ppWaitSemaphores[i]);
    }
    streamerFlush(pCopyEngine);
    pCopyEngine->activeSet = (activeSet + 1) % pCopyEngine->bufferCount;
}

SyncToken getLastTokenCompleted() { return tfrg_atomic64_load_acquire(&pResourceLoader->tokenCompleted); }

bool isTokenCompleted(const SyncToken* token) { return *token <= tfrg_atomic64_load_acquire(&pResourceLoader->tokenCompleted); }

void waitForToken(const SyncToken* token) { waitForToken(pResourceLoader, token); }

SyncToken getLastTokenSubmitted() { return tfrg_atomic64_load_acquire(&pResourceLoader->tokenSubmitted); }

bool isTokenSubmitted(const SyncToken* token) { return *token <= tfrg_atomic64_load_acquire(&pResourceLoader->tokenSubmitted); }

void waitForTokenSubmitted(const SyncToken* token) { waitForTokenSubmitted(pResourceLoader, token); }

bool allResourceLoadsCompleted()
{
    SyncToken token = tfrg_atomic64_load_relaxed(&pResourceLoader->tokenCounter);
    return token <= tfrg_atomic64_load_acquire(&pResourceLoader->tokenCompleted);
}

void waitForAllResourceLoads()
{
    SyncToken token = tfrg_atomic64_load_relaxed(&pResourceLoader->tokenCounter);
    waitForToken(pResourceLoader, &token);
}

bool isResourceLoaderSingleThreaded()
{
    ASSERT(pResourceLoader);
    return pResourceLoader->desc.singleThreaded;
}

Semaphore* getLastSemaphoreSubmitted(uint32_t nodeIndex)
{
    UNREF_PARAM(nodeIndex);
    acquireMutex(&pResourceLoader->semaphoreMutex);
    Semaphore* sem = pResourceLoader->copyEngine.pLastSubmittedSemaphore;
    releaseMutex(&pResourceLoader->semaphoreMutex);
    return sem;
}

/************************************************************************/
// Shader loading
/************************************************************************/
static bool load_shader_stage_byte_code(Renderer* pRenderer, const char* name, ShaderStage stage, BinaryShaderStageDesc* pOut,
                                        ShaderByteCodeBuffer* pShaderByteCodeBuffer, FSLMetadata* pOutMetadata)
{
    UNREF_PARAM(pRenderer);
    UNREF_PARAM(stage);
    char binaryShaderPath[FS_MAX_PATH];

    {
        const char* rendererApi = getShaderPlatformName();

        int length = 0;
        if (rendererApi[0])
        {
            length = snprintf(binaryShaderPath, sizeof binaryShaderPath, "%s/%s", rendererApi, name);
        }
        else
        {
            length = snprintf(binaryShaderPath, sizeof binaryShaderPath, "%s", name);
        }

        if (length >= FS_MAX_PATH)
        {
            LOGF(eERROR, "Shader name is too long: '%s'", name);
            return false;
        }
    }

    FileStream binaryFileStream = {};

    // NOTE: On some platforms, we might not be allowed to write in the `RD_SHADER_BINARIES` directory.
    // If we want to load re-compiled binaries, then they must be cached elsewhere and queried here.

    const bool result = fsOpenStreamFromPath(RD_SHADER_BINARIES, binaryShaderPath, FM_READ, &binaryFileStream);
    ASSERT(result);
    if (!result)
        return result;

    ssize_t size = fsGetStreamFileSize(&binaryFileStream);
    ASSERT(size > 0);

    FSLHeader header = {};
    if (sizeof(FSLHeader) != fsReadFromStream(&binaryFileStream, (void*)&header, sizeof(FSLHeader)))
        ASSERT(false);

    if (strncmp("@FSL", header.magic, 4) != 0)
    {
        // Shader was not compiled using FSL script
        fsSeekStream(&binaryFileStream, SBO_START_OF_FILE, 0);
        pOut->pByteCode = allocShaderByteCode(pShaderByteCodeBuffer, 256, (uint32_t)size, binaryShaderPath);
        pOut->byteCodeSize = (uint32_t)size;
        fsReadFromStream(&binaryFileStream, (void*)pOut->pByteCode, size);
    }
    else
    {
        ASSERT(strncmp("@FSL", header.magic, 4) == 0);
        const size_t   derivativesSize = sizeof(FSLDerivative) * header.derivativeCount;
        FSLDerivative* pDerivatives = (FSLDerivative*)alloca(derivativesSize);
        if (derivativesSize != fsReadFromStream(&binaryFileStream, (void*)pDerivatives, derivativesSize))
            ASSERT(false);

        if (pOutMetadata)
            *pOutMetadata = header.metadata;

        for (uint32_t i = 0; i < header.derivativeCount; ++i)
        {
            if (header.derivativeCount == 1 || pDerivatives[i].hash == 0)
            {
                if (!fsSeekStream(&binaryFileStream, SBO_START_OF_FILE, pDerivatives[i].offset))
                {
                    LOGF(eERROR, "Failed to read file '%s'", binaryShaderPath);
                    break;
                }

                size = pDerivatives[i].size;
                pOut->pByteCode = allocShaderByteCode(pShaderByteCodeBuffer, 256, (uint32_t)size, binaryShaderPath);
                pOut->byteCodeSize = (uint32_t)pDerivatives[i].size;
                if (fsReadFromStream(&binaryFileStream, (void*)pOut->pByteCode, size) != (size_t)size)
                {
                    LOGF(eERROR, "Failed to read file '%s'", binaryShaderPath);
                }
                break;
            }
        }

        ASSERT(pOut->pByteCode);
    }

    fsCloseStream(&binaryFileStream);
    return true;
}

const char* getShaderPlatformName()
{
    ASSERT(gPlatformParameters.selectedRendererApi == RENDERER_API_D3D12);
    return "DIRECT3D12";
}

static bool find_shader_stage(const char* extension, BinaryShaderDesc* pBinaryDesc, BinaryShaderStageDesc** pOutStage, ShaderStage* pStage)
{
    if (stricmp(extension, "vert") == 0)
    {
        *pOutStage = &pBinaryDesc->vert;
        *pStage = SHADER_STAGE_VERT;
    }
    else if (stricmp(extension, "frag") == 0)
    {
        *pOutStage = &pBinaryDesc->frag;
        *pStage = SHADER_STAGE_FRAG;
    }
    else if (stricmp(extension, "tesc") == 0)
    {
        *pOutStage = &pBinaryDesc->hull;
        *pStage = SHADER_STAGE_HULL;
    }
    else if (stricmp(extension, "tese") == 0)
    {
        *pOutStage = &pBinaryDesc->domain;
        *pStage = SHADER_STAGE_DOMN;
    }
    else if (stricmp(extension, "geom") == 0)
    {
        *pOutStage = &pBinaryDesc->geom;
        *pStage = SHADER_STAGE_GEOM;
    }
    else if (stricmp(extension, "comp") == 0)
    {
        *pOutStage = &pBinaryDesc->comp;
        *pStage = SHADER_STAGE_COMP;
    }
    else
    {
        return false;
    }

    return true;
}

static BinaryShaderStageDesc* get_shader_stage_desc(BinaryShaderDesc* pBinaryDesc, ShaderStage stage)
{
    switch (stage)
    {
    case SHADER_STAGE_VERT:
        return &pBinaryDesc->vert;
    case SHADER_STAGE_FRAG:
        return &pBinaryDesc->frag;
    case SHADER_STAGE_GEOM:
        return &pBinaryDesc->geom;
    case SHADER_STAGE_HULL:
        return &pBinaryDesc->hull;
    case SHADER_STAGE_DOMN:
        return &pBinaryDesc->domain;
    case SHADER_STAGE_COMP:
        return &pBinaryDesc->comp;
    default:
        return nullptr;
    }
}

void addShader(Renderer* pRenderer, const ShaderLoadDesc* pDesc, Shader** ppShader)
{
    BinaryShaderDesc binaryDesc = {};

    ShaderByteCodeBuffer shaderByteCodeBuffer = {};

    ShaderStage stages = SHADER_STAGE_NONE;
    for (uint32_t i = 0; i < SHADER_STAGE_COUNT; ++i)
    {
        if (pDesc->stages[i].pFileName && pDesc->stages[i].pFileName[0] != 0)
        {
            ShaderStage            stage;
            BinaryShaderStageDesc* pStage = nullptr;
            char                   ext[FS_MAX_PATH] = { 0 };
            fsGetPathExtension(pDesc->stages[i].pFileName, ext);
            if (find_shader_stage(ext, &binaryDesc, &pStage, &stage))
                stages |= stage;
        }
    }
    for (uint32_t i = 0; i < SHADER_STAGE_COUNT; ++i)
    {
        const char* fileName = pDesc->stages[i].pFileName;
        if (!fileName || !*fileName)
            continue;

        ShaderStage            stage;
        BinaryShaderStageDesc* pStage = nullptr;
        {
            char ext[FS_MAX_PATH];
            fsGetPathExtension(fileName, ext);
            if (!find_shader_stage(ext, &binaryDesc, &pStage, &stage))
                continue;
        }

        FSLMetadata metadata = {};
        if (!load_shader_stage_byte_code(pRenderer, fileName, stage, pStage, &shaderByteCodeBuffer, &metadata))
        {
            freeShaderByteCode(&shaderByteCodeBuffer, &binaryDesc);
            return;
        }

        binaryDesc.stages |= stage;
        pStage->pName = fileName;
#if defined(QUEST_VR)
        binaryDesc.isMultiviewVR |= metadata.useMultiView;
#endif
        if (pDesc->stages[i].pEntryPointName)
            pStage->pEntryPoint = pDesc->stages[i].pEntryPointName;
        else
            pStage->pEntryPoint = "main";
    }

    binaryDesc.constantCount = pDesc->constantCount;
    binaryDesc.pConstants = pDesc->pConstants;

    addShaderBinary(pRenderer, &binaryDesc, ppShader);
    freeShaderByteCode(&shaderByteCodeBuffer, &binaryDesc);

    Shader* pShader = *ppShader;

    if (SHADER_STAGE_COMP == binaryDesc.stages)
    {
        pShader->numThreadsPerGroup[0] = pShader->pReflection->stageReflections[0].numThreadsPerGroup[0];
        pShader->numThreadsPerGroup[1] = pShader->pReflection->stageReflections[0].numThreadsPerGroup[1];
        pShader->numThreadsPerGroup[2] = pShader->pReflection->stageReflections[0].numThreadsPerGroup[2];
    }
}

void addShaderSrc(Renderer* pRenderer, const ShaderLoadDesc* pDesc, Shader** ppShader)
{
    BinaryShaderDesc binaryDesc = {};

    ShaderByteCodeBuffer shaderByteCodeBuffer = {};

    ShaderStage stages = SHADER_STAGE_NONE;
    for (uint32_t i = 0; i < SHADER_STAGE_COUNT; ++i)
    {
        if (pDesc->stages[i].pFileName && pDesc->stages[i].pFileName[0] != 0)
        {
            // ShaderStage            stage;
            // BinaryShaderStageDesc* pStage = NULL;
            // char                   ext[FS_MAX_PATH] = { 0 };
            // fsGetPathExtension(pDesc->stages[i].pFileName, ext);
            // if (find_shader_stage(ext, &binaryDesc, &pStage, &stage))
            stages |= pDesc->stages[i].stage;
        }
    }
    for (uint32_t i = 0; i < SHADER_STAGE_COUNT; ++i)
    {
        const char* fileName = pDesc->stages[i].pFileName;
        if (!fileName || !*fileName)
            continue;

        ShaderStage            stage = pDesc->stages[i].stage;
        BinaryShaderStageDesc* pStage = get_shader_stage_desc(&binaryDesc, stage);

        if (stage == ShaderStage::SHADER_STAGE_NONE || !pStage)
            continue;

        FSLMetadata metadata = {};
        if (!load_shader_stage_byte_code(pRenderer, fileName, stage, pStage, &shaderByteCodeBuffer, &metadata))
        {
            freeShaderByteCode(&shaderByteCodeBuffer, &binaryDesc);
            return;
        }

        binaryDesc.stages |= stage;
        pStage->pName = fileName;
        if (pDesc->stages[i].pEntryPointName)
            pStage->pEntryPoint = pDesc->stages[i].pEntryPointName;
        else
            pStage->pEntryPoint = "main";
    }

    binaryDesc.constantCount = pDesc->constantCount;
    binaryDesc.pConstants = pDesc->pConstants;

    addShaderBinary(pRenderer, &binaryDesc, ppShader);
    freeShaderByteCode(&shaderByteCodeBuffer, &binaryDesc);

    Shader* pShader = *ppShader;

    if (SHADER_STAGE_COMP == binaryDesc.stages)
    {
        pShader->numThreadsPerGroup[0] = pShader->pReflection->stageReflections[0].numThreadsPerGroup[0];
        pShader->numThreadsPerGroup[1] = pShader->pReflection->stageReflections[0].numThreadsPerGroup[1];
        pShader->numThreadsPerGroup[2] = pShader->pReflection->stageReflections[0].numThreadsPerGroup[2];
    }
}

/************************************************************************/
// Pipeline cache save, load
/************************************************************************/
void loadPipelineCache(Renderer* pRenderer, const PipelineCacheLoadDesc* pDesc, PipelineCache** ppPipelineCache)
{
#if defined(DIRECT3D12)

    char rendererApi[FS_MAX_PATH] = "DIRECT3D12/";

    ASSERT(strlen(rendererApi) + strlen(pDesc->pFileName) < sizeof(rendererApi));
    strcat(rendererApi, pDesc->pFileName);

    FileStream stream = {};
    bool       success = fsOpenStreamFromPath(RD_PIPELINE_CACHE, rendererApi, FM_READ, &stream);
    ssize_t    dataSize = 0;
    void*      data = nullptr;
    if (success)
    {
        dataSize = fsGetStreamFileSize(&stream);
        data = nullptr;
        if (dataSize)
        {
            data = tf_malloc(dataSize);
            fsReadFromStream(&stream, data, dataSize);
        }

        fsCloseStream(&stream);
    }

    PipelineCacheDesc desc = {};
    desc.flags = pDesc->flags;
    desc.pData = data;
    desc.size = dataSize;
    addPipelineCache(pRenderer, &desc, ppPipelineCache);

    if (data)
    {
        tf_free(data);
    }
#endif
}

void savePipelineCache(Renderer* pRenderer, PipelineCache* pPipelineCache, PipelineCacheSaveDesc* pDesc)
{
#if defined(DIRECT3D12)

    char rendererApi[FS_MAX_PATH] = "DIRECT3D12/";

    ASSERT(strlen(rendererApi) + strlen(pDesc->pFileName) < sizeof(rendererApi));
    strcat(rendererApi, pDesc->pFileName);

    FileStream stream = {};
    if (fsOpenStreamFromPath(RD_PIPELINE_CACHE, rendererApi, FM_WRITE, &stream))
    {
        size_t dataSize = 0;
        getPipelineCacheData(pRenderer, pPipelineCache, &dataSize, NULL);
        if (dataSize)
        {
            void* data = tf_malloc(dataSize);
            getPipelineCacheData(pRenderer, pPipelineCache, &dataSize, data);
            fsWriteToStream(&stream, data, dataSize);
            tf_free(data);
        }

        fsCloseStream(&stream);
    }
#endif
}
/************************************************************************/
/************************************************************************/

void waitCopyQueueIdle() { waitQueueIdle(pResourceLoader->copyEngine.pQueue); }
