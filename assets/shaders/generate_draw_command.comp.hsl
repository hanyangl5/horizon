struct IndirectDrawCommand {
    uint index_count;
    uint instance_count;
    uint first_index;
    uint vertex_offset;
    uint first_instance;
};

RES(RWBuffer(IndirectDrawCommand), draw_command, UPDATE_FREQ_PER_FRAME, u1, binding = 1);

CBUFFER(exposure_constants, UPDATE_FREQ_PER_FRAME, b0, binding = 2)
{
    DATA(float4, exposure_ev100__, None);
};

NUM_THREADS(8, 8, 1)
void CS_MAIN( SV_DispatchThreadID(uint3) threadID) 
{
    INIT_MAIN;
    
    RETURN();
}

