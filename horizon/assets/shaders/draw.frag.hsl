STRUCT(VSOutput)
{
    DATA(float4, position, SV_Position);
    DATA(float4, normal, NORMAL);
};

STRUCT(PSOutput)
{
    DATA(float4, color, SV_Target0);
};

PSOutput PS_MAIN(VSOutput vsout)
{
    INIT_MAIN;
    PSOutput psout;
    psout.color = abs(vsout.normal);
    RETURN(psout);
}
