diff --git a/CMakeLists.txt b/CMakeLists.txt
index 7462a05..f2251e9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -3,6 +3,7 @@ cmake_minimum_required(VERSION 3.18)
 project(Horizon)
 
 include(${CMAKE_SOURCE_DIR}/cmake/compiler_args.cmake)
+include(FetchContent)
 
 set(CMAKE_CXX_STANDARD 17)
 set(CMAKE_CXX_STANDARD_REQUIRED ON)
@@ -11,6 +12,21 @@ set(SOLUTION_DIR ${CMAKE_CURRENT_SOURCE_DIR})
 set_property(GLOBAL PROPERTY USE_FOLDERS ON)
 set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
 
+# Volk: meta-loader for Vulkan (loads all function pointers at runtime, fixes Android/extension link errors)
+if(ANDROID AND DEFINED ANDROID_NDK)
+    set(VULKAN_HEADERS_INSTALL_DIR "${ANDROID_NDK}/sources/third_party/vulkan/src" CACHE PATH "Vulkan headers for Volk")
+endif()
+FetchContent_Declare(volk
+    GIT_REPOSITORY https://github.com/zeux/volk.git
+    GIT_TAG master
+)
+set(VOLK_PULL_IN_VULKAN ON)
+FetchContent_MakeAvailable(volk)
+# Android dlfcn.h does not define RTLD_DEEPBIND (Linux-only)
+if(ANDROID)
+    target_compile_definitions(volk PRIVATE RTLD_DEEPBIND=0)
+endif()
+
 add_subdirectory(third_party)
 add_subdirectory(framework)
 add_subdirectory(unit_tests)
diff --git a/CMakePresets.json b/CMakePresets.json
index 7d5941f..953b494 100644
--- a/CMakePresets.json
+++ b/CMakePresets.json
@@ -8,28 +8,28 @@
       "generator": "Unix Makefiles",
       "binaryDir": "${sourceDir}/build/${presetName}",
       "cacheVariables": {
-        "CMAKE_TOOLCHAIN_FILE": "D:/UEAndroidDEV/5.61/sdk/ndk/27.2.12479018/build/cmake/android.toolchain.cmake",
-        "CMAKE_MAKE_PROGRAM": "D:/UEAndroidDEV/5.61/sdk/ndk/27.2.12479018/prebuilt/windows-x86_64/bin/make.exe",
+        "CMAKE_TOOLCHAIN_FILE": "E:/ansdk/ndk/28.2.13676358/build/cmake/android.toolchain.cmake",
+        "CMAKE_MAKE_PROGRAM": "E:/ansdk/ndk/28.2.13676358/prebuilt/windows-x86_64/bin/make.exe",
         "ANDROID_PLATFORM": "31",
         "ANDROID_ABI": "arm64-v8a",
         "CMAKE_EXPORT_COMPILE_COMMANDS": "1",
         "ANDROID": "ON",
         "USE_VULKAN": "ON",
-        "USE_DX12": "ON"
+        "USE_DX12": "OFF"
       }
     },
     {
       "name": "msvcwin64",
       "displayName": "msvcwin64",
       "description": "Build for Windows x64 (Debug)",
-      "generator": "Visual Studio 17 2022",
+      "generator": "Visual Studio 18 2026",
       "binaryDir": "${sourceDir}/build/${presetName}",
       "architecture": {
         "value": "x64",
         "strategy": "external"
       },
       "cacheVariables": {
-        "CMAKE_BUILD_TYPE": "Debug",
+        "CMAKE_BUILD_TYPE": "Release",
         "CMAKE_EXPORT_COMPILE_COMMANDS": "ON",
         "USE_VULKAN": "ON",
         "USE_DX12": "ON"
@@ -46,7 +46,7 @@
         "strategy": "external"
       },
       "cacheVariables": {
-        "CMAKE_BUILD_TYPE": "Debug",
+        "CMAKE_BUILD_TYPE": "Release",
         "CMAKE_EXPORT_COMPILE_COMMANDS": "ON",
         "USE_VULKAN": "ON",
         "USE_DX12": "ON"
diff --git a/cmake/compiler_args.cmake b/cmake/compiler_args.cmake
index ee9bfbc..00cf4b5 100644
--- a/cmake/compiler_args.cmake
+++ b/cmake/compiler_args.cmake
@@ -4,5 +4,8 @@ function(configure_compile_options target)
         target_compile_options(${target} PRIVATE /MP /permissive /w14640 /W3 /WX /external:anglebrackets /external:W0 /GR- /wd4100 /wd4101 /wd4189)
     elseif(CMAKE_CXX_COMPILER_ID MATCHES "Clang")
         target_compile_options(${target} PRIVATE -Wall -Wextra -Werror -Wshadow -pedantic -fms-extensions -Wno-language-extension-token -Wno-switch -Wno-missing-field-initializers -Wno-unused-variable -Wno-unused-parameter -Wno-unused-function)
+        if(ANDROID)
+            target_compile_options(${target} PRIVATE -Wno-gnu-anonymous-struct -Wno-nested-anon-types)
+        endif()
     endif()
 endfunction()
\ No newline at end of file
diff --git a/framework/CMakeLists.txt b/framework/CMakeLists.txt
index 286c60b..7ed6bac 100644
--- a/framework/CMakeLists.txt
+++ b/framework/CMakeLists.txt
@@ -17,8 +17,8 @@ list(FILTER SOURCE_FILES EXCLUDE REGEX ".*rhi/vulkan/.*")
 
 # Exclude GLFW-related files on Android (GLFW is not supported on Android)
 if(ANDROID)
-    list(FILTER SOURCE_FILES EXCLUDE REGEX ".*core/glfw.*")
-    list(FILTER HEADER_FILES EXCLUDE REGEX ".*core/glfw.*")
+    list(FILTER SOURCE_FILES EXCLUDE REGEX ".*core/glfwinput.*")
+    list(FILTER HEADER_FILES EXCLUDE REGEX ".*core/glfwinput.*")
 endif()
 
 # Include Android sources on Android
@@ -78,6 +78,10 @@ endif()
 # 根据变量设置编译定义
 if(USE_VULKAN)
     target_compile_definitions(${PROJECT_NAME} PUBLIC USE_VULKAN)
+    # VK_NO_PROTOTYPES: Vulkan headers must not declare function prototypes so Volk can provide them (avoids redefinition on Windows)
+    target_compile_definitions(${PROJECT_NAME} PRIVATE VK_NO_PROTOTYPES)
+    # Volk: load Vulkan at runtime (avoids undefined symbols on Android for vkCmdBeginRendering etc.)
+    target_link_libraries(${PROJECT_NAME} PRIVATE volk::volk)
     
     # Android-specific configuration
     if(ANDROID)
diff --git a/framework/core/android/android_jni.cpp b/framework/core/android/android_jni.cpp
index bcfe059..78ef9d2 100644
--- a/framework/core/android/android_jni.cpp
+++ b/framework/core/android/android_jni.cpp
@@ -1,6 +1,7 @@
 #ifdef __ANDROID__
 
 #include "android_native.h"
+#include <cstdlib>
 #include <android/native_window_jni.h>
 #include <core/log.h>
 #include <jni.h>
diff --git a/framework/core/android/android_native.h b/framework/core/android/android_native.h
index f704988..2667fc8 100644
--- a/framework/core/android/android_native.h
+++ b/framework/core/android/android_native.h
@@ -2,10 +2,16 @@
 
 #ifdef __ANDROID__
 
+// Include C stdlib/math first so ::ldiv, ::hypot, ::sqrt etc. are in global namespace (NDK libc++ fix)
+#include <stdlib.h>
+#include <math.h>
+#include <functional>
+
 #include <android/native_window.h>
-#include <core/definations.h>
 #include <jni.h>
 
+#include <core/definations.h>
+
 namespace Horizon
 {
 
@@ -49,7 +55,7 @@ void OnAppDestroy();
 
 // Application entry point callback type
 // Using std::function to allow capturing lambdas
-#include <functional>
+
 using AppEntryPoint = std::function<void()>;
 
 // Register application entry point (called from app layer)
diff --git a/framework/core/android_simplemath.h b/framework/core/android_simplemath.h
index d6808e0..fb6ae98 100644
--- a/framework/core/android_simplemath.h
+++ b/framework/core/android_simplemath.h
@@ -241,6 +241,7 @@ struct Vector3
     {
         return Vector3(x * S, y * S, z * S);
     }
+    
     Vector3 operator/(float S) const noexcept
     {
         float inv = 1.0f / S;
@@ -308,6 +309,11 @@ struct Vector3
     static const Vector3 Backward;
 };
 
+inline Vector3 operator*(float S, const Vector3 &V) noexcept
+{
+    return Vector3(V.x * S, V.y * S, V.z * S);
+}
+
 //------------------------------------------------------------------------------
 // 4D Vector
 struct Vector4
@@ -322,11 +328,15 @@ struct Vector4
     }
     constexpr Vector4(float ix, float iy, float iz, float iw) noexcept : x(ix), y(iy), z(iz), w(iw)
     {
+    }
+    
+    constexpr Vector4(const Vector3 &v, float w = 0.0f) noexcept : x(v.x), y(v.y), z(v.z), w(w) {
+
     }
     explicit Vector4(const float *pArray) noexcept : x(pArray[0]), y(pArray[1]), z(pArray[2]), w(pArray[3])
     {
     }
-
+    
     Vector4(const Vector4 &) = default;
     Vector4 &operator=(const Vector4 &) = default;
 
@@ -448,7 +458,15 @@ struct Vector4
 // Matrix (4x4)
 struct Matrix
 {
-    float m[4][4];
+    union {
+        float m[4][4];
+        struct {
+            float _11, _12, _13, _14;
+            float _21, _22, _23, _24;
+            float _31, _32, _33, _34;
+            float _41, _42, _43, _44;
+        };
+    };
 
     Matrix() noexcept
     {
@@ -456,7 +474,28 @@ struct Matrix
     }
     Matrix(const Matrix &) = default;
     Matrix &operator=(const Matrix &) = default;
-
+    Matrix(float m00, float m01, float m02, float m03,
+        float m10, float m11, float m12, float m13,
+        float m20, float m21, float m22, float m23,
+        float m30, float m31, float m32, float m33) noexcept
+    {
+        m[0][0] = m00;
+        m[0][1] = m01;
+        m[0][2] = m02;
+        m[0][3] = m03;
+        m[1][0] = m10;
+        m[1][1] = m11;
+        m[1][2] = m12;
+        m[1][3] = m13;
+        m[2][0] = m20;
+        m[2][1] = m21;
+        m[2][2] = m22;
+        m[2][3] = m23;
+        m[3][0] = m30;
+        m[3][1] = m31;
+        m[3][2] = m32;
+        m[3][3] = m33;
+    }
     float &operator()(size_t row, size_t col) noexcept
     {
         return m[row][col];
@@ -556,6 +595,45 @@ struct Matrix
         return result;
     }
 
+    Matrix Transpose() const noexcept
+    {
+        Matrix result;
+        for (size_t i = 0; i < 4; ++i)
+            for (size_t j = 0; j < 4; ++j)
+                result.m[i][j] = m[j][i];
+        return result;
+    }
+
+    Matrix Invert() const noexcept
+    {
+        // Cofactor-based 4x4 inversion: M^(-1) = adj(M) / det(M)
+        auto det3 = [this](size_t r0, size_t r1, size_t r2, size_t c0, size_t c1, size_t c2) -> float {
+            return m[r0][c0] * (m[r1][c1] * m[r2][c2] - m[r1][c2] * m[r2][c1])
+                 - m[r0][c1] * (m[r1][c0] * m[r2][c2] - m[r1][c2] * m[r2][c0])
+                 + m[r0][c2] * (m[r1][c0] * m[r2][c1] - m[r1][c1] * m[r2][c0]);
+        };
+        auto cofactor = [&det3](size_t i, size_t j) -> float {
+            size_t r[3], c[3];
+            for (size_t ri = 0, k = 0; k < 4; ++k)
+                if (k != i) r[ri++] = k;
+            for (size_t cj = 0, k = 0; k < 4; ++k)
+                if (k != j) c[cj++] = k;
+            float sign = ((i + j) % 2 == 0) ? 1.0f : -1.0f;
+            return sign * det3(r[0], r[1], r[2], c[0], c[1], c[2]);
+        };
+        float det = m[0][0] * cofactor(0, 0) + m[0][1] * cofactor(0, 1)
+                  + m[0][2] * cofactor(0, 2) + m[0][3] * cofactor(0, 3);
+        const float eps = 1e-10f;
+        if (std::fabs(det) < eps)
+            return Identity; // singular
+        float invDet = 1.0f / det;
+        Matrix result;
+        for (size_t i = 0; i < 4; ++i)
+            for (size_t j = 0; j < 4; ++j)
+                result.m[i][j] = cofactor(j, i) * invDet; // adjugate = transpose of cofactor
+        return result;
+    }
+
     static const Matrix Identity;
 };
 
diff --git a/framework/core/glfwwindow.cpp b/framework/core/glfwwindow.cpp
index a83f9e8..b6842ac 100644
--- a/framework/core/glfwwindow.cpp
+++ b/framework/core/glfwwindow.cpp
@@ -37,7 +37,7 @@ Window::Window(const char *_name, u32 _width, u32 _height) noexcept : m_width(_w
     }
     glfwMakeContextCurrent(m_window);
     glfwSwapInterval(0);
-    LOG_DEBUG("vsync : {}", m_vsync_enabled); // TODO(hylu): vsync not working now
+    //LOG_DEBUG("vsync : {}", m_vsync_enabled); // TODO(hylu): vsync not working now
 #endif
 }
 
diff --git a/framework/core/glfwwindow.h b/framework/core/glfwwindow.h
index 70da6b4..ece0945 100644
--- a/framework/core/glfwwindow.h
+++ b/framework/core/glfwwindow.h
@@ -53,7 +53,7 @@ class Window
 #endif
     u32 m_width{};
     u32 m_height{};
-    bool m_vsync_enabled = true;
+    //bool m_vsync_enabled = true;
 };
 
 } // namespace Horizon
\ No newline at end of file
diff --git a/framework/resource/resource_loader/texture/texture_loader.cpp b/framework/resource/resource_loader/texture/texture_loader.cpp
index 197f334..be60acc 100644
--- a/framework/resource/resource_loader/texture/texture_loader.cpp
+++ b/framework/resource/resource_loader/texture/texture_loader.cpp
@@ -3,16 +3,16 @@
 #include <stb_image.h>
 
 #include "texture_loader.h"
-
+#ifdef _WIN32
 #include <DXGIFormat.h>
 
 #include "ddspp.h"
-
+#endif
 #include <core/log.h>
 #include <core/path.h>
 namespace Horizon
 {
-
+#ifdef _WIN32
 TextureFormat GetTextureFormatFromDXGIForamt(ddspp::DXGIFormat format)
 {
     switch (format)
@@ -349,7 +349,7 @@ TextureFormat GetTextureFormatFromDXGIForamt(ddspp::DXGIFormat format)
     }
     return {};
 }
-
+#endif
 TextureDataDesc TextureLoader::Load(const char *path)
 {
     TextureDataDesc texture_info{};
@@ -413,6 +413,7 @@ void TextureLoader::LoadPNG(const char *path, TextureDataDesc &texture_info)
     texture_info.layer_count = 1;
     stbi_image_free(data);
 }
+#ifdef _WIN32
 void TextureLoader::LoadDDS(const char *path, TextureDataDesc &texture_info)
 {
 
@@ -449,7 +450,7 @@ void TextureLoader::LoadDDS(const char *path, TextureDataDesc &texture_info)
         }
     }
 }
-
+#endif
 void TextureLoader::LoadTGA(const char *path, TextureDataDesc &texture_info)
 {
     int channels;
diff --git a/framework/resource/resources/mesh/Mesh.cpp b/framework/resource/resources/mesh/Mesh.cpp
index 99fa39b..5d6ce81 100644
--- a/framework/resource/resources/mesh/Mesh.cpp
+++ b/framework/resource/resources/mesh/Mesh.cpp
@@ -3,7 +3,8 @@
 #include <algorithm>
 #include <thread>
 
-#include <assimp/pbrmaterial.h>
+#include <assimp/material.h>
+#include <assimp/GltfMaterial.h>
 #include <assimp/postprocess.h>
 #include <assimp/scene.h>
 
@@ -64,9 +65,9 @@ void Mesh::ProcessMaterials(const aiScene *scene)
     [[maybe_unused]] aiReturn ret;
     for (u32 i = 0; i < scene->mNumMaterials; i++)
     {
-        bool unlit;
-        scene->mMaterials[i]->Get(AI_MATKEY_GLTF_UNLIT, unlit);
-        if (unlit == true)
+        int shading_model = 0;
+        scene->mMaterials[i]->Get(AI_MATKEY_SHADING_MODEL, shading_model);
+        if (shading_model == aiShadingMode_Unlit)
         {
             materials[i].shading_model = ShadingModel::SHADING_MODEL_UNLIT;
         }
diff --git a/framework/resource/resources/mesh/mesh.cpp b/framework/resource/resources/mesh/mesh.cpp
index 99fa39b..5d6ce81 100644
--- a/framework/resource/resources/mesh/mesh.cpp
+++ b/framework/resource/resources/mesh/mesh.cpp
@@ -3,7 +3,8 @@
 #include <algorithm>
 #include <thread>
 
-#include <assimp/pbrmaterial.h>
+#include <assimp/material.h>
+#include <assimp/GltfMaterial.h>
 #include <assimp/postprocess.h>
 #include <assimp/scene.h>
 
@@ -64,9 +65,9 @@ void Mesh::ProcessMaterials(const aiScene *scene)
     [[maybe_unused]] aiReturn ret;
     for (u32 i = 0; i < scene->mNumMaterials; i++)
     {
-        bool unlit;
-        scene->mMaterials[i]->Get(AI_MATKEY_GLTF_UNLIT, unlit);
-        if (unlit == true)
+        int shading_model = 0;
+        scene->mMaterials[i]->Get(AI_MATKEY_SHADING_MODEL, shading_model);
+        if (shading_model == aiShadingMode_Unlit)
         {
             materials[i].shading_model = ShadingModel::SHADING_MODEL_UNLIT;
         }
diff --git a/framework/rhi/vulkan/rhi_vulkan.cpp b/framework/rhi/vulkan/rhi_vulkan.cpp
index b09fdeb..11ac2c1 100644
--- a/framework/rhi/vulkan/rhi_vulkan.cpp
+++ b/framework/rhi/vulkan/rhi_vulkan.cpp
@@ -1,11 +1,11 @@
 #include "rhi_vulkan.h"
 
+#include <volk.h>
 #include <core/path.h>
 #include <thread>
 
 #define VMA_IMPLEMENTATION
 #include <vk_mem_alloc.h>
-#include <vulkan/vulkan.hpp>
 
 #include <core/memory.h>
 #include <rhi/vulkan/vulkan_buffer.h>
@@ -255,8 +255,15 @@ void RHIVulkan::InitializeVulkanRenderer(const std::string &app_name)
     // device_extensions.emplace_back(VK_GOOGLE_HLSL_FUNCTIONALITY_1_EXTENSION_NAME);
     // device_extensions.emplace_back(VK_GOOGLE_USER_TYPE_EXTENSION_NAME);
 
+    if (volkInitialize() != VK_SUCCESS)
+    {
+        LOG_ERROR("volkInitialize failed");
+        return;
+    }
     CreateInstance(app_name, instance_layers, instance_extensions);
+    volkLoadInstance(m_vulkan.instance);
     CreateDevice(device_extensions);
+    volkLoadDevice(m_vulkan.device);
     InitializeVMA();
     // create sync objects
     CreateSyncObjects();
@@ -477,6 +484,10 @@ void RHIVulkan::InitializeVMA()
     VmaVulkanFunctions vulkan_functions{};
     vulkan_functions.vkGetInstanceProcAddr = vkGetInstanceProcAddr;
     vulkan_functions.vkGetDeviceProcAddr = vkGetDeviceProcAddr;
+#if VMA_VULKAN_VERSION >= 1003000
+    vulkan_functions.vkGetDeviceBufferMemoryRequirements = vkGetDeviceBufferMemoryRequirements;
+    vulkan_functions.vkGetDeviceImageMemoryRequirements = vkGetDeviceImageMemoryRequirements;
+#endif
 
     VmaAllocatorCreateInfo vma_create_info{};
     vma_create_info.device = m_vulkan.device;
diff --git a/framework/rhi/vulkan/vulkan_buffer.h b/framework/rhi/vulkan/vulkan_buffer.h
index 98caee3..fb2ae62 100644
--- a/framework/rhi/vulkan/vulkan_buffer.h
+++ b/framework/rhi/vulkan/vulkan_buffer.h
@@ -1,7 +1,7 @@
 #pragma once
 
 #include <vk_mem_alloc.h>
-#include <vulkan/vulkan.h>
+#include <volk.h>
 
 #include <core/definations.h>
 #include <rhi/buffer.h>
diff --git a/framework/rhi/vulkan/vulkan_command_context.h b/framework/rhi/vulkan/vulkan_command_context.h
index ebc480c..dc72691 100644
--- a/framework/rhi/vulkan/vulkan_command_context.h
+++ b/framework/rhi/vulkan/vulkan_command_context.h
@@ -1,6 +1,6 @@
 #pragma once
 
-#include <vulkan/vulkan.h>
+#include <volk.h>
 
 #include <rhi/command_context.h>
 #include <rhi/vulkan/vulkan_command_list.h>
diff --git a/framework/rhi/vulkan/vulkan_command_list.h b/framework/rhi/vulkan/vulkan_command_list.h
index cb23c12..eb8504e 100644
--- a/framework/rhi/vulkan/vulkan_command_list.h
+++ b/framework/rhi/vulkan/vulkan_command_list.h
@@ -1,6 +1,6 @@
 #pragma once
 
-#include <vulkan/vulkan.h>
+#include <volk.h>
 
 #include <rhi/command_list.h>
 #include <rhi/vulkan/vulkan_buffer.h>
diff --git a/framework/rhi/vulkan/vulkan_descriptor_set_allocator.h b/framework/rhi/vulkan/vulkan_descriptor_set_allocator.h
index 4c48dab..93ed90a 100644
--- a/framework/rhi/vulkan/vulkan_descriptor_set_allocator.h
+++ b/framework/rhi/vulkan/vulkan_descriptor_set_allocator.h
@@ -1,6 +1,6 @@
 #pragma once
 
-#include <vulkan/vulkan.h>
+#include <volk.h>
 
 #include <core/definations.h>
 
diff --git a/framework/rhi/vulkan/vulkan_shader.h b/framework/rhi/vulkan/vulkan_shader.h
index 9552e10..32539f5 100644
--- a/framework/rhi/vulkan/vulkan_shader.h
+++ b/framework/rhi/vulkan/vulkan_shader.h
@@ -4,7 +4,7 @@
 #include <rhi/shader.h>
 #include <rhi/vulkan/vulkan_utils.h>
 
-#include <vulkan/vulkan.h>
+#include <volk.h>
 
 #include <vector>
 
diff --git a/framework/rhi/vulkan/vulkan_swap_chain.cpp b/framework/rhi/vulkan/vulkan_swap_chain.cpp
index b7c3cb9..3a08d12 100644
--- a/framework/rhi/vulkan/vulkan_swap_chain.cpp
+++ b/framework/rhi/vulkan/vulkan_swap_chain.cpp
@@ -7,7 +7,7 @@
 #include <android/native_window.h>
 #endif
 
-#include <vulkan/vulkan.hpp>
+#include <volk.h>
 
 #include "vulkan_render_target.h"
 #include "vulkan_semaphore.h"
diff --git a/framework/rhi/vulkan/vulkan_utils.h b/framework/rhi/vulkan/vulkan_utils.h
index 9558437..351d77a 100644
--- a/framework/rhi/vulkan/vulkan_utils.h
+++ b/framework/rhi/vulkan/vulkan_utils.h
@@ -3,7 +3,7 @@
 #include <functional>
 
 #include <vk_mem_alloc.h>
-#include <vulkan/vulkan.h>
+#include <volk.h>
 
 #include <core/definations.h>
 #include <rhi/enums.h>
diff --git a/framework/scene/camera/caemra_controller.cpp b/framework/scene/camera/caemra_controller.cpp
index 8efe19f..3a78c5d 100644
--- a/framework/scene/camera/caemra_controller.cpp
+++ b/framework/scene/camera/caemra_controller.cpp
@@ -8,6 +8,8 @@
 
 #include "camera_controller.h"
 
+#include <cassert>
+
 #include <core/glfwinput.h>
 
 namespace Horizon
diff --git a/framework/scene/light/Light.cpp b/framework/scene/light/Light.cpp
index 9c2346f..f684671 100644
--- a/framework/scene/light/Light.cpp
+++ b/framework/scene/light/Light.cpp
@@ -1,5 +1,7 @@
 #include "light.h"
 
+#include <cassert>
+
 namespace Horizon
 {
 
diff --git a/framework/scene/light/light.cpp b/framework/scene/light/light.cpp
index 9c2346f..f684671 100644
--- a/framework/scene/light/light.cpp
+++ b/framework/scene/light/light.cpp
@@ -1,5 +1,7 @@
 #include "light.h"
 
+#include <cassert>
+
 namespace Horizon
 {
 
diff --git a/samples/CMakeLists.txt b/samples/CMakeLists.txt
index eb6474d..bece8c8 100644
--- a/samples/CMakeLists.txt
+++ b/samples/CMakeLists.txt
@@ -28,5 +28,8 @@ macro(addproject projectdir)
 
 endmacro()
 
-addproject(deferred)
-addproject(hellotriangle)
\ No newline at end of file
+# Desktop samples use main(); on Android the app entry is android_main/JNI, so skip building these executables
+if(NOT ANDROID)
+    addproject(deferred)
+    addproject(hellotriangle)
+endif()
\ No newline at end of file
diff --git a/third_party/directxtk12 b/third_party/directxtk12
--- a/third_party/directxtk12
+++ b/third_party/directxtk12
@@ -1 +1 @@
-Subproject commit 6a23cb18b40b967a4161bdb40eaf30983af64745
+Subproject commit 6a23cb18b40b967a4161bdb40eaf30983af64745-dirty
